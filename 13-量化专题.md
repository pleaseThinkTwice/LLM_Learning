# 第 13 章｜量化专题

> **本章定位**：第 12 章给出了 Decode 的速度上界 $\text{TPOT}_{\min} = \dfrac{\text{模型大小}}{\text{HBM 带宽}}$，并指出三条优化路线。**量化就是"减小分子"那一条**——也是**唯一一条能同时省显存、省带宽、省钱的**。本章的主线是一个反复出现的母题：**量化的难点从来不是权重，而是激活的离群值（outlier）；而应对 outlier 的手段，本质上都是同一个数学技巧——per-channel 等价缩放**。GPTQ、AWQ、SmoothQuant 三篇论文，只有 GPTQ 走了另一条路（二阶重构）。看懂这句话，本章就通了一半。

> **配套章节**：Roofline / memory-bound 分析 → 第 12 章 Q3 / KV Cache 显存公式 → 第 12 章 Q2 / QLoRA 的 NF4 → 第 8 章 / 混合精度训练 bf16 vs fp16 → 第 6 章 Q6 / 推理引擎对量化的支持 → 第 15 章

---

## 📌 本章导航

| 序号 | 题目 | 难度 | 频率（字节/阿里/Meta/DeepSeek） | 类型 |
|---|---|---|---|---|
| Q1 | **量化基础：映射、对称性、粒度** | ⭐⭐⭐⭐⭐ | **8 / 7 / 6 / 5** | 必问·可能手撕 |
| Q2 | PTQ vs QAT | ⭐⭐⭐⭐ | 5 / 5 / 4 / 3 | 概念 |
| Q3 | **GPTQ：从 OBS 到 Hessian 近似** | ⭐⭐⭐⭐⭐ | **8 / 7 / 4 / 5** | 必问·可能手撕 |
| Q4 | **AWQ：为什么"激活感知"是对的** | ⭐⭐⭐⭐⭐ | **8 / 6 / 4 / 4** | 必问 |
| Q5 | **激活 outlier 与 SmoothQuant** | ⭐⭐⭐⭐⭐ | **7 / 6 / 6 / 5** | 必问 |
| Q6 | **W4A16 vs W8A8 vs FP8：怎么选** | ⭐⭐⭐⭐⭐ | **8 / 7 / 5 / 6** | 必问·实战 |
| Q7 | KV Cache 量化 | ⭐⭐⭐⭐ | 6 / 5 / 3 / 4 | 进阶 |
| Q8 | 量化评估的陷阱：掉点掉在哪 | ⭐⭐⭐⭐ | 6 / 5 / 4 / 3 | 实战 |

---

## Q1：量化基础——映射、对称性、粒度 ⭐⭐⭐⭐⭐（必问，可能手撕）

### 🎯 一句话标答

> 量化是把连续的 fp16 值域**仿射映射**到离散的低比特整数格点：$x_q = \text{round}(x/s) + z$；三个设计维度是**对称 vs 非对称**（$z$ 是否为 0）、**粒度**（per-tensor / per-channel / per-group）、**位宽**——粒度越细精度越高但 scale 的存储和 kernel 开销越大，**LLM 权重量化的工业默认是 per-group（group_size=128）非对称 int4**。

### 🗣️ 30 秒口语版

"量化的本质是**用离散格点近似连续值**。

**基本公式**——
$$x_q = \text{clamp}\Big(\text{round}\big(\tfrac{x}{s}\big) + z,\ 0,\ 2^b-1\Big), \qquad \hat{x} = s\,(x_q - z)$$

`s` 是 **scale**（步长），`z` 是 **zero-point**（零点偏移）。反量化就是把整数乘回 scale。注意 $\hat x \ne x$——**中间那个 round 是不可逆的，误差就在这里产生**。

**第一个维度：对称 vs 非对称**
- **对称**：$z=0$，$s = \max|x| / (2^{b-1}-1)$。值域关于 0 对称。计算快（少一次减法），但如果数据分布偏斜（比如 ReLU 后全是正数），**一半格点被浪费**。
- **非对称**：用 $[\min, \max]$ 的完整范围，$s = \frac{\max-\min}{2^b-1}$，$z = -\text{round}(\min/s)$。精度更高，但 kernel 里多一步。
- **实践**：**权重用非对称**（分布不一定对称，且权重量化是离线的，慢一点无所谓）；**激活常用对称**（在线算的，要快）。

**第二个维度：粒度**——这是**最关键的**。
- **per-tensor**：整个权重矩阵共用一个 s。存储开销几乎为 0，但**一个离群值就毁掉整张表**。
- **per-channel**：每个输出通道（每一行/列）一个 s。
- **per-group**：把一行切成若干组，比如**每 128 个元素一组**，每组独立的 s 和 z。**这是 GPTQ/AWQ 的默认**。

为什么是 128？这是个甜点——
- 组越小，越能隔离离群值，精度越高
- 但每组要额外存 s 和 z。group=128、int4 下，额外开销约 **0.125 bit/权重**（fp16 的 s + int4 的 z 摊到 128 个权重上），**等效位宽从 4.0 变成 ~4.15**，可接受
- 组太小（比如 32）开销就到 0.5 bit 了，还不如直接用更高位宽

**第三个维度：位宽**。int8 基本无损，int4 需要 GPTQ/AWQ 这类算法才能保住精度，int2/int3 目前 PTQ 路线掉点明显。

**最后一句必须说出来的话**：**权重好量化，激活难量化**。权重是训练好的静态参数，分布接近高斯、有界、没有极端离群值；激活是输入相关的、动态的，而且大模型有 **systematic outlier**（Q5 会详谈）。这个不对称性，决定了本章后面所有技术的形态。"

### 📐 详细推导

#### 仿射量化的完整定义

**非对称（asymmetric / affine）**，量化到 $b$ 比特无符号整数：

$$s = \frac{x_{\max} - x_{\min}}{2^b - 1}, \qquad z = \text{round}\left(-\frac{x_{\min}}{s}\right)$$
$$x_q = \text{clamp}\big(\text{round}(x/s) + z,\ 0,\ 2^b-1\big), \qquad \hat{x} = s(x_q - z)$$

**对称（symmetric）**，量化到有符号整数：

$$s = \frac{\max|x|}{2^{b-1}-1}, \quad z = 0$$
$$x_q = \text{clamp}\big(\text{round}(x/s),\ -2^{b-1},\ 2^{b-1}-1\big), \qquad \hat{x} = s \cdot x_q$$

**量化误差的量级**：round 到最近格点，误差均匀分布在 $[-s/2, s/2]$，所以

$$\mathbb{E}[(x - \hat{x})^2] = \frac{s^2}{12}$$

> 🚨 **记住这个式子——它是理解 AWQ 的钥匙**。误差**只取决于 $s$**，而 $s$ 由组内的 $\max|x|$ 决定。这意味着：**如果我把某个权重放大 2 倍，而组内最大值不变（$s$ 不变），那这个权重的<u>相对</u>误差就降了一半**。AWQ 干的就是这件事（Q4）。

#### 粒度对比

以一个 $[4096, 4096]$ 的权重矩阵为例：

| 粒度 | scale 个数 | 额外存储 | 抗离群值 | 典型用途 |
|---|---|---|---|---|
| per-tensor | 1 | ~0 | ❌ 极差 | 激活量化（配 SmoothQuant） |
| per-channel（每行） | 4096 | 8 KB (~0.004 bit/w) | 中 | W8A8 的权重端 |
| **per-group (128)** | 4096×32 = 131K | 256 KB (**~0.125 bit/w**) | ✅ 好 | **GPTQ / AWQ 默认** |
| per-group (32) | 524K | 1 MB (~0.5 bit/w) | 更好 | 端侧极限压缩 |

**等效位宽计算**（面试可能让你算）：
```
int4 + group_size=128 + fp16 scale + int4 zero-point:
  每 128 个权重的开销 = 128×4 bit (权重) + 16 bit (scale) + 4 bit (zero)
                      = 512 + 20 = 532 bit
  等效位宽 = 532 / 128 = 4.16 bit
  相对 fp16 的压缩比 = 16 / 4.16 ≈ 3.85×    ← 不是 4×！
```

> **这个 3.85 而不是 4 的细节，答出来会加分**——说明你真的算过，而不是背"int4 就是 4 倍压缩"。

#### 为什么权重好量化、激活难量化

| | 权重 | 激活 |
|---|---|---|
| 何时确定 | **训练完就固定** → 可以离线慢慢优化 | **运行时才知道** → 要么在线算 scale（慢），要么离线标定（不准） |
| 分布 | 接近高斯、有界、无极端值 | **有 systematic outlier**（>6.7B 模型上，个别 channel 幅度是其他的 100 倍） |
| 动态范围 | 窄 | **极宽** |
| 量化到 int8 | 几乎无损 | **直接崩**（per-tensor 下） |

**这张表是本章的总纲**：
- 权重端 → 追求**极限压缩**（int4、int3）→ **GPTQ、AWQ**
- 激活端 → 追求**能不能量化**（int8 就够）→ **SmoothQuant、LLM.int8()、FP8**

### 💻 关键代码片段

```python
import torch

def quantize_per_group(w: torch.Tensor, num_bits=4, group_size=128, symmetric=False):
    """
    w: [out_features, in_features]
    按 in_features 维度分组（因为矩阵乘时这一维是被规约掉的）
    """
    out_f, in_f = w.shape
    assert in_f % group_size == 0
    w = w.reshape(out_f, in_f // group_size, group_size)   # [O, G, 128]

    if symmetric:
        s = w.abs().amax(dim=-1, keepdim=True) / (2**(num_bits-1) - 1)
        z = torch.zeros_like(s)
        qmin, qmax = -2**(num_bits-1), 2**(num_bits-1) - 1
    else:
        w_min = w.amin(dim=-1, keepdim=True)
        w_max = w.amax(dim=-1, keepdim=True)
        s = (w_max - w_min) / (2**num_bits - 1)
        z = torch.round(-w_min / s)
        qmin, qmax = 0, 2**num_bits - 1

    s = s.clamp(min=1e-8)                                   # ← 防止整组全 0 时除零
    w_q = torch.clamp(torch.round(w / s) + z, qmin, qmax)
    w_hat = (s * (w_q - z)).reshape(out_f, in_f)            # 反量化
    return w_q.to(torch.uint8), s, z, w_hat


# 感受一下误差
w = torch.randn(4096, 4096) * 0.02
_, _, _, w_hat = quantize_per_group(w, num_bits=4, group_size=128)
print(f"相对误差: {(w - w_hat).norm() / w.norm():.4%}")   # ≈ 1.2%

# 对比 per-tensor —— 注意人为塞一个离群值
w[0, 0] = 10.0                                              # 一个离群值
_, _, _, w_hat_g = quantize_per_group(w, 4, group_size=128)
s_t = w.abs().max() / 7
w_hat_t = torch.round(w / s_t).clamp(-8, 7) * s_t
print(f"per-group: {(w - w_hat_g).norm()/w.norm():.4%}")   # ≈ 1.2%（离群值只毁一个组）
print(f"per-tensor: {(w - w_hat_t).norm()/w.norm():.4%}")  # ≈ 90%+（离群值毁掉全部）
```

> 最后这两行的对比就是**"为什么必须 per-group"**的实验证明。面试时能描述这个现象（一个离群值把 per-tensor 的 scale 拉大 500 倍，导致所有正常权重都被 round 到 0）就够了。

### 🔥 高频追问 Top 3

**Q：量化后模型变小了 4 倍，推理是不是就快 4 倍？**

A：**只有在 memory-bound 的 Decode 阶段、且 batch 小的时候才接近 4 倍。** 这题直接接第 12 章 Q3。

拆开看：
- **Decode（batch 小）**：memory-bound，$\text{TPOT}_{\min} = \frac{\text{模型大小}}{\text{带宽}}$。模型小 4 倍 → **理论快 4 倍** ✅（实测通常 2-3 倍，因为反量化有开销、且 attention 部分没变）
- **Prefill**：**compute-bound**。W4A16 的计算还是 fp16 GEMM（要先反量化回 fp16 再算），**不但没快，反而多了一步反量化 → 更慢** ❌
- **Decode（batch 很大）**：算术强度 ≈ batch，已经接近 compute-bound → W4A16 的优势**衰减甚至倒挂**

**所以正确的说法是**："W4A16 优化的是**显存和带宽**，不是算力。它在**小 batch / 低并发 / 端侧**收益最大，在**大 batch 高吞吐服务**下收益有限。" 这个判断是 Q6 选型题的核心，**也是区分'看过博客'和'压过测'的分水岭**。

**Q：round 用什么舍入方式？round-to-nearest 是最优的吗？**

A：**RTN（Round-To-Nearest）是最直观的，但不是最优的**——这正是 GPTQ 存在的理由。

- **RTN**：每个权重独立地 round 到最近格点。**逐权重最优，但整体不最优**。
- **为什么不最优**：我们真正在意的**不是权重本身准不准，而是这一层的输出准不准**。$\|WX - \hat{W}X\|$ 里有个 $X$——某些权重乘的输入大，它的误差会被放大；某些权重的误差可以**被其他权重的误差抵消**。RTN 完全不管这些。
- **GPTQ 的做法**：量化一个权重后，**主动调整还没量化的权重来补偿这个误差**（Q3）。补偿之后，那些权重可能就不再 round 到最近格点了——**故意"round 错"，让整体输出更准**。

还有一个中间方案：**AdaRound**——学一个每权重的"向上还是向下 round"的决策（0/1），比 RTN 好，比 GPTQ 轻。LLM 时代用得少。

**Q：为什么是 group_size=128，不是 64 或 256？**

A：**经验甜点，权衡三件事**：

1. **精度**：越小越好（离群值影响范围越小）
2. **等效位宽**：越小越差（scale 开销摊薄得少）—— 128 是 ~4.16 bit，32 是 ~4.6 bit
3. **kernel 效率（最容易被忽略、但很关键）**：GPU 的 warp 是 32 线程，tensor core 的 tile 通常是 16/32/64 的倍数。**group_size 必须和 kernel 的 tiling 对齐**，否则反量化时要处理跨界，性能塌方。128 正好对齐主流 kernel 的 K 维 tile。

所以 128 是"**精度够用 + 开销可接受 + 硬件对齐**"的交点。端侧（llama.cpp）会用更小的 group（32）换精度，因为那边**显存比速度更稀缺**。

### ⚠️ 常见陷阱

1. **说"int4 压缩 4 倍"** —— 实际是 **~3.85 倍**，因为 scale/zero 有开销。答出来是加分。
2. **忘了 clamp** —— round 之后必须 clamp 到值域，否则溢出。手撕时漏掉会被抓。
3. **搞混分组的维度** —— 权重量化是沿**输入维度（in_features，被规约的那维）**分组，不是输出维度。因为矩阵乘是沿这维求和，误差在这维累积。
4. **说"量化让模型变快"** —— 要主动限定"**在 memory-bound 场景下**"。不限定就是没理解第 12 章。

### 🏢 大厂偏好

- **所有大厂**：Q1 是量化题的必答第一层，**可能要求现场手撕 quantize/dequantize**
- **字节 / 阿里 Infra**：会追问 group_size 的 kernel 对齐问题
- **端侧团队（手机、车机）**：会问更细的 group 和混合位宽

### 📚 延伸阅读
- [A White Paper on Neural Network Quantization](https://arxiv.org/abs/2106.08295) - Qualcomm 出品，**量化领域最好的综述，Q1/Q2 的内容全在里面**
- [Integer Quantization for Deep Learning Inference](https://arxiv.org/abs/2004.09602) - NVIDIA 的实践指南

---

## Q2：PTQ vs QAT ⭐⭐⭐⭐

### 🎯 一句话标答

> **PTQ**（训练后量化）不碰训练，只用**几百条校准数据**在几分钟到几小时内完成，**LLM 时代的绝对主流**（GPTQ/AWQ/SmoothQuant 全是 PTQ）；**QAT**（量化感知训练）把伪量化插进训练图、用 **STE** 直通梯度，精度更好但**要重训**——对 LLM 成本不可接受，只在**极低比特（≤2 bit）**场景才复活（BitNet）。

### 🗣️ 30 秒口语版

"两条路线的分界线是：**要不要重新训练**。

**PTQ（Post-Training Quantization）**
- 拿一个训好的模型，**不改权重的训练过程**，直接量化
- 需要一小批**校准数据**（calibration set，典型 **128 条、每条 2048 token**）来统计激活范围、算 Hessian 或者搜 scale
- 成本：**GPTQ 量化 175B 模型约 4 GPU-小时**——一台机器一顿饭的时间
- 代表：**GPTQ、AWQ、SmoothQuant、LLM.int8()**

**QAT（Quantization-Aware Training）**
- 在训练图里插入"**伪量化节点**"（fake quant）：前向 `quantize → dequantize`，让模型**在训练中就感受到量化误差**并自己适应
- 问题：`round` 的导数**几乎处处为 0**，梯度传不回去 → 用 **STE（Straight-Through Estimator）**：前向照常量化，**反向假装量化不存在，梯度直接透传**
- 成本：**要跑完整的训练/微调流程**。对 7B 模型可能要几百 GPU-小时，对 70B 更离谱
- 代表：LLM-QAT、**BitNet b1.58**

**为什么 LLM 时代 PTQ 赢了？**
1. **成本**：QAT 的重训成本可能**超过量化带来的全部收益**
2. **数据**：预训练数据往往拿不到（尤其是用别人的开源模型）
3. **PTQ 够好了**：GPTQ/AWQ 在 int4 上已经接近无损，QAT 的边际收益不值那个价
4. **泛化风险**：QAT 在特定数据上重训，可能反而**损伤通用能力**

**QAT 什么时候还有价值？** —— **当 PTQ 彻底失效的时候**，也就是**极低比特**。int2、三值、1-bit 这些，PTQ 怎么调都崩，只能从训练里长出来。BitNet b1.58 就是这条路：**从头训一个"天生就是三值"的模型**。这不是"量化一个模型"，而是"**训练一个低精度模型**"——哲学上已经不同了。

**顺便澄清一个高频误解**：**QLoRA 不是 QAT**。QLoRA 是"**基座 NF4 冻结 + LoRA 分支 bf16 可训**"——基座的量化是 PTQ 式的一次性操作，训练中**没有伪量化节点，量化误差也没有被反向传播修正**。它是"在量化模型上做 PEFT"，不是"量化感知训练"。（见第 8 章）"

### 📐 STE 的机制

问题：$\dfrac{\partial}{\partial x}\text{round}(x) = 0$（几乎处处），梯度一到量化节点就死了。

**STE 的解法——前向和反向用不同的函数**：

$$\text{前向：} \quad \hat{x} = s \cdot \text{round}(x/s) \qquad\qquad \text{反向：} \quad \frac{\partial \hat{x}}{\partial x} \triangleq \mathbb{1}[x_{\min} \le x \le x_{\max}]$$

也就是说，**反向时假装 `round` 是恒等函数**，梯度原样透传；只在**超出量化范围的地方**把梯度截断为 0（因为那里被 clamp 了，确实不该有梯度）。

```python
class FakeQuantSTE(torch.autograd.Function):
    @staticmethod
    def forward(ctx, x, s, qmin, qmax):
        ctx.save_for_backward(x)
        ctx.qmin, ctx.qmax, ctx.s = qmin, qmax, s
        return s * torch.clamp(torch.round(x / s), qmin, qmax)   # 量化再反量化

    @staticmethod
    def backward(ctx, grad_out):
        x, = ctx.saved_tensors
        # 核心：直接透传，只在被 clamp 的区域截断
        mask = (x >= ctx.qmin * ctx.s) & (x <= ctx.qmax * ctx.s)
        return grad_out * mask, None, None, None
```

> **STE 是"数学上错的、工程上对的"典型**——它的梯度不是真梯度（真梯度是 0），只是一个**有偏但有用的代理**。这类"故意用错的梯度"在深度学习里很常见（Gumbel-Softmax、VQ-VAE 的 codebook 也是同一套路）。**面试时能点出 VQ-VAE 的类比是个漂亮的加分。**

### 🔍 对比表

| 维度 | PTQ | QAT |
|---|---|---|
| 是否重训 | ❌ | ✅ |
| 数据需求 | **128 条校准样本** | 完整训练/微调数据集 |
| 时间成本 | 分钟~小时 | 天~周 |
| int8 精度 | 基本无损 | 无损 |
| **int4 精度** | **接近无损（GPTQ/AWQ）** | 略好 |
| **int2 / 三值** | ❌ **崩** | ✅ **唯一可行路线** |
| LLM 时代地位 | **绝对主流** | 小众 / 极低比特 |
| 代表 | GPTQ, AWQ, SmoothQuant | LLM-QAT, **BitNet b1.58** |

### 🔥 高频追问 Top 3

**Q：校准数据（calibration set）该怎么选？128 条够吗？**

A：**这是 PTQ 最容易被忽视、但线上最容易翻车的地方**。

**数量**：128 条 × 2048 token 是 GPTQ/AWQ 的经验默认。再多收益很小——因为要估的东西（激活的幅度分布、Hessian 的主要方向）**收敛得很快**。

**分布才是关键**：
- ❌ **反模式**：用 WikiText 校准，然后部署到**代码补全**场景 → 代码的激活分布和百科文本完全不同 → 掉点明显
- ✅ **正确做法**：用**贴近线上真实分布**的数据校准。做 RAG 就用带检索上下文的样本，做 Agent 就用带工具定义的长 prompt，做中文就别用纯英文语料
- ⚠️ **但要小心过拟合**：GPTQ 因为是"在校准集上做重构优化"，**存在 overfit calibration set 的风险**——在校准分布上很准，换个分布就掉。**AWQ 的一大卖点正是它不做重构（只搜一个 scale），所以对校准集不敏感、泛化更好**（Q4 会讲）。

**实践建议**：**混合**——70% 业务分布 + 30% 通用语料，兼顾针对性和泛化。

**Q：QAT 用 LoRA 做能不能省成本？**

A：能，这个方向叫 **QA-LoRA / LoftQ / PEQA** 这一族，思路是"**用 PEFT 的成本拿 QAT 的收益**"。

但要**严格区分它和 QLoRA**（第 8 章）：

| | QLoRA | QA-LoRA / LoftQ |
|---|---|---|
| 目的 | **省显存做微调**（最终要 merge 回 fp16） | **让量化后的模型更准** |
| 基座 | NF4 冻结 | 量化后冻结 |
| 训练中有伪量化吗 | ❌ 没有 | ✅ 有 |
| 部署时 | 通常 merge 回 fp16 | **直接部署量化版** |

**核心区别**：QLoRA 把量化当成"**省显存的手段**"，训练完就不要了；QA-LoRA 把量化当成"**最终形态**"，训练是为了**让模型适应量化误差**。

**一句话辨析**：**QLoRA 是"在量化的模型上训练"，QA-LoRA 是"为了量化而训练"**。这题问得不多，但答混了会暴露对第 8 章理解不深。

**Q：BitNet 的 1.58 bit 是怎么来的？为什么不是 1 bit 或 2 bit？**

A：**1.58 = $\log_2 3$** —— 因为权重只有 **{-1, 0, +1} 三个取值**，信息论下界是 $\log_2 3 \approx 1.585$ bit。

为什么三值而不是二值 {-1, +1}？
- **那个 0 太重要了**——它提供了**特征筛选**能力（相当于内建稀疏性），让模型能"忽略"某个输入
- 纯 1-bit（BitNet 的第一版）质量掉得多；加上 0 之后，BitNet b1.58 **在 3B 以上规模能对齐 fp16 的 LLaMA**

**最诱人的地方**：三值权重的矩阵乘**不需要乘法**——只有**加、减、跳过**。这意味着可以设计**没有乘法器的芯片**，能效比提升一个数量级。

**为什么还没普及**：必须**从头训**，没法把已有的 LLaMA 转过去。这是个"**新模型**"而不是"**新量化方法**"，生态成本太高。**这是"量化的终局是什么"这类开放题的好素材。**

### ⚠️ 常见陷阱

1. **把 QLoRA 说成 QAT** —— 最高频的混淆，见追问 2。
2. **不知道 STE 的存在** —— 被问"round 不可导怎么训"答不上来是硬伤。
3. **忽略校准数据的分布** —— 说"随便找 128 条数据"会显得没上过线。
4. **说 QAT 一定比 PTQ 好** —— 在 int4 上，GPTQ/AWQ 已经和 QAT 差距很小，而成本差几百倍。**PTQ 在 LLM 上是压倒性的性价比选择。**

### 🏢 大厂偏好

- **算法岗**：会问 STE 的数学
- **端侧 / 芯片团队**：会深挖 QAT 和 BitNet
- **服务端应用岗**：知道"LLM 时代 PTQ 主导 + 为什么"就够了

### 📚 延伸阅读
- [LLM-QAT](https://arxiv.org/abs/2305.17888) - Meta 的 LLM QAT 尝试（用模型自己生成的数据做 data-free QAT，思路很巧）
- [BitNet b1.58](https://arxiv.org/abs/2402.17764) - 三值 LLM
- [QA-LoRA](https://arxiv.org/abs/2309.14717)

---

## Q3：GPTQ——从 OBS 到 Hessian 近似 ⭐⭐⭐⭐⭐（必问，可能手撕）

### 🎯 一句话标答

> GPTQ 的目标**不是让权重准，而是让这一层的输出准**：$\arg\min_{\hat{W}} \|WX - \hat{W}X\|_F^2$；它继承 **OBS** 的思路——量化一个权重后，用 **Hessian 逆**（$H = 2XX^\top$）计算并**补偿**误差到尚未量化的权重上——再用**三个工程近似**（固定顺序、Lazy Batch Update、Cholesky）把 OBQ 的 $O(d^4)$ 降到 $O(d^3)$，实现 **175B 模型 4 GPU-小时完成 int4 量化**。

### 🗣️ 30 秒口语版

"GPTQ 要分三层讲：**目标、原理、工程近似**。

**第一层：目标函数。这是最重要的、也是最多人答漏的一点。**

朴素量化（RTN）在优化什么？$\min |w - \hat{w}|$——**让权重本身准**。

GPTQ 在优化什么？

$$\arg\min_{\hat{W}} \ \|WX - \hat{W}X\|_F^2$$

**让这一层的输出准**。多了一个 $X$，全部的区别都在这里。

为什么这个更对？因为**权重不是拿来看的，是拿来乘的**。如果某个权重乘的输入恰好总是接近 0，那它错得再离谱也不影响输出；反过来，乘大输入的权重必须准。**RTN 对所有权重一视同仁，GPTQ 让 $X$ 来决定谁重要。**

而且注意它是 **layer-wise（逐层）** 的——不是端到端优化整个模型，而是**一层一层独立地重构**。这是个关键的简化：让问题变成一个个小的、可解的二次优化。

**第二层：原理——OBS 的误差补偿**

这个目标函数对权重是**二次的**，所以 Hessian 有解析解：$H = 2XX^\top$。

有了 Hessian，就能用 **OBS（Optimal Brain Surgeon，1993 年的老技术！）** 的框架：
1. 量化某个权重 $w_q$，产生误差 $\delta = w_q - \text{quant}(w_q)$
2. **把这个误差"摊"给还没量化的权重**——按 Hessian 逆指示的方向调整它们，让输出的变化被抵消掉：
   $$\delta_F = -\frac{w_q - \text{quant}(w_q)}{[H_F^{-1}]_{qq}} \cdot [H_F^{-1}]_{:,q}$$
3. 从"待量化集合"里删掉 $q$，更新 $H^{-1}$，继续下一个

**精髓**：后面的权重会"**故意偏离最近格点**"，来补偿前面的错误。**局部次优，整体最优。**

**第三层：为什么需要 GPTQ——OBQ 太慢**

OBQ（GPTQ 的前身）还要**贪心地挑"当前误差最小的那个权重"先量化**。这个贪心要每步重排、每行的顺序都不同 → 复杂度 $O(d^4)$。对 175B 模型，**要算几年**。

**GPTQ 的三个工程近似**——这是论文真正的贡献：

1. **任意顺序都一样好（Arbitrary Order Insight）**：实验发现，**对大模型来说，贪心挑顺序 vs 固定从左到右，最终精度几乎没差**。为什么？因为大矩阵里权重数量巨大，贪心带来的那点优势被平均掉了。
   **一旦固定顺序 → 所有行共享同一个 $H^{-1}$**（因为它们共享同一个 $X$！）→ **所有行可以完全并行处理** → $O(d^4) \to O(d^3)$。**这是最大的一刀。**

2. **Lazy Batch-Updates**：更新 $H^{-1}$ 和权重的操作是**访存密集**的（读写整个大矩阵，但只做很少的计算——又是 memory-bound！）。解法：**攒够 128 列再批量更新一次全局**，中间只在这 128 列的局部块内更新。访存量降一个数量级。

3. **Cholesky 重构**：反复用 Gaussian 消元更新 $H^{-1}$ 会**累积浮点误差**，几千步之后 $H^{-1}$ **不再正定**，算法数值爆炸（这在大模型上是真实发生的）。解法：**一次性做 Cholesky 分解**，把需要的所有信息提前算好；再配一个 **dampening**（$H \mathrel{+}= \lambda \cdot \text{mean}(\text{diag}(H)) \cdot I$，$\lambda$ 取 1%），保证正定。

**结果**：175B 模型 **4 GPU-小时**、int4 接近无损、int3 可用。"

### 📐 完整推导

#### Step 1：目标函数与 Hessian

对权重矩阵的**每一行** $\mathbf{w} \in \mathbb{R}^{d}$（$d$ = in_features），设校准输入 $X \in \mathbb{R}^{d \times N}$：

$$E(\hat{\mathbf{w}}) = \|\mathbf{w}X - \hat{\mathbf{w}}X\|_2^2$$

对 $\hat{\mathbf{w}}$ 求二阶导：

$$H = \frac{\partial^2 E}{\partial \hat{\mathbf{w}}^2} = 2XX^\top \in \mathbb{R}^{d\times d}$$

> 🚨 **三个必须点破的关键**：
> 1. **$H$ 有解析解，不需要反向传播**——因为目标是二次的。这是 GPTQ 能这么快的根本原因。
> 2. **$H$ 只依赖 $X$，不依赖 $W$** → **同一层的所有行共享同一个 $H$**！这是"所有行并行"的数学基础。
> 3. **$H$ 的大小是 $[d, d]$**（in_features × in_features），对 4096 维就是 $4096^2$ ——**可以放进显存**。如果是端到端的 Hessian，那是 $[\text{参数量}]^2$，175B 的平方，宇宙都装不下。**逐层分解是让 Hessian 可用的前提。**

#### Step 2：OBS 的误差补偿公式

假设我们决定量化第 $q$ 个权重，$F$ 是**剩余未量化权重**的集合。

**(a) 量化 $q$ 造成的损失增量**：

$$\Delta E_q = \frac{\big(w_q - \text{quant}(w_q)\big)^2}{2\,[H_F^{-1}]_{qq}}$$

> **读懂这个式子**：分子是量化误差；分母 $[H^{-1}]_{qq}$ 越大，损失越小。$[H^{-1}]_{qq}$ 大意味着**这个方向上曲率小**——也就是"这个权重动一动，输出不太变"。**所以 OBQ 的贪心策略就是：优先量化那些"无关紧要"（曲率小）的权重。**

**(b) 对剩余权重的最优补偿**：

$$\boldsymbol{\delta}_F = -\frac{w_q - \text{quant}(w_q)}{[H_F^{-1}]_{qq}} \cdot [H_F^{-1}]_{:,q}$$

> **读懂这个式子**：$[H^{-1}]_{:,q}$ 这一列告诉我们"**权重 $q$ 的变动，和其他每个权重的变动，在输出上是怎么耦合的**"。沿着这个方向调整剩余权重，能**最大程度抵消** $q$ 的量化误差对输出的影响。

**(c) 更新 $H^{-1}$（删掉第 $q$ 行列）**：

$$H_{-q}^{-1} = \left(H^{-1} - \frac{1}{[H^{-1}]_{qq}} H^{-1}_{:,q} H^{-1}_{q,:}\right)_{-q}$$

这就是 **Gaussian 消元**——也是**数值误差累积的元凶**（做几千次之后 $H^{-1}$ 会失去正定性）。

#### Step 3：GPTQ 的三刀

| | OBQ | **GPTQ** | 收益 |
|---|---|---|---|
| **顺序** | 贪心（每行顺序不同） | **固定从左到右（所有行相同）** | 所有行共享 $H^{-1}$ → **并行** → $O(d^4)\to O(d^3)$ |
| **更新** | 每量化 1 个权重就全局更新 | **Lazy：攒 128 列批量更新** | 访存降一个数量级（**又是 memory-bound 的胜利**） |
| **数值** | 反复 Gaussian 消元 | **预先 Cholesky + dampening** | 从"会崩"变成"稳定" |

**为什么"固定顺序"没损失？** 论文的解释：**大模型的权重矩阵很大，贪心相对固定顺序的优势会被"平均掉"**。直觉上——如果你有 4096 个权重要量化，无论按什么顺序，"总的误差预算"是差不多的；贪心只是把误差**重新分配**了一下，在大数定律下差别不大。**这是个纯经验性的发现，也是论文最"取巧"的一步——但它是有效的。**

#### Step 4：伪代码（核心循环）

```python
def gptq_quantize_layer(W, X, blocksize=128, group_size=128, damp_percent=0.01):
    """
    W: [out_features, in_features]  —— 所有行一起处理（这就是 GPTQ 的并行性）
    X: [in_features, num_samples]   —— 校准数据在这一层的输入
    """
    d = W.shape[1]
    Q = torch.zeros_like(W)          # 量化结果
    E = torch.zeros_like(W)          # 误差缓存（Lazy Update 用）

    # ---- ① Hessian（注意：只算一次，所有行共享）----
    H = 2 * X @ X.T                          # [d, d]
    dead = torch.diag(H) == 0                # 从未被激活的输入维度
    H[dead, dead] = 1; W[:, dead] = 0        # 处理掉，避免除 0

    # ---- ② Dampening：保证正定 ----
    damp = damp_percent * torch.mean(torch.diag(H))
    H[range(d), range(d)] += damp

    # ---- ③ Cholesky：一次性拿到全部 H^{-1} 信息，避免迭代累积误差 ----
    H_inv = torch.cholesky_inverse(torch.linalg.cholesky(H))
    H_inv = torch.linalg.cholesky(H_inv, upper=True)   # 上三角，行 i 即第 i 步所需

    # ---- ④ 主循环：按列分块（block），块内逐列，块间 lazy ----
    for i1 in range(0, d, blocksize):
        i2 = min(i1 + blocksize, d)
        W_blk, E_blk = W[:, i1:i2].clone(), torch.zeros(W.shape[0], i2-i1)

        for j in range(i2 - i1):                       # 块内：逐列
            col = i1 + j
            w = W_blk[:, j]
            if col % group_size == 0:
                scale, zero = find_params(W[:, col:col+group_size])  # 每组重算 scale

            q = quantize(w, scale, zero)               # 量化这一列
            Q[:, col] = q

            d_jj = H_inv[col, col]
            err = (w - dequantize(q, scale, zero)) / d_jj   # 归一化误差

            # ★ 核心：把误差补偿到块内还没量化的列
            W_blk[:, j:] -= err.unsqueeze(1) @ H_inv[col, col:i2].unsqueeze(0)
            E_blk[:, j] = err

        # ★ Lazy Batch Update：这一块处理完，才批量更新块外的所有列
        W[:, i2:] -= E_blk @ H_inv[i1:i2, i2:]

    return Q
```

> **面试要能指着这段代码说出三处**：
> 1. `H = 2 * X @ X.T` —— **所有行共享**，这是并行的来源
> 2. `W_blk[:, j:] -= err @ H_inv[...]` —— **误差补偿**，OBS 的灵魂
> 3. `W[:, i2:] -= E_blk @ H_inv[...]` —— **Lazy Update**，128 列才做一次全局更新
>
> 这三行就是 GPTQ 的全部。

### 🔥 高频追问 Top 3

**Q：`act-order`（desc_act）是什么？为什么开了更准但更慢？**

A：**这是 GPTQ 最实用的一个 flag，也是很好的深度考点。**

**是什么**：不按 0,1,2,...,d 的自然顺序量化列，而是**按 $\text{diag}(H)$ 从大到小排序**——也就是**先量化"激活幅度最大 / 最重要"的列**。

**为什么更准**：**先量化的列享有最多的补偿预算**。想想那个循环——量化第 1 列时，后面 4095 列都还没定，可以一起帮它补偿；量化最后一列时，前面全定死了，**没人能帮它**。所以"**重要的先量化**"是对的。

**为什么更慢**：**打乱了 group 的连续性**。原本 group_size=128 意味着"第 0-127 列共享一个 scale"，这在显存里是连续的、kernel 一次读一个 scale 就行。act-order 之后，同一个 group 的列在**物理上是散的**，kernel 要**按 permutation 索引去 gather scale**——多一层间接寻址，**推理速度掉 10-30%**。

**实践**：
- 精度优先（模型小、掉点明显）→ **开**
- 速度优先（大 batch 服务）→ **关**
- 现在 Marlin 等新 kernel 已经能较好地处理 act-order 的开销，差距在缩小

**能主动说出"act-order 是拿推理速度换量化精度，因为它破坏了 group 的内存连续性"——这句话的信息密度很高，是很强的信号。**

**Q：GPTQ 是逐层独立优化的，误差会不会跨层累积？**

A：**会，这是 GPTQ（以及所有 layer-wise PTQ）的根本局限**。

问题：第 1 层量化后输出有误差 → 第 2 层的输入**已经不是原始的 $X$ 了** → 第 2 层拿"干净的 $X$"算的 Hessian 就**不匹配真实情况**。误差**层层放大**。

**实现层面的缓解（GPTQ 已经做了）**：**顺序量化 + 用量化后的输出做下一层的输入**。也就是：量化完第 1 层，用**量化后的第 1 层**跑一遍校准数据，拿到的输出作为第 2 层的 $X$。这样至少 Hessian 是"见过误差"的。

**更彻底的方案（后续工作）**：
- **BRECQ**：以 **block（几层一组）**为单位重构，而不是单层 —— 在"逐层（快但近视）"和"端到端（准但贵）"之间取中间点
- **QuIP / QuIP#**：用**随机正交变换**（incoherence processing）把权重和 Hessian 都"打散"，让离群方向消失，然后再量化 —— **这条路能做到 int2**
- **OmniQuant**：把 scale 和 clipping 阈值变成**可学习参数**，用少量梯度下降优化

**能说出"layer-wise 是个近似，误差会跨层累积，BRECQ/QuIP 是后续的改进方向"，说明你读的不只是 GPTQ 一篇。**

**Q：GPTQ 和 AWQ 到底选哪个？**

A：Q4 会详细对比，这里给结论：

| | GPTQ | AWQ |
|---|---|---|
| 核心思路 | **二阶重构**（改权重值补偿误差） | **等价缩放**（不改权重"含义"，只换表示） |
| 是否依赖校准集重构 | ✅ 强依赖 | ❌ 只搜一个标量 α |
| **overfit 校准集的风险** | **有** | **小** |
| 对 instruction-tuned / 多模态模型 | 可能伤泛化 | **更稳** |
| 量化速度 | 慢（要算 Hessian、逐列） | **快**（只做 grid search） |
| 精度（int4） | 接近 | 接近，**通常略好** |
| 推理速度 | 略慢（尤其 act-order） | **快**（TinyChat / Marlin kernel） |

**一句话**：**AWQ 是现在的默认推荐**，GPTQ 生态更老、支持面更广。但**别说"AWQ 全面碾压 GPTQ"**——在很多 benchmark 上两者差距在噪声范围内，**选型主要看 kernel 支持和你的校准数据质量**。

### ⚠️ 常见陷阱

1. **说 GPTQ 是"最小化权重误差"** —— **致命错误**。它最小化的是**层输出**误差 $\|WX - \hat WX\|$。那个 $X$ 是全部的灵魂。
2. **说不清"为什么 Hessian 能算"** —— 因为**目标是二次的**，$H = 2XX^\top$ 有解析解，不需要反向传播。而且是**逐层**的，所以 $H$ 只有 $[d,d]$ 大。
3. **漏掉三个工程近似** —— 只讲 OBS 等于只讲了 1993 年的东西。**GPTQ 的论文贡献恰恰是那三刀**（固定顺序 / Lazy / Cholesky）。
4. **不知道 Cholesky 是为了数值稳定** —— 很多人以为是为了加速。**是为了不崩。**
5. **不知道 act-order** —— 这是实际用 GPTQ 时的第一个 flag。

### 🏢 大厂偏好

- **字节 / 阿里（推理、量化团队）**：**必问，会深挖推导**，可能让你写误差补偿公式
- **算法岗**：会问 OBD → OBS → OBQ → GPTQ 的技术谱系
- **应用岗**：知道"目标是层输出误差 + 误差补偿 + 三个工程近似"这个三层结构就够了

### 📚 延伸阅读
- [GPTQ: Accurate Post-Training Quantization for Generative Pre-trained Transformers](https://arxiv.org/abs/2210.17323) - **原论文，本章最该精读**
- [Optimal Brain Compression (OBQ)](https://arxiv.org/abs/2208.11580) - GPTQ 的直接前身，误差补偿的推导在这里
- [Optimal Brain Surgeon (1992)](https://proceedings.neurips.cc/paper/1992/hash/303ed4c69846ab36c2904d3ba8573050-Abstract.html) - 三十年前的老祖宗
- [QuIP#](https://arxiv.org/abs/2402.04396) - int2 的代表作

---

## Q4：AWQ——为什么"激活感知"是对的 ⭐⭐⭐⭐⭐（必问）

### 🎯 一句话标答

> AWQ 的核心发现是：**只有约 1% 的权重是"显著（salient）"的，而判断显著性的正确标准不是权重大小，是<u>对应输入通道的激活幅度</u>**；由于混合精度对硬件不友好，AWQ 用**等价的 per-channel 缩放**（$Y = (W\cdot s)(X/s)$）把显著通道的权重**放大**——组内 $\max$ 不变则 scale 不变，而权重放大 $s$ 倍 ⇒ **相对量化误差降低 $s$ 倍**——从而在**不做任何重构、不用反向传播**的前提下逼近混合精度的效果。

### 🗣️ 30 秒口语版

"AWQ 的论文是一步一步"做实验做出来"的，按它的实验顺序讲最清楚。

**实验一：所有权重同等重要吗？**
拿一个 int4 量化的模型，**挑 1% 的权重保留成 FP16**，其余保持 int4，看 PPL。
- **随机挑 1%** → 几乎没改善（PPL 还是崩的）
- 结论：**权重不是同等重要的，但随机挑挑不到重要的**

**实验二：按权重大小挑？**
挑**绝对值最大的 1%** 保留 FP16 → **改善也很有限**。
- 结论：**"权重大 = 重要"是错的**！这个反直觉的发现是 AWQ 的起点。

**实验三：按激活幅度挑？**
看每个**输入通道**的平均激活幅度 $\overline{|X_j|}$，挑激活最大的 1% 个通道，把**对应的权重列**保留 FP16 → **PPL 几乎回到 FP16 水平**！
- 结论：**判断权重重要性的标准，是它乘的那个输入有多大**

**为什么？** 因为输出是 $y = \sum_j w_j x_j$。$w_j$ 的量化误差 $\epsilon_j$ 对输出的贡献是 $\epsilon_j \cdot x_j$——**误差被 $x_j$ 放大了**。所以 $x_j$ 大的那些 $j$，$w_j$ 必须准。**$w_j$ 自己有多大，跟这个完全无关。**

（顺便——这和 GPTQ 是**同一个洞察的两种表达**：GPTQ 的 $H = 2XX^\top$，对角线正是 $2\sum_n x_j^2$，**也是激活幅度**！两篇论文殊途同归，只是 GPTQ 用二阶展开严格推导，AWQ 用实验直接观察。**能点破这个联系是本章最漂亮的加分点。**）

**问题来了：混合精度不能用。**
1% FP16 + 99% INT4 意味着 kernel 要处理两种数据格式、要做 gather/scatter、要判断分支——**实际推理反而更慢**。硬件讨厌不规则。

**AWQ 的解法：用缩放代替混合精度。**

利用一个恒等式：
$$Y = W X = (W \cdot \text{diag}(s)) \cdot (\text{diag}(s)^{-1} X)$$

给显著通道的权重**乘 $s$（放大）**，同时把对应的激活**除以 $s$**——**数学上完全等价，输出不变**。

**为什么放大能降误差？** 回到 Q1 那个式子：量化误差 $\approx \Delta/2$，$\Delta = \frac{\max|W_{\text{group}}|}{2^{N-1}}$。
- 只放大**一小部分** channel（1%）→ **组内的 $\max$ 基本不变** → **$\Delta$ 不变**
- 但那个通道的权重变成 $s \cdot w$ → **相对误差 $\frac{\Delta/2}{s\cdot w}$ 降了 $s$ 倍** ✅

**免费的午餐**——同样的量化格点，重要的权重占据更"值钱"的位置。

**$s$ 怎么定？** 极简：
$$s = \big(\overline{|X_j|}\big)^{\alpha}, \qquad \alpha \in [0, 1]\ \text{网格搜索}$$

只有**一个标量超参 $\alpha$**，在校准集上搜 20 个点，选 MSE 最小的。**不需要反向传播、不需要 Hessian、不需要重构**。

**这就是 AWQ 相对 GPTQ 的最大优势**——因为它只搜一个标量、不动权重的"含义"，所以**几乎不可能 overfit 校准集**。GPTQ 是在校准集上做了几千次重构优化的，**换个数据分布就可能掉**。这在 **instruction-tuned 模型、多模态模型**上尤其明显——AWQ 论文专门做了这两组实验。"

### 📐 详细推导

#### 为什么"权重大 ≠ 重要"——一行数学

输出：$y = \sum_j w_j x_j$

量化后：$\hat{y} = \sum_j (w_j + \epsilon_j) x_j = y + \sum_j \epsilon_j x_j$

**输出误差** $= \sum_j \epsilon_j x_j$

看清楚：
- $\epsilon_j$ 的大小由 **scale** 决定（组内共享，跟 $w_j$ 自己多大**无关**）
- $\epsilon_j$ 对输出的影响被 **$x_j$ 放大**

$$\boxed{\text{某个权重的重要性} \propto |x_j|, \quad \text{与 } |w_j| \text{ 无关}}$$

> **这就是"激活感知（Activation-aware）"这个名字的全部含义**。三行数学，一个反直觉的结论——**这是我见过的 LLM 论文里"洞察密度"最高的之一**。

#### 缩放为什么有效——误差分析

设某个 group 内，原始权重的最大绝对值是 $M$，某个显著权重是 $w$。

**不缩放**：
$$\Delta = \frac{M}{2^{N-1}}, \qquad \text{Err}(w) \approx \frac{\Delta}{2} = \frac{M}{2^N}$$

**把 $w$ 放大 $s$ 倍（$s>1$），其他不变**：
$$\Delta' = \frac{\max(M,\, s\cdot w)}{2^{N-1}} \approx \frac{M}{2^{N-1}} = \Delta \quad (\text{只要 } s\cdot w \le M)$$

反量化回去时要除以 $s$，所以等效误差：
$$\text{Err}'(w) = \frac{\Delta'/2}{s} \approx \frac{1}{s}\cdot\frac{\Delta}{2}$$

$$\boxed{\text{相对误差降低了 } s \text{ 倍，而 } \Delta \text{ 几乎没变}}$$

**关键前提**：**只放大少数通道**。如果所有通道都放大 $s$ 倍，$M$ 也变成 $sM$，$\Delta$ 跟着变 $s\Delta$，**完全白干**。

> **这就是为什么 AWQ 必须"1%"** —— 稀疏性是免费午餐的来源。AWQ 论文里 $\alpha$ 的搜索也是在找这个平衡：$\alpha$ 太大 → 放大太多通道 → $M$ 被拉高 → 收益归零；$\alpha=0$ → 不缩放 → 退化成 RTN。

#### $\alpha$ 的搜索

$$s_j = \big(\overline{|X_j|}\big)^\alpha, \qquad \alpha^* = \arg\min_{\alpha\in[0,1]} \ \big\|W X - Q\big(W\cdot\text{diag}(s)\big)\cdot \text{diag}(s)^{-1} X\big\|$$

```python
def search_scale(W, X, n_grid=20):
    """AWQ 的全部'训练'就是这 10 行"""
    x_mean = X.abs().mean(dim=1)              # [in_features] 每个输入通道的平均激活
    y_ref = W @ X                             # FP16 参考输出
    best_loss, best_s = float('inf'), None

    for i in range(n_grid):
        alpha = i / n_grid                    # 网格搜索 α ∈ [0, 1)
        s = x_mean.pow(alpha).clamp(min=1e-4)
        s = s / (s.max() * s.min()).sqrt()    # 归一化，避免整体漂移

        W_scaled = W * s.unsqueeze(0)         # ← 放大显著通道的权重
        W_q = fake_quantize(W_scaled)         # 常规的 per-group RTN 量化
        y_hat = (W_q / s.unsqueeze(0)) @ X    # 反缩放回来

        loss = (y_ref - y_hat).pow(2).mean()
        if loss < best_loss:
            best_loss, best_s = loss, s
    return best_s
```

> **注意这段代码里没有的东西**：没有 `backward()`、没有 `optimizer`、没有 Hessian、没有逐列循环。**AWQ 的"优化"就是一个 20 次的 for 循环**。这就是它快、且不 overfit 的原因。
>
> 还有一个工程细节：`s` 最终会被 **fuse 到前一个算子里**（比如 LayerNorm 的 weight，或者上一个 Linear 的输出），所以**推理时的除法是零开销的**——这也是 AWQ kernel 快的原因之一。

### 🔍 AWQ vs GPTQ vs SmoothQuant——三者的关系

**这张表是本章的枢纽，务必吃透**：

| | GPTQ | AWQ | SmoothQuant |
|---|---|---|---|
| **数学工具** | 二阶 Hessian 重构 | **per-channel 缩放** | **per-channel 缩放** |
| **目标** | 权重 int4 精度 | **权重** int4 精度 | **激活** int8 可行性 |
| **缩放方向** | — | **权重 ×s（放大重要的）** | **激活 ÷s（压平离群的）** |
| 是否改权重值 | ✅ 改（补偿） | ❌ **不改语义**（等价变换） | ❌ 不改语义 |
| 需要反向传播 | ❌ | ❌ | ❌ |
| overfit 风险 | **有** | 小 | 小 |
| 典型配置 | W4A16 | W4A16 | **W8A8** |

> 🚨 **必须点破的联系**：**AWQ 和 SmoothQuant 的数学形式几乎一模一样**——都是 $Y = (W\cdot s)(X/s)$ 这个恒等式，都是 per-channel scaling。
>
> **但目标完全相反**：
> - **SmoothQuant**：$s$ 用来**除激活**，把激活的离群值压平 → 让**激活**能被 int8 量化。代价是权重变难一点（权重有余量，扛得住）。
> - **AWQ**：$s$ 用来**乘权重**，把重要权重放大 → 让**权重**的 int4 误差更小。
>
> **同一把刀，一个用来救激活，一个用来救权重。** 这个对比讲出来，面试官会知道你是真的把这三篇论文读通了，而不是分别背了三段。

### 🔥 高频追问 Top 3

**Q：AWQ 和 GPTQ 的洞察，本质上是同一个吗？**

A：**是的，这是本章最深的一层，答出来是强烈的信号。**

- **GPTQ**：$H = 2XX^\top$，Hessian 的**对角线** $H_{jj} = 2\sum_n x_{jn}^2$ —— 这**正是第 $j$ 个输入通道的激活能量**！$H_{jj}$ 大 ⇒ 这个方向曲率大 ⇒ 权重 $j$ 必须准。
- **AWQ**：直接用 $\overline{|x_j|}$ 衡量重要性。

**两者衡量的是同一个东西**——只不过 GPTQ 用二阶泰勒展开**严格地**推出来（还额外用了 $H$ 的**非对角元**做误差补偿），AWQ 用实验**直接观察**到并选择了一个**更简单的利用方式**。

**关键差异在于"怎么用这个信息"**：
- GPTQ：用它来**修改权重的值**（补偿）→ 强大，但**可能过拟合校准集**
- AWQ：用它来**选一个缩放**（等价变换，不改语义）→ 温和，**泛化更好**

**这个对比的启发**："**更强的方法不一定更好**"——GPTQ 用了更多信息（整个 Hessian，包括非对角），AWQ 只用了对角信息，但 AWQ 在很多场景反而更稳。**因为多用的那部分信息是从 128 条校准数据里估出来的，噪声很大。** 这是很好的 ML 泛化直觉。

**Q：AWQ 的 scale 会不会把激活的离群值放大，反而搞坏了？**

A：**好问题，正好戳到 AWQ 和 SmoothQuant 的方向冲突。**

AWQ 是 **W4A16**——**激活保持 FP16，根本不量化**。所以"把激活除以 $s$"不会引入任何量化问题，FP16 的动态范围足够大。**AWQ 完全不关心激活的可量化性。**

但如果你想做 **W4A8**（激活也要量化），那 AWQ 和 SmoothQuant 就**打架**了：
- AWQ 想放大权重、缩小激活
- SmoothQuant 想缩小激活的离群值……**方向其实一致**！

**所以它们其实可以叠加** —— 后续工作（如 QServe/QoQ、Atom）就在做 W4A8 的联合优化：一个统一的 per-channel scale，同时兼顾"权重 int4 误差"和"激活 int8 可行性"，用一个更复杂的目标去搜。

**能说到"W4A8 是 AWQ 和 SmoothQuant 的合流方向"，是很好的前沿感知。**

**Q：为什么 AWQ 对多模态和 instruction-tuned 模型更友好？**

A：**因为校准集的分布问题。**

- **多模态模型**：校准数据里的 image token 和 text token 分布**差异巨大**。GPTQ 在这上面做重构，容易**偏向某一种模态**（取决于校准集里哪种多）。
- **Instruction-tuned 模型**：能力是"**通用**"的（写作、数学、代码、多轮对话）。用 WikiText 校准 + GPTQ 重构 → 权重被**往百科文本的方向"拧"了一下** → **代码和数学能力掉得比 PPL 显示的多**。
- **AWQ**：只搜一个标量 $\alpha$。它**没有能力**去"记住"校准集的细节——**表达能力的匮乏在这里反而是优点（正则化）**。

这是个很经典的 **bias-variance 权衡**：GPTQ 是低 bias 高 variance（能拟合但会过拟合），AWQ 是高 bias 低 variance（拟合能力弱但稳）。在**校准数据只有 128 条**的情况下，**低 variance 往往赢**。

### ⚠️ 常见陷阱

1. **说 AWQ 保留 1% 权重为 FP16** —— **错！那只是论文的<u>动机实验</u>**。AWQ 的最终方案是**纯 int4 + 缩放**，**没有任何混合精度**。这是最高频的错误理解。
2. **说不清"为什么放大能降误差"** —— 必须点出"**只放大少数通道 ⇒ 组内 max 不变 ⇒ scale 不变 ⇒ 相对误差降 s 倍**"。少一环就不成立。
3. **把 AWQ 和 SmoothQuant 混为一谈** —— 数学形式一样，**目标相反**（一个救权重、一个救激活）。
4. **不知道 AWQ 只有一个超参** —— "AWQ 的全部训练是一个 20 次 for 循环"这句话很有冲击力。

### 🏢 大厂偏好

- **字节 / 阿里**：**必问**，且喜欢问"为什么权重大小不是判断标准"
- **应用岗**：会问"上线该选 GPTQ 还是 AWQ"——答"AWQ 泛化更好、kernel 更快，但看你的 engine 支持"
- **多模态团队**：会问 AWQ 在 VLM 上的表现（AWQ 论文有专门实验）

### 📚 延伸阅读
- [AWQ: Activation-aware Weight Quantization](https://arxiv.org/abs/2306.00978) - **MLSys 2024 Best Paper，本章必读**
- [QServe / QoQ](https://arxiv.org/abs/2405.04532) - W4A8KV4，AWQ 团队的后续，三者合流

---

## Q5：激活 outlier 与 SmoothQuant ⭐⭐⭐⭐⭐（必问）

### 🎯 一句话标答

> 超过 **~6.7B** 参数后，LLM 会**涌现（emergent）**出激活离群值：**极少数固定的 channel**，幅度是其他 channel 的 **~100 倍**，导致 per-tensor 激活量化直接崩溃；**LLM.int8()** 用混合精度分解绕过（慢），**SmoothQuant** 则用 per-channel 等价缩放 $Y=(X/s)(sW)$ 把**量化难度从激活迁移到权重**（权重有余量扛得住），从而让硬件友好的 **W8A8 per-tensor** 成为可能。

### 🗣️ 30 秒口语版

"这题要先讲清 **outlier 这个现象本身**，它比 SmoothQuant 更重要——**它是激活量化一切困难的根源**。

**现象（Dettmers 在 LLM.int8() 里最早系统性发现的）**：
- 模型**小于 ~6.7B** 时，激活分布很正常，per-tensor int8 量化基本无损
- **越过 6.7B 这个坎**，突然出现一批 **outlier feature**：
  - 幅度是正常 channel 的 **~100 倍**
  - 只占 **0.1% 左右的 channel**
  - **系统性（systematic）**：**永远集中在那几个固定的 channel 上**，跟输入是什么无关
  - 出现在**所有层**
- 而且是**涌现的**——不是随规模平滑变化，是在某个规模**突然出现**的相变

**这些 outlier 有什么用？** 后续研究（比如 attention sink 相关工作）发现，它们和"**模型需要一个'什么都不关注'的默认出口**"有关——是模型自己学出来的一种机制。**所以不能简单地把它们裁掉，它们承载了真实的功能。**

**为什么它毁掉 per-tensor 量化？**
per-tensor 的 scale 由**全局最大值**决定。一个 100 倍的离群值 → scale 被拉大 100 倍 → **99.9% 的正常激活全部被 round 到 0 或 ±1**。int8 的 256 个格点，**只有几个被用上**。模型直接废掉。

**方案一：LLM.int8()（Dettmers, 2022）—— 绕开**
既然只有 0.1% 的 channel 有问题，那就**把它们挑出来单独用 FP16 算**，剩下 99.9% 走 int8：
$$Y = \underbrace{X_{\text{outlier}} W_{\text{outlier}}}_{\text{FP16}} + \underbrace{X_{\text{normal}} W_{\text{normal}}}_{\text{INT8}}$$
- ✅ 精度**几乎完全无损**，是当时的救命稻草
- ❌ **慢**——要在线检测 outlier、要拆分矩阵、要跑两个 kernel 再相加。**实测甚至可能比 FP16 还慢**。它是"能省显存"，不是"能加速"。

**方案二：SmoothQuant（Xiao et al., ICML 2023）—— 迁移**

核心洞察：**权重很好量化（分布平滑），激活很难量化（有 outlier）。那能不能把难度<u>从激活转移一部分到权重</u>？**

用和 AWQ 一样的恒等式：
$$Y = XW = \big(X \cdot \text{diag}(s)^{-1}\big)\big(\text{diag}(s) \cdot W\big)$$

给 outlier channel 一个**大的 $s$**：
- 激活 $X_j / s_j$ → **被压平了**，outlier 消失 ✅
- 权重 $s_j \cdot W_j$ → 变陡了一点 ❌ ——**但权重原本很平滑、有大量余量，扛得住**

**$s$ 怎么定**——一个漂亮的"迁移强度旋钮"：
$$s_j = \frac{\big(\max|X_j|\big)^{\alpha}}{\big(\max|W_j|\big)^{1-\alpha}}$$
- $\alpha = 0$：完全不迁移（等于没做）
- $\alpha = 1$：把激活压得完全平，**但权重会被搞崩**
- $\alpha = 0.5$：**两边难度均分**——大部分模型的甜点
- OPT-175B 这种 outlier 极端的，要调到 **0.75**（多往权重推一点）

**结果**：**W8A8 per-tensor 变得可行** → 能直接用 **INT8 Tensor Core** → **真正的算力加速**（不只是省显存）。论文报告 **1.56× 加速 + 2× 显存节省，精度几乎无损**。

**和 AWQ 的关系再强调一次**：同一个数学恒等式，**SmoothQuant 把难度从激活推给权重，AWQ 把权重放大来降低相对误差**。一把刀，两个用法。"

### 📐 详细分析

#### Outlier 的三个特征（必须能背）

| 特征 | 描述 | 后果 |
|---|---|---|
| **幅度极大** | ~100× 正常值 | per-tensor scale 被拉爆 |
| **极度稀疏** | ~0.1% 的 channel | 混合精度可行（LLM.int8()） |
| **系统性 / 固定** | **永远是那几个 channel** | ✅ **per-channel 缩放可行**（SmoothQuant 的前提！） |

> 🚨 **第三条是 SmoothQuant 成立的全部前提，务必点出来。**
>
> 如果 outlier 是**随机跳的**（这个 token 在 channel 5，下个 token 在 channel 900），那 $s$ 就必须**每步在线重算**——开销巨大，SmoothQuant 就没意义了。
>
> **正因为 outlier 固定在特定 channel**，我们才能**离线**用 128 条校准数据统计出 $\max|X_j|$，算好一个 $s$，然后**永久 fuse 到前一层的权重里**（比如 LayerNorm 的 weight）——**运行时零开销**。
>
> **"SmoothQuant 之所以能离线做，是因为 outlier 是 systematic 的"** —— 这句话把现象和方法连起来了，是这题的题眼。

#### 为什么激活难、权重易——一张分布图

```
【权重分布】—— 平滑、有界、类高斯
        ▁▂▃▅▇█▇▅▃▂▁
     ───┼───┼───┼───┼───►
       -0.1  0  0.1
     int8 的 256 个格点均匀铺开 → 每个格点都有货 ✅

【激活分布】—— 绝大部分挤在 0 附近，几个离群值飞在天上
     █▇▅▃▂▁                                    ▁
     ───┼───┼───┼───┼───┼───┼───┼───┼───┼───┼──►
        0   1   2                              100
                                                ↑
                                          outlier channel
     scale 由 100 决定 → 0~2 的所有值全部 round 到 0 或 1 ❌
     256 个格点，实际只用上了 2 个

【SmoothQuant 之后】—— 把 outlier channel 除以 s=50
     ▁▂▃▅▇█▇▅▃▂▁▁▁
     ───┼───┼───┼───┼──►
        0   1   2
     格点重新被用满 ✅   （代价：对应的权重列乘了 50，但权重有余量）
```

#### $\alpha$ 的作用——迁移强度

| $\alpha$ | 激活的量化难度 | 权重的量化难度 | 适用 |
|---|---|---|---|
| 0.0 | 极难 ❌ | 易 ✅ | 等于没做 |
| **0.5** | 中 | 中 | **大部分模型的默认** |
| **0.75** | 易 | 较难 | **OPT-175B 等 outlier 极端的模型** |
| 1.0 | 易 ✅ | 极难 ❌ | 权重被搞崩，不可用 |

```python
def smooth_quant_scale(act_max, weight_max, alpha=0.5):
    """
    act_max:    [in_features]  校准集上每个输入通道的 |X| 最大值
    weight_max: [in_features]  每个输入通道对应的 |W| 最大值
    """
    s = act_max.pow(alpha) / weight_max.pow(1 - alpha)
    return s.clamp(min=1e-5)

# 使用：数学上完全等价，但两边的量化难度被重新分配了
s = smooth_quant_scale(act_max, W.abs().amax(dim=0), alpha=0.5)
X_smooth = X / s          # ← 这个除法最终 fuse 进上一层（LayerNorm/Linear）→ 零开销
W_smooth = W * s.unsqueeze(0)
# 现在 X_smooth 和 W_smooth 都能舒服地量化到 int8
```

### 🔥 高频追问 Top 3

**Q：为什么 outlier 会"涌现"？它到底是什么？**

A：**这是个开放问题，但有几条有说服力的线索，能讲出来是很深的信号**。

1. **和 attention sink 强相关**：第 12 章 Q2 提过 StreamingLLM 发现的 attention sink——开头的 token（尤其是 BOS）拿走大量注意力。研究发现，**制造 attention sink 的机制，正是靠这些 outlier channel 的极大值**：softmax 里一个极大的 logit 会吸走全部概率。
2. **"什么都不关注"的出口**：Transformer 的 softmax **强制概率和为 1**——模型**没法说"这一层我不想关注任何东西"**。于是它学会了一个 hack：**把注意力全倒给一个语义无关的 token**（通常是 BOS），等于变相实现了 "no-op"。而实现这个 hack 的手段，就是让某几个 channel 的值极大。
3. **旁证：改了 softmax 就没了**——有工作提出 **Softmax-1**（分母加 1，即 `softmax1(x) = exp(x)/(1+Σexp(x))`），允许"全部不关注"，**结果 outlier 显著减少**。这是对上面假说的强支持。也有工作用 **gated attention / attention bias** 达到类似效果。

**所以 outlier 不是 bug，是模型在架构约束下的<u>适应性产物</u>。** 这也解释了为什么**不能简单裁掉它们**——裁掉 = 破坏了模型的 no-op 机制。

> 💡 这正好呼应你笔记里反复出现的那条主线：**"涌现 vs 设计"** —— MoE 的专家专业化、长期记忆的识别、这里的 outlier，**都是训练目标逼出来的，不是设计出来的**。面试时把这条线串起来讲，会显得你的知识是有骨架的。

**Q：SmoothQuant 和 AWQ 到底什么关系？能一起用吗？**

A：**数学形式一样，目标正交，可以（也应该）一起用。**

| | SmoothQuant | AWQ |
|---|---|---|
| 恒等式 | $Y = (X/s)(sW)$ | $Y = (X/s)(sW)$ **← 一模一样** |
| $s$ 的设计目标 | 让 **$X/s$** 好量化（压平 outlier） | 让 **$sW$** 的重要列相对误差小（放大显著权重） |
| 救的是 | **激活** | **权重** |
| 配套位宽 | W8A8 | W4A16 |

**方向其实是一致的**（都是"激活除以 s、权重乘以 s"），只是**优化目标不同**。所以做 **W4A8** 时，可以设计一个**统一的 $s$，同时满足两个目标**——这就是 **QServe/QoQ、Atom** 这些工作在做的事。

**面试时的高阶答法**："SmoothQuant 和 AWQ 是**同一个技术工具（per-channel 等价缩放）在不同瓶颈下的两次应用**。它们的合流就是 W4A8。" —— 这句话能把三篇论文一句话串起来。

**Q：FP8 是不是能直接绕开 outlier 问题？**

A：**很大程度上能，这也是 H100 时代的答案。**

**为什么**：outlier 的问题本质是**动态范围**——int8 是**均匀格点**，256 个点必须平铺在 $[-100, 100]$ 上，那么 $[-1,1]$ 区间只分到 2 个点。而 **FP8（E4M3）是浮点**——**格点是指数分布的**，在 0 附近**极密**，在远处稀疏。这恰好匹配"绝大部分值在 0 附近 + 少数离群值"的分布。

$$\text{E4M3 动态范围} \approx \pm 448, \quad \text{但 0 附近的分辨率远高于 int8}$$

**所以**：
- **FP8 几乎不需要 SmoothQuant** —— 可以直接 per-tensor 量化，精度就够
- **H100 / Ada 有原生 FP8 Tensor Core**，算力是 FP16 的 2 倍
- **DeepSeek-V3 甚至用 FP8 来<u>训练</u>** —— 这在两年前是不可想象的

**但要说清代价**：
- **只有 Hopper/Ada 及以后**支持。A100 **没有 FP8**，只能走 int8 → **在 A100 上 SmoothQuant 仍然是刚需**
- FP8 的**精度（尾数位）比 int8 少**——E4M3 只有 3 位尾数，int8 有 8 位。所以在**动态范围窄、需要高精度**的场景（比如某些层的权重），int8 反而更准
- 所以是"**FP8 用范围换精度**"，对激活（范围宽）是赚的，对权重（范围窄）不一定

**一句话总结**：**int8 + SmoothQuant 是"用软件补硬件的短板"，FP8 是"用硬件直接解决问题"。** 有 H100 就用 FP8，只有 A100 就老老实实 SmoothQuant。

### ⚠️ 常见陷阱

1. **不知道 outlier 是"系统性/固定 channel"的** —— 这是 SmoothQuant 能**离线**做的全部前提。不提这条，你的解释就不成立。
2. **说 LLM.int8() 能加速** —— **不能**！它省显存，但因为要拆分矩阵跑两个 kernel，**速度可能比 FP16 还慢**。这是高频陷阱。
3. **把 SmoothQuant 说成"量化算法"** —— 它是**量化前的预处理 / 等价变换**，本身不做量化。做完 smooth 之后还要跑常规的 int8 量化。
4. **忘记 6.7B 这个涌现的坎** —— 这个具体数字来自 LLM.int8() 论文，答出来显得读过原文。
5. **不知道 $s$ 是 fuse 进上一层的** —— 如果以为运行时要真做一次除法，那 SmoothQuant 就没有加速意义了。

### 🏢 大厂偏好

- **所有大厂**：outlier 现象**必问**，SmoothQuant 高频
- **Meta**：LLM.int8() 是 Meta（Dettmers 在 UW 但和 Meta 深度合作）的工作，会深挖
- **有 H100 的团队**：会引向 FP8，问"那你还需要 SmoothQuant 吗"
- **DeepSeek**：会问 FP8 训练（V3 的核心工程贡献之一）

### 📚 延伸阅读
- [LLM.int8()](https://arxiv.org/abs/2208.07339) - **outlier 涌现现象的原始发现，本章必读**
- [SmoothQuant](https://arxiv.org/abs/2211.10438) - ICML 2023
- [Attention Is Off By One](https://www.evanmiller.org/attention-is-off-by-one.html) - Softmax-1 的提出，解释 outlier 成因的经典博客
- [FP8 Formats for Deep Learning](https://arxiv.org/abs/2209.05433) - NVIDIA 的 FP8 标准

---

## Q6：W4A16 vs W8A8 vs FP8——怎么选 ⭐⭐⭐⭐⭐（必问·实战）

### 🎯 一句话标答

> **W4A16（GPTQ/AWQ）只优化访存不优化算力**，适合**小 batch / 低并发 / 显存紧张 / 端侧**；**W8A8（SmoothQuant）能用 INT8 Tensor Core，真正省算力**，适合**大 batch 高吞吐服务**；**FP8** 在 H100/Ada 上是**当前甜点**——天然抗 outlier、标定简单、算力翻倍。**选型的第一性原理来自第 12 章的 Roofline：你的场景是 memory-bound 还是 compute-bound。**

### 🗣️ 30 秒口语版

"这题是量化的**终极实战题**，答案必须回到第 12 章的 Roofline，**不能靠背结论**。

**先建立判据**：
- **memory-bound**（小 batch decode）→ 瓶颈是**搬权重** → **压权重最有效** → **W4A16**
- **compute-bound**（prefill、大 batch decode）→ 瓶颈是**算** → **要能用低精度 Tensor Core** → **W8A8 / FP8**

**W4A16（GPTQ / AWQ）**
- 权重 int4，**激活保持 fp16**
- kernel 干什么：**先把 int4 反量化回 fp16，再做 fp16 GEMM**
- ⇒ **计算量一点没少，反而多了反量化的开销**
- ✅ 权重小 4 倍 → **decode 快 2-3 倍**（memory-bound 下），显存省 4 倍
- ❌ **prefill 变慢**（本来就 compute-bound，你还多加一步反量化）
- ❌ **大 batch 下优势衰减甚至倒挂**
- **适合**：端侧、单用户、显存装不下、低并发

**W8A8（SmoothQuant）**
- 权重和激活**都 int8** → **能直接喂 INT8 Tensor Core**
- ✅ **真·算力加速**：A100 的 INT8 算力是 FP16 的 **2 倍**（624 vs 312 TOPS）
- ✅ prefill 和 decode **都受益**
- ❌ 压缩只有 **2 倍**（不如 int4）
- ❌ **必须处理 outlier**（要 SmoothQuant 预处理），标定更麻烦
- **适合**：大 batch 高吞吐服务，显存不是主要矛盾

**FP8（E4M3 / E5M2）—— H100 时代的答案**
- ✅ **天然抗 outlier**（浮点格点非均匀，见 Q5 追问 3）→ **标定极简**，很多时候 per-tensor 直接量化就行
- ✅ H100 原生支持，**算力是 FP16 的 2 倍**
- ✅ 精度损失通常**比 int8 更小**
- ❌ **A100 及以前不支持**（这是最大的现实约束）
- ❌ 压缩仍是 2 倍
- **适合**：**有 H100/Ada 就闭眼选它**

**再往上还有组合拳**：**W4A8**（QServe/QoQ）—— 想同时拿到"权重压 4 倍"和"用 INT8 Tensor Core"。这是当前的前沿。

**最实用的一句话（面试请说这句）**：
> "**W4A16 是给显存受限的场景用的，W8A8/FP8 是给吞吐受限的场景用的。我会先测线上真实的 batch 分布——如果平均 batch < 8，选 AWQ；如果 batch 稳定 > 32，选 FP8（有 H100）或 SmoothQuant W8A8（A100）；如果显存是硬约束（比如要在单卡上跑 70B），那没得选，只能 W4A16。**"

这个回答体现了**"先看瓶颈再选方案"**的工程素养，比背任何论文都值钱。"

### 🔍 完整选型表

| | **W4A16** (GPTQ/AWQ) | **W8A8-INT** (SmoothQuant) | **W8A8-FP8** | W4A8 (QoQ) |
|---|---|---|---|---|
| 权重压缩 | **4×** ✅ | 2× | 2× | **4×** ✅ |
| 算力加速 | ❌ **无**（还有反量化开销） | ✅ **2×** (INT8 TC) | ✅ **2×** (FP8 TC) | ✅ 2× |
| Prefill | ❌ **变慢** | ✅ 快 | ✅ 快 | ✅ 快 |
| Decode (batch=1) | ✅ **快 2-3×** | 快 ~1.5× | 快 ~1.5× | ✅ **最快** |
| Decode (batch=64) | ⚠️ 优势消失 | ✅ 快 | ✅ 快 | ✅ 快 |
| Outlier 处理 | 不需要（激活 fp16） | **必须**（SmoothQuant） | 基本不用 | 需要 |
| 标定难度 | 中 | **高** | **低** ✅ | 高 |
| 硬件要求 | 任意 | 任意（INT8 TC） | **H100 / Ada / L40S** | H100 |
| 精度损失 | 小 | 小-中 | **最小** ✅ | 中 |
| **典型场景** | **端侧 / 单用户 / 显存紧** | **A100 高吞吐服务** | **H100 高吞吐服务** | 前沿 |

### 📐 为什么 W4A16 在大 batch 下会倒挂——回到 Roofline

设权重 $P$ 个参数，batch = $B$。

| | 访存（读权重） | 计算 | 算术强度 |
|---|---|---|---|
| FP16 | $2P$ 字节 | $2PB$ FLOP | $B$ |
| **W4A16** | $0.5P$ 字节 | $2PB$ FLOP **+ 反量化开销** | **$4B$** |
| W8A8 | $1P$ 字节 | $2PB$ **INT8** OP（算力翻倍） | $2B$ |

**W4A16 的算术强度是 $4B$** —— 也就是说，**它只需要 $B = 150/4 \approx 38$ 就撞上 A100 的平衡点**，而 FP16 要 $B=150$。

**推论（这是本题的核心洞察）**：
- $B < 38$：**W4A16 仍是 memory-bound → 4 倍访存优势全部兑现** ✅
- $B > 38$：**W4A16 已经变成 compute-bound → 访存优势失效，只剩反量化的额外开销** → **可能比 FP16 还慢** ❌
- 而 **W8A8 在 compute-bound 区间依然有 2× 的算力优势** ✅

```
吞吐 (tok/s)
   ▲
   │                    ┌───────  W8A8 (算力翻倍，一路领先)
   │              ┌─────┘
   │        ┌─────┘  ┌─────────  FP16
   │   ┌────┘   ┌────┘
   │ ┌─┘  ┌─────┘
   │ │┌───┘ ╲
   │ ││      ╲╲╲╲╲╲╲  W4A16 (小 batch 王者，大 batch 掉队)
   │ ││
   └─┴┴──────┬────────────────────────► batch
     1       ~38
             ↑
        W4A16 的临界点：过了这里，4× 访存优势兑不了现
```

> **这张图能画出来 + 解释清楚，这题就满分了。** 它把第 12 章的 Roofline 和第 13 章的量化选型**焊在了一起**——这正是"知识成体系"的样子。

### 🔥 高频追问 Top 3

**Q：Marlin kernel 是什么？它改变了 W4A16 的结论吗？**

A：**部分改变了，这是个很新的考点。**

**问题**：朴素的 W4A16 kernel 在大 batch 下很烂——因为反量化是**串行**在 GEMM 前面的，而且反量化本身要读 scale、做位运算、shuffle，开销固定。

**Marlin（IST Austria，现已进 vLLM）的做法**：
- 把**反量化和 GEMM 深度融合**，反量化在 **tensor core 的 pipeline 里**做，和计算 overlap
- 精细的 **async copy + double buffering**，让访存和计算完全流水
- 结果：**在 batch 16-64 的区间仍能保持接近 4× 的加速**（朴素 kernel 在这个区间已经掉到 1.x× 了）

**结论修正**：
- 朴素 W4A16：$B > 8$ 优势就开始掉
- **Marlin W4A16**：$B$ 到 **32-64** 都还能打

**但物理定律没变**——batch 再大（>128），W4A16 终究是 compute-bound，**算力上限还是 fp16 的算力**，而 W8A8 有 2×。

**面试的正确表述**："**Marlin 把 W4A16 的适用 batch 区间从 <8 拓宽到 <64，但没有改变'W4A16 不省算力'这个根本事实。**" —— 这个回答既跟上了工程现状，又守住了第一性原理。**很加分。**

**Q：为什么 W4A4 不流行？**

A：**因为激活到 int4 就崩了。**

回到 Q5 的 outlier：激活的动态范围极宽，int8 的 256 个格点配上 SmoothQuant 才勉强够。**int4 只有 16 个格点** —— 就算把 outlier 压平了，正常激活的分辨率也不够了。

有工作在尝试（Atom、QuaRot、SpinQuant），思路是**旋转（Hadamard 变换）**：用随机正交矩阵把激活"打散"，**让能量在所有 channel 上均匀分布，outlier 被稀释掉**。这和 QuIP 的 incoherence processing 是同一个思路。

$$Y = XW = (XQ)(Q^\top W), \quad Q \text{ 是正交矩阵（Hadamard）}$$

**又是同一个恒等式的变奏**——只不过这次 $s$ 从对角矩阵变成了正交矩阵！**per-channel scaling 是"对角变换"，Hadamard 旋转是"稠密变换"，后者打散 outlier 的能力更强。**

现状：W4A4 掉点仍明显，**未大规模上生产**。但这是活跃前沿。

> 💡 到这里你应该看出本章的**元结构**了：**AWQ、SmoothQuant、QuIP、QuaRot —— 全都是"找一个等价变换 $T$，使得 $W' = TW$、$X' = T^{-1}X$ 更好量化"**。区别只在 $T$ 是什么（对角 / 正交）和优化目标是什么（救权重 / 救激活）。**面试时能把这句话说出来，等于把整个量化领域用一句话概括了。**

**Q：量化和 MoE 一起用有什么特别的？**

A：**有，而且很关键**——这是很好的组合考点。

1. **MoE 的显存瓶颈更严重**：DeepSeek-V3 有 671B 总参数（37B 激活）。**显存必须装下全部 671B**，虽然每次只算 37B。→ **MoE 是量化的最大受益者**，因为它的痛点就是"参数多、显存装不下"。
2. **专家的量化敏感度不同**：**被路由到得多的"热门专家"更重要**，量化误差影响更大；冷门专家可以压得更狠。→ **混合位宽 MoE**（热专家 int8、冷专家 int4）是个自然的想法，有工作在做。
3. **共享专家要更谨慎**：DeepSeek 的架构里有 shared expert（**每个 token 都过**）—— 它的误差会影响**所有** token，**必须保精度**。
4. **校准数据要覆盖全部专家**：如果校准集只有 128 条数据，**冷门专家可能一次都没被激活到** → Hessian 是全 0 → GPTQ 直接崩（代码里 `dead = diag(H)==0` 那行就是在处理这个）。**MoE 的校准集要比 dense 大得多，且要保证专家覆盖率。**

**第 4 点很少有人想到，答出来非常加分**——它是"真的量化过 MoE 模型"才会踩到的坑。

### ⚠️ 常见陷阱

1. **说"int4 比 int8 快"** —— **只在小 batch 下成立**。大 batch 下 int8 因为能用 Tensor Core 反而更快。
2. **说 W4A16 能加速 prefill** —— **反了，会变慢**。
3. **不区分"省显存"和"省算力"** —— 这是这题的全部。W4A16 省显存，W8A8 省算力。
4. **在 A100 上推荐 FP8** —— A100 **没有 FP8 Tensor Core**。硬件常识错误很致命。
5. **不问业务场景就给结论** —— "选哪个量化"这题**没有标准答案**，正确的回答是**先反问 batch 分布和显存约束**。

### 🏢 大厂偏好

- **所有大厂的应用/Infra 岗**：**这是量化的压轴题**，且高度实战
- **字节 / 阿里**：会给具体场景（"我们线上平均 batch=4，显存 40G"）让你选
- **DeepSeek**：会引向 MoE + FP8
- **端侧团队**：只关心 W4A16 和更极端的方案（GGUF k-quants）

### 📚 延伸阅读
- [Marlin Kernel](https://github.com/IST-DASLab/marlin) - W4A16 的极致 kernel
- [QuaRot](https://arxiv.org/abs/2404.00456) - Hadamard 旋转做 W4A4
- [QServe (W4A8KV4)](https://arxiv.org/abs/2405.04532)

---

## Q7：KV Cache 量化 ⭐⭐⭐⭐

### 🎯 一句话标答

> 第 12 章证明了 **KV Cache 决定并发数、并发数决定吞吐**——所以把 KV 从 fp16 压到 **fp8 就能直接让并发翻倍**；难点在于 **K 和 V 的分布不同**：**Key 有 per-channel 的离群值（和激活 outlier 同源），Value 没有**，所以 **KIVI 的方案是"K 按 channel 量化、V 按 token 量化"**；工业上最省事的是 **fp8 KV**（vLLM 的 `kv_cache_dtype="fp8"`），几乎无损、容量翻倍。

### 🗣️ 30 秒口语版

"这题要接第 12 章的结论来讲，才有说服力。

**动机（一条完整的因果链）**：
第 12 章算过——A100-80G 跑 7B，装完权重后剩 66GB 给 KV Cache，一条 4K 请求要 2GB → **最多 33 并发**。而 Roofline 说要 **batch≈150** 才能吃满算力。**所以显存是吞吐的真凶。**

**KV 压到 int8 → 并发翻倍 → 吞吐几乎翻倍。** 这是**投入产出比极高**的一刀，比压权重更直接（权重只有 14GB，KV 有 66GB！）。

**难点：K 和 V 的分布不一样。**

这是 **KIVI 论文的核心发现**，也是这题的题眼——
- **Key 有强烈的 per-channel outlier**：某几个固定的 channel 幅度极大。**这和 Q5 的激活 outlier 是同源的**（K 就是激活经过一个 Linear 得到的，outlier 被继承下来了）
- **Value 没有明显的 outlier**，分布很均匀

**所以量化策略必须不对称**：
- **Key → per-channel 量化**（沿着 channel 维度算 scale，把 outlier channel 隔离开）
- **Value → per-token 量化**（沿着 token 维度算 scale，简单够用）

**但这里有个工程矛盾**——**K 的 per-channel 量化和流式生成天然冲突**。per-channel 意味着 scale 要在**所有 token 上**统计 $\max$。但 decode 是一个一个来的，**新 token 来了 max 可能就变了 → 整个 cache 的 scale 要重算 → 全部重新量化**。不可接受。

**KIVI 的解法**：**分组 + 残差窗口**
- 把 token 按 128 个一组，**每组独立算 per-channel scale**（组内闭合，不受后续影响）
- **最近的 128 个 token 保持 fp16**（叫 residual），攒够一组才量化归档
- 效果：绝大部分历史被量化，最近的窗口保精度（而最近的 token 恰恰是 attention 权重最高的）

**工业界的实用答案：直接上 fp8。**
- vLLM 一个参数：`kv_cache_dtype="fp8"` （或 `"fp8_e5m2"` / `"fp8_e4m3"`）
- **几乎无损**（因为 fp8 的非均匀格点天然抗 outlier，见 Q5 追问 3 —— **根本不用管 K/V 分布不同的问题**）
- **容量直接翻倍**
- **H100 有原生 fp8 支持，A100 也能用（软件模拟转换，开销很小）**

**int4 KV 呢？** 掉点明显，且 KIVI 那套非对称策略实现复杂。**目前 fp8 是甜点，int4 KV 还在研究阶段。**"

### 📐 K 和 V 的分布差异

```
【Key 的分布】—— 沿 channel 看，有几根"尖刺"
  channel:  0    1    2  ...  47   ...  4095
  |K| max: 0.8  0.9  1.1  ... 92.3  ...  1.0
                              ↑↑↑
                        outlier channel（固定！和激活 outlier 同源）

  → per-token 量化会崩：某个 token 的 scale 被 channel 47 拉爆
  → per-channel 量化 OK：channel 47 自己一个 scale，不影响别人 ✅

【Value 的分布】—— 均匀，没有结构性尖刺
  channel:  0    1    2  ...  47   ...  4095
  |V| max: 1.1  0.9  1.0  ...  1.2  ...  0.8

  → per-token 量化就够了 ✅（且实现简单，天然适合流式追加）
```

**为什么 K 有而 V 没有？**

一个有说服力的解释：**K 参与 $QK^\top$ 的点积，要产生"极大的 logit"来实现 attention sink**（Q5 追问 1 讲的那个机制）。制造极大 logit 的最经济方式，就是**让某个 channel 的值极大**——于是 outlier 就长在 K 上了。

而 **V 只是"被加权求和的内容"**，不参与点积、不需要制造极值，所以分布平和。

> 💡 **这个解释和第 2 章 MLA 的"为什么只有 K 要解耦 RoPE、V 不用"是<u>同一个道理</u>**：**K 参与位置/相似度的点积，V 只是被搬运的内容**。
>
> **"K 是索引，V 是内容"** —— 这一条直觉可以同时解释：
> - MLA 为什么只解耦 K 的 RoPE（第 2 章）
> - KV 量化为什么 K 难 V 易（本章）
> - 为什么 RoPE 只作用在 Q、K 上（第 2 章）
>
> **面试时把这三个点串起来，是本章能给出的最强信号之一。**

### 💻 实践

```python
# vLLM —— 一个参数的事
from vllm import LLM

llm = LLM(
    model="meta-llama/Llama-3-8B",
    kv_cache_dtype="fp8_e4m3",   # ← KV Cache 用 FP8，容量直接翻倍
    # calculate_kv_scales=True,  # 部分模型需要在线标定 scale
)

# 效果（8B, GQA-8, 128 KB/token @ fp16）：
#   fp16 KV: 128 KB/token → 4K 上下文 0.5 GB → 66GB 可跑 132 并发
#   fp8  KV:  64 KB/token → 4K 上下文 0.25GB → 66GB 可跑 264 并发  ← 翻倍
```

**为什么 E4M3 而不是 E5M2？**
- **E4M3**：4 位指数 + 3 位尾数，范围 ±448，**精度高**
- **E5M2**：5 位指数 + 2 位尾数，范围 ±57344，**范围大但精度低**
- **KV Cache 用 E4M3** —— 因为 KV 的动态范围没那么夸张，**精度更值钱**
- （对比：训练时的**梯度**用 **E5M2**，因为梯度的动态范围极宽，范围比精度重要）

**这个"E4M3 给前向、E5M2 给梯度"的分工能说出来，说明你懂 FP8 的设计哲学。**

### 🔥 高频追问 Top 3

**Q：KV 量化和 GQA/MLA 是重复投入吗？**

A：**完全不是——它们是<u>乘法关系</u>，正交叠加。**

- **GQA / MLA**：减少**要存多少个 KV**（减头数 / 压维度）—— **减少数量**
- **KV 量化**：减少**每个 KV 占几个字节** —— **减少单价**

$$\text{KV Cache} = \underbrace{\text{数量}}_{\text{GQA/MLA 治}} \times \underbrace{\text{单价}}_{\text{量化治}}$$

**叠加效果（以 DeepSeek-V3 为例）**：
```
MHA + fp16    : 基准
GQA-8 + fp16  : ÷4
MLA + fp16    : ÷7  （见第 12 章的表：~70 KB vs ~500 KB）
MLA + fp8     : ÷14 ✅ ← 两个技术叠乘
```

**这就是 DeepSeek 能把 API 价格打到那么低的组合拳。** 面试时能算出这个叠乘，比单独讲任何一个技术都有说服力。

**Q：KV 量化会影响长上下文的能力吗？**

A：**会，而且这是它最主要的风险，必须主动提。**

原因：**误差会随上下文长度累积**。
- 第 1 个 token 的 KV 有量化误差 $\epsilon$
- 生成第 10000 个 token 时，attention 要看**全部 10000 个**有误差的 KV
- 误差在 softmax 里**竞争性地放大**——如果某个不该被关注的 token 因为量化误差"蒙"到了一个高 logit，它就会**抢走注意力**

**实测规律（要记住）**：
- **短上下文（<4K）**：fp8 KV 几乎无损 ✅
- **长上下文（32K+）**：**"大海捞针"（needle-in-haystack）类任务会明显掉点** ⚠️
- **int4 KV**：短上下文就开始掉，长上下文崩

**所以正确的实践是**：
- **必须在<u>目标上下文长度</u>上评测**，不能只测 4K 就上线
- 长上下文场景优先 **fp8**（不要 int4）
- 或者 **KIVI 式的"最近窗口保 fp16"** —— 保住最重要的部分

**这也呼应 Q8 的主题：量化的伤害是<u>不均匀</u>的，它专挑最难的任务下手。**

**Q：能不能只量化一部分层的 KV？**

A：**能，这是个有效的进阶技巧。**

观察：**不同层的 attention 模式差异很大**。
- **浅层**：attention 比较"发散"（关注局部、语法），**对 KV 精度不敏感**
- **深层**：attention 更"尖锐"（做精确的语义检索、事实召回），**对精度敏感**
- **第 0-2 层**：往往有最强的 attention sink，**outlier 最严重，最难量化**

所以可以：**浅层 int4、深层 int8/fp16**——混合位宽。

**相关工作**：
- **PyramidKV**：不是量化，而是**驱逐**——但发现了同一个规律：浅层可以留少量 KV，深层要留多。
- **MiniCache**：发现**相邻层的 KV 高度相似**，可以**跨层合并**。
- **KV Cache 的跨层共享（CLA / YOCO）**：更激进——**架构层面**让多层共享同一份 KV。

**答这题的正确姿态**：这些都是**有损**的、**还没成为工业默认**的技术。要说清"我知道有这些路，但我会先上 fp8 这个几乎无损的方案，只有在显存实在不够时才考虑有损方案"。**这个优先级判断比技术名词更重要。**

### ⚠️ 常见陷阱

1. **不知道 K 和 V 分布不同** —— 这是这题的题眼，也是 KIVI 的核心贡献。
2. **忽略长上下文的累积效应** —— 只在 4K 上测了就说"无损"，是线上事故的经典来源。
3. **以为 KV 量化和 GQA 冲突** —— 它们是乘法叠加。
4. **不知道 per-channel 量化 K 和流式生成的矛盾** —— 这个工程细节能体现"真的想过怎么实现"。

### 🏢 大厂偏好

- **字节 / 阿里（推理）**：会问 KIVI 的 K/V 非对称策略
- **应用岗**：知道 `kv_cache_dtype="fp8"` 这个开关 + **长上下文要单独评测**就够了
- **长文本团队（Kimi、MiniMax）**：**必问**，这是他们的核心战场

### 📚 延伸阅读
- [KIVI: A Tuning-Free Asymmetric 2bit Quantization for KV Cache](https://arxiv.org/abs/2402.02750) - **K/V 非对称策略的原始发现**
- [KVQuant](https://arxiv.org/abs/2401.18079)
- [vLLM FP8 KV Cache 文档](https://docs.vllm.ai/en/latest/features/quantization/fp8.html)

---

## Q8：量化评估的陷阱——掉点掉在哪 ⭐⭐⭐⭐

### 🎯 一句话标答

> **PPL 是个极其不敏感的指标**——PPL 只掉 0.1 可能对应 MMLU 掉 3 分、GSM8K 掉 8 分；量化的伤害是**高度不均匀**的，**专挑长链推理、长上下文、多语言/低资源语言、代码这些"最难的能力"下手**，而这些恰恰是 PPL 看不见的；此外**校准集分布偏移**和**对齐/安全能力退化**是两个最常见的线上翻车点。

### 🗣️ 30 秒口语版

"这题是**上过线的人才答得好**的题，非常有区分度。

**陷阱一：PPL 骗人。**

论文里都说"int4 量化，PPL 只掉 0.05，几乎无损"。**但 PPL 测的是什么？**——是模型对**下一个 token** 的平均预测能力。**它是一个高度平均化的指标**：99% 的 token 是简单的（虚词、常见搭配），量化对它们毫无影响，把整个平均值**稀释**了。

而我们真正在意的能力，**恰恰藏在那 1% 的关键 token 里**——数学题的那个关键推导步骤、代码里的那个变量名、长文档里那个被检索的事实。

**实测的经验规律（要能报数）**：
```
int4 量化后：
  PPL:      +0.05 ~ +0.15   → "几乎无损"  ✅
  MMLU:     -1 ~ -3 分        → "还行"      ⚠️
  GSM8K:    -3 ~ -8 分        → "明显掉了"  ❌
  HumanEval: -3 ~ -6 分       → "明显掉了"  ❌
  长上下文检索: -10 分以上      → "崩了"      ❌❌
```

**为什么推理任务掉得最多？** 因为 **CoT 是长链条的、误差会累积**——第 3 步错一点，第 10 步就全错了。而且推理任务是**"全对才给分"**的（数学答案对就是对，错就是错），**没有部分分**。PPL 的那点"平均的小误差"，在长链推理里会被放大成"整题错"。

**陷阱二：校准集分布偏移。**

用 WikiText 校准 → 部署到代码场景 → **掉点远超预期**。因为 GPTQ 是在校准集上做重构优化的，它"记住"了百科文本的激活模式。

**陷阱三：对齐能力退化（最容易被忽视）。**

RLHF 学到的安全边界是**很微妙的**——拒绝和不拒绝之间可能只差几个 logit。量化误差足以**把这个边界推翻**。有研究发现**量化后模型的越狱成功率上升**。

**所以：量化后必须重跑安全评测。** 这一点在有合规要求的业务上是硬性的。

**正确的评估方法（面试请说这套）**：
1. **PPL 只做 sanity check**（如果 PPL 崩了肯定有 bug），**不作为质量判据**
2. **必测下游任务**，且要覆盖**你的业务真实分布**
3. **必测最难的能力**：数学（GSM8K）、代码（HumanEval）、长上下文（needle-in-haystack）
4. **必测目标上下文长度**，不能只测短的
5. **必测安全对齐**
6. **最终判据是线上 A/B**，不是任何离线指标"

### 📐 为什么 PPL 不敏感——数学解释

$$\text{PPL} = \exp\left(-\frac{1}{N}\sum_{i=1}^{N} \log p(x_i \mid x_{<i})\right)$$

关键在那个 $\frac{1}{N}$ ——**它是全部 token 的<u>算术平均</u>**。

假设 10000 个 token 里：
- **9900 个"简单" token**（"的"、"是"、"，"）：量化前后 $\log p$ 变化 ~0.001
- **100 个"关键" token**（数学推导的关键步骤）：量化后 $\log p$ **暴跌 2.0**

$$\Delta \text{PPL} \approx \exp\left(\frac{9900 \times 0.001 + 100 \times 2.0}{10000}\right) - 1 \approx \exp(0.02) - 1 \approx \mathbf{2\%}$$

**PPL 只涨 2%，看起来"几乎无损"。**

但那 100 个关键 token —— **每一个都可能让整道题的答案错掉**。如果一道 GSM8K 题需要 10 步推理，每步依赖一个关键 token，那么：

$$P(\text{整题对}) = (1-p_{\text{err}})^{10}$$

**关键 token 的错误率从 5% 涨到 15%** → 整题正确率从 $0.95^{10}=60\%$ 掉到 $0.85^{10}=20\%$ —— **掉 40 个百分点**！

$$\boxed{\text{PPL 是算术平均，任务成功率是<u>几何</u>累积 —— 这就是两者脱节的根源}}$$

> **这段推导能讲出来，这题就封顶了。** 它把"PPL 骗人"从一句经验之谈变成了一个可以严格论证的结论。

### 🔍 量化伤害的不均匀性

| 能力 | int4 后的典型损失 | 为什么 |
|---|---|---|
| 常识问答 | **小** ✅ | 靠记忆，冗余度高，误差被平均掉 |
| 语法/流畅度 | **小** ✅ | 高度冗余 |
| **数学推理 (GSM8K)** | **大** ❌ | **长链累积 + 全对才给分** |
| **代码 (HumanEval)** | **大** ❌ | **一个 token 错就编译不过**，零容错 |
| **长上下文检索** | **大** ❌ | KV 误差累积 + 要精确定位 |
| **多语言 / 低资源语言** | **大** ❌ | **训练数据少 → 权重本来就"勉强" → 没有冗余可损失** |
| **对齐 / 安全** | **中-大** ⚠️ | 决策边界微妙，且**没人测** |

**统一的规律（一句话）**：

> **量化消耗的是"冗余"。哪个能力的冗余最少，哪个就先崩。**

- 常识问答：模型见过一万遍"法国首都是巴黎"，**冗余极高** → 抗量化
- 低资源语言：模型可能只见过几千条，**权重本来就在"勉强够用"的边缘** → 一量化就崩
- 长链推理：**每一步都不能错**，容错为零 → 崩

> 💡 **这条"量化消耗冗余"的直觉，和第 8 章 LoRA 的"内在秩很低"是同一个硬币的两面**：
> - **LoRA 说**：模型的**更新**是低秩的（冗余度高，所以能压）
> - **量化说**：模型的**权重**是有冗余的（所以能压）
> - **两者都在利用"大模型是过参数化的"这个事实**
> - **也都在同一个地方失效**：当任务需要模型的**全部容量**时（长链推理、低资源语言），冗余不够用了，压缩就有代价了
>
> **面试时能把量化和 LoRA 用"过参数化/冗余"这条线串起来，是本章最高级的加分点。**

### 🔥 高频追问 Top 3

**Q：那用什么指标代替 PPL？**

A：**没有单一指标能代替，必须用"任务组合 + 业务分布"。**

**推荐的评测矩阵**：

| 层次 | 测什么 | 作用 |
|---|---|---|
| **L0 冒烟** | PPL (WikiText) | **只判断有没有 bug**（PPL 崩了 = 实现错了）。**不用来判断质量** |
| **L1 通用** | MMLU, C-Eval, CMMLU | 知识能力基线 |
| **L2 难任务** | **GSM8K, HumanEval, MATH** | **最敏感的探针**——量化的伤先在这里显形 |
| **L3 长上下文** | **Needle-in-Haystack, RULER, LongBench** | KV 量化的必测项 |
| **L4 业务** | **你自己的评测集** | **最终判据**——前面全过了但业务掉了，就是校准集偏了 |
| **L5 安全** | 越狱测试、拒答率 | **合规硬要求** |
| **L6 线上** | **A/B 测试** | 真理 |

**如果只能测一个**：**测 GSM8K（或你业务里最难的那个任务）**。因为它是**最灵敏的探针**——GSM8K 没掉，其他基本都没问题；GSM8K 掉了，说明量化伤到了模型的"硬能力"。

**Q：量化的损失能"补"回来吗？**

A：**能，有三条路，代价递增**：

1. **换更好的量化算法**（免费）：RTN → GPTQ/AWQ → QuIP#，或者提高等效位宽（group 128→64，或 int4→int8 混合）
2. **换更好的校准集**（几乎免费）：**这是最容易被忽视、性价比最高的一步**。很多"量化掉点"其实是"校准集选错了"。用业务分布的数据重新校准，经常能白捡回来几个点。
3. **量化后微调**（有成本）：在量化模型上做 LoRA 微调，**让 LoRA 分支去补偿量化误差** —— 这就是 **QA-LoRA / LoftQ** 那一族（Q2 追问 2）。能补回大部分，但需要训练数据和算力。

**实践的正确顺序**：**先怀疑校准集（免费），再换算法（免费），最后才考虑微调（有成本）。** 这个排序体现工程判断力。

**Q：为什么量化对小模型伤害更大？**

A：**因为小模型的冗余更少 —— 这是本章"量化消耗冗余"这条主线的直接推论。**

**实测规律**：
```
70B  int4: 掉点很小 ✅  （参数多，冗余大，压缩空间大）
13B  int4: 掉点中等 ⚠️
7B   int4: 掉点明显 ❌
1.5B int4: 掉点严重 ❌❌  （每个权重都"身兼数职"，动不得）
```

**推论（这是个很实用的判断）**：

$$\boxed{\text{"70B-int4" 通常显著优于 "13B-fp16"，即使两者显存占用相近}}$$

**在给定显存预算下，"大模型 + 激进量化" > "小模型 + 高精度"。**

**为什么**：Scaling Law 带来的能力提升，**远大于**量化造成的损失。70B 的知识量、推理能力是 13B 望尘莫及的，而 int4 只让它损失几个点。

**这条经验法则非常实用**——它直接回答了"我有 40GB 显存，该跑什么模型"这个真实问题：**跑 70B-int4，不要跑 13B-fp16**。

> ⚠️ **但要说清例外**：如果你的任务**恰好是量化最伤的那类**（长链数学推理、低资源语言、超长上下文精确检索），这个结论**可能反转**。所以还是那句话：**测你自己的任务。**

### ⚠️ 常见陷阱

1. **拿 PPL 说"无损"** —— 这题的核心。**PPL 是算术平均，任务成功率是几何累积。**
2. **只测短上下文** —— KV 量化的伤在长上下文才显形。
3. **不测安全** —— 量化会破坏 RLHF 的对齐边界，且**没人会主动发现**（因为不测）。
4. **用论文的校准集** —— 论文用 WikiText/C4 是为了可复现，**不是为了你的业务**。
5. **不知道"小模型更不能量化"** —— 会给出"13B-fp16 比 70B-int4 好"的错误建议。

### 🏢 大厂偏好

- **应用岗 / 业务岗**：**高频，且是区分度最高的一题**——因为它考的是"你有没有真的上过线"
- **字节 / 阿里**：会问"你怎么验证量化没有伤害业务"
- **有合规要求的团队（金融、医疗、政务）**：**必问安全评测**

### 📚 延伸阅读
- [A Comprehensive Evaluation of Quantized Instruction-Tuned LLMs](https://arxiv.org/abs/2409.11055) - 系统性的量化掉点评测
- [How Does Quantization Affect Multilingual LLMs?](https://arxiv.org/abs/2407.03211) - 低资源语言掉得更多的实证
- [RULER](https://arxiv.org/abs/2404.06654) - 长上下文评测基准

---

## 📝 本章小结

| 关键点 | 你必须能脱口而出 |
|---|---|
| **量化公式** | $x_q = \text{round}(x/s)+z$，误差 $\approx s^2/12$，**只取决于组内 max** |
| **粒度** | per-group(128) 是默认；int4+g128 的**等效位宽是 4.16，压缩 3.85× 不是 4×** |
| **核心不对称** | **权重好量化（静态、类高斯），激活难量化（动态、有 outlier）** |
| **PTQ vs QAT** | LLM 时代 **PTQ 压倒性主流**；QAT 只在 ≤2bit 复活（BitNet）；**QLoRA ≠ QAT** |
| **GPTQ 目标** | $\min\|WX-\hat WX\|$ —— **是层输出误差，不是权重误差**！那个 $X$ 是灵魂 |
| **GPTQ 三刀** | 固定顺序（→并行，$O(d^4)\to O(d^3)$）/ Lazy Update（→省访存）/ Cholesky（→数值稳定） |
| **AWQ 洞察** | **权重大 ≠ 重要；乘它的激活大才重要**。因为输出误差 $=\sum\epsilon_j x_j$ |
| **AWQ 手段** | 放大显著通道 ⇒ 组内 max 不变 ⇒ scale 不变 ⇒ **相对误差降 s 倍**（前提：只放大少数） |
| **GPTQ vs AWQ** | 同一个洞察（$H_{jj}$ 就是激活能量）；GPTQ 改权重值（强但**易 overfit 校准集**），AWQ 只搜一个 α（**泛化好**） |
| **Outlier** | **>6.7B 涌现**、~100×、**~0.1% channel**、**系统性固定**（← SmoothQuant 能离线的前提）；与 **attention sink 同源** |
| **SmoothQuant** | $Y=(X/s)(sW)$，**把量化难度从激活迁移到权重**；$s=\max\|X\|^\alpha/\max\|W\|^{1-\alpha}$，$\alpha$=0.5 |
| **AWQ vs SmoothQuant** | **同一个恒等式，目标相反**：一个救权重、一个救激活 |
| **本章元结构** | **AWQ / SmoothQuant / QuIP / QuaRot = 找等价变换 $T$，使 $TW$ 和 $T^{-1}X$ 更好量化**（$T$ = 对角 or 正交） |
| **选型** | **W4A16 省访存（小 batch 王者，$B{>}38$ 倒挂）；W8A8/FP8 省算力（大 batch）；有 H100 就 FP8** |
| **KV 量化** | **K 有 per-channel outlier，V 没有**（"K 是索引、V 是内容"，同第 2 章 MLA）；**fp8 是甜点**；和 GQA/MLA **乘法叠加** |
| **评估** | **PPL 是算术平均，任务成功率是几何累积** → PPL 骗人；**GSM8K 是最灵敏的探针** |
| **一句话主线** | **量化消耗的是冗余。哪个能力冗余最少，哪个先崩**（→ 小模型更不能量化；→ 70B-int4 > 13B-fp16） |

## ✅ 自测题

1. 现场手撕 `quantize_per_group`，并算出 int4 + group_size=128 的**等效位宽和真实压缩比**。
2. 用**三行数学**证明"权重大小不是重要性的判据"，并说清 AWQ 为什么放大就能降误差、以及**为什么必须只放大少数通道**。
3. GPTQ 的目标函数是什么？为什么 Hessian 有解析解、且只有 $[d,d]$ 大？三个工程近似各解决什么问题？
4. AWQ 和 SmoothQuant 的数学形式几乎一样，为什么一个配 W4A16、一个配 W8A8？
5. 面试官："我们线上是 A100，7B 模型，平均 batch=4，显存够用，要不要量化？量化选哪个？" —— **注意这题有坑**（显存够用 + batch=4 = memory-bound，量化仍然值得，选 AWQ）。
6. 画出 W4A16 / W8A8 / FP16 的吞吐-batch 曲线，标出 W4A16 的临界点并解释为什么在那里。
7. 你把 70B 量化到 int4，PPL 只掉 0.08。老板说"上线吧"。**你要说服他再等等——给出三条理由和对应的测试方案。**
8. 为什么 KV Cache 里 K 比 V 难量化？这个原因和第 2 章 MLA "只解耦 K 的 RoPE"是同一个吗？

---

> **下一章预告**：第 14 章｜解码策略与投机解码——temperature / top-k / top-p 的采样数学、**为什么 LLM 不用 Beam Search**、以及第 12 章说的第三条优化路线：**投机解码**（接受率的数学证明为什么它是<u>无损</u>的、Medusa / EAGLE 的演化）。本章的量化是"减小模型"，第 14 章的投机解码是"**一次多出几个 token**"——**至此第 12 章 Roofline 给出的三条路线就全部走完了**。

