# 第 14 章｜解码策略与投机解码

> **本章定位**：第 12 章的 Roofline 给出了三条优化路线——**减小模型**（第 13 章量化）、**提高 batch**（第 12 章调度）、**一次多出几个 token**。**本章走完最后一条**。
>
> 本章有两条主线，看似无关其实同源：
> - **前半（Q1-Q3）**：**采样策略**。核心命题是 **"高概率 ≠ 好文本"**——LLM 的概率分布有一条**不可信的长尾**，采样策略的本质是"信任头部、切掉尾部"。
> - **后半（Q4-Q7）**：**投机解码**。核心命题是 **"验证是并行的，生成是串行的"**——而它之所以能成立，**100% 依赖第 12 章的结论：Decode 是 memory-bound，所以多验证几个 token 几乎是免费的**。
> - **Q8** 是给 Agent 岗的桥：**约束解码**——Function Calling 能稳定输出合法 JSON，靠的就是它。

> **配套章节**：Roofline / memory-bound → 第 12 章 Q3 / 量化（路线一）→ 第 13 章 / Continuous Batching → 第 12 章 Q5 / Function Calling → 第 17 章 / 推理引擎的投机解码支持 → 第 15 章

---

## 📌 本章导航

| 序号 | 题目 | 难度 | 频率（字节/阿里/Meta/DeepSeek） | 类型 |
|---|---|---|---|---|
| Q1 | **采样策略全景：temperature / top-k / top-p / min-p** | ⭐⭐⭐⭐⭐ | **8 / 7 / 5 / 4** | 必问·可能手撕 |
| Q2 | 重复退化与三种惩罚 | ⭐⭐⭐⭐ | 5 / 5 / 3 / 3 | 实战 |
| Q3 | **为什么 LLM 不用 Beam Search** | ⭐⭐⭐⭐⭐ | **7 / 6 / 5 / 4** | 必问 |
| Q4 | **投机解码原理与"为什么无损"** | ⭐⭐⭐⭐⭐ | **8 / 6 / 5 / 6** | 必问·要推导 |
| Q5 | **接受率与加速比的数学** | ⭐⭐⭐⭐⭐ | **7 / 5 / 4 / 5** | 必问·要推导 |
| Q6 | **Draft 从哪来：Medusa / EAGLE / n-gram** | ⭐⭐⭐⭐⭐ | **7 / 6 / 4 / 5** | 必问 |
| Q7 | **投机解码的工程现实：什么时候没用** | ⭐⭐⭐⭐ | 6 / 5 / 4 / 4 | 实战 |
| Q8 | **约束解码与结构化输出（Agent 必备）** | ⭐⭐⭐⭐ | 6 / 5 / 3 / 3 | 应用 |

---

## Q1：采样策略全景 ⭐⭐⭐⭐⭐（必问，可能手撕）

### 🎯 一句话标答

> LLM 的 softmax **强制给每个 token 非零概率**，导致存在一条**不可信的长尾**（unreliable tail）；采样策略的本质是"**信任头部、切掉尾部**"——**temperature** 调整分布的锐度（作用在 logits 上，softmax 之前），**top-k** 用固定数量截断（僵硬），**top-p** 用累积概率动态截断（主流），**min-p** 用相对最高概率的比例截断（对高 temperature 更稳健）。

### 🗣️ 30 秒口语版

"要先讲清**为什么需要采样策略**，不然后面全是背名词。

**根本问题：softmax 的长尾是不可信的。**

Softmax 的定义决定了——**每个 token 都会分到非零概率**，哪怕它完全不合理。词表 15 万，就算每个垃圾 token 只有 $10^{-6}$，**加起来也有 10% 的概率质量落在'完全不该出现'的区域**。

如果你纯随机采样（temperature=1，不截断），**平均每 10 个 token 就会蹦出一个胡话**。而 LLM 是自回归的——**一个胡话进了上下文，后面就会跟着跑偏**。这就是 Holtzman 那篇 "The Curious Case of Neural Text Degeneration" 的核心论点：**模型的头部是可信的，尾部是噪声**。

**所以所有采样策略都在做同一件事：把尾巴切掉。**

**Temperature**：
$$p_i = \frac{\exp(z_i / T)}{\sum_j \exp(z_j / T)}$$
- **作用在 logits 上，在 softmax <u>之前</u>**（这个细节面试常问）
- $T < 1$：**锐化**——大的更大，小的更小，趋向 greedy
- $T > 1$：**平滑**——趋向均匀分布
- $T \to 0$：等价于 greedy（argmax）
- 注意：**temperature 不改变排序，只改变相对差距**

**Top-k**：保留概率最高的 $k$ 个，其余置零、重新归一化。
- 问题：**$k$ 是固定的，但分布的锐度是变的**
  - "中国的首都是" → 分布极尖锐，只有"北"一个合理答案。top-k=50 **硬把 49 个垃圾放进候选**
  - "我今天想吃" → 分布很平坦，几百个合理答案。top-k=50 **又切掉了合理的**
- 一个僵硬的旋钮，配不上动态的分布

**Top-p（Nucleus Sampling）**：保留**累积概率达到 $p$ 的最小集合**。
- **候选集大小是动态的**——分布尖锐时自动变小（可能只有 1 个），平坦时自动变大（可能几百个）
- **这正好解决了 top-k 的问题**，所以它是当前主流（$p$ 典型取 0.9-0.95）

**Min-p（2024 年的新方案）**：阈值 $= p_{\max} \times \text{min\_p}$，保留所有概率高于这个阈值的 token。
- 比 top-p **更简单**（不用排序求累积和，一次比较就行）
- **在高 temperature 下更稳健**——top-p 在高温下会把大量垃圾纳入 nucleus（因为分布被压平了，累积到 0.9 需要很多 token），而 min-p 是**相对于最高分**的比例，**天然抗温度**
- 所以 min-p **允许你开更高的 temperature 拿创造性，同时不放垃圾进来**——这是它的核心卖点

**实践默认**：
- **确定性任务**（代码、数学、抽取、Function Calling）→ **temperature=0**（greedy）
- **通用对话** → temperature 0.7 + top_p 0.9
- **创意写作** → temperature 1.0-1.2 + **min_p 0.05**（而不是 top_p）"

### 📐 详细推导

#### Temperature 的数学性质

$$p_i(T) = \frac{\exp(z_i / T)}{\sum_j \exp(z_j / T)}$$

**性质 1：不改变排序**。$z_i > z_j \Rightarrow p_i(T) > p_j(T)$ 对任意 $T > 0$ 成立。**所以 temperature 不会让"第二名超过第一名"**——它只改变差距。

**性质 2：极限行为**
$$\lim_{T\to 0^+} p_i(T) = \mathbb{1}[i = \arg\max z] \quad(\text{greedy}), \qquad \lim_{T\to\infty} p_i(T) = \frac{1}{|V|} \quad(\text{均匀})$$

**性质 3：对 logit 差的作用**——这是理解 temperature 最好的角度。

两个 token 的概率比：
$$\frac{p_i}{p_j} = \exp\left(\frac{z_i - z_j}{T}\right)$$

**$T$ 直接缩放的是 logit 之差**。设 $z_i - z_j = 2$：
- $T=1$：$p_i/p_j = e^2 \approx 7.4$
- $T=0.5$：$p_i/p_j = e^4 \approx 55$（差距拉大 7 倍）
- $T=2$：$p_i/p_j = e^1 \approx 2.7$（差距缩小）

> **所以 temperature 的正确直觉是**："**它调的是模型对自己判断的<u>自信程度</u>**"，而不是"随机性"。$T<1$ = 让模型更自信，$T>1$ = 让模型更犹豫。

#### 三种截断的对比

| | Top-k | Top-p | **Min-p** |
|---|---|---|---|
| 规则 | 前 $k$ 个 | 累积概率 $\ge p$ 的最小集合 | $p_i \ge \text{min\_p} \times p_{\max}$ |
| 候选集大小 | **固定** ❌ | **动态** ✅ | **动态** ✅ |
| 计算 | 排序 + 取前 k | **排序 + 累积和** | **一次比较**（不用排序！）✅ |
| 尖锐分布下 | 强行纳入垃圾 ❌ | 自动变小 ✅ | 自动变小 ✅ |
| 平坦分布下 | 切掉合理选项 ❌ | 自动变大 ✅ | 自动变大 ✅ |
| **高 temperature 下** | — | **会纳入大量垃圾** ❌ | **稳健** ✅ |
| 典型值 | 40-50 | **0.9-0.95** | **0.05-0.1** |

**为什么 top-p 在高温下会失效？**（这是 min-p 论文的核心论证）

高 temperature 把分布压平 → 每个 token 的概率都变小 → **要累积到 0.9 就需要纳入非常多的 token** → **nucleus 里混进大量垃圾**。

```
【T=1，正常分布】
  p = [0.6, 0.25, 0.08, 0.04, 0.02, 0.01, ...]
  top_p=0.9 → 纳入前 3 个 (0.6+0.25+0.08=0.93) ✅ 合理

【T=2，被压平】
  p = [0.15, 0.10, 0.06, 0.05, 0.04, 0.04, 0.03, 0.03, ... 长长的尾巴]
  top_p=0.9 → 要纳入 40+ 个才能累积到 0.9 ❌ 后面全是垃圾
  min_p=0.05 → 阈值 = 0.15 × 0.05 = 0.0075 → 只纳入 p ≥ 0.0075 的 ✅
                （相对最高分的比例，不受整体被压平的影响）
```

> **min-p 的精髓是"<u>相对</u>阈值"**——它问的是"**这个候选和最佳候选比，差多少倍**"，而不是"**它的绝对概率是多少**"。**相对量比绝对量稳健** —— 这条原则在你笔记里已经出现过多次（PPO 的 Advantage、RLHF 的 reward shaping），**这里是它的第三次登场**。

#### 顺序问题（高频细节）

标准流程（HuggingFace / vLLM 的默认）：
```
logits → [温度缩放] → [top-k 截断] → [top-p 截断] → softmax → 采样
          ↑ 作用在 logits          ↑ 这两步是把非法 token 的 logit 设为 -inf
```

**为什么 temperature 要在截断之前？** 因为截断依赖概率排序，而 temperature **不改变排序**——所以先后顺序**对"选出哪些候选"没影响**，只对"候选内部的相对概率"有影响。

**但有个真实的争议**：有人主张 **temperature 应该在截断<u>之后</u>**。理由：先截断确定"哪些是合理候选"，再用 temperature 调整这些**合理候选**之间的随机性。这样调高 temperature **不会把垃圾拉回来**（因为已经切掉了）。

- HF/vLLM 默认：**temperature 先**
- SillyTavern 等社区工具提供可配置的采样器顺序
- **实际影响**：因为 temperature 不改排序，影响主要在 top-p 的累积计算上（温度变了，累积到 0.9 需要的 token 数就变了）。**所以 temperature 在前时，它会间接影响 nucleus 的大小；在后时不会。**

**这个细节答出来是"真的调过参"的信号。**

### 💻 关键代码片段

```python
import torch

def sample(logits, temperature=1.0, top_k=0, top_p=1.0, min_p=0.0):
    """
    logits: [vocab_size]
    标准顺序：temperature → top_k → top_p → min_p → softmax → 采样
    """
    # ---- greedy 短路 ----
    if temperature == 0:
        return logits.argmax()

    # ---- ① Temperature：作用在 logits 上，softmax 之前 ----
    logits = logits / temperature

    # ---- ② Top-k ----
    if top_k > 0:
        kth = torch.topk(logits, top_k).values[-1]
        logits[logits < kth] = -float('inf')

    # ---- ③ Top-p (nucleus) ----
    if top_p < 1.0:
        sorted_logits, sorted_idx = torch.sort(logits, descending=True)
        cum_probs = torch.cumsum(torch.softmax(sorted_logits, dim=-1), dim=-1)
        # 找出累积概率超过 p 的位置 —— 注意要保留"刚好越过 p 的那一个"
        remove = cum_probs > top_p
        remove[1:] = remove[:-1].clone()   # ← 右移一位，保证至少留一个
        remove[0] = False                  # ← 最高分永远保留
        logits[sorted_idx[remove]] = -float('inf')

    # ---- ④ Min-p：不用排序！ ----
    if min_p > 0.0:
        probs = torch.softmax(logits, dim=-1)
        threshold = min_p * probs.max()    # ← 相对最高分的比例
        logits[probs < threshold] = -float('inf')

    # ---- ⑤ 采样 ----
    return torch.multinomial(torch.softmax(logits, dim=-1), num_samples=1)
```

> **手撕时最容易被抓的两处**：
> 1. **top-p 的右移**（`remove[1:] = remove[:-1]`）—— 不移的话，如果第一个 token 概率就是 0.95 > top_p=0.9，**它自己会被切掉**，结果一个候选都不剩。
> 2. **temperature=0 要短路** —— 直接除会得到 `inf`/`nan`。

### 🔥 高频追问 Top 3

**Q：为什么不能纯 greedy？确定性不好吗？**

A：**分任务。这题的正确答案是"看有没有唯一正确答案"。**

**greedy 适合的**：代码、数学、抽取、分类、Function Calling —— **有客观正确答案**的任务。这些场景你要的是"模型最好的那个猜测"，随机性只会带来噪声。**而且 greedy 可复现，方便调试和回归测试。**

**greedy 不适合的**：开放式生成（对话、写作）。原因有两条——

1. **重复退化**（Q2 详谈）：greedy 会陷入 "A B A B A B..." 的死循环。这是**正反馈**：模型看到自己刚写的 "A B"，会认为这是个强 pattern，进一步提高 A 的概率。**没有随机性就跳不出去。**

2. **更深的原因：人类文本的 likelihood 不是一路最高的**。Holtzman 的经典实验——把人写的文章拿给模型算每个词的概率，你会发现是**剧烈波动**的：人经常用低概率的词（这正是**信息量**的来源）。而 greedy 生成的文本 likelihood **一路平滑地高**——读起来就是**乏味、正确、没有信息量**。

$$\boxed{\text{"最可能的文本"} \ne \text{"最好的文本"}}$$

> **这句话是 Q1 和 Q3 的共同主题**——Q3 的 Beam Search 之所以被淘汰，是**同一个原因的更极端版本**。

**Q：temperature 到底该怎么调？有没有原则？**

A：**有一条实用的原则：<u>temperature 调的是"你愿意接受多少偏离最优解"</u>，所以先问任务能容忍多少错。**

| 任务 | 推荐 | 理由 |
|---|---|---|
| Function Calling / JSON 输出 | **0** | 格式错一个字符就废，零容错 |
| 代码生成 | **0 ~ 0.2** | 有正确答案；但 pass@k 场景可以调高（见下） |
| 数学 / 推理 | **0** 或 **0.6**（配 self-consistency） | 见下面的辨析 |
| RAG / 抽取 / 摘要 | **0 ~ 0.3** | 要忠于原文，不要发挥 |
| 通用对话 | **0.7 + top_p 0.9** | 平衡 |
| 创意写作 | **1.0-1.2 + min_p 0.05** | 要多样性；用 min_p 而不是 top_p 挡垃圾 |

**数学那一行的辨析很重要**——
- **单次回答**（pass@1）：**temperature=0**，要模型最好的那一击
- **Self-Consistency**（采样 N 条 CoT 投票）：**必须 temperature > 0**（典型 0.6-0.7）！因为 **temperature=0 采 N 次会得到 N 条一模一样的路径**，投票毫无意义。**多样性是 self-consistency 的燃料。**
- 同理 **pass@k 的代码生成**、**Best-of-N** 都需要 temperature > 0

**这个"单次要确定性、多次要多样性"的辨析能说出来，是很好的信号**——它说明你理解 temperature 不是一个"质量旋钮"，而是"**exploration 旋钮**"。

**Q：推理模型（R1/o1）的 temperature 该怎么设？**

A：**这是个很新的考点，DeepSeek 官方专门给过建议。**

DeepSeek-R1 的官方推荐是 **temperature = 0.5-0.7（推荐 0.6）**，并且**明确不建议用 temperature=0**。这很反直觉——推理任务不是应该要确定性吗？

原因有两条：
1. **temperature=0 会导致 R1 出现重复退化**——长 CoT 有几千个 token，greedy 在这么长的链条上**极易陷入循环**（"等等，让我重新检查一下…等等，让我重新检查一下…"）。这是 Q2 说的正反馈在超长生成上的放大。
2. **推理需要探索**。R1 的 CoT 里有大量的"回溯、自我怀疑、换个思路"——**这些行为本身就需要一点随机性来触发**。temperature=0 会让模型沿着第一个想法一条道走到黑（**"aha moment" 出不来**）。

**同时官方还建议**：**不要加 system prompt**（把要求放 user message 里）、**数学题要求模型把答案放在 `\boxed{}` 里**。

**能说出"推理模型反而不能用 temperature=0，因为长 CoT 会重复退化 + 推理需要探索"，是很强的前沿感知信号。**

### ⚠️ 常见陷阱

1. **说 temperature 作用在概率上** —— **作用在 logits 上，softmax 之前**。这是最高频的细节题。
2. **说 temperature 会改变排序** —— **不会**。它只改变差距。
3. **手撕 top-p 时漏掉右移** —— 会导致极端情况下候选集为空。
4. **不知道 min-p** —— 这是 2024 年的新方案，知道它说明跟进了社区。
5. **对 self-consistency 用 temperature=0** —— **N 条一样的路径，投票是笑话**。这是个真实的实现 bug。

### 🏢 大厂偏好

- **所有大厂**：**必问，且可能手撕 top-p**
- **应用岗**：会问"你的场景 temperature 怎么设，为什么"
- **DeepSeek**：可能问 R1 的采样建议

### 📚 延伸阅读
- [The Curious Case of Neural Text Degeneration](https://arxiv.org/abs/1904.09751) - **Nucleus Sampling 原论文，"高概率≠好文本"的论证在这里，本章必读**
- [Turning Up the Heat: Min-p Sampling](https://arxiv.org/abs/2407.01082) - min-p 原论文
- [DeepSeek-R1 官方使用建议](https://github.com/deepseek-ai/DeepSeek-R1#usage-recommendations)

---

## Q2：重复退化与三种惩罚 ⭐⭐⭐⭐

### 🎯 一句话标答

> 重复退化的成因是**自我强化的正反馈**——模型看到自己刚生成的 pattern，会**进一步提高**该 pattern 的概率，形成吸引子；三种惩罚是 **repetition_penalty**（对已出现 token 的 logit 做**乘/除**，注意负 logit 的非对称处理）、**frequency_penalty**（按**出现次数**线性减）、**presence_penalty**（**出现过就减**，不计次数）——但它们对**代码、JSON、专有名词**是有害的，因为那些场景**必须重复**。

### 🗣️ 30 秒口语版

"先讲**成因**，才知道该怎么治。

**重复退化的机制：正反馈循环。**

假设模型生成了 "我很开心我很开心"。现在它看这个上下文，会怎么想？
- Transformer 的 in-context learning 能力**极强**——它看到 "我很开心" 出现了两次，会**认为这是一个强 pattern**
- 于是它给下一个 "我" 更高的概率
- 生成之后，pattern 出现了三次，**信号更强了**
- **正反馈** → 概率越来越高 → **收敛到一个吸引子，永远出不来**

**关键**：这**不是模型坏了，恰恰是模型的 in-context learning 太强了**。它忠实地执行了"发现规律、延续规律"。**greedy 让这个循环变成绝对的**——一旦某个 token 概率最高，就永远选它。

**所以第一道防线其实是采样**（Q1）——temperature > 0 给了跳出吸引子的机会。惩罚是第二道防线。

**三种惩罚：**

**1. repetition_penalty（CTRL 论文提出）**
$$z_i' = \begin{cases} z_i / \theta & z_i > 0 \\ z_i \times \theta & z_i \le 0 \end{cases} \quad (\text{对已出现过的 token},\ \theta > 1)$$

**为什么要分正负？** 因为如果一律除以 $\theta$，负 logit 除以 1.2 会**变大**（-10/1.2 = -8.3 > -10）—— **惩罚变成了奖励**！所以负数要乘。这个非对称设计是个**必答的细节**。

- $\theta = 1.0$：无惩罚
- $\theta = 1.1 \sim 1.2$：典型
- $\theta > 1.5$：**模型开始说胡话**（被逼着用怪词）

**2. frequency_penalty（OpenAI）**
$$z_i' = z_i - \alpha \cdot \text{count}(i)$$
出现越多，罚越重。**线性累加**。

**3. presence_penalty（OpenAI）**
$$z_i' = z_i - \beta \cdot \mathbb{1}[\text{count}(i) > 0]$$
**只要出现过就罚固定的量**，不管几次。

**三者的分工（这个辨析是题眼）**：
- **repetition_penalty**：乘性，**和 logit 大小相关** → 对高置信的 token 惩罚更重
- **frequency_penalty**：**治"车轱辘话"** —— 同一个词说十遍，罚十倍
- **presence_penalty**：**治"话题不发散"** —— 逼模型引入**新词汇/新话题**，不管旧词说了几遍

**最重要的陷阱——这些惩罚经常是有害的：**

| 场景 | 后果 |
|---|---|
| **代码生成** | 变量名**必须**重复（`for i in range(n): arr[i] = arr[i] + 1`）→ 惩罚会逼模型换名字 → **代码直接错** |
| **JSON / 结构化输出** | `"`、`{`、`:` **必须**高频重复 → 惩罚会让模型**输出非法 JSON** |
| **长文本** | 专有名词、主角名字必须重复 → 模型开始用代词、或者**改名字** |
| **RAG / 抽取** | 要忠实引用原文 → 惩罚逼它改写 → **失真** |

**所以：代码、JSON、Function Calling 场景，全部惩罚设为 0（或 1.0）。** 这是个真实的线上事故来源。"

### 📐 三种惩罚对比

| | repetition_penalty | frequency_penalty | presence_penalty |
|---|---|---|---|
| 来源 | CTRL (Salesforce) | OpenAI API | OpenAI API |
| 形式 | **乘性**（÷θ 或 ×θ） | **加性**，按次数 | **加性**，0/1 |
| 是否累加 | ❌ 出现 1 次和 10 次一样 | ✅ **线性累加** | ❌ |
| 与 logit 大小 | **相关**（高 logit 罚得多） | 无关 | 无关 |
| 治什么 | 通用重复 | **车轱辘话** | **话题不发散** |
| 典型值 | 1.05 - 1.2 | 0.1 - 0.5 | 0.1 - 0.5 |
| 危险值 | **> 1.5 说胡话** | > 1.0 | > 1.0 |

```python
def apply_penalties(logits, generated_ids, rep_p=1.0, freq_p=0.0, pres_p=0.0):
    counts = torch.bincount(generated_ids, minlength=logits.size(-1))
    appeared = counts > 0

    # ① repetition_penalty —— 注意正负号的非对称处理！
    if rep_p != 1.0:
        pos = appeared & (logits > 0)
        neg = appeared & (logits <= 0)
        logits[pos] = logits[pos] / rep_p    # 正的：除 → 变小 ✅
        logits[neg] = logits[neg] * rep_p    # 负的：乘 → 变小 ✅（除会变大！）

    # ② frequency_penalty —— 按次数线性罚
    if freq_p != 0.0:
        logits = logits - freq_p * counts.float()

    # ③ presence_penalty —— 出现过就罚
    if pres_p != 0.0:
        logits = logits - pres_p * appeared.float()

    return logits
```

### 🔥 高频追问 Top 3

**Q：为什么模型训练时用了那么多正常文本，还是会重复退化？**

A：**根本原因是训练和推理的<u>不匹配</u>——Exposure Bias（暴露偏差）。**

- **训练时（Teacher Forcing）**：模型每一步的输入都是**真实的人类文本**。它从来没见过"自己生成的、有点跑偏的文本"长什么样。
- **推理时**：模型的输入是**自己生成的**。一旦某一步稍微偏了，它就进入了一个**训练时从未见过的分布**（out-of-distribution）。
- 而在 OOD 区域，模型的行为是**不可预测**的——它会抓住最显眼的统计规律（"刚才那个 pattern 重复了"）疯狂放大。

**更深一层**：**MLE 训练目标本身就鼓励"安全"的输出**。最小化交叉熵 = 最大化对训练文本的 likelihood → 模型学会"把概率质量堆在最保险的选项上" → 生成时就倾向于选最保险的 → **保险的选择往往就是"重复刚才说过的"**。

**缓解手段（按代价排序）**：
1. **采样**（temperature/top-p）—— 免费，第一道防线
2. **惩罚** —— 免费，但**有副作用**（见陷阱）
3. **SFT / DPO** —— 有成本，但**治本**。对齐后的模型重复退化**大幅减少**，因为 RLHF 的 reward 会惩罚重复的回答
4. **改训练目标** —— Unlikelihood Training、对比学习（SimCTG）等。学术方案，工业上少用

> **注意第 3 条**：**这就是为什么 base model 比 chat model 重复得多**。你在 HF 上直接跑 `Llama-3-8B`（base）会发现它疯狂重复，而 `Llama-3-8B-Instruct` 好得多。**很多人以为是采样参数问题，其实是对齐的功劳。**

**Q：为什么有的模型（尤其推理模型）特别容易重复？**

A：**因为生成长度。重复退化的概率随生成长度<u>超线性</u>增长。**

正反馈需要"**积累证据**"——生成 50 个 token 时，模型还没足够的自我重复来触发循环；生成 5000 个 token 时，**统计上必然会出现某个 pattern 重复两次**，然后就滚起来了。

所以：
- **短回答**（<200 token）：很少重复
- **长文章**（2000+ token）：偶尔重复
- **推理模型的长 CoT**（5000-20000 token）：**高发**

R1 的典型症状：`"等等，让我重新验证一下… 等等，让我重新验证一下…"` —— 这正是 Q1 追问 3 里 DeepSeek 官方**不建议 temperature=0** 的原因。

**其他缓解**：
- 设 `max_tokens` 硬截断（保底）
- 检测循环（**n-gram 重复检测**）主动打断
- **推理模型专用**：检测到"思考"超时就强制注入 `</think>` 让它出答案（这是 R1 部署的实用 trick）

**Q：DRY / no_repeat_ngram_size 是什么？**

A：**是"精确匹配"路线的惩罚，和上面三种"统计"路线不同。**

- **`no_repeat_ngram_size=n`**：**硬禁止**任何 $n$-gram 出现第二次。
  - ✅ 简单粗暴、有效
  - ❌ **太粗暴**——"the United States of America" 是个 5-gram，设 `n=4` 就**不许它出现第二次**，长文里根本不现实
  - 只适合短文本

- **DRY（Don't Repeat Yourself，社区方案）**：更聪明——检测**当前正在生成的序列是不是某个历史序列的前缀**，**匹配越长，对下一个 token 的惩罚指数增长**。
  - 直觉：如果你已经重复了 8 个 token，那第 9 个 token **几乎肯定**是在复读，罚死它
  - 但如果只匹配了 2 个 token，可能是正常的语言搭配，**轻罚**
  - ✅ **对代码和长文本友好得多**——因为它罚的是"**长串复读**"，不是"**单个词重复**"
  - 社区（角色扮演、创意写作）广泛使用，主流 API 还没提供

**答这题的正确姿态**：能说出"`no_repeat_ngram` 太粗暴不能用于长文本和代码"，以及"DRY 罚的是**长串复读**而不是**词频**，所以副作用小得多"，就说明你理解了**这几种惩罚的设计哲学差异**——是"罚词频"还是"罚复读"。

### ⚠️ 常见陷阱

1. **不知道 repetition_penalty 对负 logit 要乘不能除** —— 除会**变成奖励**。这是必答细节。
2. **在代码/JSON 场景开惩罚** —— **真实的线上事故**。这些场景必须关。
3. **只治标不问因** —— 要能说出"正反馈 + exposure bias + MLE 的保守偏好"这条因果链。
4. **不知道对齐能治本** —— base model 重复是常态，chat model 好得多。
5. **把三种惩罚混为一谈** —— frequency 治车轱辘话，presence 治话题不发散，分工不同。

### 🏢 大厂偏好

- **应用岗**：**高频**——会问"用户反馈模型复读，你怎么办"（标准答案：先看是不是 base model / temperature=0 / 生成太长，再考虑惩罚，且要问清是不是代码场景）
- **算法岗**：会问 exposure bias 和训练目标的关系

### 📚 延伸阅读
- [CTRL](https://arxiv.org/abs/1909.05858) - repetition_penalty 的出处
- [Neural Text Generation with Unlikelihood Training](https://arxiv.org/abs/1908.04319) - 从训练目标治本
- [A Contrastive Framework for Neural Text Generation (SimCTG)](https://arxiv.org/abs/2202.06417)

---

## Q3：为什么 LLM 不用 Beam Search ⭐⭐⭐⭐⭐（必问）

### 🎯 一句话标答

> 因为 **"最大概率序列" 在开放式生成里恰恰是<u>最差</u>的文本**——人类文本的 likelihood 是**剧烈波动**的，而 beam search 精确地找出了那条"一路平滑地高概率"的路径，**读起来重复、乏味、无信息量**；再加上**成本高**（KV Cache × beam 宽度）、**和 continuous batching 打架**、以及**对齐之后 top-1 已经足够好**，所以它在 NMT 时代是标配，到 LLM 时代被彻底淘汰。

### 🗣️ 30 秒口语版

"这题的关键是理解**任务性质变了**。

**Beam Search 在做什么**：维护 $B$ 条候选序列，每步扩展所有可能、按累积 log-prob 排序、保留最好的 $B$ 条。目标是**近似求解**：
$$\hat{y} = \arg\max_y \ \log P(y \mid x)$$

**在 NMT 时代这是对的**：翻译有**近乎唯一的正确答案**，"最可能的译文"就是"最好的译文"。Beam search 相对 greedy 稳定提升 BLEU 1-2 个点，是**标配**。

**到了 LLM 的开放式生成，这个假设崩了。**

**理由一（最根本）：人类文本的 likelihood 不是一路最高的。**

Holtzman 那篇论文有一张著名的图——把人写的文章喂给模型算每个词的概率，你会看到**剧烈的锯齿波动**：人经常用低概率的词。**这正是信息量的定义**（$I = -\log p$）——**低概率 = 高信息量 = 有意思**。

而 beam search 生成的文本，likelihood 曲线是**一条平滑的高位直线**。翻译成人话：**它精确地找到了那条最无聊的路**。

$$\boxed{\arg\max_y P(y) \ne \text{最好的文本}}$$

**理由二：beam search 会加重重复退化。**
Beam search 比 greedy **更容易**陷入循环——因为重复的序列往往**累积 log-prob 更高**（每一步都选安全的），所以 **beam 里会挤满互相之间只差一点点的重复序列**，多样性反而更差。这很反直觉：**"搜索得更狠 = 更容易掉进坑里"**。

**理由三：成本。**
- **KV Cache × B**（这是硬的——第 12 章说了 KV Cache 决定并发）
- 计算 × B
- 而 beam=4 只是为了**输出一条**序列 —— **4 倍的成本换一条更无聊的输出**

**理由四：和 serving 架构打架。**
Beam search 要**分叉、剪枝、重排序** —— 每一步 beam 的成员都在变。这和 **continuous batching** 的调度逻辑（第 12 章 Q5）严重冲突。vLLM 支持 beam search，但**明确不推荐**，而且性能很差。（PagedAttention 的 COW fork 倒是为 beam search 设计过，但生态已经不用了。）

**理由五：RLHF 之后没必要了。**
对齐后的模型，**top-1 已经很好了**——RLHF 的作用就是把"人类喜欢的回答"的概率推到最高。**Beam search 的边际收益趋近于 0**，但成本是实打实的 4 倍。

**但要说清什么时候 beam search 还有价值**（不要一棍子打死）：
- **有客观正确答案的任务**：翻译（DeepL 仍在用）、语音识别（Whisper 默认 beam=5）、约束满足
- **代码生成的 pass@1**：有工作发现 beam search 略优于 greedy
- **投机解码的 tree attention** —— 某种意义上是 **beam search 的复活**（Q6）！不过它搜的是"draft 的候选树"，不是"最终输出"，目标完全不同。"

### 🔍 Beam Search vs Sampling

| 维度 | Beam Search | Sampling (top-p) |
|---|---|---|
| 目标 | $\arg\max P(y\mid x)$ | 从 $P(y\mid x)$ **采样** |
| 适合 | **有唯一答案**（翻译、ASR） | **开放式生成** |
| 多样性 | **极差**（beam 内高度同质） | 好 |
| 重复退化 | **比 greedy 更严重** | 轻 |
| KV Cache | **× B** ❌ | × 1 |
| 与 continuous batching | **冲突** ❌ | 兼容 |
| 长度偏置 | **有**（需 length penalty hack） | 无 |
| LLM 时代 | **淘汰** | **主流** |

**长度偏置那一行值得展开**——beam search 用**累积 log-prob** 排序，而 $\log p < 0$，**序列越长分数越负** → **天然偏好短序列** → 必须加一个 length penalty：

$$\text{score}(y) = \frac{\log P(y)}{|y|^\lambda}$$

$\lambda$ 是个**没有理论依据的 hack**，要靠调。**"一个需要 hack 才能工作的方法"本身就是设计有问题的信号**——这个评论能加分。

### 🔥 高频追问 Top 3

**Q：那 Best-of-N 采样呢？它和 beam search 什么区别？**

A：**这是个非常好的对比，两者常被混淆。**

| | Beam Search | **Best-of-N (Rejection Sampling)** |
|---|---|---|
| 何时剪枝 | **每一步都剪**（贪心地） | **不剪**，生成完整 N 条 |
| 打分依据 | **模型自己的 log-prob** | **外部 Reward Model** ✅ |
| 多样性 | 差（beam 高度同质） | **好**（独立采样） |
| 能否并行 | 半并行（每步同步） | **完全并行** ✅ |
| LLM 时代 | 淘汰 | **主流**（RLHF、o1 的 test-time scaling） |

**核心区别**：
1. **打分的东西不同**——beam search 相信**模型自己的概率**，而我们刚论证了"概率不等于质量"。Best-of-N 用**外部 RM** 打分——**RM 学的正是"人类觉得好不好"**，这才是我们真正想优化的东西。
2. **剪枝时机不同**——beam search **每步都贪心剪枝**，可能过早砍掉"开头平淡但后面精彩"的路径。Best-of-N **生成完整序列再评判**，不会误杀。

**所以正确的说法是**："**Beam Search 不是死了，而是<u>进化</u>了**——它的位置被 Best-of-N + RM 取代了。因为我们终于有了一个比 log-prob 更好的打分器。" 这句话能把第 9 章的 RLHF 和本章串起来。

**再往前一步**：o1/R1 的 **test-time scaling** 本质上就是这条路线的延伸——**用更多的采样 + 更好的验证器换质量**。**Beam Search 是"用模型自己搜自己"，Best-of-N 是"用一个更强的裁判来选"** —— 后者显然更对。

**Q：Beam Search 在投机解码里"复活"了？**

A：**形式上像，本质不同——但这个联系确实有洞察。**

Medusa/EAGLE 的 **Tree Attention**（Q6）确实在维护一棵候选树：每个 draft head 出 top-k 候选 → 组合成树 → **一次 attention 验证整棵树**。这个"多候选并行推进"的形态和 beam search 很像。

**但目标完全不同**：
- **Beam Search**：树是**最终输出**的候选，选一条**最高分**的给用户 → **它在替模型做决定**
- **Speculative Tree**：树是**draft 的候选**，用 target model 验证，**最终输出严格服从 target 的分布**（Q4 的无损性）→ **它不做任何决定，只是在猜**

$$\boxed{\text{Beam Search 改变了输出分布；投机解码的树不改变输出分布}}$$

**所以**：投机解码的树 = "**多押几注，反正有人验**"；beam search = "**我来替你选**"。**前者无损，后者有偏。这是本质区别。**

**能把这个辨析讲清楚，说明你真的理解了"投机解码为什么无损"这件事的深刻性。**

**Q：Contrastive Search 是什么？它解决了 beam search 的问题吗？**

A：**是个有意思的中间路线，知道它显得视野宽。**

**动机**：既要 beam search 的"确定性、可控"，又不要它的"重复乏味"。

**做法**：每步在 top-k 候选里，选择使下式最大的 token：
$$x_t = \arg\max_{v \in V^{(k)}} \Big\{ (1-\alpha)\underbrace{p(v \mid x_{<t})}_{\text{模型置信度}} - \alpha \underbrace{\max_{j<t} \text{sim}(h_v, h_j)}_{\textbf{退化惩罚}} \Big\}$$

第二项是关键：**惩罚"和历史 token 的 hidden state 太相似"的候选** —— 直接从**表示空间**上阻止重复，而不是从**token 计数**上（Q2 的惩罚都是后者）。

**评价**：
- ✅ 思路漂亮——**在语义空间治重复，比 token 层面的惩罚更本质**（同义词换着说也会被抓）
- ✅ 完全确定性，无随机性
- ❌ 每步要算 **hidden state 的两两相似度** → **额外开销**
- ❌ 需要配套的 **SimCTG 对比学习训练**（模型的表示空间要足够各向同性）才能发挥最佳效果
- ❌ **工业界没普及**——因为 top-p + 对齐已经够好了，不值得为这点提升付出复杂度

**这是个很典型的"学术上优雅、工业上不划算"的案例**。能给出这个评价，比只会背名词强。

### ⚠️ 常见陷阱

1. **说 beam search 因为"太慢"被淘汰** —— **只答对了次要原因**。根本原因是**"最大概率 ≠ 最好文本"**，这是个**质量**问题不是**速度**问题。答成速度问题会被追问到死。
2. **不知道 beam search 会<u>加重</u>重复** —— 很反直觉，答出来加分。
3. **一棍子打死** —— 翻译、ASR、约束满足场景 beam search **仍是标配**（Whisper 默认 beam=5）。要说清"任务性质"这个判据。
4. **不知道它被 Best-of-N + RM 取代了** —— 这是完整答案的最后一块。

### 🏢 大厂偏好

- **所有大厂**：必问，是个**很好的"你懂不懂 LLM 和传统 NLP 的区别"的探针**
- **算法岗**：会追问 Best-of-N、test-time scaling 的关系
- **有 NMT / ASR 背景的团队**：会问"那我们的翻译服务还要用 beam 吗"（答：要）

### 📚 延伸阅读
- [The Curious Case of Neural Text Degeneration](https://arxiv.org/abs/1904.09751) - **beam search 被判死刑的那篇**
- [A Contrastive Framework for Neural Text Generation](https://arxiv.org/abs/2202.06417) - Contrastive Search
- [If beam search is the answer, what was the question?](https://arxiv.org/abs/2010.02650) - 一篇有意思的理论反思

---

## Q4：投机解码原理与"为什么无损" ⭐⭐⭐⭐⭐（必问，要推导）

### 🎯 一句话标答

> 核心洞察是 **"生成是串行的，验证是并行的"**——用小模型串行猜 $\gamma$ 个 token，大模型**一次前向并行验证全部 $\gamma+1$ 个位置**；它之所以**赚**，**100% 依赖第 12 章的结论：Decode 是 memory-bound，大模型算 1 个 token 和算 5 个 token 读权重的时间<u>一模一样</u>，所以多验证几个几乎免费**；它之所以**无损**，是因为用了**修正的拒绝采样**——关键恒等式 $\min(p,q) + \max(0, p-q) = p$ 保证输出分布**严格等于**从大模型直接采样。

### 🗣️ 30 秒口语版

"这题要分三层：**为什么能快、算法是什么、为什么无损**。三层缺一不可。

**第一层：为什么能快——这是全部的地基。**

回忆第 12 章 Q3：**Decode 是 memory-bound**。大模型每步要读 14 GB 权重，但只做 14 GFLOP 的计算——**算力利用率不到 1%**。

那么问题来了：**如果我一次给它 5 个 token 算，会慢 5 倍吗？**

**不会！** 读权重还是 14 GB（**一模一样**），计算变成 70 GFLOP——**但算力本来就闲着**。所以**验证 5 个 token 和验证 1 个 token，耗时几乎相同**。

$$\boxed{\text{"验证是免费的"} \Longleftarrow \text{Decode 是 memory-bound}}$$

**这是投机解码的全部前提。** 记住这句话——它同时解释了 Q7 的"为什么大 batch 下投机解码失效"（因为那时不再是 memory-bound 了）。

**第二层：算法。**

设小模型（draft）分布 $q$，大模型（target）分布 $p$。

1. **Draft**：小模型**串行**生成 $\gamma$ 个 token（比如 5 个），很快（模型小 100 倍）
2. **Verify**：大模型**一次前向**，拿到这 $\gamma+1$ 个位置的分布 $p_1, \dots, p_{\gamma+1}$
   - 注意：**能一次算完，是因为这些 token 已知了** —— **这本质上是一次 prefill！**
3. **接受/拒绝**：逐个检查 draft token $x_i$：
   - **若 $q(x_i) \le p(x_i)$：直接接受** —— draft 比 target 还保守，target 更喜欢它，白捡
   - **若 $q(x_i) > p(x_i)$：以概率 $p(x_i)/q(x_i)$ 接受**，否则**拒绝并停止**
4. **拒绝后**：从**修正分布** $p'(x) = \text{norm}(\max(0, p(x)-q(x)))$ 重采一个
5. **全部接受时**：额外从 $p_{\gamma+1}$ 采一个 **bonus token** → 所以 $\gamma$ 个 draft **最多产出 $\gamma+1$ 个 token**

> **两个关键点**：
> - **一旦拒绝就停止**——后面的 draft token 全部作废。因为它们是**基于被拒绝的那个 token** 生成的，前提已经错了。
> - **每轮至少产出 1 个 token**——就算第一个就被拒，修正采样也会给你一个。**所以投机解码永远不会比原来慢（除了 draft 的开销）。**

**第三层：为什么无损——必须能证明。**

这是本题的**核心考点**。要证明：**这个过程输出 $x$ 的概率，严格等于 $p(x)$**。

$$
\begin{aligned}
P(\text{输出 } x) &= \underbrace{q(x)\cdot\min\left(1, \tfrac{p(x)}{q(x)}\right)}_{\text{采到 }x\text{ 且接受}} + \underbrace{P(\text{拒绝})\cdot p'(x)}_{\text{拒绝后重采到 }x} \\[4pt]
&= \min(q(x), p(x)) + \max(0,\ p(x)-q(x)) \\[4pt]
&= p(x) \qquad\checkmark
\end{aligned}
$$

最后一步靠一个**初等恒等式**：$\min(a,b) + \max(0, a-b) = a$。

- 若 $p \ge q$：$\min = q$，$\max = p - q$，和 $= p$ ✅
- 若 $p < q$：$\min = p$，$\max = 0$，和 $= p$ ✅

**中间那步的关键**是要证明 $P(\text{拒绝}) \cdot p'(x) = \max(0, p(x)-q(x))$ ——也就是**拒绝的总概率，恰好等于修正分布的归一化常数**。这个"恰好"不是巧合，我一会儿推给你看。

**所以投机解码是<u>数学上严格无损</u>的**——不是"效果差不多"，是**输出分布逐 token 完全相同**。**这和量化（有损）、KV 驱逐（有损）有本质区别**。"

### 📐 无损性的完整证明（必须能写）

**记号**：target 分布 $p$，draft 分布 $q$，词表 $V$。

**算法**：
1. 采样 $x \sim q$
2. 以概率 $\min\left(1, \frac{p(x)}{q(x)}\right)$ 接受
3. 若拒绝，采样 $x \sim p'$，其中 $p'(x) = \dfrac{\max(0,\ p(x) - q(x))}{\sum_{x'} \max(0,\ p(x') - q(x'))}$

---

**Step 1：接受的那一支**

$$P(\text{采到 }x\text{ 且接受}) = q(x)\cdot\min\left(1, \frac{p(x)}{q(x)}\right) = \min\big(q(x),\ p(x)\big)$$

**Step 2：拒绝的总概率**

$$P(\text{拒绝}) = 1 - \sum_{x} \min(q(x), p(x))$$

**Step 3：关键引理——$P(\text{拒绝})$ 恰好是 $p'$ 的归一化常数**

$$
\begin{aligned}
\sum_{x} \max\big(0,\ p(x) - q(x)\big) &= \sum_{x} \Big[p(x) - \min\big(p(x), q(x)\big)\Big] \\
&= \underbrace{\sum_x p(x)}_{=1} - \sum_x \min(p(x), q(x)) \\
&= 1 - \sum_x \min(p(x), q(x)) \\
&= P(\text{拒绝}) \qquad\checkmark
\end{aligned}
$$

> 🚨 **第一个等号是全部的技巧**：$\max(0, a-b) = a - \min(a, b)$。**这个恒等式对任意实数成立**，代进去就通了。
>
> 这个引理说明——**"被拒绝的概率质量"和"$p$ 比 $q$ 多出来的那部分质量"是<u>同一个东西</u>**。所以拒绝之后，我们只需要把这份质量**按 $\max(0,p-q)$ 的形状还回去**，$p$ 就被完整重建了。**这个直觉比公式更值得记。**

**Step 4：合并**

$$
\begin{aligned}
P(\text{输出 } x) &= \min(q(x), p(x)) + P(\text{拒绝}) \cdot p'(x) \\
&= \min(q(x), p(x)) + \cancel{P(\text{拒绝})} \cdot \frac{\max(0, p(x)-q(x))}{\cancel{P(\text{拒绝})}} \quad (\text{由 Step 3}) \\
&= \min(q(x), p(x)) + \max(0, p(x) - q(x)) \\
&= p(x) \qquad \blacksquare
\end{aligned}
$$

---

> **面试时能在白板上写出这四步，是本章最高价值的输出。** 尤其是 **Step 3 的引理**——那是整个证明的枢纽，也是最容易卡住的地方。
>
> **一句话记忆版**："**接受的部分给了 $\min(p,q)$，拒绝后重采的部分给了 $\max(0,p-q)$，两者相加正好是 $p$。**"

#### 一个数值例子（帮助建立直觉）

词表 = {A, B, C}：

| | A | B | C |
|---|---|---|---|
| **q (draft)** | 0.5 | 0.4 | 0.1 |
| **p (target)** | 0.3 | 0.4 | 0.3 |
| **接受概率** $\min(1, p/q)$ | $0.3/0.5 = 0.6$ | $\min(1, 1) = 1$ | $\min(1, 3)=1$ |

**逐 token 追踪**：

| | 采到并接受 $\min(p,q)$ | 修正分布补充 $\max(0,p-q)$ | **合计** |
|---|---|---|---|
| A | $0.5 \times 0.6 = 0.3$ | $\max(0, 0.3-0.5) = 0$ | **0.3** = $p(A)$ ✅ |
| B | $0.4 \times 1 = 0.4$ | $\max(0, 0.4-0.4) = 0$ | **0.4** = $p(B)$ ✅ |
| C | $0.1 \times 1 = 0.1$ | $\max(0, 0.3-0.1) = 0.2$ | **0.3** = $p(C)$ ✅ |

**验证**：
- $P(\text{拒绝}) = 1 - (0.3+0.4+0.1) = 0.2$
- $\sum \max(0, p-q) = 0 + 0 + 0.2 = 0.2$ ✅ **相等**（Step 3 的引理）
- 修正分布 $p' = [0, 0, 1]$ —— **拒绝之后 100% 输出 C**

**读懂这个例子**：draft 模型**高估了 A、低估了 C**。算法的修正机制是——**A 被采到时有 40% 概率被拒**，而**拒绝之后必定补给 C**。**多退少补，分毫不差。**

### 💻 伪代码

```python
def speculative_decode_step(target, draft, prefix, gamma=5):
    """一轮投机解码，返回本轮产出的 token 列表（长度 1 ~ gamma+1）"""

    # ---- ① Draft：小模型串行生成 gamma 个（快，因为模型小）----
    draft_tokens, draft_probs = [], []
    cur = prefix
    for _ in range(gamma):
        q = draft(cur)                       # [vocab]
        x = torch.multinomial(q, 1)
        draft_tokens.append(x); draft_probs.append(q)
        cur = cur + [x]

    # ---- ② Verify：大模型 ★一次★ 前向，拿到全部 gamma+1 个位置的分布 ----
    #     这一步本质上是一次 prefill —— 因为 token 已知，可以并行！
    #     ★ 关键：这一次前向的耗时 ≈ 平时 decode 一个 token 的耗时（memory-bound）
    target_probs = target(prefix + draft_tokens)     # [gamma+1, vocab]

    # ---- ③ 逐个接受/拒绝 ----
    accepted = []
    for i in range(gamma):
        x, q, p = draft_tokens[i], draft_probs[i], target_probs[i]
        r = torch.rand(1)
        if r < min(1.0, p[x] / q[x]):
            accepted.append(x)                        # ✅ 接受
        else:
            # ❌ 拒绝 → 从修正分布重采一个，然后【立刻停止】
            p_corrected = torch.clamp(p - q, min=0)   # ← max(0, p-q)
            p_corrected /= p_corrected.sum()          # ← 归一化常数 = P(拒绝)
            accepted.append(torch.multinomial(p_corrected, 1))
            return accepted                           # ★ 后面的 draft 全部作废

    # ---- ④ 全部接受 → 白送一个 bonus token ----
    accepted.append(torch.multinomial(target_probs[gamma], 1))
    return accepted                                   # 长度 = gamma + 1
```

> **面试时要能指着这段代码说出三处**：
> 1. `target(prefix + draft_tokens)` —— **一次前向验证全部**，这是加速的来源，**本质是一次 prefill**
> 2. `p_corrected = clamp(p - q, min=0)` —— **修正分布**，无损性的来源
> 3. `return accepted` 在拒绝时**立刻返回** —— 后面的 draft 全废，这是接受率呈 $\alpha^k$ 衰减的原因（Q5）

### 🔥 高频追问 Top 3

**Q：为什么拒绝之后要用 $\max(0, p-q)$ 而不是直接从 $p$ 采？**

A：**因为直接从 $p$ 采会<u>过度采样</u>那些"$q$ 已经贡献过的" token —— 分布会偏。**

看数值例子里的 B：$p(B) = q(B) = 0.4$，接受率 100%。**它已经通过"接受"这条路拿到了完整的 0.4**。如果拒绝后还从 $p$ 采，B 又会额外拿到 $0.2 \times 0.4 = 0.08$ —— **总共 0.48 > 0.4，偏了**！

$$\text{修正分布必须只补"}q\text{ 欠 }p\text{ 的那部分"}$$

**$\max(0, p-q)$ 的每一项含义**：
- $p(x) > q(x)$：**draft 低估了 $x$**，欠了 $p(x)-q(x)$ → **补上**
- $p(x) \le q(x)$：**draft 高估了 $x$**，接受环节已经通过"随机拒绝"把多的削掉了 → **不用补，置 0**

**这就是"多退少补"的精确含义**：
- **"多退"** = 接受概率 $p/q < 1$，随机拒掉一部分（削峰）
- **"少补"** = 修正分布 $\max(0, p-q)$（填谷）

**能用"多退少补"这四个字概括，再配上数值例子，这题就满分了。**

**Q：投机解码对 greedy（temperature=0）也适用吗？**

A：**适用，而且更简单——但要小心一个坑。**

**greedy 下**，$p$ 和 $q$ 都退化成 one-hot 分布。算法简化为：

$$\text{接受} \iff x_{\text{draft}} = \arg\max p$$

也就是**直接比对**："大模型在这个位置会选什么？和 draft 选的一样吗？一样就接受，不一样就用大模型的。" **不需要任何随机数**。

**无损性依然严格成立**（代入 one-hot 到那个恒等式即可验证）。

**但有个真实的坑**：**浮点非确定性**。
- 大模型**一次前向验证 5 个位置** vs **逐个 decode 一个位置** —— 两者走的 **kernel 路径不同**（一个是 GEMM，一个是 GEMV！见第 12 章 Q1 追问 3），**归约顺序不同 → 浮点结果有 $10^{-6}$ 级差异**
- 如果两个 token 的 logit **差距小于这个误差**，argmax 就可能翻转
- **结果：投机解码的输出和纯 decode 的输出，偶尔会差一两个 token**

**这不是算法的问题，是浮点的问题** —— 但它意味着 **"bit-wise 可复现" 做不到**，做回归测试时要注意。

**能主动说出这个"理论无损但工程上有浮点差异"的细节，是很强的信号**——它和第 12 章 Q6 说的 Chunked Prefill 的浮点差异是**同一类问题**（并行归约顺序改变）。

**Q：接受率 $\alpha$ 和 $p$、$q$ 的距离有什么严格关系？**

A：**有，而且非常漂亮——$\alpha = 1 - D_{TV}(p, q)$。**

推导（用 Step 3 的引理）：

$$\alpha = \sum_x q(x)\min\left(1, \frac{p(x)}{q(x)}\right) = \sum_x \min(p(x), q(x)) = 1 - \sum_x \max(0, p(x)-q(x))$$

而全变差距离（Total Variation Distance）的定义：

$$D_{TV}(p,q) = \frac{1}{2}\sum_x |p(x)-q(x)| = \sum_x \max(0, p(x)-q(x))$$

（第二个等号：因为 $\sum(p-q) = 0$，所以正的部分和负的部分绝对值相等，各占一半）

$$\boxed{\alpha = 1 - D_{TV}(p, q)}$$

**这个式子的三个推论，全都很有用**：
1. **draft 和 target 分布越接近，接受率越高** —— 这是**数学定律**，不是经验。
2. **所以 draft model 必须和 target <u>同源</u>**：同一个 tokenizer、同一个训练数据分布、最好是同一个模型家族。用 Qwen 给 LLaMA 做 draft，$D_{TV}$ 大到没法用。
3. **它给出了 Medusa/EAGLE 的理论目标** —— 它们的训练目标本质上就是**最小化 $D_{TV}(p, q)$**。EAGLE 之所以比 Medusa 强，就是因为它的 $q$ 更接近 $p$（Q6）。

**这个式子能推出来，说明你把 Q4 和 Q5 打通了。这是本章能给的最强信号之一。**

### ⚠️ 常见陷阱

1. **说不清"为什么验证是免费的"** —— **必须回到第 12 章的 memory-bound**。答不出这个，就是没理解投机解码的地基，后面 Q7 也一定答不好。
2. **说投机解码"效果差不多"** —— **是<u>数学上严格</u>无损**，不是差不多。这是它相对量化的最大优势。
3. **写不出无损性证明** —— 大厂会真的让你推。记住 **$\min(a,b)+\max(0,a-b)=a$** 和 **Step 3 的引理**。
4. **以为拒绝后从 $p$ 重采** —— **分布会偏**。必须从 $\max(0,p-q)$ 采。
5. **忘了 bonus token** —— 全接受时会多送一个，所以是 $\gamma+1$ 不是 $\gamma$。这个 +1 在 Q5 的公式里很关键。
6. **忘了"拒绝即停止"** —— 后面的 draft 是基于错误前提的，必须全废。

### 🏢 大厂偏好

- **字节 / 阿里（推理引擎）**：**必问，且会让你在白板上推无损性**
- **DeepSeek**：会引向 MTP（Multi-Token Prediction，V3 的原生投机解码，见 Q6）
- **应用岗**：至少要能说清"为什么快"和"是无损的"这两点
- **Meta**：会问工程实现和浮点确定性

### 📚 延伸阅读
- [Fast Inference from Transformers via Speculative Decoding](https://arxiv.org/abs/2211.17192) - **Google, ICML 2023，无损性证明在附录 A.1**
- [Accelerating Large Language Model Decoding with Speculative Sampling](https://arxiv.org/abs/2302.01318) - DeepMind，同期独立工作
- [Blockwise Parallel Decoding](https://arxiv.org/abs/1811.03115) - 2018 年的祖师爷，Medusa 的思想源头

---

## Q5：接受率与加速比的数学 ⭐⭐⭐⭐⭐（必问，要推导）

### 🎯 一句话标答

> 设期望接受率为 $\alpha$、draft 长度 $\gamma$、draft/target 成本比 $c$，则**每轮期望产出** $\mathbb{E}[\#\text{tokens}] = \dfrac{1-\alpha^{\gamma+1}}{1-\alpha}$，**加速比** $= \dfrac{1-\alpha^{\gamma+1}}{(1-\alpha)(\gamma c + 1)}$；$\gamma$ 存在**最优值（典型 3-5）**——太小则摊薄不了 target 的前向成本，太大则后段 draft 以 $\alpha^k$ 指数衰减地被拒、白算；而 $\alpha = 1 - D_{TV}(p,q)$，**所以一切都归结为"draft 有多像 target"**。

### 🗣️ 30 秒口语版

"这题要能**当场推**，不能只背结论。

**Step 1：每轮期望产出多少 token？**

设每个 draft token 被接受的概率是 $\alpha$（独立同分布的简化假设）。令 $n$ = 本轮被接受的 draft 数。

因为**一旦拒绝就停止**，所以"接受至少 $k$ 个"意味着"前 $k$ 个全接受"：
$$P(n \ge k) = \alpha^k, \quad k \le \gamma$$

用**尾和公式** $\mathbb{E}[n] = \sum_{k\ge1} P(n \ge k)$：
$$\mathbb{E}[n] = \sum_{k=1}^{\gamma} \alpha^k = \frac{\alpha(1-\alpha^\gamma)}{1-\alpha}$$

产出的 token = 接受的 $n$ 个 + **总有 1 个**（要么是修正采样的，要么是 bonus）：
$$\mathbb{E}[\#\text{tokens}] = 1 + \frac{\alpha(1-\alpha^\gamma)}{1-\alpha} = \boxed{\frac{1-\alpha^{\gamma+1}}{1-\alpha}}$$

**Step 2：每轮花多少钱？**

以 target 的一次前向为单位，$c$ = draft 成本 / target 成本：
$$\text{Cost} = \underbrace{\gamma \cdot c}_{\gamma \text{ 次 draft 前向}} + \underbrace{1}_{1 \text{ 次 target 前向}}$$

（注意：**target 验证 $\gamma+1$ 个位置只算 1 次成本** —— 这就是 memory-bound 的红利）

**Step 3：加速比**
$$\boxed{\text{Speedup} = \frac{1-\alpha^{\gamma+1}}{(1-\alpha)(\gamma c + 1)}}$$

**代几个数感受一下**（取 $c = 0.1$，比如 1B draft + 70B target 的实际延迟比）：

| $\alpha$ | $\gamma=1$ | $\gamma=3$ | $\gamma=5$ | $\gamma=7$ | 最优 $\gamma$ |
|---|---|---|---|---|---|
| **0.5** | 1.36× | 1.44× | **1.32×** | 1.16× | ~3 |
| **0.7** | 1.55× | 2.03× | **2.10×** | 1.98× | ~5 |
| **0.8** | 1.64× | **2.39×** | 2.66× | 2.68× | ~7 |
| **0.9** | 1.73× | 2.83× | 3.42× | **3.84×** | ~10+ |

**读这张表要读出三件事**：

1. **$\alpha$ 是绝对的主角**。$\alpha$ 从 0.5 到 0.9，加速比从 1.4× 到 3.8×。**所有的工程努力（Medusa、EAGLE）都在提高 $\alpha$。**
2. **$\gamma$ 有最优值，而且随 $\alpha$ 增大而增大**。因为接受率高的时候，猜长一点不容易白费。
3. **$\alpha$ 低的时候，$\gamma$ 大反而<u>更慢</u>**（看 $\alpha=0.5$ 那行：$\gamma$ 从 3 到 7，加速比从 1.44 掉到 1.16）—— **因为后面的 draft 以 $\alpha^k$ 指数衰减地被拒，纯粹是白算**。

**Step 4：最重要的一句话——$\alpha$ 从哪来？**

Q4 追问 3 证明了：
$$\alpha = 1 - D_{TV}(p, q)$$

**所以一切归结为：draft 分布有多像 target 分布。** 这不是玄学，是**可以测量、可以优化**的量。

- 独立小模型：**同族**（LLaMA-1B for LLaMA-70B）$\alpha \approx 0.7$-0.8；**异族**（Qwen for LLaMA）→ tokenizer 都不一样，**根本没法用**
- Medusa（head 独立预测）：**$\alpha$ 低**，靠 tree 补
- EAGLE（feature 层自回归）：**$\alpha$ 高**，所以加速比最好

**这就是 Q6 的全部主线**：**一部 draft 方法的进化史，就是一部"如何提高 $\alpha$"的历史。**"

### 📐 推导细节

#### 尾和公式（面试可能会问为什么）

对非负整数随机变量 $n$：
$$\mathbb{E}[n] = \sum_{k=1}^{\infty} P(n \ge k)$$

**为什么**：$\sum_{k\ge1} P(n\ge k) = \sum_{k\ge1}\sum_{m\ge k} P(n=m) = \sum_{m\ge1} \sum_{k=1}^{m} P(n=m) = \sum_{m\ge1} m\,P(n=m) = \mathbb{E}[n]$ ✅
（交换求和顺序，数一数每个 $P(n=m)$ 被数了几次——正好 $m$ 次）

#### 化简验证

$$1 + \frac{\alpha(1-\alpha^\gamma)}{1-\alpha} = \frac{(1-\alpha) + \alpha - \alpha^{\gamma+1}}{1-\alpha} = \frac{1-\alpha^{\gamma+1}}{1-\alpha}\ \checkmark$$

**边界检查**（面试时做一遍，显得严谨）：
- $\alpha \to 0$（draft 全废）：$\mathbb{E} = \frac{1-0}{1-0} = 1$ ✅ —— **每轮仍产出 1 个 token**（修正采样保底）。**这就是"投机解码永不比原来慢"的数学表达**（除去 draft 的开销 $\gamma c$）。
- $\alpha \to 1$（draft 全对）：$\mathbb{E} \to \gamma+1$ ✅ —— **全部接受 + bonus**
- $\gamma \to \infty$，$\alpha<1$：$\mathbb{E} \to \frac{1}{1-\alpha}$ —— **收敛！**说明**猜再长也没用**，期望产出有上限 $\frac{1}{1-\alpha}$。$\alpha=0.8$ 时上限是 5，$\alpha=0.9$ 时是 10。**这个上限解释了为什么 $\gamma$ 不能无限大。**

#### 最优 $\gamma$

$$\gamma^* = \arg\max_\gamma \frac{1-\alpha^{\gamma+1}}{(1-\alpha)(\gamma c+1)}$$

分子是**饱和的**（趋于 $\frac{1}{1-\alpha}$），分母是**线性增长**的 → **必有最优点**。

$$
\begin{array}{c}
\text{分子：} 1-\alpha^{\gamma+1} \ \nearrow \ \text{但饱和} \\
\text{分母：} \gamma c + 1 \ \nearrow \ \text{线性，不饱和} \\
\hline
\Rightarrow \text{比值先升后降，存在 } \gamma^*
\end{array}
$$

**实践**：**不用求导，直接 grid search $\gamma \in \{1,...,8\}$，在真实流量上测。**

**更进阶：动态 $\gamma$**。既然最优 $\gamma$ 取决于 $\alpha$，而 $\alpha$ **是随内容变化的**——
- 生成 `"""` 后的 docstring、JSON 的固定字段名、代码的样板 → $\alpha \approx 0.95$，**应该猜长一点**
- 生成一个关键的数学推导步骤、一个创造性的比喻 → $\alpha \approx 0.3$，**应该猜短一点甚至不猜**

**SpecDec++、动态 draft 长度**这类工作就是在做这个：**用 draft model 自己的置信度（比如 $q$ 的熵）来决定要不要继续猜**。$q$ 的熵低（draft 很自信）→ 继续猜；熵高 → 停。

**这个"动态 $\gamma$"能主动提出来，是很好的工程直觉。**

### 🔥 高频追问 Top 3

**Q：$c$（draft 成本比）怎么估？7B draft 给 70B target 是 0.1 吗？**

A：**不是！这是个高频陷阱——参数量比 ≠ 延迟比。**

**朴素想法**：7B/70B = 0.1，所以 $c = 0.1$。**错。**

**为什么错**：draft 模型的一次前向也是 **memory-bound**（第 12 章）。它的耗时 ≈ **它自己的权重大小 / 带宽**，加上**固定开销**：
- kernel launch 开销（几十微秒，**和模型大小无关**）
- Python/调度开销
- **采样开销**（softmax over 150K vocab —— **这个开销和模型大小完全无关！**）

**对小模型，这些固定开销占比很大**：
```
7B  draft on A100:  14 GB / 2000 GB/s = 7.0 ms（理论）+ ~1 ms 固定 → c ≈ 8/70  ≈ 0.11
1B  draft:          2 GB / 2000 GB/s  = 1.0 ms（理论）+ ~1 ms 固定 → c ≈ 2/70  ≈ 0.03
68M draft:          0.14 GB / 2000    = 0.07ms（理论）+ ~1 ms 固定 → c ≈ 1.1/70 ≈ 0.016
                                                        ↑↑↑
                                          固定开销 >> 计算时间，"再小也没用了"
```

**关键推论**：**draft 模型小到一定程度，再小就没有收益了**（固定开销主导）—— 但**再小 $\alpha$ 会掉**！

$$\boxed{\text{所以 draft 模型的最优大小，是"}\alpha\text{ 够高" 和 "}c\text{ 够小" 的平衡点，通常是 target 的 } 1/10 \sim 1/50}$$

**而这正是 Medusa/EAGLE 的杀手锏**——它们的 draft head **只有几层 MLP，$c \approx 0.01$，而且共享 backbone 的 hidden state**（连 embedding 都不用重算）。**$c$ 几乎为 0，同时 $\alpha$ 还很高。**

**能说出"c 不等于参数量比，因为有固定开销"，是压过测的信号。**

**Q：$\alpha$ 在实际中是怎么分布的？真的是 i.i.d. 吗？**

A：**不是 i.i.d.，这是理论模型的最大简化。实际 $\alpha$ 是<u>强烈内容相关</u>的。**

**实测规律（要能举例）**：

| 内容 | $\alpha$ | 为什么 |
|---|---|---|
| **代码的样板**（`import numpy as np`、`if __name__ ==`） | **0.95+** | 几乎确定，小模型也会 |
| **JSON 的固定结构**（`{"name": "`） | **0.95+** | 格式约束强 |
| **RAG 里复制原文** | **0.9+** | 答案就在上下文里 |
| **常见搭配、虚词** | 0.85 | 语言的高频模式 |
| **实体名、数字** | **0.4** | 要"知识"，小模型不知道 |
| **关键推理步骤** | **0.3** | 要"能力"，小模型没有 |
| **创造性的比喻** | **0.2** | 本来就是低概率区 |

**所以真实的接受率是"阵发"的**：一连接受 8 个（在写样板代码），然后连拒 3 次（碰到一个需要知识的地方），再一连接受 10 个。

**$\alpha$ 的分布是<u>双峰</u>的，不是均值附近的钟形。**

**推论一（Q7 的伏笔）**：这解释了为什么**投机解码在代码和结构化输出上收益最大**（大段大段的高 $\alpha$ 区），**在创意写作上收益最小**。

**推论二**：这**强烈支持动态 $\gamma$** —— 既然 $\alpha$ 是阵发的，那固定 $\gamma$ 显然是次优的：高 $\alpha$ 区应该猛猜，低 $\alpha$ 区应该别猜。

**推论三**：这也解释了 **EAGLE-2 的"动态树"** 为什么比 EAGLE 的"静态树"强——**树的形状应该跟着置信度走**。

**Q：投机解码能和量化一起用吗？**

A：**能，而且是绝配——但有个"抢显存"的坑要说清。**

**为什么是绝配**：两者优化的是**同一个式子的不同项**（回到第 12 章的 Roofline）：

$$\text{TPOT} = \frac{\text{模型大小}}{\text{带宽}} \div \underbrace{\mathbb{E}[\#\text{tokens}]}_{\text{投机解码}}$$

- **量化**：**减小分子**（14 GB → 3.5 GB）
- **投机解码**：**除以一个 > 1 的数**（每次前向多产出 2-3 个 token）
- **两者相乘**：$4\times \times 2.5\times = 10\times$ 的理论加速 ✅ **正交叠加**

**实践中的常见组合**：**AWQ int4 的 target + 一个 fp16 的小 draft**（draft 本来就小，不用量化）。

**但有三个坑**：
1. **量化会降低 $\alpha$** ⚠️ —— 量化改变了 target 的分布 $p$！$D_{TV}(p_{\text{int4}}, q)$ 可能比 $D_{TV}(p_{\text{fp16}}, q)$ 大 → **接受率下降**。
   - **缓解**：**draft 也用同样的方式量化** —— 让两者的"量化偏差"方向一致，$D_{TV}$ 反而可能变小。
2. **显存竞争**（最现实的坑）：draft model 要占显存 → **挤占 KV Cache** → 第 12 章说了 KV Cache 决定并发 → **并发下降 → 吞吐下降**。
   - 一个 1B 的 draft 占 2 GB，够存 4 条 4K 请求的 KV。**如果你的服务是高并发的，这笔账可能是亏的**。
   - **这正是 Medusa/EAGLE 的另一个优势**：head 只有几十 MB，**几乎不占显存**。
3. **"无损"的复合含义要说清**：量化是**有损**的（改变了 $p$），投机解码是**相对量化后的 $p$ 无损**的。所以组合后的输出 = **量化模型的忠实输出**，不等于原始 fp16 模型的输出。**这个精确表述很重要**——不能说"量化+投机解码是无损的"。

**这三点里第 3 点最容易被忽略，答出来是很好的严谨性信号。**

### ⚠️ 常见陷阱

1. **忘了 +1** —— $\mathbb{E} = \frac{1-\alpha^{\gamma+1}}{1-\alpha}$ 里的 $\gamma+1$，来自 bonus token / 修正采样。漏了公式就错。
2. **以为 $\gamma$ 越大越好** —— 有最优值，且 $\alpha$ 低时大 $\gamma$ **更慢**。
3. **把 $c$ 当成参数量比** —— **固定开销**（kernel launch、采样）让小模型"再小也没用"。
4. **假设 $\alpha$ 是常数** —— 实际是**双峰、阵发**的，这直接推出动态 $\gamma$ 和动态树。
5. **忽略 draft model 抢显存** —— 高并发场景这可能让整笔账变亏。

### 🏢 大厂偏好

- **字节 / 阿里**：**会让你当场推 $\mathbb{E}[\#\text{tokens}]$ 并做边界检查**
- **应用岗**：会问"你怎么决定 $\gamma$"（答：grid search + 考虑动态 $\gamma$，因为 $\alpha$ 是阵发的）
- **所有岗**：**"$\alpha = 1 - D_{TV}$" 这个式子说出来就赢一半**

### 📚 延伸阅读
- [Fast Inference from Transformers via Speculative Decoding](https://arxiv.org/abs/2211.17192) - §3.2 有完整的加速比分析
- [SpecDec++](https://arxiv.org/abs/2405.19715) - 动态 draft 长度

---

## Q6：Draft 从哪来——Medusa / EAGLE / n-gram ⭐⭐⭐⭐⭐（必问）

### 🎯 一句话标答

> 一部 draft 方法的进化史，就是一部**"如何在 $c \to 0$ 的同时把 $\alpha$ 做高"**的历史：**独立小模型**（$\alpha$ 中、$c$ 中、**占显存**）→ **Medusa**（多个并行 LM head，$c\approx0$ 但**head 之间独立预测导致 $\alpha$ 低**，靠 **Tree Attention** 补）→ **EAGLE**（**在 feature 层做自回归**，且**把已采样的 token 喂回去消除歧义**，$\alpha$ 显著更高，是当前 SOTA）→ **n-gram / Prompt Lookup**（**零成本**，在 RAG/代码编辑等"复制原文"场景 $\alpha$ 极高）。

### 🗣️ 30 秒口语版

"按**进化线**讲最清楚，每一步都在解决上一步的问题。

**第 0 代：独立小模型（Vanilla Speculative Decoding）**
- LLaMA-1B 给 LLaMA-70B 做 draft
- **硬约束：必须同一个 tokenizer**（否则 token 都对不齐，$D_{TV}$ 无从谈起）
- ✅ $\alpha \approx 0.7$-0.8（同族模型）
- ❌ **要额外部署一个模型** → **占显存 → 挤 KV Cache → 降并发**（Q5 追问 3 的坑）
- ❌ **很多模型家族根本没有配套的小号**（Qwen-72B 有 0.5B，但 DeepSeek-V3 有什么？）
- ❌ 要跑**两个模型的调度**，工程复杂

**第 1 代：Medusa —— "不要独立模型，加几个头就行"**

洞察：**draft 和 target 可以共享 backbone**！在 target model 的**最后一层 hidden state** 上，接 $K$ 个额外的 LM head：
- Head 1 → 预测 $t+1$
- Head 2 → 预测 $t+2$
- Head 3 → 预测 $t+3$

✅ **$c \approx 0.01$** —— head 只是几层 MLP，而且**复用 backbone 已经算好的 hidden state**，等于白嫖
✅ **几乎不占显存**（几十 MB）
✅ 训练便宜（Medusa-1 冻结 backbone，只训 head，几小时搞定）

❌ **致命弱点：head 之间是<u>独立</u>的**！
Head 2 预测 $t+2$ 时，**不知道 head 1 预测了什么**。这**不是自回归**——它在做一个**边缘分布**的预测，而正确的应该是**条件分布**：
$$\text{Medusa head}_2 \text{ 学的是 } P(x_{t+2} \mid x_{\le t}), \quad \text{但我们要的是 } P(x_{t+2} \mid x_{\le t}, x_{t+1})$$
**丢掉了 $x_{t+1}$ 这个条件** → 越靠后的 head 越不准 → **$\alpha$ 衰减很快**

✅ **补救：Tree Attention（Medusa 最大的工程贡献）**
既然单个 head 不准，那就**每个 head 出 top-k 候选，组合成一棵树**：
```
head1 top-4 × head2 top-3 × head3 top-2 = 24 条候选路径
```
用一个**精心设计的 attention mask**（让每个节点只能看到自己的祖先），**一次前向验证整棵树**！总有一条路径蒙对。

**这又是 memory-bound 的红利**——验证 24 个 token 和验证 5 个 token，读权重的时间一样。

**第 2 代：EAGLE —— "在 feature 层做自回归"**

EAGLE 直接解决了 Medusa 的根本问题：**要自回归，但不要在 token 层做**。

**洞察 1：feature 层的自回归比 token 层更容易。**
Token 是**离散的、信息被压缩过的**；hidden feature 是**连续的、信息丰富的**。**预测下一个 feature 比预测下一个 token 更"平滑"、更容易学。**

**洞察 2（EAGLE 最关键的贡献）：feature 层的不确定性来自<u>采样的随机性</u>。**

想想——给定 $f_t$（第 $t$ 个位置的 feature），$f_{t+1}$ 是什么？**取决于 $x_{t+1}$ 采样采到了什么**！同一个 $f_t$，采到 "cat" 和采到 "dog"，会导向完全不同的 $f_{t+1}$。**这是不可约的歧义。**

**EAGLE 的解法：把已采样的 token 也喂进去**，消除这个歧义：
$$f_{t+1} = \text{DraftHead}\big(f_t,\ \text{Emb}(x_{t+1})\big)$$

**有了 $x_{t+1}$，歧义就没了** —— 剩下的就是一个**确定性的、可学的映射**。

这个"**feature + 已采样 token**"的双输入设计，是 EAGLE 相对之前所有工作（Medusa、Lookahead）的关键增量。

✅ **是真正的自回归**（解决了 Medusa 的独立性问题）→ **$\alpha$ 显著更高**
✅ **draft head 只有一层 decoder** → $c$ 仍然极小
✅ 加速比 **2.7-3.5×**（Medusa 是 ~2.2×）

**EAGLE-2**：**动态树** —— 树的形状不再固定，而是**根据 draft 的置信度动态调整**（把预算花在有把握的分支上）。加速比 **3.5-5×**。
> 这正好呼应 Q5 的"$\alpha$ 是阵发的" —— **既然 $\alpha$ 随内容剧烈波动，树的形状当然应该跟着变。**

**EAGLE-3**：发现"预测 feature"这个约束反而**限制了 scaling**（因为它要求 draft 去拟合一个固定的 feature 空间）。**去掉这个约束**，改用**多层 feature 融合 + 直接预测 token**，允许用更多数据训练 draft → 加速比继续提升。

**第 3 代（另一条路）：n-gram / Prompt Lookup —— "根本不要模型"**

**最简单也最被低估的方案**：**直接从 prompt 里找**。

生成到某处时，取最后 $n$ 个 token 当 key，**在 prompt 里做字符串匹配**，找到了就把后面几个 token 当 draft。

✅ **零训练、零显存、零额外模型**
✅ 在特定场景 $\alpha$ **极高**：
| 场景 | 为什么 $\alpha$ 高 |
|---|---|
| **RAG** | 答案大量引用检索到的原文 ✅ |
| **代码编辑 / 重构** | 输出 95% 是输入的复制 ✅ |
| **摘要 / 抽取** | 大段复制原文 ✅ |
| **多轮对话的引用** | "你刚才说的 X…" ✅ |

❌ 通用对话、创意写作 → 完全没用（$\alpha \approx 0$，但也不亏，因为成本为 0）

**vLLM 里一行配置**：`speculative_model="[ngram]"`。**这是 Agent / RAG 场景性价比最高的一刀**——零成本，特定场景 2-4×。

**第 4 代（架构级）：DeepSeek-V3 的 MTP**
DeepSeek-V3 在**预训练时**就加了 **Multi-Token Prediction** 目标（额外的模块预测未来多个 token）。
- **训练时**：MTP 是个**辅助 loss**，让模型学会"往前看"，**提升模型本身的质量**
- **推理时**：这些 MTP 模块**直接当 draft 用** —— **原生自带投机解码**！
- ✅ **不用额外训 draft**，$\alpha$ 天然高（同一个模型训出来的）
- 官方报告：MTP 的第二个 token **接受率 85-90%**，推理速度提升 **1.8×**

**这代表了一个方向：投机解码从"推理时的补丁"变成"预训练时的架构设计"。** 能提到这个是很强的前沿感知。"

### 🔍 完整对比

| | **独立小模型** | **Medusa** | **EAGLE(-2)** | **n-gram** | **MTP (V3)** |
|---|---|---|---|---|---|
| 需要额外模型 | ✅ **占显存** | ❌ 几十 MB | ❌ 几十 MB | ❌ **零** | ❌ 内置 |
| 需要训练 | ❌（用现成的） | ✅ 几小时 | ✅ 几小时 | ❌ **零** | ✅ **预训练时** |
| **是否自回归** | ✅ | ❌ **head 独立** | ✅ **feature 层** | — | ✅ |
| $c$ | 0.03-0.1 | **~0.01** | **~0.01** | **~0** | ~0.01 |
| $\alpha$ | 0.7-0.8 | 低（靠树补） | **高** ✅ | **场景相关**（0 或 0.9+） | **0.85-0.9** ✅ |
| Tree Attention | 可选 | ✅ **静态树** | ✅ **动态树** | ❌ | — |
| 典型加速 | 2-2.5× | ~2.2× | **3.5-5×** ✅ | 场景相关 **2-4×** | **1.8×** |
| **适合** | 有现成小号 | — | **通用最优** ✅ | **RAG / 代码编辑** ✅ | DeepSeek 自家 |

### 📐 Tree Attention 的机制

```
                    [已确认的 prefix]
                          │
              ┌───────────┼───────────┐
           "the"        "a"        "this"      ← head1 的 top-3
            │            │
        ┌───┴───┐    ┌───┴───┐
      "cat"  "dog"  "cat"  "man"                ← head2 的 top-2（每个分支各 2）
        │      │
     ┌──┴──┐ ...
   "sat" "ran"                                  ← head3 的 top-2

共 3 × 2 × 2 = 12 条候选路径，摊平成 ~12 个 token 一次送进 target
```

**关键是 attention mask —— 让每个节点只能看到自己的祖先**：

```
                prefix  the   a   this  cat₁ dog₁ cat₂ man₂ sat  ran
    prefix        ■
    "the"         ■     ■
    "a"           ■           ■
    "this"        ■                ■
    "cat"(在the下) ■     ■          ■              ← 能看到 "the"，看不到 "a"
    "dog"(在the下) ■     ■                   ■     ← 能看到 "the"，看不到 "cat"（是兄弟！）
    "cat"(在a下)   ■           ■                   ■
    ...
```

> **注意 "cat" 和 "dog" 这两行**——它们是**兄弟节点**，**互相看不见**。这就是 tree mask 的核心：**同层的兄弟是互斥的候选，绝不能互相 attend**（否则就是在"看着另一个候选做决定"，逻辑上荒谬）。
>
> **这个 mask 就是 tree attention 的全部技术含量。** 它把"验证 12 条路径"变成了"**一次前向 + 一个特殊 mask**" —— 又一次利用了 memory-bound 的红利。
>
> **和第 1 章 causal mask 的联系**：causal mask 是**链**上的 mask（只能看左边），tree mask 是**树**上的 mask（只能看祖先）。**causal mask 是 tree mask 在"树退化成链"时的特例。** 这个类比很漂亮，面试可以用。

### 🔥 高频追问 Top 3

**Q：为什么 EAGLE 要在 feature 层而不是 token 层做自回归？**

A：**三个理由，层层递进。**

1. **信息量**：token 是**离散的、被 argmax/采样压缩过的**——一个 token 只有 $\log_2(150000) \approx 17$ bit 信息；而 feature 是 4096 维的连续向量，**保留了模型的全部"想法"**（包括"我在犹豫 cat 和 dog"这种不确定性）。**从 feature 预测 feature，比从 token 预测 token 信息更充分。**

2. **平滑性**：feature 空间是**连续的**，相邻位置的 feature 变化平滑 → **好学**。token 空间是**离散的、组合爆炸的** → 难学。

3. **复用性（最实际的）**：target model 的 forward **本来就产出 feature** —— **不用重新算**，直接拿来喂给 draft head。**这是 $c \approx 0$ 的直接来源。**

**但 feature 层有个 EAGLE 之前没人解决的问题**——**采样的歧义**（洞察 2）。给定 $f_t$，$f_{t+1}$ **不确定**，因为它取决于 $x_{t+1}$ 采到了什么。

**EAGLE 的解法就是把 $x_{t+1}$ 也喂进去**：
$$f_{t+1} = \text{DraftHead}\big(f_t,\ \text{Emb}(x_{t+1})\big)$$

**这一步是 EAGLE 的全部精髓。** 加了这个条件之后，映射从"一对多的歧义"变成"**一对一的确定函数**" —— 一个单层 decoder 就能学好。

> 💡 **这个"把随机性显式地喂回去，把不确定的映射变成确定的映射"的技巧，值得单独记住**——它和 **Diffusion 模型把噪声 $\epsilon$ 作为条件输入**、**VAE 的重参数化**是同一个思想家族：**把随机性从"模型要建模的东西"变成"模型的输入"**。**面试时能给出这个类比，非常加分。**

**Q：Tree Attention 的树是越大越好吗？**

A：**不是，有明确的边际递减，且树大到一定程度会开始亏。**

**收益**：树越大，**至少有一条路径正确的概率越高** → 有效接受长度增加。但这是**次线性**的——第 2 个候选贡献很大（覆盖了最可能的 alternative），第 20 个候选贡献极小（它本来就是低概率的）。

**成本**：树越大，验证时的 token 数越多。回到 Q4 的地基——**"验证是免费的"只在 memory-bound 时成立**！树大到 64 个 token 时：
- 一次前向要算 64 个位置 × batch
- **算术强度 = $64 \times B$** —— 如果 $B=8$，那就是 512，**已经远超 A100 的平衡点 150！**
- **变成 compute-bound 了 → "免费"的前提消失 → 验证真的开始花钱了**

$$\boxed{\text{树的大小} \times \text{batch} > 150 \Rightarrow \text{compute-bound} \Rightarrow \text{tree attention 的红利消失}}$$

**这是把第 12 章的 Roofline 用到 tree attention 上的直接推论** —— **能推出这个，说明你的知识是可迁移的，而不是背下来的。**

**实践**：
- Medusa 的静态树是**离线用校准数据搜出来的**（不是拍脑袋）——搜的目标是"给定 token 预算，期望接受长度最大"
- **EAGLE-2 的动态树更优** —— 因为 $\alpha$ 是阵发的（Q5 追问 2），**静态树在高置信区浪费预算、在低置信区不够用**
- 典型树大小：**25-64 个 token**

**Q：Lookahead Decoding 是什么？它和 EAGLE 什么关系？**

A：**是另一条路——"不要 draft 模型，让 target 模型自己并行猜"。**

**核心：Jacobi 迭代。**

普通 decode 是**串行**求解一个不动点方程：$x_{t+1} = f(x_{\le t})$。

**Jacobi 迭代的想法**：**同时**猜 $n$ 个位置的值，然后**并行地**更新它们，反复迭代直到收敛（收敛 = 所有位置都等于"给定前缀该有的值"）。

$$\begin{bmatrix} x_{t+1}^{(k+1)} \\ x_{t+2}^{(k+1)} \\ \vdots \end{bmatrix} = \begin{bmatrix} f(x_{\le t}) \\ f(x_{\le t}, x_{t+1}^{(k)}) \\ \vdots \end{bmatrix}$$

**Lookahead 的增量**：Jacobi 迭代的中间产物里，**会顺手生成大量的 n-gram**。把它们**存进一个 pool**，后续步骤从 pool 里**捞候选来验证**。**迭代得越久，pool 越丰富，猜得越准。**

**评价**：
- ✅ **完全不需要 draft model、不需要训练** —— 部署零成本
- ✅ 加速 **1.5-2.3×**
- ❌ **比 EAGLE 慢**（EAGLE 3.5-5×）
- ❌ **要额外的计算**（Jacobi 迭代本身要算，且大部分是白算的）—— 又是在吃 memory-bound 的红利，但吃得没 EAGLE 高效

**定位**：**"零成本"路线的天花板**。如果你不能训 draft head（比如用的是别人的闭源模型、或者没有训练资源），Lookahead 是最好的选择。**但只要你能训，EAGLE 更优。**

**一句话总结这条技术线**：
> **n-gram lookup（零成本，靠运气）→ Lookahead（零成本，靠自己迭代）→ Medusa（小训练，靠多个头）→ EAGLE（小训练，靠 feature 自回归）→ MTP（预训练时就设计好）**
>
> **成本递增，$\alpha$ 递增。选哪个取决于你能投入多少。**

### ⚠️ 常见陷阱

1. **说 Medusa 的 head 是自回归的** —— **不是**！head 之间**独立**，这是它的根本弱点，也是 EAGLE 的立足点。
2. **说不清 EAGLE 为什么要喂 token** —— **消除采样的歧义**。这是 EAGLE 的核心贡献，答不出就是没读懂论文。
3. **不知道 tree attention 的 mask 逻辑** —— **兄弟节点必须互相不可见**。
4. **忽略 n-gram lookup** —— **它在 RAG / 代码编辑场景是性价比之王**，而这恰恰是 Agent 岗的主场。不知道它是应用岗的减分项。
5. **以为树越大越好** —— **树大 × batch 大 → compute-bound → 红利消失**。

### 🏢 大厂偏好

- **字节 / 阿里（推理）**：**必问 EAGLE**，会追问 feature vs token 的选择
- **DeepSeek**：**必问 MTP**（V3 的核心贡献之一）
- **应用岗 / Agent 岗**：**n-gram lookup 是必知**——因为 RAG 场景免费提速
- **所有岗**：能讲清"进化线"（独立模型 → Medusa → EAGLE）就够了

### 📚 延伸阅读
- [Medusa](https://arxiv.org/abs/2401.10774) - Tree Attention 的出处
- [EAGLE](https://arxiv.org/abs/2401.15077) / [EAGLE-2](https://arxiv.org/abs/2406.16858) / [EAGLE-3](https://arxiv.org/abs/2503.01840) - **feature 层自回归，本章必读**
- [Lookahead Decoding](https://arxiv.org/abs/2402.02057)
- [DeepSeek-V3 Technical Report](https://arxiv.org/abs/2412.19437) §2.2 - MTP

---

## Q7：投机解码的工程现实——什么时候没用 ⭐⭐⭐⭐

### 🎯 一句话标答

> **投机解码是<u>延迟优化</u>，不是<u>吞吐优化</u>**——它的全部红利来自"Decode 是 memory-bound、算力闲置"，而 **batch 一大，GPU 就变成 compute-bound，红利立刻消失，甚至因为多算了 $\gamma$ 倍的 token 而<u>降低</u>吞吐**；再加上 draft model **抢显存挤 KV Cache**、**$\alpha$ 高度依赖任务**（代码 0.95、创作 0.2），所以正确的部署是**按 batch size 动态开关**。

### 🗣️ 30 秒口语版

"这题是投机解码的**压轴实战题**，也是最能区分'读过论文'和'上过线'的一题。

**核心结论一句话**：
> **投机解码优化的是<u>单个用户的延迟</u>（TPOT），代价是<u>浪费算力</u>。而算力在低并发时是免费的，在高并发时是稀缺的。**

**为什么大 batch 下会失效——回到第 12 章的 Roofline。**

投机解码的地基是：**"验证 5 个 token 几乎免费，因为 decode 是 memory-bound、算力闲着"**。

但第 12 章 Q3 证明了：**Decode 的算术强度 ≈ batch size**。所以：
- **batch = 1**：算术强度 1，**算力利用 <1%** → 验证 5 个 token 变成强度 5，**还是远低于平衡点 150** → **完全免费** ✅
- **batch = 32**：算术强度 32 → 验证 5 个变成 **160** → **超过 A100 的平衡点 150 了！** → **已经 compute-bound，验证不再免费** ⚠️
- **batch = 64**：强度 64 → 验证 5 个变成 **320** → **深度 compute-bound** → 投机解码**纯粹在浪费算力** ❌

**而且要注意：投机解码是<u>净浪费</u>算力的**——每轮算 $\gamma+1$ 个位置，但期望只产出 $\frac{1-\alpha^{\gamma+1}}{1-\alpha}$ 个 token（$\alpha=0.8$、$\gamma=5$ 时约 3.4 个）。**被拒的那些全是白算。**

$$\boxed{\text{算力浪费率} = 1 - \frac{\mathbb{E}[\#\text{tokens}]}{\gamma+1} = 1 - \frac{3.4}{6} \approx \mathbf{43\%}}$$

**低并发时**：算力反正闲着，浪费 43% 无所谓 → **白赚 2.5× 的延迟** ✅
**高并发时**：算力是稀缺资源，浪费 43% 意味着 **吞吐直接掉 43%** ❌

**实测的分界线**：
```
batch 1-4    : 投机解码 2-3× 加速          ✅✅ 巨大收益
batch 8-16   : 1.3-1.8× 加速               ✅ 还行
batch 32     : ~1.0×（打平）                ⚠️ 白忙
batch 64+    : 0.7-0.9×（★倒挂，吞吐下降★）  ❌ 有害
```

**所以正确的部署方式是：动态开关。**
```python
if current_batch_size < threshold:   # 典型 8-16
    enable_speculative_decoding()
else:
    disable()                        # 大 batch 时关掉，别浪费算力
```
vLLM 有 `speculative_disable_by_batch_size` 这个参数就是干这个的。

**其他三个坑：**

**坑一：draft model 抢显存。**
1B 的 draft 占 2 GB → 少了 4 条 4K 请求的 KV Cache → **并发降低** → **吞吐降低**。这是**隐藏成本**，很多 benchmark 不算这笔账。
→ **这正是 EAGLE/Medusa 的隐藏优势**：head 只有几十 MB，**几乎不抢显存**。

**坑二：$\alpha$ 强烈依赖任务。**（Q5 追问 2）
- 代码、JSON、RAG 引用原文 → $\alpha$ 0.9+ → **收益巨大**
- 创意写作、高 temperature → $\alpha$ 0.2-0.3 → **几乎没收益，还赔上 draft 的开销**
→ **必须在你自己的真实流量上测 $\alpha$，不能信论文的数字**（论文通常在 MT-Bench 上测，那不是你的业务）

**坑三：和 prefix caching / chunked prefill 的交互。**
- **Prefix caching**：正交，**可以叠加**（一个救 TTFT，一个救 TPOT —— 完美互补）
- **Chunked prefill**：**有点冲突** —— chunked prefill 的设计是"**用 prefill 的 compute 填满 decode 闲置的算力**"（第 12 章 Q6），而投机解码**也想用那份闲置算力**。**两者在抢同一块蛋糕。**
  → 一起开时收益**不叠加**，要实测

**一句话总结定位（面试请说这句）**：
> **"投机解码是给 <u>C 端聊天、端侧、内部工具、Agent 单任务</u> 这类<u>低并发高延迟敏感</u>场景用的。如果你是高吞吐的 API 服务，先上 continuous batching 和量化，投机解码要按 batch 动态开。"**"

### 📐 算力浪费的量化

每轮：
- **算了**：$\gamma+1$ 个位置（target）+ $\gamma$ 次 draft 前向
- **产出**：$\mathbb{E}[\#\text{tokens}] = \frac{1-\alpha^{\gamma+1}}{1-\alpha}$ 个 token

**算力效率**：
$$\eta = \frac{\mathbb{E}[\#\text{tokens}]}{\gamma+1} = \frac{1-\alpha^{\gamma+1}}{(1-\alpha)(\gamma+1)}$$

| $\alpha$ | $\gamma$ | $\mathbb{E}[\#\text{tokens}]$ | **算力效率 $\eta$** | 浪费 |
|---|---|---|---|---|
| 0.9 | 5 | 4.69 | **78%** | 22% |
| 0.8 | 5 | 3.36 | **56%** | 44% |
| 0.7 | 5 | 2.65 | **44%** | **56%** |
| 0.5 | 5 | 1.97 | **33%** | **67%** |

> **读这张表**：$\alpha=0.7$ 时，**你在浪费一半以上的算力**来换延迟。
>
> **低并发时这是<u>白赚</u>**（算力反正闲着，$\eta$ 是多少都无所谓，反正 GPU 在睡觉）；
> **高并发时这是<u>纯亏</u>**（算力是钱，浪费 56% 就是烧掉 56% 的钱）。
>
> **"投机解码用算力换延迟"这句话，$\eta$ 就是汇率。**

### 🔍 什么场景该用

| 场景 | batch | $\alpha$ | 推荐 | 理由 |
|---|---|---|---|---|
| **端侧 / 本地部署** | 1 | 中 | ✅✅ **必上** | 算力全闲，白赚 |
| **代码补全（IDE）** | 1-4 | **0.9+** | ✅✅ **必上** | 低并发 + 高 $\alpha$，**完美场景** |
| **Agent 单任务** | 1-4 | 中-高 | ✅✅ | 低并发；且工具调用的 JSON 是高 $\alpha$ 区 |
| **RAG 问答** | 中 | **高**（引用原文） | ✅ **上 n-gram lookup**（零成本） | 免费的午餐 |
| **C 端聊天（高峰）** | 32+ | 中 | ⚠️ **按 batch 动态开** | 高峰期关掉 |
| **批量离线任务** | 128+ | — | ❌ **别用** | 只关心吞吐，投机解码纯亏 |
| **创意写作服务** | — | **0.2** | ❌ | $\alpha$ 太低，不划算 |

### 🔥 高频追问 Top 3

**Q：那 vLLM 里到底该怎么配？**

A：**给一套可以直接说的配置逻辑。**

```python
from vllm import LLM

# ---- 场景 A：RAG / 代码编辑 —— 先上零成本的 n-gram ----
llm = LLM(
    model="meta-llama/Llama-3-70B",
    speculative_model="[ngram]",          # ← 零成本，零显存
    ngram_prompt_lookup_max=4,            # n-gram 的最大长度
    num_speculative_tokens=5,
)
# 理由：不占显存、不用训练。RAG 场景 α 高。先试这个，不行再上重的。

# ---- 场景 B：低并发通用服务 —— 上 EAGLE ----
llm = LLM(
    model="meta-llama/Llama-3-70B",
    speculative_model="yuhuili/EAGLE-LLaMA3-Instruct-70B",
    num_speculative_tokens=5,
    speculative_disable_by_batch_size=16,  # ★ 关键：batch>16 自动关掉
)

# ---- 场景 C：高吞吐 API 服务 —— 别开 ----
llm = LLM(model="meta-llama/Llama-3-70B")  # 老老实实靠 continuous batching + 量化
```

**优先级（这个排序是回答的重点）**：
1. **先测你的真实 batch 分布** ← 这决定了值不值得做
2. **平均 batch > 32** → **别做投机解码**，去做量化和调度
3. **平均 batch < 8** → 做，且优先试 **n-gram**（零成本），不行再上 EAGLE
4. **必测**：`speculative_disable_by_batch_size` 一定要设，防止高峰期倒挂

**这个"先测 batch 分布再决定"的顺序，比任何技术细节都重要**——它是工程判断力的体现。

**Q：投机解码和 PD 分离（第 12 章）怎么配合？**

A：**天作之合——PD 分离让投机解码的适用范围大大扩展。**

回忆第 12 章 Q1 追问 2 的 PD 分离：Prefill 集群和 Decode 集群物理隔离。

**关键点**：**PD 分离之后，D 集群可以为了延迟而<u>刻意压低 batch</u>**！
- 传统混合部署：为了摊薄成本，必须堆大 batch → 投机解码没用
- **PD 分离**：P 集群负责吞吐（大 batch prefill），**D 集群可以按 SLA 分级**——
  - **高优先级 D 集群**：**小 batch + 投机解码** → 极低延迟，给付费用户
  - **低优先级 D 集群**：大 batch，不开投机 → 高吞吐，给免费用户

$$\boxed{\text{PD 分离把"延迟 vs 吞吐"从一个全局妥协变成了一个可分配的资源}}$$

**而且 draft model 只需要部署在 D 集群** —— P 集群不需要（prefill 本来就是并行的，不需要投机）。**这又省了一半的显存开销。**

**能说出这个组合，说明你把第 12 章和第 14 章打通了。这是本章能给的最好的系统级答案。**

**Q：怎么在线上监控投机解码的效果？**

A：**四个必看指标。**

| 指标 | 含义 | 健康值 | 异常时怎么办 |
|---|---|---|---|
| **接受率 $\alpha$** | 每个 draft token 被接受的比例 | **> 0.6** | < 0.5 → **换 draft 或直接关掉** |
| **平均接受长度** | $\mathbb{E}[\#\text{tokens}]$ 的实测 | **> 2.0** | < 1.5 → $\gamma$ 调小 |
| **TPOT 改善** | 有/无投机的对比 | **> 1.5×** | 打平 → 说明 batch 太大了 |
| **吞吐变化** | **总 token/s** | **不降** | **降了 → 立刻关！** ← 最重要 |

**最后一行是重点，也是最容易被忽略的**：很多人只盯着"接受率好高啊、TPOT 改善好多啊"，**没发现总吞吐掉了 20%** —— 因为高峰期 batch 上去了，投机解码开始倒挂。

**监控的正确姿态**：
> **"接受率和 TPOT 是<u>过程</u>指标，吞吐和 P99 延迟是<u>结果</u>指标。过程好看但结果变差，就是在自欺欺人。"**

**推荐做法**：**按 batch size 分桶统计** —— 你会清楚地看到一条"**收益随 batch 递减、到某个点后转负**"的曲线。**那个转折点就是 `speculative_disable_by_batch_size` 该设的值。** 这是从数据里读出配置，而不是拍脑袋。

### ⚠️ 常见陷阱

1. **把投机解码当成吞吐优化** —— **它是延迟优化，用算力换延迟**。这是最核心的定位错误。
2. **不知道大 batch 会倒挂** —— 且**说不出为什么**（因为算术强度 = 树大小 × batch，超过平衡点就 compute-bound）。
3. **忽略 draft model 抢显存** —— 高并发时这笔账可能让整体变亏。
4. **信论文的加速比** —— 论文在 batch=1、MT-Bench 上测。**你的场景 batch 和 $\alpha$ 都不一样。**
5. **不设 batch 上限开关** —— 高峰期就是事故。

### 🏢 大厂偏好

- **字节 / 阿里（推理）**：**必问"什么时候不该用"** —— 这是区分度最高的问法
- **应用岗**：会问"你们上了投机解码吗，效果怎么样"—— **要能说出 batch 分布和 $\alpha$ 的实测**
- **端侧团队**：**投机解码是刚需**（batch 永远是 1）

### 📚 延伸阅读
- [vLLM Speculative Decoding 文档](https://docs.vllm.ai/en/latest/features/spec_decode.html)
- [Sarathi-Serve](https://arxiv.org/abs/2403.02310) - chunked prefill 和投机解码抢算力的分析

---

## Q8：约束解码与结构化输出 ⭐⭐⭐⭐（Agent 必备）

### 🎯 一句话标答

> Function Calling / Agent 要求模型**必须**输出合法 JSON，靠 prompt 祈祷是不可靠的；**约束解码**在每一步**把非法 token 的 logit 置为 $-\infty$**，从而**保证 100% 合法**——**Outlines** 把正则编译成 **FSM**、**XGrammar**（vLLM/SGLang 现在的默认）用**下推自动机 PDA** 支持上下文无关文法（JSON 的括号匹配是 CFG，**正则做不到**），两者都在**编译期**预计算好"每个状态下的合法 token 集合"，**运行时只是查表 + mask，开销接近 0**；但要警惕它**会改变输出分布、可能损害推理质量**——正确姿势是**先让模型自由思考，最后才约束格式**。

### 🗣️ 30 秒口语版

"这题是**第三部分通向第四部分（Agent）的桥**，应用岗很爱问。

**问题**：Agent 调用工具时，模型必须输出严格符合 schema 的 JSON：
```json
{"name": "get_weather", "arguments": {"city": "Beijing", "unit": "celsius"}}
```
少一个引号、多一个逗号、字段名拼错 → **整个 Agent 循环崩掉**。

**方案一：Prompt + 重试。** "请输出 JSON"。
- ❌ 不可靠——GPT-4 级别的模型也有 1-5% 的失败率，小模型更差
- ❌ 重试**成倍增加延迟和成本**
- ❌ **Agent 是多轮的**，每轮 3% 失败，10 轮下来 **成功率只有 $0.97^{10} = 74\%$** —— **不可接受**

**方案二：约束解码（Constrained / Guided Decoding）—— 从根上解决。**

**核心思想极其简单**：**在每一步采样前，把所有"会导致非法"的 token 的 logit 设成 $-\infty$。**

举例——已经生成了 `{"name": `，那么下一个 token：
- ✅ 合法：`"`（JSON 的字符串必须以引号开头）
- ❌ 非法：`}`, `,`, `123`, `true` … **全部 mask 掉**

**结果：模型<u>物理上不可能</u>输出非法 JSON。100% 保证，不是 99%。**

**怎么高效实现？——这是技术含量所在。**

朴素实现：每步都跑一遍 JSON 解析器，试探每个 token 合不合法。**词表 15 万，每步试 15 万次 → 慢到没法用。**

**Outlines 的解法（关键洞察）**：**把"格式约束"编译成一个<u>自动机</u>，<u>离线</u>预计算每个状态下的合法 token 集合。**
- **编译期**（一次性，几百毫秒）：把正则/schema 转成 **FSM**，对**每个状态**遍历整个词表，算出"哪些 token 是合法的" → 存成一张表
- **运行时**：**当前状态 → 查表 → 直接拿到 mask** → **$O(1)$！**

$$\boxed{\text{把重活挪到编译期，运行时几乎零开销} \approx 1\%}$$

**XGrammar 的进化（现在 vLLM/SGLang 的默认）**：

**关键洞察：JSON 不是正则语言，是<u>上下文无关语言</u>！**

为什么？**因为括号匹配需要"记住嵌套了多深"** —— 这需要一个**栈**，而 FSM **没有栈**（这是计算理论的经典结论：$\{a^n b^n\}$ 不是正则语言）。

所以 XGrammar 用 **下推自动机（PDA）** = FSM + 栈，支持完整的 **CFG（上下文无关文法）**。

XGrammar 的两个工程优化：
1. **上下文无关 token 的预计算**：把词表里的 token 分成两类——
   - **上下文无关的**（绝大多数）：合法性只取决于当前状态 → **可以预计算**
   - **上下文相关的**（极少数）：要看栈 → 运行时判断
   
   **绝大多数 token 属于第一类** → 大部分工作被挪到编译期
2. **持久化执行栈**：支持**分叉/回滚**（配合投机解码的树）

**结果**：overhead **接近 0**，比 Outlines 快一个数量级。

**最重要的陷阱（必须主动说）：约束解码会<u>改变输出分布</u>，可能损害质量。**

Mask 掉 token 之后，剩下的概率**重新归一化** —— 这已经**不是模型原本的分布**了。如果模型"想"说的东西被 mask 了，它会被**强行推到一个它不擅长的方向**。

有论文（*"Let Me Speak Freely?"*）实测发现：**强制 JSON 输出会让某些推理任务掉点明显**。直觉上——**模型被格式束缚住了，没法自由地"想"**。

**正确的做法（这是答这题的关键）**：**分阶段**——
```
① 让模型自由生成 CoT（不约束）      ← 让它好好想
   "用户问北京天气。我需要调用 get_weather 工具，city 参数应该是 Beijing…"
② 到了输出工具调用时才开启约束       ← 只约束格式，不约束思考
   {"name": "get_weather", "arguments": {"city": "Beijing"}}
```
**这正是 Function Calling 的标准做法**（第 17 章会详谈）——**思考自由，输出严格**。"

### 📐 为什么 JSON 需要 CFG 而不是正则

**计算理论的层级**（Chomsky 层级）：

| 语言类 | 自动机 | 能表达 | JSON? |
|---|---|---|---|
| 正则语言 | **FSM**（有限状态机） | `a*b+`、固定深度的结构 | ❌ **不能**（无限嵌套） |
| **上下文无关语言** | **PDA**（下推自动机 = FSM + **栈**） | **括号匹配、任意深度嵌套** | ✅ **能** |
| 上下文相关 | 线性有界自动机 | — | 不需要 |

**为什么 JSON 需要栈**：
```json
{"a": {"b": {"c": [1, [2, [3]]]}}}
        ↑ 要正确匹配这些括号，必须"记住嵌套了几层"
        ↑ FSM 的状态数是【有限】的，无法表示【任意深度】
```

**这是计算理论的经典结论**：$\{a^n b^n \mid n \ge 0\}$（n 个左括号后必须跟 n 个右括号）**不是正则语言** —— 用**泵引理**可以严格证明。

**所以**：
- **Outlines（FSM）**：只能处理**有界深度**的 JSON（实践上够用，因为可以限制最大嵌套层数）
- **XGrammar（PDA）**：能处理**完整的 CFG** —— 不只是 JSON，还能约束 **SQL、Python 代码、任意 EBNF 定义的语法**

> **能说出"JSON 的括号匹配是 CFG 不是正则，所以需要 PDA 而不是 FSM"，是个非常漂亮的加分点** —— 它说明你有计算理论的底子，而不是只会调库。

#### FSM 编译的示意

约束：`{"name": "<string>", "age": <number>}`

```
状态机（简化）：
  S0 --'{'--> S1 --'"'--> S2(读name) --'"'--> S3 --':'--> S4 --'"'--> S5(读string) ...

每个状态的合法 token 集合（★离线★ 遍历 15 万词表算好）：
  S0: {'{', ' {', '\n{', ...}                    ← 只有 12 个 token 合法
  S1: {'"', ' "', ...}                            ← 只有 8 个
  S2: {所有能作为 "name" 前缀的 token}             ← 因为字段名是固定的！
  S5: {几乎所有 token（在读自由字符串）, 除了未转义的 '"'}

运行时：
  mask = precomputed_mask[current_state]   # ← O(1) 查表
  logits[~mask] = -inf                     # ← O(vocab) 的一次向量操作
```

> **注意 S2 那一行**：如果 schema 里字段名是固定的 `"name"`，那**模型根本不需要"生成"它** —— **约束解码可以直接把它"填"出来**（因为每一步只有一个合法 token）！
>
> **这带来一个额外的好处：结构化输出可以<u>更快</u>** —— 固定的部分（字段名、括号、引号）**可以直接跳过采样，甚至跳过前向**（这叫 **jump-forward decoding**，SGLang 的功能）。
>
> **"约束解码不只保证正确，还能加速"** —— 这个反直觉的点答出来很加分。

### 💻 实践

```python
from vllm import LLM, SamplingParams
from pydantic import BaseModel

# ---- 用 Pydantic 定义 schema ----
class WeatherCall(BaseModel):
    name: str
    city: str
    unit: str    # 可以用 Literal["celsius","fahrenheit"] 进一步收紧

llm = LLM(model="meta-llama/Llama-3-8B-Instruct")

params = SamplingParams(
    temperature=0,                                    # ★ 结构化输出用 greedy
    guided_decoding=GuidedDecodingParams(
        json=WeatherCall.model_json_schema(),         # ← 编译成 PDA
        backend="xgrammar",                           # ← 现在的默认
    ),
)
# 输出【保证】是合法的 WeatherCall JSON —— 100%，不是 99%
```

**注意 `temperature=0`** —— 结构化输出**几乎总是应该用 greedy**：
- 格式没有"创造性"可言
- 而且 Q2 说的**惩罚也要全关**（JSON 的 `"`、`{` 必须重复！）

### 🔥 高频追问 Top 3

**Q：约束解码和 Function Calling 是一回事吗？**

A：**不是，是<u>两层</u>——这个辨析很重要（第 17 章会展开）。**

| | **Function Calling**（模型能力） | **约束解码**（推理时机制） |
|---|---|---|
| 是什么 | 模型**被训练**成"知道有哪些工具、什么时候该调、参数怎么填" | 推理时**mask 非法 token**，保证格式 |
| 何时获得 | **SFT 阶段**（用大量工具调用数据训练） | **推理时**（不需要训练） |
| 解决 | **调什么、什么时候调、参数填什么**（语义） | **格式对不对**（语法） |
| 能不能单独用 | 能（GPT-4 早期就是纯训练 + prompt） | 能（约束一个没训过工具的模型输出 JSON） |

**关键辨析**：
- **约束解码保证"语法合法"，保证不了"语义正确"**。它能保证输出是合法 JSON，**保证不了 `city` 字段填的是"北京"而不是"香蕉"**。
- **Function Calling 训练保证"语义合理"，保证不了"语法一定合法"**（还是有 1-3% 概率出错）。

$$\boxed{\text{两者是互补的：训练管语义，约束管语法。生产上都要。}}$$

**这也解释了为什么 OpenAI 的 Structured Outputs 能宣称"100% schema 遵守"** —— 他们**同时**做了两件事：训练（让模型懂 schema）+ 约束解码（保证格式）。**光有前者做不到 100%。**

**Q：约束解码和投机解码能一起用吗？**

A：**能，但会互相削弱——这是个很好的深度问题。**

**冲突点**：
1. **draft model 不知道约束** → 它猜的 token 可能是**非法的** → **必被拒** → **$\alpha$ 下降**
   - 举例：draft 猜了 `{"name": "get_weather"}`，但约束要求这里还得有 `arguments` 字段 → `}` 被 mask → 拒绝
2. **约束改变了 target 分布 $p$** → 回到 Q4 追问 3：$\alpha = 1 - D_{TV}(p_{\text{constrained}}, q)$ → **$D_{TV}$ 变大 → $\alpha$ 变小**

**缓解方案**：
1. **给 draft 也套上同样的约束**（XGrammar 的持久化栈支持这个）→ **$D_{TV}$ 重新变小，$\alpha$ 恢复**
2. **Jump-forward decoding**（SGLang）：**更聪明的路子** —— 结构化输出里有大量**确定的部分**（字段名、括号、引号），**这些根本不需要投机，直接"填"进去就行**（因为只有一个合法 token）。**这比投机解码还快** —— 投机解码是"猜了再验"，jump-forward 是"根本不用猜"。

> 💡 **有意思的观察**：**约束解码在结构化场景下，某种程度上让投机解码变得多余** —— 因为约束已经把不确定性消掉了。
>
> **两者其实是同一个东西的两种形式**：
> - **投机解码**："我**猜**下一个 token 是 X，验一下" —— **概率性**
> - **Jump-forward**："我**知道**下一个 token 必须是 X" —— **确定性**
>
> **Jump-forward 是"接受率 100% 的投机解码"。** 这个类比很漂亮，面试能用。

**Q：约束解码有什么质量代价？怎么缓解？**

A：**这是这题最重要的一问，也是最能体现工程判断的地方。**

**代价的机制**：mask + 重新归一化 = **改变了模型的分布**。如果模型的概率质量大量落在被 mask 的区域，重新归一化会把它**推到一个它不熟悉的地方**。

**实证**（*"Let Me Speak Freely?"*, EMNLP 2024）：
- **强制 JSON 格式** → **推理任务（GSM8K 类）掉点明显**
- 越是要求严格的格式，掉得越多
- **小模型掉得比大模型多**（呼应第 13 章的"冗余"主线——**小模型没有余量应付额外的约束**）

**为什么**：模型的"思考"是**在生成过程中发生的**（这正是 CoT 有效的原因）。**强制它一上来就吐 JSON，等于剥夺了它思考的空间。**

**三个缓解方案（按效果排序）**：

1. **先思考，后约束**（⭐ **最重要**）：
   ```
   ① 自由生成 CoT（不约束）：
      "用户问北京天气。我需要 get_weather 工具。city 应该用英文 Beijing。
       中国用摄氏度，所以 unit=celsius。"
   ② 遇到 <tool_call> 标记后才开启约束：
      {"name": "get_weather", "arguments": {"city": "Beijing", "unit": "celsius"}}
   ```
   **这是标准做法**——思考自由，输出严格。**第 17 章的 Function Calling 就是这么设计的。**

2. **放松约束**：
   - 用**宽松的 schema**（少用 `Literal`、少用 `enum`、多用 `str`）
   - **先约束成合法 JSON，再用代码校验语义**（而不是把所有约束都塞进 grammar）

3. **配合训练**：模型**训过这个 schema**，约束就不是"外加的束缚"，而是"**和它想说的一致**" → $D_{TV}$ 小 → 质量无损。**这就是 OpenAI Structured Outputs 的做法。**

**答这题的正确姿态**：
> **"约束解码保证格式，但不是免费的——它会剥夺模型的思考空间。所以我会让模型先自由 CoT，只在输出工具调用时开启约束。如果还掉点，就把 schema 放松，用代码做二次校验。"**

**这个回答体现了"知道代价、知道缓解、有优先级"——这是高级岗要的东西。**

### ⚠️ 常见陷阱

1. **以为约束解码是免费的** —— **会改变分布、可能伤推理**。主动说出来是加分。
2. **不知道 JSON 需要 CFG 不是正则** —— 括号匹配需要栈，这是计算理论常识。
3. **把约束解码和 Function Calling 混为一谈** —— **训练管语义，约束管语法，互补**。
4. **在约束场景开 repetition_penalty** —— JSON 的 `"` 和 `{` **必须**重复！（Q2 的陷阱在这里复现）
5. **一上来就约束**，不给模型思考空间 —— 正确做法是**先 CoT 后约束**。

### 🏢 大厂偏好

- **应用岗 / Agent 岗**：**高频**——因为这是 Function Calling 的地基
- **字节 / 阿里**：会问 FSM vs PDA 的区别
- **所有做 Agent 的团队**：会问"你怎么保证工具调用的参数一定合法"

### 📚 延伸阅读
- [Efficient Guided Generation for LLMs (Outlines)](https://arxiv.org/abs/2307.09702) - FSM 方案的原论文
- [XGrammar](https://arxiv.org/abs/2411.15100) - PDA 方案，vLLM/SGLang 现在的默认
- [Let Me Speak Freely?](https://arxiv.org/abs/2408.02442) - **约束解码伤害推理能力的实证，必读**
- [SGLang: Jump-Forward Decoding](https://arxiv.org/abs/2312.07104)

---

## 📝 本章小结

| 关键点 | 你必须能脱口而出 |
|---|---|
| **采样的地基** | softmax **强制**给每个 token 非零概率 → **不可信的长尾** → 一切截断策略都在"信头部、切尾巴" |
| **Temperature** | **作用在 logits 上、softmax 之前**；**不改排序，只缩放 logit 之差**；$p_i/p_j = \exp\!\big((z_i-z_j)/T\big)$ |
| **top-k vs top-p vs min-p** | k 固定（僵硬）/ p 动态（主流）/ **min-p 是<u>相对</u>阈值 → 抗高温** |
| **相对 > 绝对** | min-p 的相对阈值 = PPO Advantage = RLHF reward shaping —— **同一条主线的第三次登场** |
| **重复退化** | **正反馈**（in-context learning 太强）+ **exposure bias** + MLE 的保守偏好；**对齐能治本** |
| **惩罚的陷阱** | **代码 / JSON / RAG 场景必须关**——那些场景**必须**重复 |
| **Beam Search 的死因** | **"最大概率序列"≠"最好文本"**；人类文本 likelihood **剧烈波动**（低概率=高信息量）；且它**加重**重复 |
| **Beam 的继承者** | **Best-of-N + RM** —— 因为我们终于有了比 log-prob 更好的打分器 |
| **投机解码的地基** | **"验证免费" ⟸ Decode 是 memory-bound**（第 12 章）。**这是全章最重要的一句话** |
| **无损性证明** | $P(x) = \min(p,q) + \max(0,p-q) = p$；枢纽引理：$\sum\max(0,p-q) = P(\text{拒绝})$ |
| **多退少补** | 接受概率 $p/q$ **削峰**，修正分布 $\max(0,p-q)$ **填谷** |
| **接受率的本质** | $\boxed{\alpha = 1 - D_{TV}(p,q)}$ —— **draft 越像 target，接受率越高。这是定律不是经验** |
| **加速比** | $\dfrac{1-\alpha^{\gamma+1}}{(1-\alpha)(\gamma c+1)}$；$\gamma$ 有最优（3-5）；$\gamma\to\infty$ 时 $\mathbb{E}\to\frac{1}{1-\alpha}$ **饱和** |
| **$c$ 的陷阱** | **$c\ne$ 参数量比** —— 固定开销（kernel launch、采样）让"再小也没用" |
| **$\alpha$ 是阵发的** | 代码 0.95 / 实体名 0.4 / 创作 0.2 —— **双峰分布 → 推出动态 $\gamma$ 和动态树** |
| **draft 进化线** | 独立模型 → Medusa（**head 独立**，靠树补）→ **EAGLE（feature 层自回归 + 喂回已采样 token 消歧义）** → MTP（预训练时设计） |
| **EAGLE 的精髓** | **把随机性显式喂回去，把一对多的歧义变成一对一的确定映射**（同 Diffusion 的 $\epsilon$、VAE 的重参数化） |
| **Tree Attention** | **兄弟节点互相不可见**；**causal mask 是 tree mask 在"树退化成链"时的特例** |
| **树的上限** | **树大小 × batch > 150 → compute-bound → 红利消失**（第 12 章 Roofline 的直接推论） |
| **投机解码的定位** | **延迟优化，不是吞吐优化**——**用算力换延迟**，$\eta$ 是汇率；**batch > 32 会倒挂，必须动态开关** |
| **约束解码** | **JSON 是 CFG 不是正则**（括号匹配要栈）→ FSM(Outlines) → **PDA(XGrammar)**；**编译期算好，运行时 O(1)** |
| **约束的代价** | **会改变分布、伤推理**；正确姿势：**先自由 CoT，最后才约束格式** |
| **Jump-forward** | **= 接受率 100% 的投机解码**（"我知道"而不是"我猜"） |
| **三条路线走完** | 减小模型（**第 13 章量化**）/ 提高 batch（**第 12 章调度**）/ **一次多出几个 token（本章）** ✅ |

## ✅ 自测题

1. 手撕 `sample(logits, temperature, top_k, top_p, min_p)`，**说清 top-p 为什么要右移一位**。
2. 为什么创意写作应该用 min-p 而不是 top-p？用"高温压平分布"的机制解释。
3. 为什么 self-consistency **必须** temperature > 0？为什么 DeepSeek-R1 官方**不建议** temperature=0？
4. 用一句话说清 beam search 被淘汰的**根本原因**（提示：不是速度）。它的位置被什么取代了，为什么那个更对？
5. **白板推导**投机解码的无损性，包括"$\sum\max(0,p-q) = P(\text{拒绝})$"这个引理。
6. 证明 $\alpha = 1 - D_{TV}(p,q)$，并说出它的**三个推论**。
7. 推导 $\mathbb{E}[\#\text{tokens}] = \frac{1-\alpha^{\gamma+1}}{1-\alpha}$，做三个**边界检查**（$\alpha\to0$、$\alpha\to1$、$\gamma\to\infty$），并解释每个边界的物理意义。
8. Medusa 的 head 为什么不是自回归的？EAGLE 用什么办法解决了这个问题，为什么必须把已采样的 token 喂回去？
9. 面试官："我们线上 QPS 很高，平均 batch=48，要不要上 EAGLE？" —— **注意这题有坑**。
10. 你的 Agent 有 3% 的工具调用 JSON 格式错误，10 轮任务的成功率是多少？给出**两层**解决方案（训练层 + 推理层），并说清约束解码的**代价**和你的**缓解措施**。
11. 综合题：把第 12-14 章串起来——**给定"7B 模型、A100 单卡、代码补全 IDE 场景、batch=1-2、要求 TPOT < 15ms"**，设计一套完整的推理优化方案，并按"先上哪个"排序。

---

> **下一章预告**：第 15 章｜推理引擎与服务化——**vLLM / SGLang / TensorRT-LLM 的架构对比**、FlashAttention 系列与 FlashDecoding、**RadixAttention**、推理侧的 TP/PP/EP、**TTFT ↔ TPOT ↔ 吞吐的三角权衡**、**PD 分离（Disaggregation）**，以及一道完整的**系统设计实战**。
>
> 第 12-14 章讲的是**单点技术**，第 15 章讲的是**怎么把它们装进一个系统**——这也是第三部分的收官。**装完之后，第四部分（RAG / Agent）才有地基。**

