# 第 4 章｜分词器

> **本章定位**：分词器经常被低估，但其实是面试中的"区分度题"——能答清楚 BPE 的具体步骤、SentencePiece 为什么解决空格问题、中文分词的工程坑，证明你**真的训过或者部署过 LLM**。本章重点：BPE 手撕、中文特殊处理、词表大小权衡。

> **配套章节**：手撕 BPE 代码 → 第 9 章 / 预训练数据处理 → 第 6 章

---

## 📌 本章导航

| 序号 | 题目 | 难度 | 频率（字节/阿里/Meta/DeepSeek） | 类型 |
|---|---|---|---|---|
| Q1 | 为什么用 subword？字 vs 词 vs subword | ⭐⭐⭐ | 4 / 3 / 2 / 2 | 概念 |
| Q2 | **BPE 算法详细原理** | ⭐⭐⭐⭐⭐ | **9 / 5 / 4 / 4** | 必考 |
| Q3 | WordPiece 和 BPE 的区别 | ⭐⭐⭐ | 3 / 2 / 2 / 1 | 对比 |
| Q4 | SentencePiece 与 Unigram | ⭐⭐⭐⭐ | 4 / 2 / 3 / 3 | 深度 |
| Q5 | **中文分词的特殊问题** | ⭐⭐⭐⭐ | 6 / 4 / 2 / 5 | 工程 |
| Q6 | 词表大小如何选择 | ⭐⭐⭐ | 3 / 2 / 2 / 2 | 工程 |
| Q7 | Tokenizer 训练数据的重要性 | ⭐⭐⭐ | 2 / 1 / 1 / 2 | 加分 |
| Q8 | 现代分词器的优化 | ⭐⭐⭐ | 3 / 1 / 2 / 2 | 前沿 |

---

## Q1：为什么用 subword？字符 vs 词 vs subword ⭐⭐⭐

### 🎯 一句话标答

> **字粒度词表小但序列长**，**词粒度词表大且 OOV 严重**，**subword 介于两者之间**——能覆盖任意字符串、词表可控、序列长度合理，是当前所有 LLM 的标配。

### 🗣️ 30 秒口语版

"分词的本质是个**粒度权衡**问题——

**字符级**（character-level）：每个字符一个 token。优点是词表极小（英文 26 字母 + 标点 = 几十个），永远不会 OOV。缺点是序列长度爆炸——一句话变成几十上百个 token，计算成本飙升，模型也难学到词级别的语义。

**词级**（word-level）：每个完整词一个 token。优点是语义单元天然对齐，序列短。缺点是词表巨大——英文几十万、中文上百万词，且新词、拼写错误立刻 OOV。

**Subword**：把词拆成更小的字符子串，作为最小单位。`unbelievable` → `un` + `believ` + `able`。优点是——
- 词表可控（通常 3-13 万）
- 没有 OOV：未知词总能拆成已知字符
- 序列长度合理（比字符短，比词长一点）
- 高频词作为整体保留，低频词被拆开

BPE、WordPiece、SentencePiece 都是 subword 方案，**思路一致、实现细节不同**。"

### 🔍 三种粒度对比

| 维度 | 字符级 | 词级 | Subword |
|---|---|---|---|
| 词表大小 | 几十~几千 | 几十万~百万 | 几万~十几万 |
| OOV 处理 | 无 OOV | 严重 | 无 OOV |
| 序列长度 | 长（×4-5） | 短 | 中（×1.3） |
| 训练效率 | 低 | 高 | 中 |
| 语义对齐 | 弱 | 强 | 中 |
| 多语言 | 友好 | 困难 | 友好 |
| 主流采用 | 早期 | 早期 | **当前主流** |

### 🔥 高频追问 Top 3

**Q：BERT 早期用 WordPiece 的字粒度版（中文是字），效果不差，为什么后来转 subword？**

A：因为**字粒度在英文等表音文字上序列太长**。一个英文单词平均 5 个字符，整篇文章变成 5 倍长度，训练和推理都贵。中文每字本身就是个语素单元，字粒度还能用；但**统一处理多语言**时必须 subword。LLaMA、GPT 等做多语言模型，自然走 subword 路线。

**Q：那字符级和 subword 之间，能不能"按需切"？**

A：可以——这就是 subword 本身的设计。**高频词作为整体保留**（相当于词级），**低频词被拆**（相当于字符级）。所以 subword 实际上是"自适应粒度"。BPE/WordPiece 的合并次数控制了平均粒度——合并次数多 → 偏词级，合并次数少 → 偏字符级。

**Q：字节级（byte-level）和字符级一样吗？**

A：不一样。字节级是把文本编码成 UTF-8 字节序列（256 个可能值），再做 BPE。优势是**绝对没有 OOV**——任何 Unicode 字符都能用字节表示。GPT-2 之后开始流行的 Byte-Level BPE 就是这种。代价是中文等多字节字符变长 2-3 倍。**LLaMA、GPT-4 用的都是字节级 BPE**。

### ⚠️ 常见陷阱

- **不要把 subword 和"中文分词工具"混淆**：jieba 等是词级分词，subword 是另一种方案
- **不要忽略字节级**：现代 LLM 几乎都是字节级 BPE

---

## Q2：BPE 算法详细原理 ⭐⭐⭐⭐⭐（必考，可能手撕）

### 🎯 一句话标答

> BPE 的核心是**贪心合并最高频字符对**——从字符开始，每轮统计相邻字符对的出现次数，把最频繁的一对合并成新 token，迭代到达到目标词表大小。

### 🗣️ 30 秒口语版

"BPE 全称 Byte Pair Encoding，原本是个数据压缩算法，2015 年 Sennrich 把它引入 NLP。

算法步骤可以分四步——

**第一步：初始化**。把训练语料的每个词拆成字符序列，比如 'low' → ['l', 'o', 'w']。再加一个结束符 '</w>' 区分词尾。**初始词表 = 所有字符**。

**第二步：统计相邻字符对的频次**。比如 'l-o' 出现 100 次，'o-w' 出现 80 次，'lo-w' 出现 70 次。

**第三步：合并最高频的对**。把 'l-o' 合并成 'lo'，更新所有出现 'l-o' 的位置。词表里新增 'lo'。

**第四步：重复 2-3 步**，直到达到预设词表大小，比如 32K。

最后得到的词表 = 字符 + 合并产生的所有 subword。

**Encode 时**：用合并规则的**反向应用**——按合并的优先级从高到低，找词中能合并的对，逐步合并直到没法再合并。

**关键性质**：合并是**贪心的、不回退的**——所以同一段文本的 tokenization 是确定的、可重现的。"

### 📐 完整算法流程（含示例）

**训练阶段**：

```
输入：训练语料，目标词表大小 V
输出：词表 + 合并规则列表

1. 把每个词预处理成 "字符序列 + 结束符"
   "low" → ('l', 'o', 'w', '</w>')

2. 统计每个词的频次（语料中"low"出现 5 次，则词频 5）

3. 初始化词表 = 所有字符的集合

4. 循环直到词表大小达到 V:
   a. 遍历所有词的字符对，按词频累加统计字符对频次
   b. 找到出现次数最多的字符对 (a, b)
   c. 合并：在所有词中，所有相邻的 a, b 合并为 ab
   d. 把 ab 加入词表，记录合并规则 (a, b) -> ab
```

**示例**（小语料）：

```
初始（已加 </w>）：
  "l o w </w>" × 5
  "l o w e r </w>" × 2
  "n e w e s t </w>" × 6
  "w i d e s t </w>" × 3
词表 = {l, o, w, e, r, n, s, t, i, d, </w>}

迭代 1：统计字符对频次
  ("e", "s"): 6+3 = 9    ← 最高
  ("s", "t"): 6+3 = 9    ← 并列
  ("l", "o"): 5+2 = 7
  ...
合并 ("e", "s") → "es"

迭代 2：
  "n e w es t </w>" × 6
  "w i d es t </w>" × 3
  ("es", "t"): 6+3 = 9   ← 最高
合并 ("es", "t") → "est"

...继续直到达到目标词表大小
```

### 💻 关键代码片段（精简版）

```python
from collections import Counter

def get_stats(vocab):
    """统计所有相邻字符对的频次"""
    pairs = Counter()
    for word, freq in vocab.items():
        symbols = word.split()
        for i in range(len(symbols)-1):
            pairs[(symbols[i], symbols[i+1])] += freq
    return pairs

def merge_pair(pair, vocab):
    """把指定的字符对合并到所有词中"""
    new_vocab = {}
    bigram = ' '.join(pair)
    replacement = ''.join(pair)
    for word in vocab:
        new_word = word.replace(bigram, replacement)
        new_vocab[new_word] = vocab[word]
    return new_vocab

def train_bpe(corpus, num_merges):
    # 1. 初始化：把每个词拆成字符
    vocab = {' '.join(list(word)) + ' </w>': freq 
             for word, freq in corpus.items()}
    
    merges = []
    for _ in range(num_merges):
        pairs = get_stats(vocab)
        if not pairs:
            break
        best = max(pairs, key=pairs.get)
        vocab = merge_pair(best, vocab)
        merges.append(best)
    return vocab, merges
```

### 🔥 高频追问 Top 3

**Q：BPE 的时间复杂度是多少？**

A：训练时**每次合并是 O(N)**（N = 语料 token 总数），合并 V 次就是 **O(VN)**。实际上有优化版本（用优先队列维护字符对频次），可以降到 O(V log N)。Encode 一段文本是 O(L²) 或 O(L log L)（取决于实现），L 是文本字符数。

**Q：BPE 怎么处理没见过的字符？**

A：**纯字符 BPE 可能 OOV**——如果训练时没见过某字符（比如特殊 Unicode）。所以现代实现都用**字节级 BPE**——先把文本编码为 UTF-8 字节序列（256 种），再做 BPE。任意字符都能用字节序列表示，**绝对不会 OOV**。GPT-2 之后这成了标配。

**Q：BPE 合并的"最高频"是绝对频次还是相对频次？**

A：**绝对频次**。这是个简单的贪心，没什么复杂统计。WordPiece 是用"相对频次"——`freq(ab) / (freq(a) × freq(b))`——的，那是它和 BPE 的核心区别。

### ⚠️ 常见陷阱

1. **要能徒手写 BPE**：字节、阿里都可能让你白板写
2. **不要忘了 `</w>` 结束符**：用来区分词内合并和跨词合并
3. **混淆字节级 BPE 和字符级 BPE**：现代 LLM 用的是字节级

### 🏢 大厂偏好

- **字节**：必考的手撕题之一
- **OpenAI / Anthropic**：会让你分析"BPE 的次优性"——它不能保证最优 tokenization

### 📚 延伸阅读
- [Neural Machine Translation of Rare Words with Subword Units](https://arxiv.org/abs/1508.07909) - Sennrich 原论文

---

## Q3：WordPiece 和 BPE 的区别 ⭐⭐⭐

### 🎯 一句话标答

> WordPiece 用**似然增益**而不是**频次**来选择合并对——理论上更优，但实际差距很小；**主要区别在合并准则**。BERT 用 WordPiece，GPT 用 BPE。

### 🗣️ 30 秒口语版

"WordPiece 和 BPE 90% 的算法逻辑一样——都是从字符开始，迭代合并字符对。**唯一区别在选择合并对的准则**。

BPE 选**最高频**的对：argmax freq(a, b)。

WordPiece 选**让语料似然增益最大**的对：argmax freq(a,b) / (freq(a) × freq(b))。

直觉理解——
- 频次最高的对，不一定是最'紧密'的——比如 'the' 频次高但很泛
- 相对频次最高的对，是**结合性最强**的——它们经常一起出现，分开很少出现

数学上 WordPiece 等价于用 unigram 语言模型做 MLE。

效果上**差距很小**，但 WordPiece 略胜在生成的 subword 更'有意义'。BERT 和 Hugging Face 早期工具用 WordPiece，GPT 和 LLaMA 用 BPE，到了 2024 年大家已经不太纠结这个区别了。"

### 🔍 算法步骤对比

| 步骤 | BPE | WordPiece |
|---|---|---|
| 初始化 | 字符表 | 字符表 |
| 合并准则 | `max freq(a, b)` | `max freq(a,b) / (freq(a) × freq(b))` |
| 编码时 | 按合并优先级 | 最大正向匹配（greedy longest match） |
| 子词标记 | `</w>` 词尾 | `##` 词中前缀（如 `##ing`） |
| 代表模型 | GPT, LLaMA, DeepSeek | BERT, DistilBERT |

### 🔥 高频追问 Top 3

**Q：WordPiece 的"似然增益"具体怎么推导？**

A：假设 unigram 语言模型 $P(s) = \prod_i P(t_i)$，合并 (a,b) → ab 后，新 token 的概率 P(ab) = freq(ab)/N，原来的 P(a)P(b) = freq(a)freq(b)/N²。

合并后整体似然变化 ≈ freq(ab) × log(P(ab) / (P(a)P(b))) = freq(ab) × log(N × freq(ab) / (freq(a) × freq(b)))。

选择**让似然增益最大**的对，等价于 argmax freq(ab) × log(freq(ab) / (freq(a) × freq(b)))。一些实现简化为直接 argmax freq(ab) / (freq(a) × freq(b))。

**Q：BERT 的 `##` 前缀是什么意思？**

A：`##` 表示**这个 subword 接在前一个 subword 后面**，不是新词的开头。比如 'playing' → 'play', '##ing'。两者拼起来还原原词时，去掉 `##` 直接连接：'play' + 'ing' = 'playing'。BPE 通常不用这种标记，靠 `</w>` 标识词尾来反推。

**Q：编码（encode）时 WordPiece 和 BPE 的区别？**

A：BPE 编码时**按合并优先级顺序**应用合并规则——先尝试最早合并的对，再试更晚合并的。WordPiece 编码时**贪心最大匹配**——从左到右扫，每次找词表中能匹配的最长前缀。两种方式都是确定性的，但具体切分结果可能略不同。

### ⚠️ 常见陷阱

- **不要把 WordPiece 当成 BPE 的简单变种**——合并准则的差异有理论支撑
- **`##` 是 BERT 独有的标记习惯**——其他模型用不同方式表示词内

---

## Q4：SentencePiece 与 Unigram ⭐⭐⭐⭐

### 🎯 一句话标答

> SentencePiece 是 Google 的**分词框架**（不是新算法），它把空格当成普通字符处理，**消除了对预分词的依赖**——直接从原始文本（含空格）训练。**Unigram 是 SentencePiece 默认的分词算法**，与 BPE 不同。

### 🗣️ 30 秒口语版

"这题容易混淆，要分清两件事——

**SentencePiece 不是分词算法，是框架**。它的两个核心特性——

第一，**把空格当字符处理**。BPE/WordPiece 在训练前需要先按空格切词（pre-tokenization），这对没有空格的语言（中文、日文）很尴尬。SentencePiece 直接处理原始文本，把空格用一个特殊字符 ▁ 替换，作为普通字符参与 BPE 或 Unigram。

第二，**支持两种算法**——BPE 和 Unigram。所以你看 LLaMA 用 SentencePiece BPE，T5 用 SentencePiece Unigram，都是合法说法。

**Unigram 是一种独立的分词算法**——和 BPE 思路完全相反——
- BPE：自下而上合并
- Unigram：自上而下剪枝

Unigram 假设每个 token 的出现概率独立，**先建一个大词表**（初始可能包含所有可能的子串），用 EM 算法估计概率，然后**迭代删掉对似然贡献最小的 token**，直到词表达到目标大小。

实际中 LLaMA、DeepSeek 用的是 SentencePiece BPE；T5、ALBERT 用 SentencePiece Unigram。两者效果接近。"

### 📐 BPE vs Unigram 对比

| 维度 | BPE | Unigram |
|---|---|---|
| 方向 | 自下而上（合并） | 自上而下（剪枝） |
| 初始词表 | 字符表 | 大候选集（所有子串） |
| 训练方式 | 贪心合并 | EM 估计 + 剪枝 |
| 编码 | 确定性，按合并优先级 | 概率性，找最大似然分词 |
| Subword 正则化 | 不支持 | 支持（多种分词采样） |
| 代表模型 | GPT, LLaMA, DeepSeek | T5, ALBERT, mT5 |

### 💡 Unigram 的关键性质：Subword Regularization

Unigram 的一个独特优势——同一个词可以有**多种合法分词**，并附带概率：

```
"unbelievable" 可能的分词：
  un + believable    (概率 0.5)
  un + believe + able (概率 0.3)
  unbelievable       (概率 0.2)
```

训练时可以**按概率采样不同分词**，作为数据增强。BPE 是确定性的，没这种灵活性。所以多语言、低资源场景，Unigram 略有优势。

### 🔥 高频追问 Top 3

**Q：为什么 LLaMA 不用 Unigram？**

A：**简单与确定性**。BPE 的训练和编码都更简单、更快、更可重现，没有 EM 迭代。LLaMA 选了简单方案，效果也够好。GPT-2/3/4 同样选 BPE。**Unigram 的"概率性分词"在大模型场景下边际收益小**——大模型本身有泛化能力，不靠分词增强。

**Q：SentencePiece 在中文上为什么更好？**

A：因为它**不依赖空格预分词**。BPE/WordPiece 原始版本假设输入已经按空格切好，中文没空格——要么调用 jieba 等先分词（引入额外依赖和错误），要么强行按字切（损失信息）。SentencePiece 直接处理原始字节，中文和英文用同一套流程，更优雅。

**Q：Subword 正则化有用吗？**

A：**理论上有用，实际工业落地少**。Subword 正则化（用 Unigram 的多分词采样）在低资源 NMT、多语言场景有提升 0.5-1 BLEU。但大模型时代，数据量够大、模型容量够大，这种数据增强的边际收益很小。所以即使用 Unigram 的模型，也很少在大模型预训练中开启采样。

### ⚠️ 常见陷阱

1. **不要把 SentencePiece 等同于 BPE 或 Unigram**——它是框架，支持两者
2. **▁ 符号要点出来**：这是空格的替代符（visualize a underscore），LLaMA tokenizer 解码时它会变回空格
3. **Unigram 的"剪枝"方向要讲清**——和 BPE 的"合并"是反方向

### 🏢 大厂偏好

- **DeepSeek / 字节**：会让你解释 SentencePiece 在中文上的优势
- **Google 系（DeepMind / Gemini 团队）**：偏爱 Unigram，可能会问 EM 推导

### 📚 延伸阅读
- [SentencePiece](https://arxiv.org/abs/1808.06226) - Google 框架论文
- [Subword Regularization](https://arxiv.org/abs/1804.10959) - Unigram + 正则化

---

## Q5：中文分词的特殊问题 ⭐⭐⭐⭐

### 🎯 一句话标答

> 中文没有显式分词边界，分词器要么用**字级 + 字节级 BPE**（无空格依赖），要么**先用 jieba 等工具切词再 BPE**（引入额外依赖）。**现代 LLM 几乎都选前者**——LLaMA 把中文按 UTF-8 字节走 BPE，最简单也最稳。

### 🗣️ 30 秒口语版

"中文分词的特殊性在于——**没有显式词边界**。英文 'I love you' 三个词靠空格隔开，中文 '我爱你' 三个字连在一起。

历史上有两条路——

**第一条：先用 jieba 等工具切词，再做 subword**。早期一些模型用，但有两个问题：① 引入外部依赖；② jieba 本身的分词错误会传递；③ 多语言场景麻烦——jieba 处理不了英文、日文。

**第二条：直接按字节级 BPE，让模型自己学**。LLaMA 走的就是这条——把'我爱你'编码成 UTF-8 字节（每字 3 字节），整个字节流交给 BPE。模型在训练中**自然学到字符或词级的合并**——'你好' 经常一起出现，会被合并成一个 token。这条路最优雅，是当前主流。

**关键工程问题**：**中文 token 利用率**。LLaMA 训练数据以英文为主，中文 BPE 合并不充分，一个汉字往往占 2-3 个 token。**Qwen、DeepSeek 等专门优化过中文 tokenizer**——增加中文训练数据，让常见汉字合并成单 token，中文压缩率从 2-3 tokens/字 降到 1-1.5 tokens/字。"

### 🔍 主流模型的中文 token 表现

测试句子："今天天气真好，我们去公园散步吧。"（共 14 字 + 标点）

| 模型 | tokenizer | token 数 | 字/token |
|---|---|---|---|
| LLaMA-2 | SentencePiece BPE (32K) | ~30 | 0.5 |
| LLaMA-3 | TikToken-like BPE (128K) | ~18 | 0.8 |
| Qwen-2 | BPE (152K) | ~12 | 1.2 |
| DeepSeek-V3 | BPE (~129K) | ~12 | 1.2 |
| GPT-4 | TikToken cl100k_base | ~15 | 0.9 |

字/token 比值越大，**中文压缩率越好，推理越快**。

### 💡 为什么 token 利用率重要？

- **推理速度**：1000 字的中文，LLaMA-2 要 2000 token，Qwen 只要 800 token——快 2.5 倍
- **上下文长度**：相同 4K 上下文，Qwen 能装 3300 字，LLaMA-2 只能装 1300 字
- **API 成本**：按 token 计价的服务，Qwen 处理中文便宜 2-3 倍

### 🔥 高频追问 Top 3

**Q：怎么提升中文 token 利用率？**

A：两个维度——
- **训练数据**：分词器训练时增加中文语料比例，让 BPE 自然合并常见汉字
- **词表大小**：扩大词表（从 32K 到 128K+），给中文 token 留出空间

实际工业方案——
- **LLaMA-3**：词表从 32K 扩到 128K，中文显著改善
- **Qwen / DeepSeek**：从零训练中英文混合分词器，词表 128K-150K，中文压缩率与英文接近
- **基于 LLaMA 增量训练中文模型**（如 Chinese-LLaMA）：扩充词表 + 词表合并

**Q：直接用 jieba 分词，再喂给 LLM 会怎样？**

A：**会非常糟糕**。LLM 期待的输入是和训练时一致的 token 流，jieba 切出来的词不在 LLM 的词表里，强行匹配会破坏分词，效果反而比直接用字节级差很多。LLM 时代 jieba 主要用于检索、NLP 流水线，不进 LLM 的输入端。

**Q：中文分词器的"压缩率"如何评估？**

A：标准做法是**用相同语料对比**：
- 取一个中文 benchmark 文本（比如 1 万字的新闻）
- 用不同 tokenizer 编码，统计 token 数
- 算 `字数 / token 数`，越大越好

也有"压缩率"指标 `原始字节数 / token 数`——衡量字节级压缩效率。Qwen、DeepSeek 在这个指标上明显优于 LLaMA。

### ⚠️ 常见陷阱

1. **不要说"中文要先用 jieba 分词"**——现代 LLM 不需要
2. **要点出"字节级 BPE 是当前主流"**
3. **要懂"中文 token 利用率"对推理速度的影响**

### 🏢 大厂偏好

- **国内大厂（字节、阿里、百度）**：必问——中文是核心场景
- **Qwen / DeepSeek 团队**：会追问中文 tokenizer 训练细节

### 📚 延伸阅读
- [Chinese-LLaMA](https://arxiv.org/abs/2304.08177) - 中文词表扩展方案

---

## Q6：词表大小如何选择？ ⭐⭐⭐

### 🎯 一句话标答

> 词表大小是个**多维权衡**——大词表 token 利用率高但参数量大、低频 token 学不好；小词表参数省但序列长。**主流选 32K-150K**，越大模型 + 越多语言越倾向于大词表。

### 🗣️ 30 秒口语版

"词表大小要考虑几个权衡——

**优势（大词表）**：
- Token 利用率高，序列更短，推理快
- 多语言友好，每种语言都有足够 token 配额
- 罕见词不用过度拆分，语义更完整

**劣势（大词表）**：
- Embedding 矩阵参数变大——词表 V × hidden d，V 翻倍参数翻倍
- 输出 softmax 计算变慢——分类到 V 类比小词表慢
- 低频 token 训练样本少，embedding 学不好
- 浪费——专业领域 token 在通用场景用不上

**主流选择**：
- 早期 BERT/GPT-2：30K-50K
- LLaMA-2：32K（偏小，中文吃亏）
- LLaMA-3：128K（大幅扩展）
- Qwen-2 / DeepSeek-V3：150K 左右

**经验法则**：
- 通用大模型：100K-200K
- 多语言模型：200K+
- 单语言或特定领域：30K-50K 就够"

### 🔍 词表大小的影响因素

| 维度 | 小词表（30K） | 大词表（150K） |
|---|---|---|
| Embedding 参数 | 30K × d | 150K × d（5×） |
| 输出 softmax 速度 | 快 | 慢 5× |
| Token 利用率（中文） | 低（2-3 字/token） | 高（1.2 字/token） |
| 低频 token 训练 | 不是问题 | 容易欠拟合 |
| 多语言支持 | 弱 | 强 |
| 上下文等效长度 | 短 | 长 |

### 🔥 高频追问 Top 3

**Q：词表大小和模型参数量的关系怎么算？**

A：Embedding 层和最后的 LM head 都依赖词表大小。

设 hidden_dim = d, 词表 V：
- Token Embedding 参数 = V × d
- LM Head 参数 = V × d（如果 tied weight 则共享，否则独立）

对 LLaMA-2 7B（d=4096, V=32K）：
- Embedding = 32K × 4096 ≈ 130M
- 不算 head：占总参数 1.8%

对 Qwen-2 7B（d=3584, V=152K）：
- Embedding = 152K × 3584 ≈ 540M
- 占总参数 7.7%

**所以扩大词表会**显著增加 embedding 参数**，但模型主体（transformer 层）不变。

**Q：词表能不能动态扩展？比如训练后追加新 token？**

A：可以——这就是"词表扩展"技术。Chinese-LLaMA 的做法是：在 LLaMA-2（32K 英文为主）基础上，**追加 20K 中文 token**，新 embedding 用平均或者初始化方法。然后**继续预训练**让新 token 学到表示。代价是新 token 训练不充分时效果差。

**Q：能不能词表无限大，让每个常见短语都有一个 token？**

A：理论上可以，工程不行——
- **数据稀疏**：每个 token 需要足够训练样本才能学好，过细的 token 训练不充分
- **存储爆炸**：100M token 词表意味着 100M × d 的 embedding，超出显存
- **没意义**：分词的本质是平衡粒度，过细等同于退化成词级分词，丢失泛化

实践中**100K-200K 是甜点**。

### ⚠️ 常见陷阱

- **不要忘记词表大小影响 embedding 参数**：扩词表会增加模型大小
- **不要说"词表越大越好"**——低频 token 训练不足是问题

### 🏢 大厂偏好

- **字节、Qwen 团队**：会问"如何为新语言扩词表"

---

## Q7：Tokenizer 训练数据的重要性 ⭐⭐⭐

### 🎯 一句话标答

> Tokenizer 训练数据的**分布直接决定**哪些 token 被合并——训练数据如果偏英文，中文必然吃亏；训练数据偏正式文体，口语/代码必然吃亏。

### 🗣️ 30 秒口语版

"很多人没意识到——**tokenizer 也是要训练的**，而且训练数据非常重要。

BPE 训练时统计字符对频次——所以**训练数据的分布就是分词器的偏好**。比如：

- 用 100% 英文维基百科训练 → 'apple' 是一个 token，'苹果' 是 2-3 个 token
- 用中英混合训练 → '苹果' 可能成为一个 token，但稀有英文词被拆开
- 用代码训练 → 'function', 'return' 是单 token；散文里的修辞被拆开

**LLaMA-1 / LLaMA-2 的中文问题**就是这个原因——分词器训练数据以英文为主，中文 token 利用率差。

**实践建议**：
- 通用模型：分词器训练数据应**覆盖目标语言和领域**
- 多语言：按目标分布混合，比如 50% 英 + 30% 中 + 20% 其他
- 专业领域（代码、医疗）：加入对应语料，让专业词成为 token"

### 🔥 高频追问 Top 3

**Q：分词器训练数据和模型训练数据要一致吗？**

A：**强烈建议一致或近似**。如果分词器在英文上训，模型在中英文上训，**中文部分的 token 化效率低，推理慢、长度短**。一致才能让模型在每种语言上都享受合理的 token 利用率。

**Q：分词器训练需要多少数据？**

A：**远少于模型训练**。一般几 GB 到几十 GB 就够——只需要让 BPE 看到足够的 n-gram 模式。模型预训练动辄几 T tokens，但分词器训练通常用几百 M-几 G 的代表性子集即可。

**Q：能不能用部分公开数据训分词器，模型用私有数据训？**

A：可以，但要确保**分布相似**。如果公开数据和私有数据领域差异大（比如公开是新闻、私有是医疗对话），分词器在私有数据上效率低。最好的做法是混合一定比例的私有数据来训分词器。

### 🏢 大厂偏好

- 这题更多在 AI Infra / 数据工程岗考察

---

## Q8：现代分词器的优化趋势 ⭐⭐⭐

### 🎯 一句话标答

> 现代趋势是**更大词表（128K+）、字节级 BPE、多语言友好、特殊 token 设计**，还有一些**新尝试**比如 Patch-Token、视觉 token 统一。

### 🗣️ 30 秒口语版

"2023-2026 的分词器演化有几个明显趋势——

**1. 词表持续扩大**。GPT-2 时代 50K，到 GPT-4 的 cl100k_base 是 100K，LLaMA-3 是 128K，Qwen-2 是 152K。**多语言 LLM 都在向 100K+ 靠拢**。

**2. 字节级 BPE 成事实标准**。彻底解决 OOV 和多语言问题。GPT-4、LLaMA、Qwen 都是字节级。

**3. 特殊 token 设计精细化**。比如——
- 系统/用户/助手的角色标记：`<|system|>` 等
- 工具调用相关：`<|tool_call|>`
- 推理模型的思考标记：`<think>`, `</think>`（DeepSeek-R1）

**4. 跨模态统一 token**。一些前沿工作把视觉 patch、音频 frame 都编码成 token，和文本 token 共享一个词表。Chameleon、Gemini Native Multimodal 等是代表。

**5. Patch / Block-level Tokenization**。受 ViT 启发，把连续 N 个字符当作一个 patch，**用于加速长文本处理**。比如 MegaByte、SpaceByte 等工作。

**6. 学习型分词器**。让分词作为模型的一部分被端到端训练——比如 CharFormer。理论上更优，但工程复杂，尚未主流。"

### 🔥 高频追问 Top 3

**Q：为什么 GPT-4 的词表比 GPT-3 大？**

A：因为 **GPT-4 重视多语言和长上下文**。词表大 → 中文、日文等利用率高 → 同样 32K 上下文能装更多内容。同时 LLM 的推理成本越来越主导，词表大能省 token，省推理钱。

**Q：特殊 token 是怎么处理的？**

A：作为**保留 ID**——分词器初始化时就预留一批 ID（比如 ID 0-100），永远不会被自然文本占用。这些 ID 留给：
- 控制 token：`<bos>`, `<eos>`, `<pad>`
- 角色 token：`<|user|>`, `<|assistant|>`
- 任务 token：`<tool>`, `<image>` 等

模型在 SFT 或 RLHF 阶段学习这些特殊 token 的语义。

**Q：未来分词器会消失吗？端到端字节模型可能吗？**

A：**理论上可行，实际仍困难**。字节级端到端模型（如 MambaByte、ByT5）已存在，但**长度太长**——一篇文章变成几十万字节，注意力 O(n²) 吃不消。Mamba 这种 SSM 架构能处理长序列，但效果上还不如带分词器的 Transformer。**短期内分词器不会消失**，但可能演化成 "粗 + 细" 的混合模式（如 MegaByte 的 patch + 字节）。

### 🏢 大厂偏好

- **OpenAI / Google / Anthropic**：会问长上下文 + 多模态下的分词演进

### 📚 延伸阅读
- [MegaByte](https://arxiv.org/abs/2305.07185) - patch-level tokenization
- [Chameleon](https://arxiv.org/abs/2405.09818) - 统一文本+图像 token

---

## 📝 本章小结

| 关键点 | 你必须能脱口而出 |
|---|---|
| 三种分词粒度 | 字符（短词表长序列）/词（长词表 OOV）/subword（平衡） |
| BPE 核心 | 贪心合并最高频字符对，确定性、可重现 |
| WordPiece 与 BPE 差别 | 似然增益 vs 频次 |
| SentencePiece | 框架不是算法，支持 BPE 和 Unigram |
| Unigram | 自上而下剪枝，支持 subword 正则化 |
| 中文方案 | 字节级 BPE + 中文友好词表 |
| 词表大小 | 通用 LLM 100K-200K，影响 embedding 参数和 token 利用率 |
| 训练数据决定偏好 | tokenizer 训练数据分布 = 分词器偏好 |
| 现代趋势 | 大词表、字节级、多语言、特殊 token、跨模态 |

## ✅ 自测题

1. 现场用 Python 实现一个简化版 BPE（不查文档）
2. 用一段中文测试 LLaMA-2 和 Qwen-2 的 tokenizer，解释为什么 token 数差别这么大
3. 设计一个支持中、英、代码三种语言的 tokenizer，你会怎么准备训练数据？
4. 解释 SentencePiece 的 ▁ 符号是什么，编码后能不能完美还原原文？
5. 如果让你扩展 LLaMA-2 的中文能力，从 tokenizer 角度有几条路？比较优劣
