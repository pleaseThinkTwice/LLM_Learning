# 第 2 章｜位置编码专题

> **本章定位**：位置编码是 LLM 面试的"第二高频区"，仅次于 Self-Attention。RoPE 几乎必考，**长上下文外推**是 2024-2026 年的热点题，**MLA 中的 RoPE 解耦**是 DeepSeek 相关岗位的标志性问题。读完本章，你要能做到：从"为什么需要位置编码"这个根问题出发，一路讲到 YaRN、MLA 的工程妥协。

> **配套章节**：Transformer 架构 → 第 1 章 / Flash Attention → 第 3 部分 / MLA 详细 → 第 6 部分 DeepSeek 专题

---

## 📌 本章导航

| 序号 | 题目 | 难度 | 频率（字节/阿里/Meta/DeepSeek） | 类型 |
|---|---|---|---|---|
| Q1 | 为什么 Transformer 需要位置编码？ | ⭐⭐⭐ | 6 / 3 / 2 / 2 | 概念基础 |
| Q2 | Sinusoidal 位置编码的设计原理 | ⭐⭐⭐⭐ | 5 / 3 / 2 / 1 | 数学推导 |
| Q3 | 绝对位置编码 vs 相对位置编码 | ⭐⭐⭐ | 4 / 2 / 1 / 1 | 概念对比 |
| Q4 | **RoPE 的完整推导** | ⭐⭐⭐⭐⭐ | **10 / 6 / 3 / 8** | 必考 |
| Q5 | RoPE 为什么能外推？ | ⭐⭐⭐⭐⭐ | 7 / 4 / 2 / 6 | 深度理解 |
| Q6 | NTK-aware / YaRN / PI 长度扩展 | ⭐⭐⭐⭐⭐ | 6 / 3 / 2 / 5 | 前沿 |
| Q7 | ALiBi 与 RoPE 对比 | ⭐⭐⭐ | 3 / 2 / 2 / 1 | 加分题 |
| Q8 | MLA 中的 RoPE 解耦问题 | ⭐⭐⭐⭐⭐ | 5 / 2 / 1 / 7 | DeepSeek 专题 |

---

## Q1：为什么 Transformer 需要位置编码？ ⭐⭐⭐

### 🎯 一句话标答

> 因为 Self-Attention 是**置换等变的**——打乱输入顺序，输出顺序也跟着打乱，但**内容不变**。模型本身感知不到位置，所以必须显式注入位置信息。

### 🗣️ 30 秒口语版

"这个问题要从 Self-Attention 的本质讲起。

Self-Attention 计算的时候，本质上是个**集合操作**——它把每个 token 当成集合里的一个元素，互相算注意力、加权求和。**集合是没有顺序的**——'我爱你'和'你爱我'对 Attention 来说是一样的，因为它看到的只是三个 token 的集合，不知道谁前谁后。

但语言是有顺序的——'狗咬人'和'人咬狗'意思完全不同。所以我们必须**在输入端就把位置信息注入进去**，让模型能区分顺序。

这就是位置编码的作用：给每个 token 加上一个'位置信号'，告诉模型这个 token 在序列的第几位。

注入的方式有两类——**绝对位置编码**（直接加在 embedding 上）和**相对位置编码**（在 attention 计算里融入相对距离）。现在主流的 LLM 用的是 RoPE，是一种很巧妙的相对位置方案。"

### 📐 严格证明：Self-Attention 的置换等变性

设输入序列 $X = [x_1, x_2, ..., x_n]$，置换矩阵 P（交换两个 token）作用后变成 $PX$。

注意力输出：
$$\text{Attn}(X) = \text{softmax}\left(\frac{(XW_Q)(XW_K)^T}{\sqrt{d}}\right) XW_V$$

置换后：
$$\text{Attn}(PX) = \text{softmax}\left(\frac{(PXW_Q)(PXW_K)^T}{\sqrt{d}}\right) PXW_V$$

注意：
$$(PXW_Q)(PXW_K)^T = PX W_Q W_K^T X^T P^T = P (XW_Q W_K^T X^T) P^T$$

所以：
$$\text{Attn}(PX) = P \cdot \text{Attn}(X)$$

**结论**：输入打乱顺序，输出也只是按相同方式打乱顺序，**每个 token 的表示不变**。这就是"置换等变"。

### 🔥 高频追问 Top 3

**Q：RNN 和 CNN 需要位置编码吗？**

A：不需要。RNN 通过**时间步的递归**自带顺序信息——h_t 是依赖 h_{t-1} 算出来的，顺序天然刻进了计算图。CNN 通过**卷积核滑动**保留局部位置关系——同一个核扫过不同位置，输出位置和输入位置一一对应。只有 Self-Attention 这种"全对全"的运算把顺序信息抹掉了，所以需要显式补回来。

**Q：能不能用一个可学习的位置 embedding？**

A：可以，BERT 和 GPT-2 就是这么做的——直接定义一个 `nn.Embedding(max_len, d)`，按位置 ID 查表。优点是简单灵活，模型可以自由学到位置表示。**缺点是不能外推**——如果训练时 max_len=512，推理时给 1024 的输入就崩了，因为 513~1024 这些位置从来没训过，对应的 embedding 是随机初始化的。这也是后来 Sinusoidal、RoPE 这些 **函数形式**的位置编码流行起来的原因——它们对任意位置都有解析形式。

**Q：能不能让模型自己从数据中"涌现"出位置感知？**

A：理论上不能。因为 Self-Attention 的置换等变性是**架构层面的硬约束**，不是参数能学出来的。这是个数学事实，不是训练问题。当然，**有些工作尝试用 Causal Mask 来引入位置信息**——因为 mask 是上三角的，"位置"在某种意义上被编码进了 mask 的结构里。这种思路叫 NoPE（No Positional Encoding），在短序列上能 work，但长序列效果差。

### ⚠️ 常见陷阱

1. **不要说"位置编码是给模型一个时间感"** —— 太模糊，要明确说是为了打破置换等变性
2. **不要忘了对比 RNN / CNN** —— 这能体现你理解架构差异
3. **可学习位置 embedding 的外推问题** —— 是 BERT 时代到 LLaMA 时代演化的关键动机

### 🏢 大厂偏好

- **字节**：会从这题切入，追问到 RoPE 的外推问题
- **OpenAI / Anthropic**：偏理论，会问"置换等变性的严格定义"

### 📚 延伸阅读
- [Self-Attention with Relative Position Representations](https://arxiv.org/abs/1803.02155) - 第一篇相对位置编码

---

## Q2：Sinusoidal 位置编码的设计原理 ⭐⭐⭐⭐

### 🎯 一句话标答

> Sinusoidal 用**不同频率的正弦余弦函数**给每个位置编码，目的是让模型能通过线性变换学到**相对位置**，同时支持任意长度的外推。

### 🗣️ 30 秒口语版

"Sinusoidal 是原 Transformer 论文里用的方案。公式是这样的——

对位置 pos，第 2i 维用 sin(pos / 10000^(2i/d))，第 2i+1 维用 cos(pos / 10000^(2i/d))。

这个设计有三个聪明的地方。

**第一**，每一维都是不同频率的波——低维高频，高维低频，类似二进制编码——低位变化快，高位变化慢，能唯一区分每个位置。

**第二**，用三角函数有个数学性质：**任意位置的编码都可以通过另一位置的编码做线性变换得到**。这意味着模型可以通过学一个固定的矩阵来表达'偏移多少位'，天然支持相对位置。

**第三**，是**函数形式**而不是查表，所以训练时只见过 512 长度，推理时给 2048 长度也能算出来——理论上支持任意外推。

不过实际外推效果一般，因为高频部分在没见过的位置上震荡很厉害。后来才有了 RoPE。"

### 📐 完整公式

$$PE_{(pos, 2i)} = \sin\left(\frac{pos}{10000^{2i/d}}\right)$$

$$PE_{(pos, 2i+1)} = \cos\left(\frac{pos}{10000^{2i/d}}\right)$$

其中：
- $pos$ 是位置索引（0, 1, 2, ...）
- $i$ 是维度索引（0 到 d/2-1）
- $d$ 是 embedding 维度

### 💡 三个设计精髓详解

**精髓 1：不同维度对应不同频率**

把 $\frac{1}{10000^{2i/d}}$ 看成角频率 $\omega_i$：
- 当 i=0 时，$\omega_0 = 1$（最高频，每移动 1 步相位变化最大）
- 当 i=d/2 时，$\omega = 10^{-4}$（最低频，几千步才变化一个周期）

**类比**：像一个 d/2 维的"时钟"——秒针、分针、时针、日历……组合起来唯一标识时间。

**精髓 2：相对位置的线性可学习性**

数学上可证明：
$$PE_{pos+k} = M_k \cdot PE_{pos}$$

其中 $M_k$ 是一个**只依赖 k 不依赖 pos** 的旋转矩阵。证明思路用三角函数和角公式：

$$\sin(\omega(pos+k)) = \sin(\omega \cdot pos)\cos(\omega k) + \cos(\omega \cdot pos)\sin(\omega k)$$

这意味着只要模型学到了 $M_k$，就能从任意位置的编码推导出偏移 k 的编码。

**精髓 3：为什么是 10000？**

10000 不是绝对的——它是个超参，控制最长周期：
- 最长周期 $\approx 2\pi \cdot 10000$
- 在 d=512 时，能覆盖到约 6 万个位置不重复

10000 是个平衡——既能覆盖足够长的序列，又不会让低频太"懒"（变化太慢，模型学不到）。

### 🔥 高频追问 Top 3

**Q：Sinusoidal 能外推到很长的序列吗？**

A：**理论上可以，实际不行**。问题在于**高频维度**。i=0 时角频率为 1，那么 pos=2000 时 $\sin(2000)$ 是一个看上去随机的值。训练时模型如果只见过 pos=0~512 的 sin/cos 模式，没见过更大 pos 的高频震荡组合，attention pattern 会失效。这就是为什么实际 Sinusoidal 外推效果一般，需要 RoPE 这种更稳的方案。

**Q：为什么不用整数 pos 直接当编码？**

A：两个问题——
- **尺度不归一**：pos=1000 和 pos=1 数值差太大，没归一化的话直接加到 embedding 上会主导信号
- **没有相对位置的线性结构**：直接用整数，模型从 pos=10 到 pos=20 的"偏移 10"没法用线性变换表达，要学的关系太散

Sinusoidal 用三角函数同时解决了这两个问题——数值范围天然在 [-1, 1]，且相对位置可线性表达。

**Q：可学习位置 embedding 和 Sinusoidal 谁好？**

A：在**长度内**差不多，BERT 用可学习的也跑得很好。**外推**上 Sinusoidal 占优。**参数量**上 Sinusoidal 是 0（无参数），可学习需要 max_len × d 的参数表。综合下来，**短序列任务可学习方便、长序列任务 Sinusoidal 更稳**——但现在两者都被 RoPE 取代了。

### ⚠️ 常见陷阱

- **不要忘了说"相对位置可线性表达"** —— 这是 Sinusoidal 的核心优势
- **不要把 10000 当成神秘常数** —— 它是超参，能调整
- **要区分理论外推能力和实际外推效果**

### 🏢 大厂偏好

- **字节**：会让你现场推导"相对位置的线性结构"
- **OpenAI**：少考，因为已经被 RoPE 取代

### 📚 延伸阅读
- [Attention Is All You Need](https://arxiv.org/abs/1706.03762) §3.5

---

## Q3：绝对位置编码 vs 相对位置编码 ⭐⭐⭐

### 🎯 一句话标答

> 绝对位置编码给每个位置一个唯一标识；相对位置编码只看**两个位置的距离**——后者**更符合语言的本质**，是当前主流。

### 🗣️ 30 秒口语版

"绝对位置编码就像给每个座位贴号码——第 1 排第 5 座、第 2 排第 3 座。模型看到的是'你是第几位'。

相对位置编码不关心绝对座位，只关心**你离我多远**——'你在我前面 3 步'。

为什么相对位置更好呢？因为**语言的局部结构是平移不变的**。'喜欢' + '苹果' 的搭配关系不应该因为它出现在句子第 5 个词还是第 50 个词而改变。相对位置直接编码了'距离 1'这个事实，模型不用为每个绝对位置重新学一遍。

实现上的差别也很大。绝对位置在输入端注入——加到 embedding 上；相对位置在 attention 计算里注入——影响 QK 的点积。这导致相对位置天然不受序列长度限制，外推性更好。

RoPE 是一种特殊的相对位置编码——它通过对 Q、K 做旋转，让点积自带相对距离。"

### 📐 两类方法的数学差异

**绝对位置编码（如 Sinusoidal）**：
$$\tilde{x}_i = x_i + p_i$$
然后做正常的 attention：
$$\text{Attn}(i, j) = \frac{(\tilde{x}_i W_Q)(\tilde{x}_j W_K)^T}{\sqrt{d}}$$

展开后：
$$= \frac{x_i W_Q W_K^T x_j^T + x_i W_Q W_K^T p_j^T + p_i W_Q W_K^T x_j^T + p_i W_Q W_K^T p_j^T}{\sqrt{d}}$$

绝对位置 p_i, p_j 通过加法和 x 耦合，影响是间接的。

**相对位置编码（如 RoPE）**：
$$\text{Attn}(i, j) = \frac{(R_i x_i W_Q)(R_j x_j W_K)^T}{\sqrt{d}}$$

其中 R_i, R_j 是位置相关的旋转矩阵。展开后：
$$= \frac{x_i W_Q (R_{i-j}) W_K^T x_j^T}{\sqrt{d}}$$

**关键**：最终只依赖 i-j 这个相对距离，不依赖 i 和 j 的绝对值。

### 🔍 主要方法对比表

| 类型 | 代表方法 | 注入位置 | 外推性 | 参数量 |
|---|---|---|---|---|
| 绝对（可学习） | BERT/GPT-2 | Embedding | 差 | max_len × d |
| 绝对（函数式） | Sinusoidal | Embedding | 中 | 0 |
| 相对（attention bias） | T5 bias | Attention | 中 | h × max_dist |
| 相对（线性偏置） | ALiBi | Attention | 好 | h |
| 相对（旋转） | **RoPE** | Q, K | 较好 | 0 |

### 🔥 高频追问 Top 3

**Q：相对位置编码的所有优点都比绝对的好吗？**

A：不是一边倒。相对位置在大多数 NLP 任务上更好，但在**任务本身依赖绝对位置**的场景下可能不如。比如指令任务中需要识别"第一句话是什么"，绝对位置反而更直接。但工程上发现 RoPE 这种相对位置也能通过 attention pattern 学到弱的绝对位置感知，所以实践中没成大问题。

**Q：为什么 BERT 用绝对位置 GPT 也用，效果都不错？**

A：因为在那个时代（2018-2020），序列长度还不长（512、1024），绝对位置 embedding 完全够用。问题是**当序列扩展到 4K、32K 甚至 128K** 时，可学习的绝对位置完全失灵——没见过的位置 ID 对应的 embedding 是随机的。相对位置编码不存在这个问题，所以现代 LLM 全面转向相对路线。

**Q：T5 的相对位置 bias 和 RoPE 哪个好？**

A：T5 在 attention logits 上加一个学习的 bias，依赖 i-j 距离——简单有效但参数量大。RoPE 通过旋转矩阵让点积**自然蕴含相对距离**——更优雅，无参数，且与 attention 公式深度集成。**实证上 RoPE 在大模型上表现更稳定，所以成了主流**。LLaMA、Qwen、DeepSeek 全用 RoPE。

### ⚠️ 常见陷阱

- 不要把 "相对位置编码"和 "RoPE"等同——RoPE 是一种相对位置编码，但相对位置编码不止 RoPE

### 🏢 大厂偏好

- 这题更多作为铺垫，引出 RoPE 的深入提问

---

## Q4：RoPE 的完整推导 ⭐⭐⭐⭐⭐（必考）

### 🎯 一句话标答

> RoPE 通过对 Q、K **做 2D 旋转变换**，让两个位置的注意力分数只依赖于**相对位置**——是绝对位置注入、相对位置生效的优雅设计。

### 🗣️ 30 秒口语版

"RoPE 全称 Rotary Position Embedding，是苏剑林老师提出的。它的核心思想可以这样描述——

我们希望两个 token 的注意力分数只跟它们的**相对距离**有关，跟绝对位置无关。怎么做到呢？RoPE 的答案是：**把 Q 和 K 看成复数（或二维向量），按位置做旋转**。

具体来说，对位置 m 的 query，旋转 mθ 角度；对位置 n 的 key，旋转 nθ 角度。然后做点积——

⟨R_m·q, R_n·k⟩ = ⟨q, R_{n-m}·k⟩

最后的结果只取决于 n-m，**绝对位置消失了，留下的全是相对位置**。这就是 RoPE 的魔法。

实现上，它把 d 维向量两两配对，每对当作一个二维平面，做不同频率的旋转。频率公式和 Sinusoidal 类似：θ_i = 10000^(-2i/d)。

RoPE 现在是 LLaMA、Qwen、DeepSeek 等几乎所有主流 LLM 的标配。"

### 📐 完整数学推导

#### Step 1：目标函数

我们希望找到函数 $f$，使得：
$$\langle f(q, m), f(k, n) \rangle = g(q, k, n-m)$$

即 Q 和 K 经过位置变换后的内积，只依赖于相对位置 $n-m$。

#### Step 2：二维情形的解（最简洁的情形）

在 2D 平面上，把向量看作复数：$q = q_0 + i \cdot q_1$。

定义 $f(q, m) = q \cdot e^{im\theta}$（即旋转 $m\theta$ 角度）。

计算内积（取实部）：
$$\text{Re}\langle f(q,m), f(k,n) \rangle = \text{Re}(q e^{im\theta} \overline{k e^{in\theta}}) = \text{Re}(q\bar{k} e^{i(m-n)\theta})$$

**结论**：内积只依赖 $m-n$ ✓

#### Step 3：写成矩阵形式

$$f(q, m) = \begin{pmatrix} \cos m\theta & -\sin m\theta \\ \sin m\theta & \cos m\theta \end{pmatrix} \begin{pmatrix} q_0 \\ q_1 \end{pmatrix}$$

#### Step 4：扩展到 d 维

把 d 维向量两两配对：$(q_0, q_1), (q_2, q_3), ..., (q_{d-2}, q_{d-1})$。每对使用不同的旋转频率：
$$\theta_i = 10000^{-2i/d}, \quad i \in \{0, 1, ..., d/2-1\}$$

完整旋转矩阵（块对角）：
$$R_m = \begin{pmatrix} R_{m, \theta_0} & & & \\ & R_{m, \theta_1} & & \\ & & \ddots & \\ & & & R_{m, \theta_{d/2-1}} \end{pmatrix}$$

每个 $R_{m, \theta_i}$ 是 2D 旋转矩阵。

#### Step 5：注意力分数

$$\text{Attention}(q_m, k_n) = (R_m q)^T (R_n k) = q^T R_m^T R_n k = q^T R_{n-m} k$$

最后一步用了旋转矩阵的性质 $R_m^T R_n = R_{n-m}$。

### 💻 高效实现（不真的造旋转矩阵）

```python
def apply_rope(x, theta_base=10000):
    """
    x: (batch, seq_len, n_head, d_head)
    输出已应用 RoPE 的 x
    """
    bsz, seq_len, n_head, d = x.shape
    
    # 1. 算 theta_i：每对维度一个频率
    theta = theta_base ** (-torch.arange(0, d, 2).float() / d)  # (d/2,)
    
    # 2. 算位置 × 频率
    pos = torch.arange(seq_len).float()                          # (seq_len,)
    freqs = torch.outer(pos, theta)                              # (seq_len, d/2)
    
    # 3. 算 cos / sin（插一个 n_head 维，才能和 x 广播）
    cos = freqs.cos()[:, None, :]                               # (seq_len, 1, d/2)
    sin = freqs.sin()[:, None, :]                               # (seq_len, 1, d/2)
    
    # 4. 把 x 拆成两半（偶数维和奇数维）
    x1, x2 = x[..., 0::2], x[..., 1::2]                         # 各 (..., d/2)
    
    # 5. 旋转
    x_rot = torch.stack([
        x1 * cos - x2 * sin,
        x1 * sin + x2 * cos
    ], dim=-1).flatten(-2)                                       # (..., d)
    
    return x_rot
```

**实现要点**：
1. **不显式构造旋转矩阵** —— 那是 d×d 的矩阵，太浪费。直接逐对处理。
2. **频率 theta_i 是预计算的** —— 不依赖输入，可以 cache
3. **维度配对方式**有两种：相邻配对（0-1, 2-3...）和间隔配对（0-d/2, 1-d/2+1...）—— LLaMA 用的是间隔配对，等价但更适合 GPU

### 🔥 高频追问 Top 3

**Q：RoPE 是绝对位置编码还是相对位置编码？**

A：**两者都是**。它通过**绝对位置 m 的旋转矩阵**实施编码（操作时用的是绝对位置），但点积结果**只依赖于相对位置 n-m**（实际生效的是相对位置）。这种"绝对实施、相对生效"是 RoPE 的精髓——既有相对位置的好处（外推、平移不变），又避免了相对位置编码常见的实现复杂度。

**Q：为什么 RoPE 只对 Q、K 旋转，不对 V 旋转？**

A：因为位置信息**只需要影响 attention 权重的计算**，不需要影响信息的"内容"。Q、K 的点积决定"看谁"，所以要带位置；V 是"被传递的内容"，不该被位置扭曲。如果 V 也旋转，相当于把信息内容和位置耦合在一起，反而降低表达力。

**Q：相邻配对和间隔配对真的等价吗？**

A：**数学上完全等价**——都是把 d 维向量两两配对做 2D 旋转，只是配对的方式不同。LLaMA 等用间隔配对（把前 d/2 维和后 d/2 维配对），主要原因是 GPU 操作友好——直接用 `x.chunk(2, dim=-1)` 切两半，不用 stride 操作。Hugging Face 实现有时候用相邻配对，跟 LLaMA 的权重不兼容，这是个工程坑。

### ⚠️ 常见陷阱

1. **数学推导写不出来**：面试官可能现场要求推导 2D 情形的内积只依赖相对距离——必须能写
2. **混淆 RoPE 和 Sinusoidal**：它们的频率公式形似（都用 10000），但作用机制完全不同
3. **忘了说"绝对实施、相对生效"**：这是体现理解深度的关键句

### 🏢 大厂偏好

- **DeepSeek / 智谱**：必问，往往要求白板推导
- **字节**：高频题，会追问外推和实现细节

### 📚 延伸阅读
- [苏剑林博客：RoPE 原文](https://kexue.fm/archives/8265) - 最权威的中文解读
- [RoFormer](https://arxiv.org/abs/2104.09864) - 原论文

---

## Q5：RoPE 为什么能外推？外推效果不好怎么办？ ⭐⭐⭐⭐⭐

### 🎯 一句话标答

> 因为 RoPE 用的是**正余弦函数形式**，对任意位置都有定义；但**实际外推效果有限**——超过训练长度太多时，高频维度的旋转角度落入未见过的区域，attention 模式会失效。

### 🗣️ 30 秒口语版

"理论上 RoPE 能外推，因为旋转角度 mθ_i 对任意 m 都有定义，不像可学习位置 embedding 那样需要查表。

但实际呢？**直接外推效果一般**，超过训练长度 1.5~2 倍就明显衰减。原因有两个——

**第一**，**高频维度的旋转角度在长位置上变化太快**。比如 θ_0 = 1 的维度，在 m=10000 的位置旋转角度已经走过了 1600 多个周期，模型在训练时只见过几十个周期的模式，新位置的 attention pattern 是分布外的，模型不认识。

**第二**，**远距离注意力分数本身有衰减**——RoPE 本身有'位置越远，平均注意力分数越低'的性质，但训练时只学过短距离衰减，长距离衰减的强度模型没见过。

所以工业界发展出了一系列长度扩展技术——**Position Interpolation（PI）**、**NTK-aware**、**YaRN**。它们的共同思路是：**调整 RoPE 的频率或位置**，让外推后的 attention pattern 落在模型见过的分布内。"

### 📐 RoPE 外推失败的本质

**关键观察**：RoPE 的频率公式 $\theta_i = 10000^{-2i/d}$ 意味着：

- 低维（i 小）→ 高频 → 周期短（最低维周期 = 2π ≈ 6.28）
- 高维（i 大）→ 低频 → 周期长（最高维周期 ≈ 6.28 × 10000）

**训练时 max_len = L_train，模型只见过的相位组合是**：
- 低频维度：$m\theta_i$ 还在第 1 个周期内（因为周期很长）
- 高频维度：$m\theta_i$ 走完了很多周期，但只在 $[0, L_{train}]$ 范围内

**推理时 m > L_train**：
- 低频维度：进入了第 2、3 个周期，模型**没见过这种相位**
- 高频维度：还是周期内运动，但相位组合"频率梳"是新的

**直接外推的效果就崩了**。

### 🔥 高频追问 Top 3

**Q：为什么 RoPE 比 Sinusoidal 外推稍好一点？**

A：因为 Sinusoidal 是**加在 embedding 上**的——pos 偏移导致 embedding 的数值范围跳变，影响所有后续计算。RoPE 是**应用在 attention 内部**的旋转——只影响 QK 点积的角度，不影响整体数值范围。后者更"温和"，崩得没那么剧烈。但都不能算"真外推"，只能说 RoPE 退化得更慢。

**Q：外推差能不能通过加训长上下文解决？**

A：**可以但贵**。直接用长上下文做继续训练（Continual Pretraining）能让模型适应长位置，但需要长序列数据，且训练成本随序列长度二次增长。所以更经济的方案是**用 Position Interpolation / YaRN 这样的 trick + 少量长上下文微调**——只需要原始训练成本的 1% 就能扩展到 4×~16× 长度。

**Q：如果输入比训练时短很多，会不会有问题？**

A：**几乎不会**。短输入只是用了 RoPE 频率表中的一个前缀，分布完全在训练见过的范围内。所以 RoPE 的外推问题是"短训长推"，反过来没事。

### ⚠️ 常见陷阱

- 不要简单说"RoPE 完美外推"——这是错误的，工业界已经形成共识
- 要把 PI、NTK、YaRN 当成"位置编码的不同改造方案"，而不是孤立的 trick

### 🏢 大厂偏好

- **DeepSeek / 字节**：必问长上下文相关
- **OpenAI / Anthropic**：会从理论分析角度问"频谱视角下的外推问题"

### 📚 延伸阅读
- [YaRN 论文](https://arxiv.org/abs/2309.00071) §2-§3 对 RoPE 外推问题的分析

---

## Q6：长上下文扩展：PI / NTK-aware / YaRN 详解 ⭐⭐⭐⭐⭐

### 🎯 一句话标答

> 三种方案都是改造 RoPE 频率以适应长上下文——**PI** 把位置压缩到训练范围，**NTK-aware** 调整 base 而非位置，**YaRN** 按频率分段精细化，**YaRN 是当前最强**。

### 🗣️ 30 秒口语版

"这三种方案是**逐步演化**的，理解了演化逻辑就好记。

**第一代：Position Interpolation（PI）**——Meta 提出。思路最简单——既然模型没见过 m=8192 的位置，那就把它**等比例压缩**到训练范围。比如训练长度 2048，推理 8192，那就把所有位置除以 4——8192 变 2048。问题是：高频维度的'分辨率'被压缩了，相邻 token 区分度下降，对短距离 attention 有损失。

**第二代：NTK-aware**——苏剑林博客提出。改进思路是**不动位置，动 base**。把 RoPE 的 10000 base 调大，比如调到 10000×16，这样所有维度的频率都会变低——但**高频维度变化最敏感，低频维度几乎不动**。等于'高频被拉宽、低频保留'，对短距离损失更小。

**第三代：YaRN**——结合 PI 和 NTK 的思路，**按维度分段**：低频维度用 NTK（外推友好），高频维度直接 PI（保留分辨率），中间维度做平滑插值。再加上一个温度系数补偿 attention 分布。这是目前最强的免训练扩展方案。

实际工业上还会配 **'YaRN + 少量长上下文微调'**——能稳定扩展到 16×、32×。"

### 📐 三种方案的数学对比

设原始 RoPE 频率为 $\theta_i = b^{-2i/d}$，扩展系数 $s = L_{target} / L_{train}$。

**Position Interpolation (PI)**：
$$\text{new position} = \frac{m}{s}, \quad \theta_i' = \theta_i$$

效果：**所有维度等比例压缩位置**，对所有频率影响一致。

**NTK-aware**：
$$\text{position} = m, \quad b' = b \cdot s^{d/(d-2)}, \quad \theta_i' = (b')^{-2i/d}$$

效果：**高频维度（小 i）几乎不变，低频维度（大 i）被显著拉低**。

更直观的等价形式：
$$\theta_i' = \frac{\theta_i}{s^{2i/(d-2)}}$$

- i = 0（最高频）：分母 = 1，几乎不变
- i = d/2 - 1（最低频，最大维度索引）：分母 = s^(2i/(d-2)) = s^((d-2)/(d-2)) = s，被降低约 s 倍（恰好等价于对最低频维度做 PI）

**YaRN**：分段策略。定义第 i 维在训练长度内转过的**周期数** $r_i = L_{train} / \lambda_i$（其中 $\lambda_i = 2\pi/\theta_i$ 是波长，$r_i$ 越大表示频率越高），配两个阈值 $\alpha < \beta$（论文中 α=1, β=32），按 $r_i$ 决定策略：

$$\theta_i' = \begin{cases} \theta_i / s & \text{if } r_i > \beta \text{（高频，已转过很多周期，PI 处理）} \\ \theta_i & \text{if } r_i < \alpha \text{（低频，不足一个周期，保留）} \\ \text{插值} & \text{otherwise} \end{cases}$$

即高频维度按 PI 压缩、低频维度保留原频率、中间维度平滑过渡。

外加一个**温度调节（mscale）**：给注意力 logits 乘上一个略大于 1 的系数（YaRN 取 $0.1\ln s + 1$），把长序列下变平（熵增）的 attention 分布重新拉尖。

### 🔍 效果对比（社区实测，LLaMA-2 7B 扩展到 8K）

| 方法 | 训练 (2K) PPL | 扩展 (8K) PPL | 短序列损失 | 是否需要微调 |
|---|---|---|---|---|
| 原生外推 | 5.6 | 200+（崩） | 0 | 否 |
| PI | 5.6 | 6.2 | 中 | 推荐 |
| NTK-aware | 5.6 | 6.0 | 小 | 否 |
| YaRN | 5.6 | 5.8 | 极小 | 推荐 |

实际 LLaMA-2 → CodeLLaMA 的 16K 上下文用的就是 PI；LLaMA-3 的 128K 用的是 YaRN 类方案。

### 🔥 高频追问 Top 3

**Q：为什么 NTK 比 PI 短序列损失小？**

A：因为 PI 是"无差别压缩"——所有维度的位置都被除以 s，包括高频维度。高频维度本来负责区分近邻 token（"我"和"爱"差 1 步），压缩后相邻 token 的相位差减小，区分度下降。NTK 反过来——它"针对性地动低频维度"（通过调高 base 来降低低频），因为低频维度的周期本来就很长，适合编码远距离信息；通过拉长周期，让外推后的相位变化仍在模型见过的分布内。高频维度基本保留，保护了短距离的区分能力。

**Q：YaRN 的"按波长分段"原理是什么？**

A：核心 insight 是——用"该维度在训练长度内转过的周期数" $r = L_{train}/\lambda$ 来分段：**$r > \beta$ 的维度（波长短、高频），训练时已经"绕过很多周期"，外推时相位会进入新周期（分布外），需要做位置压缩（PI）来约束位置**；**$r < \alpha$ 的维度（波长长、低频），训练时还没绕完一个周期，模型的相位理解是线性的，外推时相位仍在线性范围内，可以保留原频率（不需动）**；**$\alpha < r < \beta$ 的维度，做平滑插值过渡，避免突变**。

**Q：YaRN 之后还有没有更新方案？**

A：有几个方向——
- **LongRoPE**（微软 2024）：进化版搜索式调整，外推到 2M+
- **NTK-by-parts**：YaRN 的简化版
- **Context Parallel / Ring Attention**：从硬件层面解决长上下文，与位置编码正交

但 YaRN 仍是 2024-2026 的工业首选，因为简单稳定。

### ⚠️ 常见陷阱

1. **混淆 PI 和 NTK 的核心区别**：PI 动位置，NTK 动 base
2. **不知道 YaRN 还有"温度调节"**：这是个容易被遗漏的细节
3. **以为 YaRN 不需要训练**：YaRN 比 NTK 强，但**配合少量长文本微调效果才最好**

### 🏢 大厂偏好

- **DeepSeek / 字节**：会让你画图比较三种方案
- **AI Infra 岗**：会追问长上下文的工程实现（Ring Attention 等）

### 📚 延伸阅读
- [Extending Context Window of Large Language Models via Positional Interpolation](https://arxiv.org/abs/2306.15595) - PI 原论文
- [YaRN: Efficient Context Window Extension](https://arxiv.org/abs/2309.00071) - YaRN 原论文
- [苏剑林：博采众长的 RoPE 改良——NTK-aware Scaled RoPE](https://kexue.fm/archives/9675)

---

## Q7：ALiBi 与 RoPE 的对比 ⭐⭐⭐

### 🎯 一句话标答

> ALiBi 不修改 Q/K，而是**直接在 attention 分数上加一个与距离成正比的线性偏置**——更简单、外推性好，但效果略弱于 RoPE。

### 🗣️ 30 秒口语版

"ALiBi 是另一种思路——Attention with Linear Biases。

它的做法简单粗暴：不动 Q、K、V，而是在 **attention 分数矩阵上加一个偏置**——位置 i 看位置 j 时，分数减去 m·|i-j|，其中 m 是个跟头数有关的固定斜率。距离越远，惩罚越大。

这等于说'你越远，我越不想看你'，把'近距离优先'硬编码进了 attention。

优点是**外推性极好**——因为是线性的，任意距离都有定义，没有 RoPE 那种'相位跳变'的问题。BLOOM 用了 ALiBi 就能直接外推到训练长度的几倍而不崩。

缺点是表达能力略弱——RoPE 让模型自己学'看谁'，ALiBi 强行规定了一个先验。在大规模训练上，RoPE 实测效果略胜，所以最终主流选了 RoPE + 长度扩展方案，而不是 ALiBi。"

### 📐 ALiBi 公式

$$\text{Attn}(i, j) = \frac{q_i k_j^T}{\sqrt{d}} - m_h \cdot |i - j|$$

其中 $m_h$ 是第 h 个头的固定斜率，论文中：
$$m_h = 2^{-8h/H}, \quad h = 1, ..., H$$

不同头有不同斜率——有的头"看近"（大斜率），有的头"看远"（小斜率）。

### 🔍 RoPE vs ALiBi 对比

| 维度 | RoPE | ALiBi |
|---|---|---|
| 修改位置 | Q、K | Attention 分数 |
| 是否引入先验 | 否（数据驱动） | 是（远距离惩罚） |
| 外推性 | 中（需 YaRN） | 强（直接外推） |
| 短序列效果 | 强 | 略弱 |
| 实现复杂度 | 中 | 极简 |
| 工业采用 | LLaMA/Qwen/DeepSeek | BLOOM/MPT |

### 🔥 高频追问 Top 3

**Q：为什么 ALiBi 外推这么好？**

A：因为它是**线性**的——`-m·|i-j|` 对任意距离都是连续平滑的，没有周期性、没有相位、没有跳变。模型见过 m=1000 的惩罚，自然知道 m=2000 该是多少（线性插值）。RoPE 是周期函数，外推时进入未见周期就崩，本质区别。

**Q：那为什么主流不用 ALiBi？**

A：因为**短序列任务上 RoPE 效果略胜**。ALiBi 的"远距离硬惩罚"是个强先验——对于"你说的'狗咬人'让我想到 50 步前提到的同一只狗"这种长距离依赖，ALiBi 会强行降低权重，不利于学习。RoPE 让模型自己学远距离权重，更灵活。综合下来，**RoPE + YaRN 的组合在短长任务上都不输 ALiBi，而 ALiBi 在短任务上稍弱，工业界选了 RoPE。**

**Q：能不能把 RoPE 和 ALiBi 结合起来？**

A：理论上可以，叫"混合位置编码"。社区有一些尝试，比如 ChatGLM 用过类似方案。但实际上**两者的设计哲学冲突**——RoPE 信任数据，ALiBi 信任先验——硬凑反而互相干扰。工业界的选择是要么纯 RoPE+YaRN，要么纯 ALiBi。

### 🏢 大厂偏好

- 这题更多是"了解即可"，不会作为主问

### 📚 延伸阅读
- [Train Short, Test Long: Attention with Linear Biases](https://arxiv.org/abs/2108.12409) - ALiBi 原论文

---

## Q8：MLA 中的 RoPE 解耦问题 ⭐⭐⭐⭐⭐（DeepSeek 专题）

### 🎯 一句话标答

> MLA 的 KV Cache 是**压缩在低维潜在空间**的，但 RoPE 必须作用在原始 K 上——这构成矛盾。DeepSeek 的解法是**把 K 拆成两部分：压缩部分 + 独立的 RoPE 部分**，分开存储和应用。

### 🗣️ 30 秒口语版

"这题考察的是 DeepSeek-V2/V3 的工程细节，能答好是巨大加分项。

先回顾 MLA 的核心思想——把 K、V 压缩到低维潜在向量 c_t，KV Cache 只存 c_t，需要时再投影回 K、V。

问题来了——RoPE 是怎么工作的？它要对 K 做**位置相关的旋转**，每个位置的 K 都被乘上一个不同的旋转矩阵。如果 K 是从 c_t 投影出来的，那旋转之后的 K 就不能再'压回'c_t 了，因为旋转矩阵不能从 c_t 阶段提前应用。

DeepSeek 的解法非常聪明——**把 K 拆成两半**：
- 一半叫 'NoPE 部分'，从 c_t 投影出来，**不加 RoPE**，可以正常压缩
- 另一半叫 'RoPE 部分'，是一个**独立的小维度向量**，单独存储并应用 RoPE

最后 attention 计算时把两部分拼起来用。这样既享受了 MLA 的压缩好处，又保留了 RoPE 的位置感知能力。代价是 KV Cache 多存一小段（RoPE 部分），但总体仍远小于 MHA。

这个设计被称为 'Decoupled RoPE'，是 MLA 能落地的关键工程突破。"

### 📐 详细推导

#### MLA 的核心矛盾

普通 MLA（理想化）：
$$c_t^{KV} = X_t W^{DKV}, \quad K_t = c_t^{KV} W^{UK}$$

KV Cache 只需要存 $c_t^{KV}$，需要时投影。

加上 RoPE：
$$K_t^{rope} = R_t \cdot K_t = R_t \cdot c_t^{KV} W^{UK}$$

这里 $R_t$ 依赖位置 t，**没法和 $W^{UK}$ 合并**——因为不同位置的 $R_t$ 不同，无法在 c_t 阶段提前算好。

**后果**：要么放弃 RoPE，要么放弃压缩——两难。

#### DeepSeek 的解法：Decoupled RoPE

**把 K 拆成两个组件**：
$$K_t = [K_t^{C}; K_t^{R}]$$

- $K_t^C$：内容部分（NoPE），从 $c_t^{KV}$ 投影
  $$K_t^C = c_t^{KV} W^{UK}$$
- $K_t^R$：位置部分（RoPE），**独立计算**
  $$K_t^R = R_t (X_t W^{KR})$$

**KV Cache 存两个东西**：
1. 压缩向量 $c_t^{KV}$（共享给所有头）
2. 独立的 RoPE 向量 $K_t^R$（共享给所有头）

**Query 端做对称处理**：Q 也分成 NoPE 和 RoPE 两部分。

**Attention 计算**：
$$\text{Attn} \propto Q_t \cdot K_s = Q_t^C \cdot K_s^C + Q_t^R \cdot K_s^R$$

NoPE 部分 × NoPE 部分 + RoPE 部分 × RoPE 部分，两部分分别贡献。

#### KV Cache 大小（DeepSeek-V2 实际数字）

| 组件 | 维度 | 备注 |
|---|---|---|
| $c_t^{KV}$ | 512 | 多头共享 |
| $K_t^R$ | 64 | 多头共享 |
| **合计** | **576 / token** | 相比 MHA 的 8192 减少 93% |

### 💡 直觉理解

可以这样类比——

**MHA**：每个 token 给每个头独立的 "身份证"（K, V），每张身份证上面写满了"内容+位置"。

**MLA（Decoupled RoPE）**：每个 token 只有一张"低维身份证"（c_t）——所有头共享，不带位置；外加一个"位置贴纸"（K^R）——独立处理。算 attention 时，"看内容"和"看位置"两路分别计算然后相加。

### 🔥 高频追问 Top 3

**Q：为什么 K 要解耦但 V 不需要？**

A：因为**RoPE 只作用在 Q 和 K 上，不作用在 V 上**（见 Q4 第三个追问）。V 只是被加权传递的内容，不参与位置点积，自然不存在 RoPE 兼容问题。所以 V 可以完整地从 c_t 投影出来，**只有 K 需要解耦**。

**Q：Decoupled RoPE 会损失什么？**

A：理论上有一点表达能力损失——因为 NoPE 部分和 RoPE 部分是**分别**计算的，而原始 RoPE 是对整个 K 做统一旋转。但实测 DeepSeek-V2 的效果几乎无损，证明这种解耦在工程上是个非常聪明的妥协。

**Q：还有别的 MLA + RoPE 兼容方案吗？**

A：有几种思路——
- **完全放弃 RoPE，改用 ALiBi**：避开兼容问题，但放弃 RoPE 的优势
- **完全放弃压缩**：用 GQA 这种"减头数"方案，绕开问题
- **延迟 RoPE 应用**：在 attention 计算时实时算 K 然后旋转——但这破坏了 KV Cache 的意义

DeepSeek 的解耦方案是目前**质量、效率、KV Cache 大小**三者平衡得最好的一种。

### ⚠️ 常见陷阱

1. **不能只说"MLA 把 KV 压缩了"** —— 必须说明 RoPE 解耦这个工程细节
2. **K^R 和 NoPE K^C 的维度比例容易混**：在 DeepSeek-V2 里是 64 + 512
3. **要说清"独立的 RoPE 部分是从哪算出来的"** —— 它是从原始 X_t 经一个小投影 W^KR 算的，不是从 c_t

### 🏢 大厂偏好

- **DeepSeek**：必问，**几乎是该公司的"暗号题"**
- **字节 / 阿里通用大模型团队**：高频，体现对前沿架构的跟进
- **AI Infra 岗**：会进一步追问 KV Cache 的存储格式和 GPU 实现

### 📚 延伸阅读
- [DeepSeek-V2 Paper](https://arxiv.org/abs/2405.04434) §2.1.2 - MLA 详细推导
- [DeepSeek-V3 Paper](https://arxiv.org/abs/2412.19437) - MLA 的后续应用

---

## 📝 本章小结

| 关键点 | 你必须能脱口而出 |
|---|---|
| 为什么需要位置编码 | Self-Attention 置换等变，必须显式注入位置 |
| Sinusoidal 三个设计精髓 | 不同频率波 / 相对位置可线性表达 / 函数形式 |
| 绝对 vs 相对 | 相对位置符合语言本质，是当前主流 |
| RoPE 核心思想 | 对 Q、K 做旋转，绝对实施、相对生效 |
| RoPE 公式 | $\theta_i = 10000^{-2i/d}$，块对角旋转矩阵 |
| RoPE 外推不行 | 高频维度在长位置上 OOD |
| PI / NTK / YaRN 区别 | 动位置 / 动 base / 按频率分段 |
| ALiBi vs RoPE | 简单粗暴的线性偏置 vs 数据驱动的旋转 |
| MLA + RoPE 解耦 | 把 K 拆成 NoPE 压缩部分 + 独立 RoPE 部分 |

## ✅ 自测题

1. 严格证明 Self-Attention 的置换等变性（30 秒内说清）
2. 推导 RoPE 在 2D 情形下的内积只依赖 $n-m$
3. 写出 NTK-aware 调整 base 的公式，并解释为什么高频不动低频动
4. 画图说清 YaRN 的"按波长分段"策略
5. 详述 MLA 中 K 的解耦设计，包括维度和 attention 计算流程
6. 如果让你设计支持百万长度上下文的 LLM，位置编码上你会怎么选？给出 3 套方案并比较

---

> **下一章预告**：第 3 章｜归一化与激活——LayerNorm、RMSNorm、Pre-Norm vs Post-Norm 的训练稳定性之争，以及 GELU → SwiGLU 的演化。这部分容易被忽略，但是字节、Meta 的高频追问区，**很多候选人在这里掉链子**。
