# 第 6 章｜预训练

> **本章定位**：预训练是 LLM 工程的"高地"——能答清楚 Scaling Law、并行策略、显存估算，证明你**真的训过或者懂大模型的训练成本结构**。本章重点：Chinchilla Law、3D 并行、ZeRO、bf16。这些是字节、Meta、DeepSeek 等大模型团队的必考题。

> **配套章节**：损失与优化器 → 第 5 章 / 推理优化 → 第 3 部分

---

## 📌 本章导航

| 序号 | 题目 | 难度 | 频率（字节/阿里/Meta/DeepSeek） | 类型 |
|---|---|---|---|---|
| Q1 | 预训练目标对比：CLM/MLM/PrefixLM | ⭐⭐⭐ | 4 / 3 / 2 / 2 | 概念 |
| Q2 | 预训练数据：组成、清洗、配比 | ⭐⭐⭐⭐ | 6 / 4 / 3 / 5 | 工程 |
| Q3 | **Scaling Law: Kaplan vs Chinchilla** | ⭐⭐⭐⭐⭐ | **8 / 5 / 4 / 5** | 必问 |
| Q4 | **训练并行：DP/TP/PP/SP/EP** | ⭐⭐⭐⭐⭐ | **9 / 5 / 5 / 7** | 必问 |
| Q5 | **显存估算与 ZeRO** | ⭐⭐⭐⭐⭐ | **8 / 4 / 4 / 5** | 必问 |
| Q6 | 混合精度训练：bf16 vs fp16 | ⭐⭐⭐⭐ | 5 / 3 / 4 / 4 | 工程 |
| Q7 | Loss Spike 与训练监控 | ⭐⭐⭐ | 4 / 2 / 2 / 3 | 实战 |
| Q8 | 持续预训练 Continual Pretraining | ⭐⭐⭐ | 3 / 2 / 1 / 2 | 应用 |

---

## Q1：预训练目标对比：CLM / MLM / PrefixLM ⭐⭐⭐

### 🎯 一句话标答

> **CLM**（Causal LM）从左到右预测下一个 token，**MLM** 随机遮盖 15% 后双向预测被遮的 token，**PrefixLM** 前缀双向看、后缀单向预测——三者在**任务覆盖、数据利用、scaling 友好度**上各有取舍。

### 🗣️ 30 秒口语版

"预训练目标有三种主流方案——

**CLM**（GPT 系列）：从左到右逐 token 预测。Causal mask 保证只看历史。优势是**任务最通用、数据利用率高（每个 token 都贡献 loss）、scaling 友好**——所以是当前 LLM 主流。

**MLM**（BERT 系列）：随机遮盖 15% 的 token，让模型双向看其他位置来预测被遮的。优势是**双向理解强**，适合句子表示、检索。劣势：**只有 15% 的 token 产生梯度**——数据利用率低；**不能生成**——推理时不会逐 token 生成。

**PrefixLM**（GLM、T5 部分）：前缀部分可以双向看，要预测的后缀只能单向看。综合 CLM 和 MLM 的优势——前缀理解强、后缀能生成。GLM-130B 用过这种目标。

**当前格局**：CLM 一统天下，MLM 在 embedding 模型（BGE 等）里活着，PrefixLM 是研究性的尝试，工业上不主流。"

### 🔍 三种目标对比

| 维度 | CLM | MLM | PrefixLM |
|---|---|---|---|
| 注意力 | Causal mask | 全双向 | 前缀双向 + 后缀因果 |
| 训练信号密度 | 100% token | 15% token | ~85% token |
| 双向理解 | 弱（靠规模补） | 强 | 中（前缀部分） |
| 生成能力 | 强 | 无 | 中 |
| Scaling 表现 | 极佳 | 中等 | 一般 |
| 代表模型 | GPT, LLaMA, DeepSeek | BERT, RoBERTa, BGE | GLM, UL2 |
| 当前地位 | **绝对主流** | Embedding 残留 | 学术尝试 |

### 🔥 高频追问 Top 3

**Q：MLM 的 15% mask 比例是怎么定的？**

A：BERT 论文里**经验调出来的**——做过 10%、15%、20% 的对比，15% 综合最好。直觉上：mask 太少 → 训练信号弱、任务太容易；mask 太多 → 上下文信息不足、任务太难。15% 是个平衡点。**注意 BERT 还有 80/10/10 规则**：被选中的 15% 中，80% 替换为 [MASK]、10% 替换为随机词、10% 保持原词——目的是减少训练-推理的分布差异（推理时没有 [MASK] token）。

**Q：为什么 CLM 的数据利用率比 MLM 高？**

A：直接算梯度数量——
- CLM：每个 token 都参与 loss，长度 1024 的序列贡献 1024 个梯度信号
- MLM：只有 15% 的 token 贡献梯度，长度 1024 的序列只有约 154 个梯度信号

**同样训练 1T token，CLM 拿到的有效信号是 MLM 的 ~7 倍**。这就是为什么 CLM 能 scale 到几 T token 还能涨，MLM 收益递减更快。

**Q：能不能 CLM + MLM 混合训练？**

A：可以——这是 UL2（Google）和 GLM 探索的方向。UL2 同时用 R-Denoising（小片段 mask）、S-Denoising（前缀 + 后缀生成）、X-Denoising（极大跨度 mask）三种目标混合训练。**理论上更通用**，但工程复杂、训练慢、增益边际。**Decoder-Only CLM 的生态优势太大，混合目标没赢**。

### ⚠️ 常见陷阱

- 不要把 CLM 翻译成"自回归"——更准确的是"因果语言建模"
- BERT 的 80/10/10 规则是个常考的细节

### 🏢 大厂偏好

- **字节 / DeepSeek**：基础题，会从这里追问到 Decoder-Only 的优势
- **阿里 / 百度**：偏 PrefixLM 的研究背景，可能问 GLM 系列

---

## Q2：预训练数据：组成、清洗、配比 ⭐⭐⭐⭐

### 🎯 一句话标答

> 现代 LLM 数据通常是 **CommonCrawl + Code + 书籍 + 学术 + 多语言** 的混合，经过**质量过滤 + 去重 + 安全过滤**，**数据质量和配比比数据量更关键**。

### 🗣️ 30 秒口语版

"预训练数据是 LLM 的'血液'，质量决定上限。我从三个维度讲——

**第一，数据来源**。典型组成：
- **CommonCrawl**（占大头，60-80%）：互联网爬取，需要重度清洗
- **代码**（5-15%）：GitHub、StackOverflow，强化逻辑推理
- **书籍**（5-10%）：长文本、规范语言
- **学术论文**（2-5%）：arXiv、PubMed
- **百科**（1-3%）：Wikipedia
- **多语言数据**：根据目标语言比例

**第二，数据清洗**。流程通常是——
- **质量过滤**：基于 perplexity（小模型打分）、规则（重复行、标点比例）、分类器（区分高质量和低质量）
- **去重**：基于 hash 的精确去重 + 基于 MinHash 的模糊去重
- **安全过滤**：去掉 NSFW、有害、隐私信息

**第三，配比策略**。这是 2024 年才被广泛研究的——
- **DoReMi / DataComp** 等方法：动态调整数据领域配比
- **DeepSeek-V3 经验**：代码 + 数学比例显著影响推理能力
- **退火（annealing）**：训练后期切换到高质量数据 finetune-like

**业界共识**：**数据质量 > 数据量**。Chinchilla 之后大家发现，clean 的 1T token 胜过 noisy 的 5T。"

### 📊 典型 LLM 数据组成（公开信息）

| 模型 | 总量 | CommonCrawl | 代码 | 书籍/学术 | 多语言 |
|---|---|---|---|---|---|
| LLaMA-1 | 1.4T | 67% | 4.5% | 22% | - |
| LLaMA-2 | 2T | 67% | 8% | 19% | 中等 |
| LLaMA-3 | 15T | 60-70% | 17% | 10% | 5% |
| DeepSeek-V3 | 14.8T | 大部分 | 12-15% | - | 中英为主 |
| Qwen-2 | 7T+ | 大部分 | ~10% | - | 中英为主 |

### 🔍 数据清洗的核心步骤

```
原始 CommonCrawl (PB 级)
    ↓ 语言识别 (fastText)
按语言分桶
    ↓ 质量过滤
    · 规则：删除重复行/低字数/高标点比例的文档
    · ML 分类器：训练一个小分类器区分"维基质量"vs"网页质量"
    · Perplexity：用小 LM 给文本打分，过滤极低 PPL 文档
    ↓ 文档级去重
    · MinHash + LSH 找近似重复
    · URL 去重
    ↓ 句子级去重
    · 高频 n-gram 删除
    ↓ 安全过滤
    · NSFW 分类器
    · PII 检测（手机、邮箱、身份证）
最终高质量数据 (TB 级)
```

### 🔥 高频追问 Top 3

**Q：去重为什么这么重要？**

A：**去重不充分会让模型过拟合并损失泛化**。如果一段文本在训练数据里出现 100 遍，模型会**死记硬背**这段——
- 增加 memorization（隐私泄露风险）
- 浪费 token 配额（同样训练成本，多样性差）
- **可能 hurt 性能**：去重之后，同等 token 数训练的模型反而效果更好

**Anthropic 的研究**显示，文档级去重 + 句子级去重能让 PPL 改善 5-10%。这是几乎"白送"的提升。

**Q：数据配比怎么定？**

A：早期靠经验（Common Crawl 60-70%、代码 5-10% 等）。2023 之后**有了更科学的方法**——
- **DoReMi**（Google）：用小模型在不同配比下训练，找到最优 mixing weight
- **DataComp / RegMix**：类似思路，自动化数据配方搜索
- **DeepSeek 经验**：增加代码和数学数据比例显著提升推理能力（V3 中代码 + 数学合计 25-30%）

**业界趋势**：从手工配比转向**数据配方搜索**。

**Q：训练后期需要做什么特殊处理？**

A：**Annealing（数据退火）**——训练后期切换到**最高质量的数据子集**，类似"finetune-like"的过程。LLaMA-3 用了这个方法：前期用 15T 通用数据，最后阶段切换到精选高质量数据 + 部分指令数据。**效果接近无成本的预训练加 SFT**。

DeepSeek-V3 也用了类似策略——训练最后 1T 数据中调高了高质量内容比例。

### ⚠️ 常见陷阱

1. **不要只说"数据量大"** —— 现代共识是质量 > 数量
2. **要懂去重的重要性** —— 这是工业必做但学术较少提的
3. **数据配方搜索是 2024+ 的新趋势** —— 跟上这个能加分

### 🏢 大厂偏好

- **DeepSeek / 字节大模型团队**：数据是核心壁垒，必问
- **数据工程岗 / AI Infra**：会问 pipeline 设计

### 📚 延伸阅读
- [DoReMi: Optimizing Data Mixtures Speeds Up Language Model Pretraining](https://arxiv.org/abs/2305.10429)
- [LLaMA-3 技术报告](https://ai.meta.com/research/publications/the-llama-3-herd-of-models/)
- [DeepSeek-V3 技术报告](https://arxiv.org/abs/2412.19437)

---

## Q3：Scaling Law: Kaplan vs Chinchilla ⭐⭐⭐⭐⭐（必问）

### 🎯 一句话标答

> Kaplan（OpenAI, 2020）说"参数 > 数据"——同 FLOPs 下放大模型；Chinchilla（DeepMind, 2022）纠正为 "**参数和数据应该等比放大**，最优比例约 **D = 20 × N**"——大幅改变了行业训练策略。

### 🗣️ 30 秒口语版

"这是 LLM scaling 历史上的两个关键论文，理解差异能体现你对成本结构的理解。

**Kaplan Scaling Law（2020）**：固定 FLOPs 预算时，**优先放大模型参数 N，数据 D 相对少一点也没事**。这导致了 GPT-3（175B 参数，仅 300B token）——**严重欠训练**。

**Chinchilla Scaling Law（2022）**：DeepMind 用更精细的实验（数据、参数都变化）发现——**参数和数据应该等比放大**。最优配方是 **D ≈ 20 × N**：
- 70B 模型 → 需要 1.4T token
- 405B 模型 → 需要 8T token

按这个发现训了 Chinchilla 70B（比 GPT-3 175B 小，但训了 1.4T token），**性能反而超过 GPT-3**。

**实际影响**——
- LLaMA-1 65B 用 1.4T token：Chinchilla 配方
- LLaMA-2 70B 用 2T token：略超 Chinchilla
- LLaMA-3 70B 用 15T token：**远超 Chinchilla**——主要为了推理效率（模型小一点更便宜部署）

**当前共识**：
- 训练效率最优 = Chinchilla 比例（D = 20N）
- 推理成本最优 = **过训练**（D >> 20N），让模型更小、更便宜
- 工业取舍取决于训练 vs 推理成本比"

### 📐 数学形式

**Chinchilla 论文的拟合公式**：

$$L(N, D) = E + \frac{A}{N^{0.34}} + \frac{B}{D^{0.28}}$$

- E：理论最低 loss（无法消除的熵）
- N：模型参数量
- D：训练 token 数
- A, B：拟合常数

**最优分配**：在 FLOPs 预算 C ≈ 6ND 约束下最小化 loss，解出：

$$N_{opt} \propto C^{0.5}, \quad D_{opt} \propto C^{0.5}$$

即**参数和数据等比放大**——和 Kaplan 的"参数主导"截然不同。

### 🔍 不同 Scaling 选择的实际后果

| 模型 | 参数 | 训练 Token | D/N 比 | 评价 |
|---|---|---|---|---|
| GPT-3 | 175B | 300B | 1.7 | **严重欠训练** |
| Chinchilla | 70B | 1.4T | 20 | **最优训练效率** |
| LLaMA-1 65B | 65B | 1.4T | 22 | 接近最优 |
| LLaMA-2 70B | 70B | 2T | 29 | 略过训练 |
| LLaMA-3 70B | 70B | 15T | **214** | **大幅过训练**（为推理） |
| DeepSeek-V3 | 671B/37B 激活 | 14.8T | 22 | MoE，按激活算接近最优 |

### 💡 为什么 LLaMA-3 "过训练"反而是工业最优？

Chinchilla 是**训练效率最优**——同 FLOPs 下 loss 最低。但工业上要考虑——

**推理成本主导**：模型部署后会被调用几十亿次，每次推理成本 ∝ 参数量。**模型小一点，推理省一辈子**。

**取舍**：
- 训练 70B 模型 1.4T tokens（Chinchilla）：训练便宜，但推理贵
- 训练 70B 模型 15T tokens（LLaMA-3）：训练贵 10×，但模型质量更高
- **推理 100B+ 次后，方案 2 总成本更低**

**LLaMA-3 的策略**：**用过量数据训小模型**，因为推理成本主导。DeepSeek-V3 用 MoE，是另一种思路——大总参数 + 小激活 = 训练 + 推理双优。

### 🔥 高频追问 Top 3

**Q：Kaplan 为什么错了？**

A：实验设计问题——Kaplan 在拟合时**没有充分变化 batch size 和学习率等超参**。Chinchilla 论文修正了这点，**让小模型也用最优超参**，重新拟合发现数据应该等比放大。这是个**工程经验的重要教训**——拟合 scaling law 必须保证所有超参都对每个 (N, D) 调好。

**Q：Chinchilla 公式的指数 0.34 和 0.28 怎么解读？**

A：这俩指数描述 loss 对参数和数据的**敏感度**：
- $A/N^{0.34}$：参数翻倍，这一项减少为 $1/2^{0.34} ≈ 79%$
- $B/D^{0.28}$：数据翻倍，这一项减少为 $1/2^{0.28} ≈ 82%$

参数的指数稍大（0.34 > 0.28），意味着**参数的边际收益略高于数据**——所以最优比例稍偏向"参数多一点"，但两者**指数同阶**，所以"等比放大"是好近似。

**Q：MoE 模型怎么算 scaling？**

A：MoE 的"等效参数"是个争论点——
- 按**总参数 N_total** 算：高估，因为推理只激活一部分
- 按**激活参数 N_active** 算：低估，因为总参数提供存储容量

DeepSeek 的实证经验：**激活参数主导推理质量**，总参数提供"容量"——所以 DeepSeek-V3 报 671B/37B（总/激活），按激活 37B 算 Chinchilla 比例。

### ⚠️ 常见陷阱

1. **不要说 "Kaplan 已经过时"** —— Kaplan 论文里的很多 finding（如 loss 是 power law）仍正确
2. **要懂 LLaMA-3 "过训练"的工程动机** —— 区分训练最优 vs 工业最优
3. **D/N 比要能脱口而出**：20 是 Chinchilla 的关键数

### 🏢 大厂偏好

- **字节 / DeepSeek**：必问，会问"如果让你设计一个 100B 模型的训练计划"
- **AI 研究岗**：会追问公式细节和 MoE 的 scaling 问题

### 📚 延伸阅读
- [Scaling Laws for Neural Language Models (Kaplan 2020)](https://arxiv.org/abs/2001.08361)
- [Training Compute-Optimal Large Language Models (Chinchilla 2022)](https://arxiv.org/abs/2203.15556)

---

## Q4：训练并行：DP / TP / PP / SP / EP ⭐⭐⭐⭐⭐（必问）

### 🎯 一句话标答

> **DP**（数据并行）拆 batch，**TP**（张量并行）拆矩阵乘内部，**PP**（流水线并行）拆层，**SP**（序列并行）拆序列长度，**EP**（专家并行）拆 MoE 专家——大模型训练是 **3D / 4D 并行**的组合。

### 🗣️ 30 秒口语版

"现代 LLM 训练需要**多种并行策略组合**，每种解决不同的瓶颈——

**DP（Data Parallel）**：每张卡有完整模型副本，batch 切分到不同卡。**最简单**——但要求单卡能装下整个模型。LLaMA-3 70B 单卡装不下，必须配合其他并行。

**TP（Tensor Parallel）**：在矩阵乘**内部切分**——比如 Attention 的 Q/K/V 矩阵按头切到不同卡。需要**频繁通信**（all-reduce），所以只在**节点内** NVLink 网络用，跨节点带宽不够。

**PP（Pipeline Parallel）**：按**层**切分，前 N 层在节点 A，后 N 层在节点 B。**通信少**（只在层间传 activations），适合跨节点。代价是**流水线气泡**（pipeline bubble）——前段算完等后段，效率受损。

**SP（Sequence Parallel）**：在**序列长度维度**切分，主要为了解决**长上下文激活显存**。配合 TP 用。

**EP（Expert Parallel）**：MoE 模型特有——把不同 expert 放到不同卡。**需要 all-to-all 通信**（token 路由到 expert）。

**实际组合**：
- LLaMA-3 训练：TP=8（NVLink 内）× PP=16 × DP=128 = 16384 张 H100
- DeepSeek-V3：TP + EP + PP + DP，加 FP8 优化

记忆要点——**TP 节点内、PP 节点间、DP 横向 scale、SP/EP 解决特殊维度**。"

### 📐 五种并行的可视化

```
模型架构（简化）：4 层 Transformer，每层有 Attn 和 FFN
                Layer 1   Layer 2   Layer 3   Layer 4

Data Parallel (DP):
  GPU 0: [完整 4 层] + Batch[0:8]
  GPU 1: [完整 4 层] + Batch[8:16]    ← 模型复制，数据切分
  GPU 2: [完整 4 层] + Batch[16:24]
  GPU 3: [完整 4 层] + Batch[24:32]
  通信：all-reduce 梯度

Tensor Parallel (TP):
  GPU 0: [Attn 头 0-3, FFN 列 0-h/2] 所有 4 层
  GPU 1: [Attn 头 4-7, FFN 列 h/2-h] 所有 4 层  ← 切矩阵内部
  通信：all-reduce 每个 attention + FFN 输出

Pipeline Parallel (PP):
  GPU 0: [Layer 1, 2]
  GPU 1: [Layer 3, 4]   ← 切层
  通信：activations 在层间传递

Sequence Parallel (SP):
  GPU 0: [完整 4 层] 处理序列前一半 [0:n/2]
  GPU 1: [完整 4 层] 处理序列后一半 [n/2:n]
  通信：attention 时需要 all-gather

Expert Parallel (EP) - 仅 MoE:
  GPU 0: [Expert 0, 1, 2, 3]
  GPU 1: [Expert 4, 5, 6, 7]
  通信：all-to-all 路由 token 到 expert
```

### 🔍 五种并行对比表

| 并行 | 切分维度 | 通信开销 | 适用网络 | 用途 |
|---|---|---|---|---|
| DP | Batch | 小（梯度 all-reduce） | 任意 | 横向扩展 |
| TP | 矩阵列/行 | 大（每层多次） | 节点内 NVLink | 单层装不下时 |
| PP | 层 | 小（仅 activations） | 跨节点 | 模型层数多 |
| SP | 序列长度 | 中（attention 时） | 节点内 | 长上下文 |
| EP | Expert | 大（all-to-all） | 节点内/RDMA | MoE 模型 |

### 💡 实际组合：3D 并行配置

**LLaMA-3 70B 训练配置**（推测，基于公开信息）：

```
总 GPU 数：16384 张 H100
拓扑：每节点 8 卡（NVLink），节点间 InfiniBand

并行策略组合：
  TP = 8        ← 节点内，跨 NVLink
  PP = 16       ← 跨节点（16 个 stage）
  DP = 16384 / (TP × PP) = 128
  
每张 GPU 上：
  · 1/8 的每层参数（TP 切）
  · 1/16 的层数（PP 切）
  · 整体 batch 的 1/128（DP）
```

**DeepSeek-V3 训练配置**（更复杂）：

```
TP + PP + EP + DP 四维并行 + FP8 + ZeRO

· EP：256 个专家分布到多个 GPU
· TP：节点内
· PP：跨节点
· DP：剩余
```

### 🔥 高频追问 Top 3

**Q：TP 为什么必须在节点内？**

A：因为 TP 在**每个 Transformer 层**都需要 all-reduce 操作——比如 attention 的 Q/K/V 切到 8 卡，算完后要 all-reduce 汇总。一层一次 all-reduce，70B 模型有 80 层 = 80 次 all-reduce / forward + 80 次 / backward。

NVLink 带宽 600 GB/s，能撑住这种密集通信。但**节点间** InfiniBand 只有 100-400 GB/s，跨节点 TP 会让通信成为瓶颈，训练慢 5-10×。

所以经验法则：**TP 限制在节点内**（一般 8 卡 NVLink），TP 度数 = 节点内 GPU 数。

**Q：PP 的"pipeline bubble"是什么？怎么减少？**

A：Pipeline 启动时，**前段 GPU 在算，后段 GPU 在等**——等前段把第一个 mini-batch 的 activations 传过来。这段"等待"叫 bubble。

**减少 bubble 的技巧**：
- **更细的 micro-batch 切分**：把一个 batch 切成更多小块，让流水线"流"得更顺
- **1F1B（One Forward One Backward）调度**：交替前向反向，减少气泡
- **Interleaved Pipeline**：每个 GPU 持有多个不连续的层段，气泡更小

GPipe 是经典实现（带 bubble），后续 Megatron 的 Interleaved 1F1B 进一步降低气泡到 ~5%。

**Q：怎么选并行策略组合？**

A：**经验决策树**：

```
1. 模型能否单卡装下？
   是 → 用 DP 横向扩展，结束
   否 → 继续

2. 模型能否节点内（8 卡）装下？
   是 → TP（节点内）+ DP，搞定
   否 → 继续

3. 增加 PP，按层切分到多节点
   每节点：TP=8（满）
   节点间：PP，分担层数
   剩余：DP 扩 batch

4. 长上下文？加 SP
   MoE？加 EP
```

按此决策选 LLaMA-3 70B → TP=8 (节点内) × PP=16 (跨节点) × DP=128。

### ⚠️ 常见陷阱

1. **TP 度数不要超过节点内 GPU 数** —— 跨节点 TP 是性能灾难
2. **不要把 DP 和 ZeRO 搞混** —— ZeRO 是 DP 的变种（见 Q5）
3. **要懂 PP 的 bubble 问题** —— 是 PP 效率不如 TP 的根本原因

### 🏢 大厂偏好

- **AI Infra 岗 / 训练团队**：核心必问，会让你画拓扑图
- **字节 / Meta / DeepSeek**：可能问"如果让你训 100B 模型，并行策略怎么选"

### 📚 延伸阅读
- [Megatron-LM](https://arxiv.org/abs/1909.08053) - TP 经典实现
- [GPipe](https://arxiv.org/abs/1811.06965) - PP 经典
- [PaLM 论文](https://arxiv.org/abs/2204.02311) - 大规模并行实战

---

## Q5：显存估算与 ZeRO ⭐⭐⭐⭐⭐（必问）

### 🎯 一句话标答

> 单卡显存 = **参数 + 梯度 + 优化器状态 + 激活**，其中**优化器状态最大**（Adam 占 12 bytes/参数）；**ZeRO** 把这些状态在 DP 组内**分片**，按分片粒度分 1/2/3 三个阶段，**ZeRO-3 内存压力最低**。

### 🗣️ 30 秒口语版

"训练显存的四大组成——

**1. 参数（W）**：FP16/BF16 训练，每参数 2 bytes
**2. 梯度（grad）**：和参数同精度，2 bytes
**3. 优化器状态**：Adam 用 FP32 存 m, v + master copy，**每参数 12 bytes**
**4. 激活（activations）**：前向中间结果，依赖 batch × seq_len

对 7B 模型用 Adam 训练，**仅参数 + 梯度 + 优化器** = 7B × (2 + 2 + 12) = **112 GB**——单张 80GB H100 装不下！

**ZeRO（Zero Redundancy Optimizer）** 的核心 insight：DP 模式下每张卡都存了完整的 W、grad、opt——**冗余**。把它们**沿 DP 组分片**就行：

- **ZeRO-1**：分片 optimizer states，每卡只存 1/N（N = DP 度数）
- **ZeRO-2**：再分片 gradients
- **ZeRO-3**：再分片 parameters

ZeRO-3 是最激进的——每张卡只持有 1/N 的参数。需要时通过 all-gather 拼回，用完释放。**显存大幅下降，但通信增加**。

**典型应用**：DeepSpeed 实现了 ZeRO，PyTorch 的 FSDP（Fully Sharded Data Parallel）等价于 ZeRO-3。LLaMA-3 训练用了 FSDP + 3D 并行。"

### 📐 显存计算公式（Adam，BF16 训练）

对 P 参数模型：

| 组件 | 单卡显存（无 ZeRO） | 单参数字节 |
|---|---|---|
| 参数（BF16） | 2P | 2 B |
| 梯度（BF16） | 2P | 2 B |
| Adam m（FP32） | 4P | 4 B |
| Adam v（FP32） | 4P | 4 B |
| Master copy（FP32） | 4P | 4 B |
| **小计（不含激活）** | **16P** | **16 B** |
| 激活（依赖 batch、seq） | ~2P 量级 | - |
| **总计** | **~18P** | - |

**例子**：70B 参数 → 18 × 70B = **1260 GB** = 16 张 80GB H100 才能装下一个完整副本。

### 🔍 ZeRO 三阶段对比

设 N = DP 度数（比如 128 张卡）。

| 阶段 | 参数 | 梯度 | 优化器状态 | 单卡显存（70B 模型） | 通信开销 |
|---|---|---|---|---|---|
| 无 ZeRO | 完整 | 完整 | 完整 | 1260 GB | 标准 all-reduce |
| **ZeRO-1** | 完整 | 完整 | **分片 (1/N)** | 280 GB + 980/N | 多一次 all-gather |
| **ZeRO-2** | 完整 | **分片** | **分片** | 140 GB + 1120/N | + reduce-scatter |
| **ZeRO-3 (FSDP)** | **分片** | **分片** | **分片** | 1260 / N GB | 参数 all-gather (每层) |

**ZeRO-3 在 N=128 时**：1260 / 128 ≈ **10 GB 单卡**！但每次 forward 都要 all-gather 参数，**通信量大**。

### 💡 ZeRO-3 的工作流程

```
Forward:
  Layer 1:
    1. all-gather: 从所有 N 张卡收集 Layer 1 的完整参数
    2. compute: 用完整参数前向
    3. discard: 释放非本卡负责的参数

  Layer 2:
    重复上述步骤

  ...直到最后一层

Backward:
  类似流程，但反向

Optimizer Step:
  每张卡只更新自己负责的 1/N 参数（不需要通信）
```

**关键观察**：参数是**临时**集齐的，用完即弃——所以**单卡常驻显存只持有 1/N 参数**。

### 🔥 高频追问 Top 3

**Q：为什么 Adam 优化器状态占 12 bytes / 参数？**

A：拆开来——
- 一阶矩 m（FP32）：4 bytes
- 二阶矩 v（FP32）：4 bytes
- Master copy of weights（FP32）：4 bytes，**用于精确累加**

为什么需要 master copy？因为 BF16 精度有限，长时间累加会累积误差。Adam 在 FP32 维护一份高精度副本，BF16 副本只用于 forward/backward，update 时在 FP32 上算。**所以混合精度 + Adam 的总开销 = 2(BF16 W) + 2(BF16 grad) + 12(FP32 m+v+master) = 16 bytes/参数**。

**Q：ZeRO 和 TP/PP 能一起用吗？**

A：可以——**ZeRO 是 DP 的变种**，所以 ZeRO + TP + PP 是常见组合。但有些注意——
- ZeRO-1, ZeRO-2 与 TP 兼容性好
- ZeRO-3 与 TP 兼容但配置复杂，因为两者都涉及参数分片
- FSDP（ZeRO-3）+ TP 在新版 PyTorch 中支持

**经验**：模型不大（<100B）时，ZeRO-3 单独就够；超大模型加 TP + PP，ZeRO 用 1 或 2。

**Q：激活显存怎么算？怎么省？**

A：激活显存粗算 ≈ `batch × seq_len × hidden_dim × num_layers × 2 bytes`，对大 batch 长序列会很大。

省的方法——
- **Gradient Checkpointing**：只保存部分中间激活，反向时重新计算其他——**省 ~70% 激活显存，代价是计算多 33%**
- **Flash Attention**：attention 计算时不存中间矩阵
- **Sequence Parallel**：激活按序列切分到不同卡

LLaMA-3 训练几乎肯定用了这些组合。

### ⚠️ 常见陷阱

1. **18×参数 / 16×参数 这个数字要记住**：是估算显存的关键
2. **ZeRO 是 DP 的变种**，不是独立并行——很多人搞错
3. **FSDP = ZeRO-3** 等价物，PyTorch 原生实现

### 🏢 大厂偏好

- **AI Infra 岗**：必问，会要求现场算"训 13B 模型需要多少卡"
- **字节 / DeepSeek**：会问 ZeRO 各阶段通信开销

### 📚 延伸阅读
- [ZeRO: Memory Optimizations Toward Training Trillion Parameter Models](https://arxiv.org/abs/1910.02054)
- [FSDP PyTorch 文档](https://pytorch.org/docs/stable/fsdp.html)

---

## Q6：混合精度训练：bf16 vs fp16 ⭐⭐⭐⭐

### 🎯 一句话标答

> **fp16** 精度更高（10 位尾数）但表示范围窄，需要 **loss scaling** 防溢出；**bf16** 表示范围和 fp32 相同（8 位指数）但精度低（7 位尾数），**无需 loss scaling，训练更稳定**——LLM 训练几乎全用 bf16。

### 🗣️ 30 秒口语版

"混合精度训练是 LLM 标配，但 fp16 和 bf16 的选择是面试常考——

**fp16（半精度）**：1 位符号 + **5 位指数** + 10 位尾数。
- 优势：精度高（10 位尾数）
- 劣势：**表示范围窄**（指数只有 5 位，max ~65000）——大模型梯度容易溢出
- 需要 **Loss Scaling**：把 loss 放大 1000× 再反向，避免梯度下溢；optimizer 更新前再缩小

**bf16（Brain Float 16）**：1 位符号 + **8 位指数** + 7 位尾数。
- 优势：**表示范围和 FP32 相同**（max 3.4e38），不会溢出
- 优势：**不需要 loss scaling**，工程简单
- 劣势：精度低（7 位尾数）

**LLM 实测**：bf16 的精度损失对模型质量影响**几乎可忽略**，而稳定性优势巨大。所以——

**当前 LLM 训练几乎统一用 bf16**：GPT-3 之后、LLaMA、DeepSeek 全是 bf16。fp16 在 V100 时代（不支持 bf16）用过，A100/H100 之后全转 bf16。

**前沿尝试 fp8**：H100 支持 fp8，DeepSeek-V3 用 fp8 训练成功——精度更低但训练吞吐大幅提升。"

### 📐 三种精度格式对比

| 格式 | 总位 | 符号 | 指数 | 尾数 | 最大值 | 精度 |
|---|---|---|---|---|---|---|
| FP32 | 32 | 1 | 8 | 23 | 3.4e38 | 极高 |
| **BF16** | 16 | 1 | **8** | 7 | 3.4e38 | 中 |
| FP16 | 16 | 1 | 5 | **10** | ~65504 | 较高 |
| **FP8 (E4M3)** | 8 | 1 | 4 | 3 | 448 | 低 |
| FP8 (E5M2) | 8 | 1 | 5 | 2 | ~57000 | 低 |

### 🔍 混合精度训练流程

```
1. 权重 master copy 用 FP32 存（保证累加精度）
2. Forward 时把权重 cast 到 BF16
3. BF16 前向 + BF16 反向
4. 梯度（BF16）回传后转 FP32
5. Optimizer 用 FP32 梯度 + FP32 m/v 更新 master copy
6. 下一步把更新后的 master copy 再 cast 到 BF16 用

显存影响：相比纯 FP32 训练
  · 前向/反向用 BF16：激活省 50%
  · 但 master copy + Adam states 还是 FP32：这部分没省
  · 总体：从 FP32 的 4× 减到 BF16+master 的 2.5× 量级
```

### 🔥 高频追问 Top 3

**Q：为什么 bf16 比 fp16 适合 LLM？**

A：核心是**梯度分布**。LLM 训练中梯度的**绝对值范围跨度很大**——某些参数梯度可能是 1e-10，某些是 1e5。

- FP16 范围：6e-5 ~ 65000（约 9 个数量级）
- BF16 范围：1e-38 ~ 3.4e38（约 76 个数量级，等同 FP32）

FP16 训练 LLM 时——梯度小于 6e-5 直接下溢成 0，丢失梯度。需要 loss scaling 拯救。BF16 不存在这个问题。

**精度损失**：BF16 尾数 7 位，相对误差 ~0.4%；FP16 尾数 10 位，相对误差 ~0.05%。但**LLM 训练对精度不敏感**，对溢出敏感——所以选 BF16。

**Q：FP8 训练真的能用吗？**

A：能用，但工程上更激进。FP8 的两种格式——
- **E4M3**：用于前向激活、权重，精度优先
- **E5M2**：用于反向梯度，范围优先

NVIDIA Transformer Engine 提供 FP8 训练框架。DeepSeek-V3 的训练用了 FP8 —— **训练成本相比 BF16 节省约 30%**。

但 FP8 训练需要 H100 + 特殊代码 + 量化技巧，**普通团队还在 BF16**。

**Q：训练用 BF16，推理可以用什么？**

A：推理可以更激进——
- **BF16**：稳定选择
- **FP16**：精度略高（一些场景），需注意范围
- **INT8 / INT4**：通过 quantization 实现，速度更快显存更省（GPTQ、AWQ 等，第 5 章详谈）

**LLM 推理的趋势是 INT4**——质量损失可控，部署成本大幅降低。

### ⚠️ 常见陷阱

1. **不要说 "fp16 和 bf16 差不多"** —— 在 LLM 训练上 bf16 显著优于 fp16
2. **要懂 loss scaling 是 fp16 的"补丁"** —— bf16 不需要
3. **要知道 FP8 是前沿** —— DeepSeek-V3 已经在用

### 🏢 大厂偏好

- **AI Infra / 训练岗**：必问
- **NVIDIA / 字节硬件团队**：会问 FP8 和 Transformer Engine

### 📚 延伸阅读
- [BF16 vs FP16 介绍](https://en.wikipedia.org/wiki/Bfloat16_floating-point_format)
- [DeepSeek-V3 FP8 训练](https://arxiv.org/abs/2412.19437)

---

## Q7：Loss Spike 与训练监控 ⭐⭐⭐

### 🎯 一句话标答

> Loss Spike 是训练中 loss 突然飙升的现象——可能由 **数据问题、梯度爆炸、数值不稳** 引起；**Gradient Clipping + bf16 + Pre-Norm + 良好初始化** 是主要预防手段；**监控 grad norm 是早期预警**。

### 🗣️ 30 秒口语版

"训练超大模型时，loss 偶尔会**突然飙升**几个数量级，然后慢慢恢复或彻底崩坏。这叫 loss spike。

**主要成因**：
- **数据 outlier**：某 batch 数据异常（如重复 token、极端长序列）
- **梯度爆炸**：某层激活值突然变大
- **数值不稳**：FP16 下偶发的 NaN/Inf
- **学习率过大**：尤其 warmup 不充分时

**典型现象**——loss 从 2.5 跳到 8，几百步后恢复到 3.0，模型最终性能受影响。

**预防手段**：
- **Pre-Norm + RMSNorm**：基础稳定化
- **bf16 而非 fp16**：避免溢出导致的 spike
- **Gradient Clipping (max_norm=1.0)**：硬限制梯度
- **良好的初始化**：尤其大模型用更小的标准差（如 0.02）
- **充分的 Warm-up**：5000+ 步

**监控指标**：
- **训练 loss**：曲线应该平滑下降，spike 立即报警
- **梯度范数 grad_norm**：突然增大是 spike 的前兆
- **激活范数**：某层激活异常大也是预警
- **参数范数**：监控是否有参数无限增长

**实战处理**：
- 如果只是偶尔 spike 并恢复——继续训练
- 如果频繁 spike——降学习率、调 batch size
- 如果训练崩了——从最近的 checkpoint 恢复，跳过有问题的 data shard"

### 💡 实际案例：PaLM 的 Loss Spike

Google PaLM 论文（540B 模型）专门讨论过这个问题——

- 训练了 540B 模型，过程中遇到**约 20 次** loss spike
- 每次 spike 后**回滚到 spike 前 100 步**的 checkpoint
- 跳过引起 spike 的 batch，继续训练
- 最终训出来了，但工程量巨大

LLaMA-2、DeepSeek-V3 等也都报告过类似处理。**spike 是大模型训练的常态**。

### 🔥 高频追问 Top 3

**Q：如何提前预测 loss spike 要发生？**

A：监控 grad_norm 的**移动平均和瞬时值**：
- 正常情况：grad_norm 应该稳定在某个值（如 0.5-2.0）
- spike 前：grad_norm 会**突然变大 10-100×**
- 这是早期预警信号

也可以监控**激活范数**——某层激活突然增大也是兆头。

**Q：损失炸了，要不要回滚？**

A：**看严重程度**——
- Loss 上升 < 50%：继续观察，通常自己恢复
- Loss 上升 100-500%：可能要回滚 + 跳过 batch
- Loss 变 NaN/Inf：必须回滚

回滚策略：从 spike 前最近的 checkpoint 恢复，跳过引起 spike 的几个 batch（识别它们靠 spike 出现的时间戳）。

**Q：是否能用代码自动处理 spike？**

A：可以——很多大模型训练框架（Megatron、Pax）有"**自动 spike recovery**"：
- 监控 grad_norm 超过阈值时
- 跳过本步更新（不让梯度污染参数）
- 或者回滚到上 N 步的 checkpoint

DeepSpeed 也提供类似机制。**自动恢复是大模型训练的必要工具**。

### ⚠️ 常见陷阱

- **不要认为 spike 只是 bug**：是大模型训练的统计现象，要工程化处理
- **要懂监控 grad_norm**：这是预警的关键

---

## Q8：持续预训练 Continual Pretraining ⭐⭐⭐

### 🎯 一句话标答

> Continual Pretraining = **在已有预训练模型基础上，用新数据继续预训练**——常用于**领域适配（医疗、代码）**或**语言扩展（中文增强）**；关键挑战是**避免遗忘原有能力**。

### 🗣️ 30 秒口语版

"持续预训练有几种典型场景——

**场景 1：领域适配**。比如基于 LLaMA-3 继续训练医疗、法律等垂直领域。
**场景 2：语言扩展**。比如基于 LLaMA-2（英文为主）扩展中文（Chinese-LLaMA、Yi 等做过）。
**场景 3：能力增强**。比如 CodeLLaMA 是在 LLaMA-2 基础上加大量代码继续训。
**场景 4：上下文扩展**。从 4K 上下文扩到 32K，用长序列数据继续训。

**核心挑战：灾难性遗忘（Catastrophic Forgetting）**——只用新数据训会让模型忘掉原有能力。

**应对策略**：
- **数据混合**：新数据 + 一定比例的原数据（如 30% 原通用 + 70% 新领域）
- **较低学习率**：原训练时 lr=3e-4，继续训用 1e-5（10-30× 衰减）
- **较短 warmup**：1000 步而不是几万步
- **LoRA / Adapter**：只训部分参数，原参数冻结——最稳但表达力受限
- **早停**：监控原 benchmark（如 MMLU），下降到一定阈值就停

**典型成本**：相比从零预训练，继续预训练成本 1-10%——远比从头训便宜，是垂直领域 LLM 的主流路线。"

### 🔥 高频追问 Top 3

**Q：继续预训练和 SFT 的区别？**

A：**目标和方式不同**——
- 继续预训练：**next-token prediction**，用大量无监督文本，目的是注入领域知识
- SFT：**指令-响应配对**，用少量高质量数据，目的是让模型听指令

时间上的关系：通常先继续预训练（注入知识），再 SFT（学指令格式）。比如 CodeLLaMA：base → 继续预训练（代码） → SFT (instruct 版)。

**Q：怎么判断要不要继续预训练 vs 直接 SFT？**

A：**看新领域的知识 gap**——
- 新领域**词汇/概念 LLM 没见过**：必须继续预训练（如稀有的专业术语、新框架）
- 新领域**格式特殊**：SFT 就够（如客服对话、特定 JSON 输出）
- 新领域**推理逻辑特殊**：可能两者都要

经验：继续预训练 + SFT 一起用，效果通常胜过单独一项。

**Q：扩展中文能力具体怎么做？**

A：以 LLaMA-2 扩中文为例（Chinese-LLaMA-2、Yi 等的方法）：

1. **词表扩展**：在 LLaMA-2 32K 词表上加 ~20K 中文 token，让中文 token 利用率从 0.5 字/token 提到 1.2 字/token
2. **Embedding 初始化**：新加的中文 token embedding 用平均或者细致的初始化方法
3. **继续预训练**：用 ~100B 中文 token + ~30B 英文 token（防遗忘），lr=2e-5，warmup=2000
4. **SFT**：用中文指令数据微调

**总成本**：相比从零训，**约 1-3% 的计算量**，但中文能力大幅提升。

### 🏢 大厂偏好

- **国内大厂（字节、阿里、百度）**：会问"基于开源模型怎么做中文增强"
- **垂直领域团队（金融、医疗）**：会问"怎么做领域适配"

### 📚 延伸阅读
- [Chinese-LLaMA 论文](https://arxiv.org/abs/2304.08177)
- [CodeLLaMA](https://arxiv.org/abs/2308.12950)

---

## 📝 本章小结

| 关键点 | 你必须能脱口而出 |
|---|---|
| 预训练目标 | CLM 主流，MLM 用于 embedding，PrefixLM 是研究 |
| 数据组成 | CC 60-70% + 代码 10-15% + 书学百科 + 多语言 |
| 数据清洗 | 质量过滤 + 去重（MinHash）+ 安全过滤 |
| Chinchilla 法则 | **D ≈ 20 × N**，参数和数据等比放大 |
| 训练 vs 推理最优 | LLaMA-3 过训练（D>>20N），因为推理成本主导 |
| 五种并行 | DP/TP/PP/SP/EP，TP 节点内 PP 节点间 |
| 显存四大块 | 参数 2 + 梯度 2 + 优化器 12 + 激活 ≈ 18 × 参数 bytes |
| ZeRO 三阶段 | ZeRO-3 / FSDP 显存压力最低 |
| BF16 优势 | 范围广，不需 loss scaling |
| FP8 前沿 | DeepSeek-V3 实证可行 |
| Loss Spike | 大模型常态，靠 grad clip + 回滚处理 |
| 继续预训练 | 关键是混合原数据 + 小 lr 防遗忘 |

## ✅ 自测题

1. 现场估算：训 13B 模型用 Adam 优化器，BF16 混合精度，单卡需要多少显存？需要几张 A100（80GB）才能装下一个完整模型副本？
2. LLaMA-3 70B 用 16384 张 H100 训练，并行策略可能是怎样的？画出拓扑图
3. 解释 ZeRO-1, 2, 3 三阶段的差异，分别在什么场景用？
4. 为什么大模型用 BF16 而不是 FP16？给出技术原因
5. Chinchilla Law 说 D = 20N，但 LLaMA-3 用了 D = 214N，为什么这是工业最优？
6. 训练中遇到 loss spike，你的排查流程是什么？

---

> **下一章预告**：第 7 章｜SFT 指令微调——SFT 的数据工程（LIMA、Self-Instruct、Evol-Instruct、Magpie）、loss masking、多轮对话格式、Catastrophic Forgetting 等。LIMA 论文的"质量胜过数量"是面试常考点。
