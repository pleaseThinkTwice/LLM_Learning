# 第 3 章｜归一化与激活

> **本章定位**：很多候选人觉得归一化和激活函数是"小细节"，但实际上是字节、Meta 高频追问区。原因是——**这些选择直接关系训练能不能稳定跑下来**，能答清楚 RMSNorm、Pre-Norm、SwiGLU 的设计动机，证明你真的训过大模型。本章重点：训练稳定性的来龙去脉。

> **配套章节**：Transformer 架构 → 第 1 章 / 位置编码 → 第 2 章 / 预训练细节 → 第 6 章

---

## 📌 本章导航

| 序号 | 题目 | 难度 | 频率（字节/阿里/Meta/DeepSeek） | 类型 |
|---|---|---|---|---|
| Q1 | 为什么 Transformer 用 LayerNorm 不用 BatchNorm？ | ⭐⭐⭐⭐ | 6 / 4 / 3 / 2 | 概念对比 |
| Q2 | LayerNorm 的公式与作用 | ⭐⭐⭐ | 4 / 3 / 2 / 1 | 基础 |
| Q3 | **RMSNorm 和 LayerNorm 的区别** | ⭐⭐⭐⭐⭐ | 8 / 4 / 5 / 6 | 必问 |
| Q4 | **Pre-Norm vs Post-Norm 训练稳定性** | ⭐⭐⭐⭐⭐ | 9 / 5 / 6 / 5 | 必问 |
| Q5 | 激活函数演化：ReLU→GELU→SwiGLU | ⭐⭐⭐⭐ | 5 / 3 / 4 / 3 | 概念 |
| Q6 | **SwiGLU 为什么效果好？** | ⭐⭐⭐⭐⭐ | 6 / 3 / 4 / 4 | 深度 |
| Q7 | DeepNorm 等深层网络稳定化 | ⭐⭐⭐ | 2 / 1 / 2 / 1 | 加分 |
| Q8 | QK-Norm 与新型归一化 | ⭐⭐⭐ | 2 / 1 / 2 / 2 | 前沿 |

---

## Q1：为什么 Transformer 用 LayerNorm 不用 BatchNorm？ ⭐⭐⭐⭐

### 🎯 一句话标答

> 因为 **NLP 任务的输入序列长度不定、batch 内样本差异大**，BatchNorm 的统计量不稳定；而 LayerNorm 是**单样本**的归一化，与 batch 大小、序列长度无关，更适合变长序列建模。

### 🗣️ 30 秒口语版

"这个问题要从两种归一化的统计维度讲起。

**BatchNorm** 是沿着 **batch 维度** 算均值方差——它假设同一 batch 内的样本是同分布的，统计稳定。在 CV 里这成立，因为图片裁剪后大小一样、分布相近。

**LayerNorm** 是沿着 **特征维度**算均值方差——一个样本内部所有特征归一化，不跨样本，**与 batch 大小完全无关**。

NLP 场景有几个特点让 BN 不合适——

第一，**序列长度可变**。一个 batch 里有的句子 10 词，有的句子 100 词，padding 之后做 BN，padding 位置会污染统计量。

第二，**同 batch 样本差异大**。一句中文新闻和一句英文代码放一起做 BN，没什么道理。

第三，**推理时 batch=1 是常态**。BN 推理时要用训练时存的 running mean/var，但训推不一致会带来分布漂移。LayerNorm 训推一致。

所以 Transformer 从一开始就用 LayerNorm，没动过这个选择。"

### 📐 两者的数学对比

设输入 $X \in \mathbb{R}^{B \times L \times D}$（batch_size × seq_len × hidden_dim）。

**BatchNorm**（沿 B、L 维度算）：
$$\mu_d = \frac{1}{BL} \sum_{b,l} X_{b,l,d}, \quad \sigma_d^2 = \frac{1}{BL} \sum_{b,l} (X_{b,l,d} - \mu_d)^2$$
$$\hat{X}_{b,l,d} = \gamma_d \cdot \frac{X_{b,l,d} - \mu_d}{\sqrt{\sigma_d^2 + \epsilon}} + \beta_d$$

每个 hidden 维度有自己的 $\gamma_d, \beta_d$，统计量跨样本算。

**LayerNorm**（沿 D 维度算）：
$$\mu_{b,l} = \frac{1}{D} \sum_d X_{b,l,d}, \quad \sigma_{b,l}^2 = \frac{1}{D} \sum_d (X_{b,l,d} - \mu_{b,l})^2$$
$$\hat{X}_{b,l,d} = \gamma_d \cdot \frac{X_{b,l,d} - \mu_{b,l}}{\sqrt{\sigma_{b,l}^2 + \epsilon}} + \beta_d$$

**关键差别**：BN 的统计量是 D 个标量（每维一个），LN 的统计量是 B·L 个标量（每个 token 一个）。

### 🔥 高频追问 Top 3

**Q：BN 在 CNN 里效果那么好，移植到 RNN 行不行？**

A：不行——RNN 的隐状态在不同时间步差异大，跨样本同时间步做 BN 假设不成立。事实上 RNN 时代就有 LayerNorm 的提出（Ba 2016），就是为了解决 BN 在 RNN 里失效的问题。Transformer 继承了这条经验。

**Q：为什么 NLP 不能给所有句子 padding 到一样长，强行用 BN？**

A：技术上可以，但有三个问题——① padding 多了浪费计算；② padding token 进入统计会扭曲分布；③ batch 内不同语言、不同领域的样本统计量本来就不该混在一起。强行 BN 比 LN 差，几乎没人这么做。

**Q：现在有没有 NLP 任务用 BN 的？**

A：**几乎没有**。语音识别等输入相对规则的任务里能见到一些 BN 变体，但纯文本场景统一是 LayerNorm 系列（LN、RMSNorm）。这是个**全行业共识**。

### ⚠️ 常见陷阱

1. **不要只说"NLP 序列长度可变"** —— 要补充"batch 间差异大""推理 batch=1"这些角度
2. **不要混淆"沿哪个维度算"和"哪些参数被学习"**：BN 和 LN 都有可学习的 $\gamma, \beta$，区别只在统计维度
3. **不要把 GroupNorm、InstanceNorm 当成 LN 的同类** —— 它们是 CV 里的方案，NLP 几乎不用

### 🏢 大厂偏好

- **字节 / 阿里**：基础题，会从这里追问到 Pre-Norm vs Post-Norm
- **Meta**：会问"现代 LLM 都用 RMSNorm 了，为什么不用 LN"，看你是否跟进前沿

### 📚 延伸阅读
- [Layer Normalization](https://arxiv.org/abs/1607.06450) - Hinton 等人原论文

---

## Q2：LayerNorm 的公式与作用 ⭐⭐⭐

### 🎯 一句话标答

> LayerNorm 对**单个样本的特征维度**做均值方差归一化，再加可学习的缩放 $\gamma$ 和偏置 $\beta$——主要作用是**稳定训练**，让深层网络的激活值分布不要漂移。

### 🗣️ 30 秒口语版

"LayerNorm 的公式我刚才已经写过——对每个 token 的 hidden vector，沿 hidden 维度算均值和方差，归一化到均值 0 方差 1，再用可学习的 γ 和 β 做缩放和平移。

它的作用有三个层面——

**直接作用**是**稳定数值范围**。不归一化的话，深层网络激活值很容易爆炸或消失，训不下去。

**间接作用**是**让梯度更稳定**。激活值归一化后，反向传播时梯度的尺度也稳定，便于用大学习率训练。

**深层次的作用**是**让网络更容易优化**——这是 Santurkar 等人 2018 年那篇 'How Does Batch Normalization Help Optimization?' 的观点——归一化让 loss landscape 变得更平滑，不是因为减少了'internal covariate shift'。

实践上 LayerNorm 是 Transformer 训练能成功的**关键组件之一**，去掉训不动。"

### 📐 完整公式

输入 $x \in \mathbb{R}^d$（单个 token 的 hidden vector）：

$$\mu = \frac{1}{d} \sum_{i=1}^d x_i, \quad \sigma^2 = \frac{1}{d} \sum_{i=1}^d (x_i - \mu)^2$$

$$\text{LN}(x)_i = \gamma_i \cdot \frac{x_i - \mu}{\sqrt{\sigma^2 + \epsilon}} + \beta_i$$

其中：
- $\gamma, \beta \in \mathbb{R}^d$ 是可学习参数（每维独立）
- $\epsilon$ 是数值稳定项（通常 $10^{-5}$ 或 $10^{-6}$）

参数量：每层 LN 有 $2d$ 个参数。

### 💡 三个层次的作用

**层次 1：数值稳定**——直接控制激活值范围在 [-某常数, +某常数] 内，避免爆炸/消失。

**层次 2：梯度稳定**——配合残差连接，让深层网络的梯度能稳定回传。

**层次 3：优化友好**——平滑 loss landscape，允许更大的学习率，加速收敛。

### 🔥 高频追问 Top 3

**Q：γ 和 β 不是把归一化又"撤销"了吗？**

A：表面看是，但实际不是。归一化后特征是均值 0、方差 1 的标准分布；$\gamma, \beta$ 是**可学习的偏移**——模型可以选择"我要某些维度方差大、某些维度方差小"，这是个**有约束的灵活性**——不像不归一化那样完全失控。可以理解为"先标准化到一个良好的起点，再让模型从这个起点做有意义的调整"。

**Q：ε 加在根号里和加在根号外有什么区别？**

A：加在根号里（PyTorch 默认）：$\sqrt{\sigma^2 + \epsilon}$——保证分母至少 $\sqrt{\epsilon}$。加在根号外：$\sqrt{\sigma^2} + \epsilon$——保证分母至少 $\epsilon$。**前者数值更稳定**——当 σ 接近 0 时，前者分母 $\sqrt{\epsilon}$ 是非零小数；后者 ε 本身就是很小的数。所以标准做法是加在根号里。

**Q：LayerNorm 的 γ 通常初始化为多少？β 呢？**

A：$\gamma$ 初始化为 **全 1**，$\beta$ 初始化为 **全 0**。这等价于"刚开始 LN 就是个纯归一化（不做缩放），让模型从这个干净起点学"。随着训练，γ、β 慢慢偏离初始值，学到合适的缩放与偏移。

### ⚠️ 常见陷阱

- **混淆 LN 和 InstanceNorm**：IN 是 CV 里的方案，按通道分别归一化，与 LN 不同
- **忘了 γ、β 是 per-dimension**：每个 hidden 维度有独立的 γ、β，不是一个标量

---

## Q3：RMSNorm 和 LayerNorm 的区别 ⭐⭐⭐⭐⭐（必问）

### 🎯 一句话标答

> RMSNorm = **LayerNorm 砍掉减均值这一步**——只除 RMS 不减均值，参数也少一半（没有 β）。**性能几乎不掉，速度快 10-30%**，是现代 LLM 的标配。

### 🗣️ 30 秒口语版

"这题考察的是对现代 LLM 工程演化的理解。

**LayerNorm 的两个操作是'减均值 + 除标准差'**。RMSNorm 的核心 insight 是——**减均值这一步几乎没用，可以省掉**。

具体来说，RMSNorm 直接除以 RMS（均方根），不算均值：

`RMSNorm(x) = x / sqrt(mean(x²) + ε) · γ`

**好处**：
- **计算更快**——省一次求均值的归约操作，省一次减法
- **参数更少**——只有 γ，没有 β
- **效果不掉**——实验证明性能几乎一样

**为什么能省减均值**？两种解释——
- **不变性视角**：神经网络的输出对输入加常数不敏感，'减均值'带来的额外不变性其实没起作用
- **简洁性视角**：减均值这步引入了一个常数维度的'伪信息'（均值），实际作用有限

LLaMA 系列从 LLaMA-1 开始就用 RMSNorm，DeepSeek、Qwen、Mistral 也都跟进了。**现在新设计的 LLM 几乎全是 RMSNorm**。"

### 📐 公式对比

**LayerNorm**：
$$\text{LN}(x) = \gamma \odot \frac{x - \mu}{\sqrt{\sigma^2 + \epsilon}} + \beta$$
其中 $\mu = \frac{1}{d}\sum_i x_i$, $\sigma^2 = \frac{1}{d}\sum_i (x_i - \mu)^2$。

**RMSNorm**：
$$\text{RMSNorm}(x) = \gamma \odot \frac{x}{\text{RMS}(x)}$$
其中 $\text{RMS}(x) = \sqrt{\frac{1}{d}\sum_i x_i^2 + \epsilon}$。

**关键差异**：
1. RMS 不减均值（**re-centering invariance** 没有了，但 **re-scaling invariance** 保留）
2. RMS 没有 β

### 🔍 参数与计算对比

| 维度 | LayerNorm | RMSNorm |
|---|---|---|
| 可学习参数 | $\gamma, \beta \in \mathbb{R}^d$ | $\gamma \in \mathbb{R}^d$ |
| 参数量 | 2d | d |
| 主要操作 | 1 次均值 + 1 次方差 + 减法 + 除法 | 1 次平方均值 + 除法 |
| 实测速度（H100, d=4096） | 基准 | 快 7-20% |
| 性能差距 | 基准 | ≈ 0（很多任务略胜） |

### 💡 为什么减均值"几乎没用"？

这是 RMSNorm 论文（Zhang & Sennrich, 2019）的核心论点。直觉理解——

**LayerNorm 提供两种不变性**：
1. **Re-centering invariance**：对 x 加任意常数 c，输出不变（来自减均值）
2. **Re-scaling invariance**：对 x 乘任意正数 a，输出不变（来自除标准差）

实验发现——**re-centering 几乎没贡献**，因为：
- 神经网络后续是线性层 + 非线性，加常数 c 的效应会被吸收
- 减均值带来的训练稳定性提升不显著

**re-scaling 才是关键**——它控制激活值的尺度，这是稳定训练的核心。

所以砍掉减均值，保留除 RMS，既简化又不损性能。

### 🔥 高频追问 Top 3

**Q：能不能反过来——只减均值不除标准差？**

A：理论上能，但**效果会差很多**。因为不归一化标准差，激活值的尺度还是可能爆炸或消失——而 re-scaling 才是控制尺度的关键。RMSNorm 论文实际做过这个消融实验，验证了"只减均值"性能显著下降。

**Q：RMSNorm 的 γ 初始化和 LayerNorm 一样吗？**

A：一样，**都初始化为全 1**。RMSNorm 没有 β，所以也不需要初始化 β。

**Q：为什么 RMSNorm 推广这么慢——2019 年提出，2022 年 LLaMA 才用？**

A：因为前几年 BERT、GPT-2/3 都已经训好了，迁移到 RMSNorm 需要重新训。**RMSNorm 真正爆发是在大规模训练成本敏感的时代**——LLaMA 训 65B 模型，省 10% 的归一化时间就是几十万美元。这种"边际优化"积累到一定规模就值得换。

### ⚠️ 常见陷阱

1. **要能现场写出 RMSNorm 公式**：这是必背的
2. **不要说"RMSNorm 是简化版 LayerNorm 但性能差"**：性能几乎相等，部分任务还略好
3. **要点出"为什么减均值可以省"**：背公式不够，要懂背后的不变性分析

### 🏢 大厂偏好

- **字节 / DeepSeek / Mistral 系**：必问，可能让你白板写公式
- **Meta**：会让你解释"为什么 LLaMA 选择 RMSNorm"

### 📚 延伸阅读
- [Root Mean Square Layer Normalization](https://arxiv.org/abs/1910.07467) - 原论文
- LLaMA 论文 §2.2 - RMSNorm 在 LLM 的首次大规模应用

---

## Q4：Pre-Norm vs Post-Norm 训练稳定性 ⭐⭐⭐⭐⭐（必问）

### 🎯 一句话标答

> Pre-Norm 把归一化放在子层**之前**，残差是无归一化的"高速路"——**训练稳定但表达力略弱**；Post-Norm 放在子层之后——**表达力强但深层训练易崩**。**现代 LLM 几乎全是 Pre-Norm**。

### 🗣️ 30 秒口语版

"这个区别是 Transformer 工程史上的一个重要转折。

**Post-Norm**（原 Transformer 论文用的）：
```
x → SubLayer → +x → LN → 输出
```

**Pre-Norm**（GPT-2 之后的标准）：
```
x → LN → SubLayer → +x → 输出
```

差别就是 **LN 是放在残差连接的'里面'还是'外面'**。

**关键影响在残差路径上**——

Pre-Norm 的残差路径是**裸 x**，没有归一化在中间。这意味着**梯度可以无衰减地从顶层直达底层**，深层网络也能训稳。代价是子层的输出会被归一化后的 x 影响，表达力略受限。

Post-Norm 的残差路径里**有 LN**。每经过一层，残差就被归一化一次。深层网络叠加下来，梯度衰减严重，**100 层以上几乎训不动**。

所以 GPT-2 之后所有大模型都改用 Pre-Norm——LLaMA、GPT-3、PaLM、DeepSeek 全是。Post-Norm 现在主要在浅网络（如 BERT 的 12 层）还能见到。

**但** 也有些工作回头改进 Post-Norm，希望兼顾稳定和表达力，比如 DeepNet 的 DeepNorm。"

### 📐 数学分析：梯度流

**Pre-Norm 第 l 层**：
$$x_{l+1} = x_l + \text{SubLayer}(\text{LN}(x_l))$$

经过 L 层后，最顶层 $x_L$ 对最底层 $x_0$ 的偏导：
$$\frac{\partial x_L}{\partial x_0} = \prod_{l=0}^{L-1} \left(1 + \frac{\partial \text{SubLayer}(\text{LN}(x_l))}{\partial x_l}\right)$$

第一项 **1** 来自残差。乘积里有这个 +1，**梯度能完整传到底层**。

**Post-Norm 第 l 层**：
$$x_{l+1} = \text{LN}(x_l + \text{SubLayer}(x_l))$$

梯度推导更复杂——LN 包裹了整个残差，链式求导后**每一层的雅可比矩阵都被 LN 的"缩放"作用扭曲**。L 层叠加，梯度方差很容易爆炸或消失。

**结论**：
- Pre-Norm 有 **identity path**（恒等路径），梯度稳定
- Post-Norm 没有，梯度需要"穿越"每一层的 LN

### 🔍 工程对比

| 维度 | Post-Norm | Pre-Norm |
|---|---|---|
| LN 位置 | 子层之后 | 子层之前 |
| 残差路径 | 包含 LN | 不含 LN（裸 x） |
| 深层训练稳定性 | 差（>50 层就崩） | 好（100+ 层稳） |
| Warm-up 需求 | 必须，否则崩 | 可省 |
| 学习率敏感性 | 高 | 低 |
| 最终表达力 | 略强 | 略弱 |
| 代表模型 | 原 Transformer, BERT | GPT-2/3/4, LLaMA |

### 💡 为什么 Pre-Norm 表达力略弱？

苏剑林博客有个经典分析——

Pre-Norm 的递推关系：$x_{l+1} = x_l + F(LN(x_l))$

随着 l 增加，$x_l$ 的尺度会**线性增长**（因为不断累加 F 的输出）。但每个 F 又对 $x_l$ 做了 LN，所以**$x_l$ 的方向变化越来越小**——网络越深，新加的层贡献越小，**等效于一个浅一点的网络**。

Post-Norm 没这个问题——每层之后 LN 重置尺度，相邻层的变化保持显著。

**所以**——Pre-Norm 的 100 层 ≈ Post-Norm 的 60-80 层的"有效深度"。但是因为 Post-Norm 训不动，这个比较没意义。**实际是"训得稳的稍浅有效深度" > "训不动的真深度"**。

### 🔥 高频追问 Top 3

**Q：Post-Norm 必须用 warm-up，为什么？**

A：因为 Post-Norm 的梯度本来就不稳定，**初始几步如果直接用大学习率，激活值会立刻发散**。Warm-up 是从小学习率慢慢爬升到目标学习率，给网络一个"适应期"，让权重先进入稳定区域。Pre-Norm 因为梯度本身稳，warm-up 没那么关键（但通常也用，作为保险）。

**Q：DeepSeek 用 Pre-Norm 吗？**

A：用，但有改进。DeepSeek-V3 用的是 RMSNorm + Pre-Norm，是标准配置。但是它在某些设计上参考了 DeepNet 的思路，比如**用更精细的初始化**来配合深层训练。具体到 671B 参数，仍然是 Pre-Norm 路线。

**Q：能不能把 Pre-Norm 和 Post-Norm 结合？**

A：能，叫**Sandwich-Norm** 或 **Hybrid-Norm**：
$$x_{l+1} = \text{LN}_2(x_l + F(\text{LN}_1(x_l)))$$

子层前后都有 LN。GLM-130B、PaLM 用过类似方案。优点是兼顾稳定和表达力；缺点是多了一次 LN 的计算和参数。**工业上不流行**——多花的计算不划算，单纯 Pre-Norm + RMSNorm 已经够用了。

### ⚠️ 常见陷阱

1. **不要把"梯度消失"和"训练发散"混为一谈**：Pre-Norm 解决了梯度消失，Post-Norm 在小模型上效果反而更好，问题出在大深度
2. **要能现场写出递推公式**：上面那两个公式是面试现场可能要求白板的
3. **不要绝对化"Pre-Norm 一定胜"**：在 BERT 这种 12 层的浅网络上，Post-Norm 仍然有效

### 🏢 大厂偏好

- **字节 / DeepSeek**：高频题，会让你画图比较两种结构
- **Meta**：会追问"如果让你训 1000 层的模型，怎么做"，引出 DeepNorm
- **AI Infra 岗**：会问 warm-up、初始化等工程配套

### 📚 延伸阅读
- [On Layer Normalization in the Transformer Architecture](https://arxiv.org/abs/2002.04745) - Pre-Norm 系统分析
- [苏剑林：模型优化漫谈：BERT 的初始标准差为什么是 0.02？](https://kexue.fm/archives/8747)

---

## Q5：激活函数演化：ReLU → GELU → SwiGLU ⭐⭐⭐⭐

### 🎯 一句话标答

> 三代演化的核心思想是**让激活更平滑、更带门控**——ReLU 简单粗暴有"死亡神经元"问题，GELU 加了概率门控让小负数也能流过，SwiGLU 进一步引入**乘法门控**，提升表达力。

### 🗣️ 30 秒口语版

"激活函数的演化讲三个里程碑就行——

**ReLU**：max(0, x)，2012 年统治 CV。优点是简单、梯度好；缺点是**死亡神经元**——一旦输入 < 0，梯度恒为 0，神经元永久失活。

**GELU**：高斯误差线性单元，**对负数做平滑过渡**而不是硬截断。在 x=0 附近有平滑曲线，小负数还能漏出一点信号。BERT、GPT-2/3 都用 GELU。

**SwiGLU**：Swish + GLU 的组合。Swish 是 x·sigmoid(x)，类似 GELU 但更简单。GLU 是**门控线性单元**——把 FFN 拆成两路，一路是数据，一路是门控，相乘后输出。SwiGLU 把两者结合，**表达力比 GELU 强一截**，是 LLaMA 后所有现代 LLM 的标配。

简单记忆——**ReLU 硬截断、GELU 平滑过渡、SwiGLU 加门控**。"

### 📐 三种激活的公式

**ReLU**：
$$\text{ReLU}(x) = \max(0, x)$$

**GELU**（高斯误差线性单元）：
$$\text{GELU}(x) = x \cdot \Phi(x)$$
其中 $\Phi$ 是标准正态的 CDF。常用近似：
$$\text{GELU}(x) \approx 0.5 x \left(1 + \tanh\left(\sqrt{\frac{2}{\pi}}(x + 0.044715 x^3)\right)\right)$$

**SwiGLU**（用在 FFN 里）：
$$\text{SwiGLU}(x, W, V, b, c) = \text{Swish}(xW + b) \odot (xV + c)$$
其中 $\text{Swish}(x) = x \cdot \sigma(x)$（也叫 SiLU）。

**FFN 用 SwiGLU 的完整公式**：
$$\text{FFN}(x) = (\text{Swish}(xW_1) \odot xW_2) W_3$$

注意——FFN-SwiGLU 用了 **3 个矩阵**（W_1, W_2, W_3），普通 FFN 只用 2 个。

### 🔍 三者直观对比

```
ReLU 曲线：
  ╱
 ╱
 ─ ─ ─ ─    (x<0 部分梯度为 0)
        │
        0


GELU 曲线（平滑版 ReLU）：
   ╱
  ╱
 ╱
─╱       (x 接近 0 处平滑过渡，小负数有微弱信号)
   ╲ (x 远负数处趋近 0)
        │
        0


Swish 曲线（类似 GELU 但形状略不同）：
  类似 GELU，更简单：x·sigmoid(x)
```

### 🔥 高频追问 Top 3

**Q：GELU 比 ReLU 好在哪里？**

A：三点——
- **小负数不死**：x = -0.5 时 ReLU 输出 0，GELU 输出 -0.15，仍有信号
- **平滑可导**：ReLU 在 x=0 处不可导（虽然实践上不影响），GELU 处处光滑，优化更稳定
- **概率解释**：GELU 可以理解为"按 $\Phi(x)$ 的概率把 x 留下"，是个软门控

**Q：为什么 LLaMA 选 SwiGLU 不选 GELU？**

A：**实测胜出 + 工程友好**。Noam Shazeer 2020 年的论文 "GLU Variants Improve Transformer" 系统比较了多种 GLU 变体（ReGLU、GEGLU、SwiGLU），**SwiGLU 平均胜出 0.5-2 个点**。LLaMA 训练时跟进了这个发现。工程上，Swish 比 GELU 计算稍简单，量化也更友好（GELU 的 tanh 近似在低精度下数值不稳）。

**Q：FFN 用 SwiGLU 多了一个矩阵，参数量怎么算？**

A：普通 FFN：`FFN(x) = W_2(GELU(W_1·x))`，两个矩阵，中间维度通常 4d。SwiGLU 用三个矩阵（W_1, W_2 都映到中间维度，W_3 映回 d），为了**等参数量**，LLaMA 把中间维度从 4d 改成了 **8d/3 ≈ 2.67d**，使总参数量保持。所以 SwiGLU 的"中间维度 8d/3"是设计上的 trick，不要忘了说。

### ⚠️ 常见陷阱

1. **混淆 Swish 和 SwiGLU**：Swish 是激活函数（x·sigmoid(x)），SwiGLU 是 FFN 结构（门控 + Swish）
2. **不知道 LLaMA 的中间维度是 8d/3**：这是面试官追问的高频点
3. **以为 SwiGLU 一定胜 GELU**：在小模型上差距不明显，**大模型 + 长训练下 SwiGLU 优势才显著**

### 🏢 大厂偏好

- **字节**：会问"激活函数的演化逻辑是什么"
- **Mistral / Meta 系**：会让你解释 SwiGLU 的具体公式

---

## Q6：SwiGLU 为什么效果好？ ⭐⭐⭐⭐⭐

### 🎯 一句话标答

> 因为 **GLU 引入了乘法门控**——让模型可以"按需要选择信息"，比单纯加非线性的表达力更强；Swish 提供平滑激活的好处。**两者结合，加宽了 FFN 的"信息瓶颈"**。

### 🗣️ 30 秒口语版

"SwiGLU 好的原因可以从两个角度讲。

**角度一：门控机制**。普通 FFN 是 `linear → activation → linear`，激活函数只对单一通道做非线性。GLU 把信号拆成**两路**——一路是数据 (xW)，一路是门 (xV)。门控通过激活函数（Swish/Sigmoid 等）变成 0-1 的权重，乘到数据上。这相当于让模型动态决定**让多少信息通过**——类似 LSTM 里的门。

**角度二：表达力增强**。普通 FFN 的中间表示是单线性的，SwiGLU 的中间是两路相乘，**包含了二次项**——表达力直接上升一个量级。从数学角度看，`(xW) · sigmoid(xV)` 是 x 的非线性组合，逼近能力更强。

**实证结果**：Noam Shazeer 2020 的论文做了详尽消融——所有 GLU 变体（ReGLU、GEGLU、SwiGLU）都比对应的非门控版本好 0.5-2 个 perplexity 点。SwiGLU 略胜其他变体，所以成为最终选择。

**代价**：参数量增加 50%（3 矩阵 vs 2 矩阵），但通过缩小中间维度 8d/3 抵消。"

### 📐 GLU 的演化谱

GLU 家族都共享同一结构 `(xW) ⊙ activation(xV)`，区别在 activation：

| 变体 | activation 选择 | 公式 |
|---|---|---|
| GLU（原始） | Sigmoid | $xW \odot \sigma(xV)$ |
| ReGLU | ReLU | $xW \odot \text{ReLU}(xV)$ |
| GEGLU | GELU | $xW \odot \text{GELU}(xV)$ |
| **SwiGLU** | Swish | $xW \odot \text{Swish}(xV)$ |

**注意**：很多文章把"门控"那一路写成 activation(xV)，但 SwiGLU 论文里把 Swish 放在**数据路径**上，门控用线性。本质等价，记忆时不要纠结。

### 💡 门控的深层意义

可以从三个视角理解 SwiGLU 为什么好——

**视角 1：Hadamard 积引入非线性**。两个向量逐元素相乘是非线性操作（不是 vector × vector 的内积，而是按位相乘），相比单纯加 activation，**引入了更丰富的特征交互**。

**视角 2：注意力机制的类比**。门控 $\sigma(xV)$ 相当于"对每个维度独立的 attention 分数"，决定每个维度的传递强度。这种"按维度的软选择"比全部一刀切的 activation 更细致。

**视角 3：稀疏激活**。Sigmoid/Swish 输出在 0-1 之间，许多门会接近 0，**等效于稀疏激活**——这在某种程度上类似 MoE 的稀疏专家，但代价更小。

### 🔍 SwiGLU vs GEGLU 等变体的小差距

为什么是 SwiGLU 而不是 GEGLU？

Shazeer 论文实验：

| 变体 | T5-Base PPL |
|---|---|
| GELU (普通 FFN) | 26.5 |
| ReGLU | 25.5 |
| GEGLU | 25.4 |
| **SwiGLU** | **25.3** |

**差距很小**，但 SwiGLU 略优。可能原因：
- Swish 在 x=0 附近的"小幅负值穿透"特性更优
- Swish 的计算更简单（无 tanh 近似），数值更稳

实际上**任何 GLU 变体都比非门控胜**，选 SwiGLU 是个微调而非根本性差异。

### 🔥 高频追问 Top 3

**Q：SwiGLU 的中间维度为什么是 8d/3 而不是 4d？**

A：为了**保持参数量**与普通 FFN 一致。普通 FFN 用 2 个矩阵 $d \to 4d \to d$，参数量约 $8d^2$。SwiGLU 用 3 个矩阵 $d \to h \to d, d \to h$，参数量 $3dh$。设 $3dh = 8d^2$ 得 $h = 8d/3$。这是 LLaMA 的标准设置——既享受 SwiGLU 的效果，又不增加参数。

**Q：SwiGLU 计算量真的没增加吗？**

A：**实际有小幅增加**，因为多算了一次矩阵乘 + 一次 Hadamard 积。但是 LLaMA 在量化、Flash Attention 等其他优化下，整体推理速度仍然胜过 GELU 版本。**参数量是持平了，FLOPs 略多但可接受**。

**Q：门控机制是不是和 Transformer 整体哲学冲突？Attention 已经是门控了。**

A：很好的问题。Attention 是 **token 间** 的门控（决定看谁），SwiGLU 是 **feature 间** 的门控（决定哪些维度强化哪些削弱）。**两者维度正交**——一个是"横向通信的选择"，一个是"纵向加工的选择"。所以不冲突，反而互补。

### ⚠️ 常见陷阱

1. **不要说"SwiGLU 是更好的激活函数"**——它是 FFN 结构层面的改进，不是单纯激活函数
2. **要点出"门控引入二次交互"**——这是核心理论支撑
3. **要懂"8d/3"这个数字**——面试官追问"参数量怎么对齐"时的答案

### 🏢 大厂偏好

- **DeepSeek / Mistral / 字节**：会让你完整写出 SwiGLU + 中间维度的公式
- **Meta**：会从 Shazeer 论文角度问理论解释

### 📚 延伸阅读
- [GLU Variants Improve Transformer](https://arxiv.org/abs/2002.05202) - Noam Shazeer 的关键论文

---

## Q7：DeepNorm 等深层网络稳定化方案 ⭐⭐⭐

### 🎯 一句话标答

> DeepNorm 是为了**让 1000 层 Transformer 也能稳定训练**——在 Post-Norm 的残差上**加一个常数缩放因子 α**，配合**特定初始化系数 β**，理论保证梯度有界。

### 🗣️ 30 秒口语版

"DeepNorm 是微软 2022 年的工作，要解决一个特定问题——**怎么训 1000 层的 Transformer**。

我们前面说 Pre-Norm 稳但深度递减、Post-Norm 表达力强但崩。DeepNorm 是个'回到 Post-Norm 但加稳定化'的方案——

它的核心是**两个常数**：
- α 加在残差上：`x_{l+1} = LN(α·x_l + F(x_l))`
- β 加在初始化上：FFN 和 attention 的某些矩阵初始化时乘 β

α 和 β 是根据**层数 N** 算出的，理论推导出 α·β = 常数，保证梯度方差有界。

实测能把 1000 层 Transformer 训稳定，性能也胜过同参数量的浅模型。不过工业上**1000 层是过深的**——大家更倾向于 100-200 层 + 多 head + 大 hidden dim，所以 DeepNorm 知名度不高。但它代表了一类'返回 Post-Norm + 精细初始化'的研究路线。"

### 📐 DeepNorm 公式

$$x_{l+1} = \text{LN}(\alpha \cdot x_l + F(x_l))$$

其中 F 是子层（attention 或 FFN）。

**系数推导**（针对 Encoder-Decoder 模型）：

| 模型类型 | α | β |
|---|---|---|
| Encoder | $(2N)^{1/4}$ | $(8N)^{-1/4}$ |
| Decoder | $(2N)^{1/4}$ | $(8N)^{-1/4}$ |

其中 N 是 Encoder 或 Decoder 的层数。初始化时 FFN 和 V、O 投影矩阵的标准差乘以 β。

直觉解释——
- α > 1，让残差**主导**前向；
- β < 1，让子层的**初始贡献小**，避免冲击残差路径；
- 两者配合保证梯度方差等于 1。

### 🔥 高频追问 Top 3

**Q：DeepNorm 和 Pre-Norm 比，本质区别是什么？**

A：**DeepNorm 是改进的 Post-Norm**，Pre-Norm 是另一条路。DeepNorm 通过"放大残差 + 缩小子层初始化"来稳定，保留了 Post-Norm 的表达力优势；Pre-Norm 通过"恒等路径"稳定，牺牲表达力。两者哲学不同——一个"修补"Post-Norm，一个"绕开"Post-Norm。

**Q：为什么工业界没大规模用 DeepNorm？**

A：因为 **Pre-Norm + RMSNorm 已经够用**。1000 层模型在工业上不是主流——投资回报递减，宽而浅（更多 head、更大 hidden）+ MoE 比深而窄更好。DeepNorm 的价值在学术意义上证明了"超深网络也能训"，但实际部署没必要。

**Q：还有别的深度稳定方案吗？**

A：有几条线——
- **DeepNet**（DeepNorm 提出文）：1000 层 Encoder-Decoder
- **Sandwich-Norm / Hybrid-Norm**：子层前后都 LN
- **ReZero / Skip Init**：在残差上加可学习的缩放系数 α，初始化为 0，让网络从"恒等映射"开始学
- **Fixup Init**：用特殊初始化代替归一化

工业主流还是 Pre-Norm + RMSNorm，其他都是研究性质。

### 🏢 大厂偏好

- 这题是**加分题**，不是必问。能答出来体现你跟进文献。

### 📚 延伸阅读
- [DeepNet: Scaling Transformers to 1,000 Layers](https://arxiv.org/abs/2203.00555)

---

## Q8：QK-Norm 与新型归一化 ⭐⭐⭐

### 🎯 一句话标答

> QK-Norm 是**对 Q 和 K 分别做归一化再算 attention**——主要为了**避免 attention 分数爆炸**，在长上下文和大模型训练中越来越重要。

### 🗣️ 30 秒口语版

"QK-Norm 是 ViT-22B 和后续一些工作中流行的 trick。

普通 attention 是 `softmax(QKᵀ/√d_k) · V`，但训大模型时发现——**Q、K 的某些维度可能数值过大**，导致 QKᵀ 的某些分数极高，softmax 退化成 one-hot，attention 模式僵化。

QK-Norm 的做法是**对 Q 和 K 在 hidden 维度上做归一化**：

`attention = softmax(LN(Q) · LN(K)ᵀ · t) · V`

其中 t 是温度参数（替代 √d_k）。这样 Q、K 的尺度被强行控制，attention 分数稳定。

**应用场景**：
- 超大模型（ViT-22B、Chameleon 等）
- 长上下文场景（避免长序列下 attention 异常）
- 多模态对齐（不同模态的 Q、K 尺度差异大时）

不过现在 LLaMA、DeepSeek 等还没用，是个**正在普及中的 trick**。"

### 📐 QK-Norm 公式

标准 attention：
$$\text{Attn}(Q, K, V) = \text{softmax}\left(\frac{QK^T}{\sqrt{d_k}}\right) V$$

QK-Norm：
$$\text{Attn}(Q, K, V) = \text{softmax}\left(\text{LN}(Q) \cdot \text{LN}(K)^T \cdot t\right) V$$

或者用 RMSNorm 版：
$$\text{Attn} = \text{softmax}\left(\text{RMSNorm}(Q) \cdot \text{RMSNorm}(K)^T \cdot t\right) V$$

t 通常是可学习的标量（每个头独立），或者固定为 $\sqrt{d_k}$。

### 🔥 高频追问 Top 3

**Q：QK-Norm 解决了什么问题？**

A：**Attention 分数爆炸**。在大模型训练中，Q 或 K 的某些维度可能学到很大的值（比如 100+），QKᵀ 累加后变成 1000+，softmax 后这一对就是 1，其他全是 0，attention 退化成 hard selection。QK-Norm 通过先归一化 Q、K，把它们限制在合理范围，分数稳定。

**Q：为什么不直接归一化 attention 分数？**

A：可以，但 QK-Norm 更"前置"——在算分数之前就解决。直接归一化分数（比如除以 max 或 LN）会引入 token 间的耦合，破坏 attention 的并行性。QK-Norm 是**每个 token 内独立**的归一化，不破坏并行结构。

**Q：QK-Norm 是新发明吗？**

A：思想不新——CLIP 等模型早就在做"特征向量归一化"。但**在 Transformer attention 内显式应用**是 ViT-22B（2023）开始流行的。最近被很多大模型借鉴，比如 Chameleon、部分 Gemini 版本。LLaMA、DeepSeek 还没采用，可能是因为它们当前规模下还没遇到严重的 attention 爆炸问题。

### ⚠️ 常见陷阱

- 不要把 QK-Norm 和"温度系数"等同——QK-Norm 是归一化，温度是缩放系数，常配合使用但本质不同

### 🏢 大厂偏好

- **OpenAI / Anthropic / Google**：会问"如果让你训 100B+ 模型，怎么稳 attention"，引到 QK-Norm

### 📚 延伸阅读
- [Scaling Vision Transformers to 22 Billion Parameters](https://arxiv.org/abs/2302.05442) - ViT-22B，QK-Norm 来源
- [Query-Key Normalization for Transformers](https://arxiv.org/abs/2010.04245) - 早期工作

---

## 📝 本章小结

| 关键点 | 你必须能脱口而出 |
|---|---|
| LN vs BN | NLP 序列变长，BN 统计不稳，LN 单样本归一化 |
| LN 公式 | 沿 feature 维减均值除标准差，γ、β 缩放偏移 |
| RMSNorm | 砍掉减均值，只除 RMS，参数减半，速度快 7-20% |
| Pre-Norm 优势 | 残差是裸 x 的恒等路径，深层训练稳定 |
| Post-Norm 劣势 | 残差被 LN 包裹，深层梯度衰减，需 warm-up |
| ReLU→GELU→SwiGLU | 硬截断→平滑→门控，表达力递增 |
| SwiGLU 中间维度 | 8d/3，保持参数量与普通 FFN 持平 |
| GLU 门控本质 | Hadamard 积引入二次特征交互 |
| DeepNorm | 改进版 Post-Norm，专为 1000 层设计 |
| QK-Norm | 在 attention 内对 Q、K 归一化，防止分数爆炸 |

## ✅ 自测题

1. 现场写出 LayerNorm 和 RMSNorm 的公式，并解释"减均值"为什么可以省
2. 解释 Pre-Norm 训练稳定的原因，写出梯度回传的形式
3. 写出 SwiGLU 的 FFN 完整公式，并说明为什么 LLaMA 用 8d/3 作为中间维度
4. 如果你训了一个 32 层的 Transformer，loss 在第 5 个 epoch 突然炸了，你会从归一化和激活角度排查哪些问题？
5. DeepNorm 和 Pre-Norm 的核心区别是什么？工业上为什么 Pre-Norm 胜出？

---

> **下一章预告**：第 4 章｜分词器——BPE / WordPiece / SentencePiece 的算法细节，**中文分词的特殊问题**（字符 vs 字节 vs 词），以及 LLaMA 选 SentencePiece 的工程考量。这部分容易被忽视但**是工业落地的关键环节**——分词决定了模型对中文的友好度。
