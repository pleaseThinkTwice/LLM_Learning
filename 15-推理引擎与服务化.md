# 第 15 章｜推理引擎与服务化

> **本章定位**：**第三部分的收官**。第 12-14 章讲的是**单点技术**（PagedAttention、量化、投机解码），本章讲**怎么把它们装进一个能扛线上流量的系统**。
>
> 本章有一条贯穿的主线：**"三角权衡"——TTFT、TPOT、吞吐，三个目标互相拉扯，你不可能同时最优**。前面每一章的技术，本质上都是在这个三角里选一个方向：
> - **Chunked Prefill** 换 TPOT，牺牲一点 TTFT
> - **Prefix Caching** 换 TTFT，不动 TPOT
> - **大 batch** 换吞吐，牺牲 TTFT 和 TPOT
> - **投机解码** 换 TPOT，牺牲吞吐
> - **W4A16** 换 TPOT（小 batch），牺牲吞吐（大 batch）
>
> 而 **PD 分离** 是唯一一个试图**跳出这个三角**的方案——它把"全局妥协"变成"**可分配的资源**"。
>
> 本章还会引入一个比"吞吐"更正确的指标：**Goodput（满足 SLA 的吞吐）**。理解了它，你就理解了为什么大厂要做 PD 分离。

> **配套章节**：Prefill/Decode 两阶段与 Roofline → 第 12 章 / 量化选型 → 第 13 章 / 投机解码 → 第 14 章 / MoE 架构 → Frontier Topics / MLA → 第 2 章

---

## 📌 本章导航

| 序号 | 题目 | 难度 | 频率（字节/阿里/Meta/DeepSeek） | 类型 |
|---|---|---|---|---|
| Q1 | **性能指标与三角权衡（含 Goodput）** | ⭐⭐⭐⭐⭐ | **8 / 8 / 6 / 5** | 必问 |
| Q2 | **FlashAttention 全家桶与 FlashDecoding** | ⭐⭐⭐⭐⭐ | **8 / 6 / 6 / 6** | 必问·要推导 |
| Q3 | **vLLM / SGLang / TensorRT-LLM 选型** | ⭐⭐⭐⭐⭐ | **8 / 7 / 4 / 5** | 必问·实战 |
| Q4 | **推理侧并行：TP / PP / EP / DP** | ⭐⭐⭐⭐⭐ | **7 / 7 / 6 / 6** | 必问 |
| Q5 | **PD 分离（Disaggregated Serving）** | ⭐⭐⭐⭐⭐ | **7 / 6 / 5 / 7** | 必问·前沿 |
| Q6 | **MoE 推理部署与超大 EP** | ⭐⭐⭐⭐ | 6 / 5 / 4 / **8** | 进阶·DeepSeek 必问 |
| Q7 | **容量规划：需要几张卡** | ⭐⭐⭐⭐ | 7 / 7 / 5 / 4 | 实战·要算数 |
| Q8 | **系统设计实战** | ⭐⭐⭐⭐⭐ | **8 / 7 / 6 / 5** | 压轴 |

---

## Q1：性能指标与三角权衡 ⭐⭐⭐⭐⭐（必问）

### 🎯 一句话标答

> 四个核心指标：**TTFT**（首 token 延迟 = 排队 + Prefill）、**TPOT/ITL**（每 token 延迟，决定"打字速度"）、**E2E** = TTFT + TPOT × 输出长度、**吞吐**（token/s/GPU，决定成本）；三者构成**不可能三角**——**增大 batch 提吞吐，但同时恶化 TTFT 和 TPOT**；而**"吞吐"本身是个会骗人的指标**，正确的目标是 **Goodput = 满足 SLA 的请求吞吐**——因为**把 batch 堆到 256 拿到的超高吞吐，如果每个用户都超时了，有效吞吐是 0**。

### 🗣️ 30 秒口语版

"面试问'推理延迟'时，**主动把它拆开**是专业度的第一个信号。

**四个指标：**

**TTFT（Time To First Token）** = **排队时间 + Prefill 时间**
- 用户感知："我按了回车，多久看到第一个字"
- **注意排队时间**——很多人只算 prefill，但线上高峰期**排队往往是大头**
- 人类感知阈值：**< 200ms 感觉即时**，> 1s 开始烦躁

**TPOT（Time Per Output Token）/ ITL（Inter-Token Latency）** = decode 每步的时间
- 用户感知："字往外蹦得快不快"
- **两者的区别很重要**：**TPOT 是平均值**（总生成时间 ÷ token 数），**ITL 是逐个 token 的分布**
- **只看 TPOT 平均值会漏掉 generation stall**！第 12 章 Q6 说的"长 prefill 插进来卡半秒"——平均 TPOT 可能还是 8ms 很好看，但 **ITL 的 P99 是 500ms**，用户明确感觉到了卡顿
- **所以要看 ITL 的 P99，不是 TPOT 的均值**

**E2E Latency** = TTFT + TPOT × 输出长度
- **注意**：输出 500 token 时，TPOT 被**放大 500 倍**。所以 **TPOT 优化 1ms = E2E 优化 500ms**——TPOT 的杠杆比 TTFT 大得多

**吞吐（Throughput）** = output token/s/GPU
- **这是成本指标**：直接决定"每百万 token 多少钱"

**三角权衡（这是本题的核心）：**

$$\text{batch} \uparrow \Rightarrow \begin{cases} \text{吞吐} \uparrow & (\text{权重读取被摊薄，第 12 章 Roofline}) \\ \text{TPOT} \uparrow & (\text{每步算更多，且 attention 部分不被摊薄}) \\ \text{TTFT} \uparrow & (\text{排队更久}) \end{cases}$$

**你不可能同时最优。** 前面几章的每个技术，都是在这个三角里选一个方向：

| 技术 | TTFT | TPOT | 吞吐 |
|---|---|---|---|
| 大 batch | ❌ | ❌ | ✅✅ |
| Prefix Caching | ✅✅ | — | ✅ |
| Chunked Prefill | ⚠️ 略降 | ✅✅ | ✅ |
| 投机解码 | — | ✅✅ | ❌（大 batch 时） |
| W4A16 量化 | ❌ | ✅✅ | ⚠️ 看 batch |
| **PD 分离** | ✅ | ✅ | ✅ | ← **唯一跳出三角的** |

**最重要的概念：Goodput（有效吞吐）。这是 DistServe 论文提出的，也是这题的题眼。**

**"吞吐"是个会骗人的指标**。我可以把 batch 堆到 512，拿到一个 **10 倍**于别人的吞吐数字——但：
- TTFT 变成 5 秒（排队排死了）
- TPOT 变成 200ms（每步算 512 条）
- **每个用户都超时了 → 这个吞吐一文不值**

$$\boxed{\text{Goodput} = \text{每秒完成的、且<u>同时满足 TTFT SLA 和 TPOT SLA</u> 的请求数}}$$

**这才是你真正该优化的东西。** 它把"延迟"和"吞吐"统一成了**一个**目标——而不是两个互相打架的目标。

**举个例子说明差距**：
```
配置 A（大 batch）: 吞吐 5000 tok/s，但 TTFT P99 = 4s（SLA 要求 < 1s）
                   → 90% 的请求违约 → Goodput ≈ 500 tok/s ❌

配置 B（小 batch）: 吞吐 2000 tok/s，TTFT P99 = 0.6s
                   → 全部满足 → Goodput = 2000 tok/s ✅

【吞吐】A 是 B 的 2.5 倍；【Goodput】B 是 A 的 4 倍。
```

**面试时能主动提出 Goodput，是这题最强的信号**——它说明你知道"优化吞吐"这个目标本身就是错的。"

### 📐 指标的精确定义与陷阱

```
时间轴：
  t0        t1              t2      t3      t4    ...    tn
  │─排队──→│──── Prefill ──→│─decode→│─decode→│  ...  │
  请求到达                 首token   第2个    第3个        最后一个
            
  TTFT = t2 - t0  ← ★包含排队！很多人只算 t2-t1，是错的
  ITL_i = t_{i+1} - t_i   ← 逐个的分布，要看 P99
  TPOT = (tn - t2) / (n - 1)   ← 平均值，会掩盖 stall
  E2E  = tn - t0
```

**三个必须说清的陷阱**：

**陷阱 1：TTFT 必须包含排队时间。**
线上高峰期，一个请求可能排队 2 秒、prefill 只要 100ms。如果你的监控只测 prefill，**你会觉得系统很健康，而用户在骂街**。

**陷阱 2：只看 TPOT 均值会漏掉 stall。**
```
无 chunked prefill 时的 ITL 序列（ms）：
  8, 8, 8, 8, ★520★, 8, 8, 8, 8, 8, 8, ★480★, 8, 8, ...
  
  TPOT 均值 = 8 × 12 / 14 + 1000/14 ≈ 78ms   ← 看起来"还行"
  ITL P99   = 520ms                           ← ★真相在这里★
```
**用户不会感知"平均"，用户感知的是"卡了一下"。**

**陷阱 3：平均输入/输出长度会骗人。**
真实流量的长度分布是**长尾**的：
```
输入长度：中位数 200，均值 800，P99 = 8000   ← 均值被长尾拉高，且远离中位数
输出长度：中位数 150，均值 400，P99 = 2000
```
**用均值做容量规划会严重低估峰值压力**（Q7 详谈）。

### 🔍 人类感知的锚点（要能报数）

| 指标 | 阈值 | 依据 |
|---|---|---|
| **TTFT** | **< 200ms** = 即时；< 1s = 可接受；> 2s = 流失 | 人机交互的经典阈值（Nielsen） |
| **TPOT** | **< 150ms**（≈ 7 tok/s）= 跟得上阅读 | 人的阅读速度 ≈ 250 词/分 ≈ 4 词/s ≈ **6-7 token/s** |
| **TPOT** | **< 50ms**（≈ 20 tok/s）= 流畅 | 明显快于阅读速度，"不用等" |
| **ITL P99** | **< 200ms** | 超过这个用户会感知到"卡顿" |

> 🚨 **但 Agent 场景完全不同——这是个重要的辨析，也是应用岗的加分点。**
>
> Agent 的输出**不是给人看的，是给程序看的**：
> - 工具调用的 JSON → 解析器读
> - 中间的 CoT → 有时候根本不展示
>
> **所以 Agent 场景不关心"流式体验"，只关心 E2E！**
>
> **推论（很实用）**：
> 1. Agent 场景**可以接受大 batch**（TPOT 差一点无所谓，反正没人盯着看）→ **应该优先优化吞吐/成本**
> 2. 但 Agent 是**多轮**的——**E2E 会被放大 N 倍**（10 轮工具调用 = 10 × E2E）→ **总延迟仍然敏感**
> 3. **Agent 的正确优化目标是 E2E，不是 TPOT** → 这直接改变了技术选型：**投机解码的价值下降**（它优化 TPOT），**Prefix Caching 的价值上升**（它优化 TTFT，而 Agent 每轮都在重复前缀！）
>
> **"C 端聊天优化 TPOT，Agent 优化 E2E 和 TTFT"** —— 这句话能说出来，说明你想过自己的业务场景。

### 🔥 高频追问 Top 3

**Q：怎么找到系统的"饱和点"？**

A：**压测画一条曲线，找那个拐点。这是必须会的实操。**

固定输入/输出长度分布，**逐步增大并发**，画三条线：

```
   吞吐(tok/s)              延迟(ms)
      ▲                        ▲
      │      ┌──────────       │              ╱  ← 延迟开始垂直飙升
      │    ╱                   │            ╱
      │  ╱                     │      ╱────
      │╱                       │──────
      └──────────────► 并发     └──────────────► 并发
              ↑                        ↑
           饱和点                    同一个点
```

**饱和点的定义**：**吞吐不再增长（曲线拐平），但延迟开始垂直上升**的那个并发数。

**物理含义**（回到第 12 章）：
- **饱和点之前**：memory-bound，加并发 = 摊薄权重读取 = **白赚吞吐**
- **饱和点之后**：要么 **compute-bound 了**（算术强度到 150 了），要么 **KV Cache 满了开始抢占**（第 12 章 Q4 追问 1）——**后者更常见**
- 再加并发，**只是在排队**，不产生任何额外吞吐，纯粹恶化延迟

**实践**：
- **`max_num_seqs` 应该设在饱和点附近**（略低一点留余量）
- **超过饱和点的请求应该在网关层拒绝或排队**，而不是塞给 GPU 让它抢占
- **饱和点会随输入长度变化**——长 prompt 的饱和点更低（KV Cache 更早满）

**Q：P50 / P95 / P99 该看哪个？**

A：**看 P99，但要理解为什么。**

| | 含义 | 什么时候看 |
|---|---|---|
| **P50（中位数）** | "典型体验" | **容量规划**（算平均成本） |
| **P95** | 大部分人的最差体验 | 一般 SLA |
| **P99** | **尾延迟** | **★真实的用户体验★** |
| **均值** | ⚠️ **被长尾污染，最没用** | 别用 |

**为什么 P99 才是真相**：
1. **一个用户会发很多次请求**。10 轮对话，每轮 1% 的概率遇到 P99 → **这个用户有 $1-0.99^{10} = 10\%$ 的概率遇到糟糕体验**。**P99 的坏事，在用户尺度上是 P90 的事。**
2. **Agent 场景更极端**：一个 Agent 任务 = 20 次 LLM 调用 → **$1-0.99^{20} = 18\%$ 的概率至少踩一次 P99**。而 Agent 是**串行**的——**一次慢，整个任务都慢**。
3. **尾延迟往往是 bug 的信号**：抢占、swap、OOM 重试、GC——这些都藏在 P99 里，P50 完全看不到。

**"P99 在单次请求上是 1%，在多轮会话上是 10%，在 Agent 任务上是 18%"** —— 这个换算能说出来，很有说服力。

**Q：为什么不能只优化吞吐？**

A：**因为吞吐是<u>可以作弊</u>的指标。**

我给你演示怎么"提升 10 倍吞吐"：
```python
llm = LLM(model="...", max_num_seqs=512)   # batch 堆到 512
# 吞吐：从 2000 → 8000 tok/s，涨了 4 倍！🎉
# TTFT P99：从 0.5s → 6s        （排队排死）
# ITL  P99：从 20ms → 300ms     （每步算 512 条）
# → 所有用户都超时了 → 真实价值 = 0 ❌
```

**吞吐这个指标的问题**：它**只关心 GPU 产出了多少 token，不关心这些 token 有没有及时到达用户**。

**这就是 Goodput 存在的理由**：
$$\text{Goodput} = \text{每秒完成的、} \underbrace{\text{TTFT} \le \text{SLA}_1}_{\text{首字够快}} \text{ 且 } \underbrace{\text{TPOT} \le \text{SLA}_2}_{\text{打字够快}} \text{ 的请求数}$$

**它把三角权衡变成了一个单一目标** —— 这在工程上是巨大的简化：你不再需要"在延迟和吞吐之间找平衡"，你只需要**最大化一个数**。

> **Goodput 是 DistServe 论文（Q5）的核心动机**：他们发现，混合部署下**为了满足 TTFT SLA 要小 batch、为了满足 TPOT SLA 也要小 batch** —— **两个约束互相叠加，把 goodput 死死压住**。而 **PD 分离让两个 SLA 可以<u>独立</u>优化** → goodput 提升 **4.48×**（SLA 严格时 **10.2×**）。
>
> **"分离之所以有效，是因为它解开了两个 SLA 的耦合"** —— 这是 Q5 的题眼，在这里先埋下。

### ⚠️ 常见陷阱

1. **TTFT 不算排队时间** —— 线上高峰期排队是大头，漏了就是自欺欺人。
2. **只看 TPOT 均值** —— **看不到 generation stall**。要看 **ITL 的 P99**。
3. **用平均长度做规划** —— 真实分布是**长尾**的。
4. **只报吞吐不报延迟** —— **吞吐可以靠堆 batch 作弊**。
5. **不知道 Goodput** —— 这是这题最重要的概念。
6. **对 Agent 场景优化 TPOT** —— **Agent 该优化 E2E 和 TTFT**，因为输出不给人看，但轮次会放大延迟。

### 🏢 大厂偏好

- **所有大厂**：**必问**，且会问"你们线上的 SLA 是多少"
- **字节 / 阿里**：会问饱和点怎么找、`max_num_seqs` 怎么设
- **Meta**：会深挖 P99 和 SLA 分级
- **应用岗**：**主动提 Goodput 是最强信号**

### 📚 延伸阅读
- [DistServe](https://arxiv.org/abs/2401.09670) - **Goodput 概念的出处，本章必读**
- [LLM Inference Performance Engineering: Best Practices](https://www.databricks.com/blog/llm-inference-performance-engineering-best-practices) - Databricks 的实践指南

---

## Q2：FlashAttention 全家桶与 FlashDecoding ⭐⭐⭐⭐⭐（必问，要推导）

### 🎯 一句话标答

> 标准 attention 要把 $[N,N]$ 的矩阵**实例化到 HBM**（$N$=8K 时单个 head 就是 128 MB），而 attention 本身是 **memory-bound** 的——**FlashAttention 用 Tiling 把计算留在 SRAM 里**（SRAM 带宽 ~19 TB/s，是 HBM 的 10 倍），靠 **Online Softmax** 增量地 rescale 来避免全局归一化，靠 **重算**（而非存储）解决反向传播——把 HBM 访问从 $O(N^2)$ 降到 $O(N^2 d/M)$；而 **Decode 阶段 Q 只有 1 行、$[N,N]$ 根本不存在**，所以 FlashAttention 那套没意义，真正的瓶颈是**并行度不够**（batch×head < SM 数），因此有了 **FlashDecoding——切 KV 的序列维度并行，再用 log-sum-exp 归约合并**。

### 🗣️ 30 秒口语版

"这题要分两半：**Prefill 的 FlashAttention** 和 **Decode 的 FlashDecoding**——**它们解决的是完全不同的问题**，这是第 12 章 Q1 追问 3 埋下的伏笔。

**第一半：FlashAttention（给 Prefill 用）**

**问题**：标准 attention 的三步——
```
S = Q @ K.T        # [N, N]  ← ★写进 HBM★
P = softmax(S)     # [N, N]  ← ★读出来、算完、再写回 HBM★
O = P @ V          # [N, d]  ← ★又读一遍★
```
$N=8192$ 时，$[N,N]$ 的 fp16 矩阵是 **128 MB**——**一个 head 就 128 MB**！× 32 heads × batch，直接爆炸。

**关键认知**：attention **本身就是 memory-bound 的**。三次 HBM 往返，而计算量很小。**瓶颈不是算，是搬。**

**FlashAttention 的三招**：

**① Tiling（分块）**：把 Q、K、V 切成小块，**加载到 SRAM 里算**。
- A100 每个 SM 有 **192 KB SRAM**，带宽 **~19 TB/s**（**HBM 的 10 倍**！）
- 在 SRAM 里算完一块的 $S$、softmax、乘 $V$，**只把 $[N,d]$ 的输出写回 HBM**
- **$[N,N]$ 的矩阵从来没有在 HBM 里存在过**

**② Online Softmax（这是数学难点）**：
Tiling 的障碍是——**softmax 需要全局的 max 和 sum**，但你只有一块！

解法：**增量地维护 running max 和 running sum，每来一块就 rescale 之前的结果**：
$$m^{(i)} = \max(m^{(i-1)}, \max(S_i)), \quad \ell^{(i)} = e^{m^{(i-1)}-m^{(i)}}\ell^{(i-1)} + \textstyle\sum e^{S_i - m^{(i)}}$$
$$O^{(i)} = e^{m^{(i-1)}-m^{(i)}} O^{(i-1)} + e^{S_i-m^{(i)}} V_i$$

那个 $e^{m^{(i-1)}-m^{(i)}}$ 就是**修正因子**——新来的块如果有更大的 max，就把**之前累积的结果按比例缩小**。**数学上完全等价，不是近似。**

**③ Recomputation（反向传播）**：
不存 $[N,N]$，**反向时重算**。用前向存下的 $(m, \ell)$（只有 $O(N)$）可以快速重建。

**为什么重算是"免费"的？** 因为 attention 是 **memory-bound**——**重算消耗的是闲置的算力，省下的是稀缺的带宽**。**用算力换访存，在 memory-bound 场景永远是赚的。**

**结果**：HBM 访问 $O(N^2) \to O(N^2 d / M)$（$M$ = SRAM 大小），**内存 $O(N^2) \to O(N)$**，加速 2-4×。

**FA2**：三个改进——
1. **减少非 matmul 的 FLOPs**（tensor core 的 matmul 吞吐是 non-matmul 的 **16 倍**！所以哪怕 non-matmul 只占 1% 的 FLOPs，也可能占 **十几%** 的时间）
2. **并行 sequence 维度**（FA1 只并行 batch×head，**batch×head < SM 数时 GPU 空转**）
3. **更好的 warp 分工**（FA1 是 split-K 要跨 warp 通信，FA2 是 split-Q 各 warp 独立）
→ 达到 A100 峰值的 **50-73%**

**FA3**（Hopper 专用）：
1. **Warp specialization + 异步**（TMA + WGMMA 的 producer-consumer 模式，**搬数据和算重叠**）
2. **交错 matmul 和 softmax**（softmax 用不了 tensor core，**把它藏在 matmul 后面**）
3. **FP8 + 借鉴 QuIP 的 Hadamard 变换**处理 outlier（**又是第 13 章的等价变换！**）
→ H100 上 **1.5-2× FA2**，达到 **75% 利用率**

**第二半：FlashDecoding（给 Decode 用）**

**Decode 的情况完全不同**：$Q$ 只有 **1 行**！$[1, N]$ 的 attention。
- **$[N,N]$ 矩阵根本不存在** → **FlashAttention 的 tiling 没有意义**
- **真正的问题：并行度不够！**
  - FA2 并行的是 **batch × head × seq_Q**，但 **seq_Q = 1**！
  - batch=1、32 个 head → **只有 32 个并行单元**
  - **A100 有 108 个 SM** → **70% 的 SM 在睡觉** ❌

**FlashDecoding 的解法：切 KV 的序列维度。**
把 32K 的 KV Cache 切成 8 段，**每段独立算一个 partial attention**（各自带自己的 $m$、$\ell$），最后用 **log-sum-exp 归约**合并。

$$32 \text{ heads} \times 8 \text{ splits} = 256 \text{ 个并行单元} > 108 \text{ SM} \quad\checkmark$$

**长上下文 decode 快 8×。**

**一句话总结（这是这题的题眼）**：
> **Prefill 用 FlashAttention（问题是 $[N,N]$ 太大 → tiling），Decode 用 FlashDecoding（问题是并行度不够 → split-KV）。同一个 attention，两个完全相反的问题。**

vLLM 的 `paged_attention_v2`、FlashInfer 干的就是后者。"

### 📐 Online Softmax 的完整推导

**目标**：不看全部数据，增量地算 softmax。

**朴素 softmax**（需要全局 max）：
$$\text{softmax}(x)_i = \frac{e^{x_i - m}}{\sum_j e^{x_j - m}}, \quad m = \max_j x_j$$
（减 $m$ 是为了数值稳定，防止 $e^{x}$ 溢出）

**问题**：$m$ 和分母都需要**看完全部** $x$。

**Online 版本**：设已处理了前 $i-1$ 块，维护 $(m^{(i-1)}, \ell^{(i-1)}, O^{(i-1)})$。来了第 $i$ 块 $S_i$：

**Step 1：更新 max**
$$m^{(i)} = \max\big(m^{(i-1)},\ \max(S_i)\big)$$

**Step 2：更新分母**（关键——旧的 $\ell$ 是基于旧 max 算的，要**修正**）
$$\ell^{(i)} = \underbrace{e^{m^{(i-1)} - m^{(i)}}}_{\text{修正因子}} \cdot \ell^{(i-1)} + \sum_j e^{S_{ij} - m^{(i)}}$$

**为什么这个修正因子对？**
$$\ell^{(i-1)} = \sum_{\text{old}} e^{x - m^{(i-1)}} \ \Longrightarrow\ e^{m^{(i-1)}-m^{(i)}}\ell^{(i-1)} = \sum_{\text{old}} e^{x - m^{(i-1)}} \cdot e^{m^{(i-1)}-m^{(i)}} = \sum_{\text{old}} e^{x-m^{(i)}} \quad\checkmark$$
**旧的和被"换算"到了新的 max 基准上。** 因为 $m^{(i)} \ge m^{(i-1)}$，修正因子 $\le 1$——**旧的贡献被按比例缩小**。

**Step 3：更新输出**（同样的修正逻辑）
$$O^{(i)} = e^{m^{(i-1)}-m^{(i)}} \cdot O^{(i-1)} + e^{S_i - m^{(i)}} V_i$$

**Step 4：最后归一化**
$$O = \frac{O^{(N)}}{\ell^{(N)}}$$

> 🚨 **这个推导是 FlashAttention 的全部数学**。**它是<u>精确</u>的，不是近似** —— 每一步都严格等于全局 softmax 的对应部分。
>
> **更重要的是：它是可复用的思想。** 你已经在两个地方见过它了：
> - **第 12 章 Q4**：PagedAttention 分块处理 KV，**也要用 online softmax 合并**
> - **本题的 FlashDecoding**：切 KV 并行，**也要用 online softmax（log-sum-exp）归约**
>
> **"只要你想把 attention 分块算，你就必须用 online softmax。"** 这是一条硬约束。

#### FlashDecoding 的归约

每个 split $s$ 算出局部结果 $(O_s, m_s, \ell_s)$，然后合并：

$$m = \max_s m_s, \qquad \ell = \sum_s e^{m_s - m}\ell_s, \qquad O = \frac{\sum_s e^{m_s-m} O_s}{\ell}$$

**就是 online softmax 的"并行版"** —— 不是顺序增量，而是**一次性归约所有 split**。数学完全一样。

### 🔍 FA 全家桶对比

| | **FA1** (2022) | **FA2** (2023) | **FA3** (2024) | **FlashDecoding** |
|---|---|---|---|---|
| 针对 | 通用（Prefill） | Prefill | Prefill (**Hopper 专用**) | **Decode** ✅ |
| 核心 | **Tiling + Online Softmax + 重算** | 减少 non-matmul FLOPs / **并行 seq** / warp 分工 | **异步 (TMA/WGMMA)** / 交错 / **FP8** | **切 KV 维度并行** |
| 解决的问题 | $[N,N]$ 太大 | **并行度 + non-matmul 开销** | 隐藏延迟 + 低精度 | **并行度不够**（seq_Q=1） |
| 利用率 | ~25-40% | **50-73%** (A100) | **75%** (H100) | — |
| 加速 | 2-4× | ~2× FA1 | **1.5-2× FA2** | **长上下文 8×** |

> **注意 FA2 的第一条"减少 non-matmul FLOPs"** —— 这是个很反直觉但极其重要的点：
>
> **A100 的 matmul（tensor core）吞吐是 312 TFLOPS，non-matmul（CUDA core）只有 19.5 TFLOPS —— 差 16 倍！**
>
> 所以哪怕 softmax 的 rescale 只占 **1% 的 FLOPs**，它也可能占 **$1\% \times 16 = 16\%$ 的时间**。**FA2 的一大半工作就是在"少做几次除法和 rescale"** —— 比如把中间的除法推迟到最后一次性做。
>
> **"在 tensor core 时代，non-matmul 操作的成本被放大了 16 倍"** —— 这个认知能解释很多 kernel 优化的动机，答出来很加分。

### 🔥 高频追问 Top 3

**Q：为什么说 attention 本身是 memory-bound 的？它不是有 $O(N^2 d)$ 的计算量吗？**

A：**算算术强度就知道了——这是第 12 章 Roofline 的又一次应用。**

标准 attention（不含 FlashAttention），序列长度 $N$、head_dim $d$：

| | FLOPs | HBM 访问（fp16） |
|---|---|---|
| $S = QK^\top$ | $2N^2d$ | 读 $2Nd$，**写 $N^2$** |
| $P=\text{softmax}(S)$ | $\sim 5N^2$ | **读 $N^2$，写 $N^2$** |
| $O = PV$ | $2N^2d$ | **读 $N^2$**，读 $Nd$，写 $Nd$ |
| **合计** | $\approx 4N^2 d$ | $\approx 4N^2 \times 2 = 8N^2$ 字节 |

$$\text{算术强度} = \frac{4N^2d}{8N^2} = \frac{d}{2} = \frac{128}{2} = \mathbf{64\ \text{FLOP/byte}}$$

**A100 的平衡点是 ~150** → **64 < 150 → memory-bound** ❌

**关键洞察**：算术强度**只和 $d$ 有关，和 $N$ 无关**！$N$ 在分子分母上**约掉了**。**所以序列再长，attention 也还是 memory-bound** —— 长度不解决问题。

**而 FlashAttention 之后**：$[N,N]$ 不落 HBM，访存变成 $O(Nd)$：
$$\text{算术强度} \approx \frac{4N^2d}{\sim 6Nd} = \frac{2N}{3}$$
**现在它和 $N$ 成正比了！** $N > 225$ 就 compute-bound 了。

$$\boxed{\text{FlashAttention 把 attention 从 memory-bound 变成了 compute-bound}}$$

**这是对 FlashAttention 最精确的一句话总结。** 它做的不是"加速计算"，而是"**把瓶颈从带宽转移到算力**"——而算力是我们有富余的那个。

**能算出这个 $d/2$ 并指出"和 $N$ 无关"，是这题的满分答案。**

**Q：FlashAttention 是近似算法吗？和 Linear Attention / 稀疏 attention 什么关系？**

A：**FlashAttention 是<u>精确</u>的，这是它最重要的性质，也是它赢的原因。**

这是个**必须分清的三分法**：

| | **FlashAttention** | Linear Attention (Performer, Linformer) | 稀疏 Attention (Longformer, BigBird) |
|---|---|---|---|
| 是否精确 | ✅ **完全精确**（bit-level 可能有浮点差异） | ❌ **近似** | ❌ **近似**（丢掉了部分连接） |
| 复杂度 | $O(N^2)$ **计算不变** | $O(N)$ | $O(N\sqrt N)$ 或 $O(N)$ |
| 优化的是 | **HBM 访问**（$O(N^2)\to O(N^2d/M)$） | **计算量** | **计算量** |
| 质量 | **无损** ✅ | 有损 | 有损 |
| **工业采用** | ✅ **绝对标配** | ❌ 基本没有 | ⚠️ 少数长文本场景 |

**为什么 FlashAttention 赢了、而 Linear Attention 输了？**

**因为它们优化错了东西。** Linear Attention 把计算从 $O(N^2)$ 降到 $O(N)$ —— 但 **attention 本来就不是 compute-bound 的**（追问 1 算过：算术强度 64 < 150）！**优化一个不是瓶颈的东西，等于没优化**，还赔上了质量。

$$\boxed{\text{"优化前先测瓶颈"—— Linear Attention 是这条铁律的反面教材}}$$

**FlashAttention 抓住了真正的瓶颈（HBM 访问），而且<u>不牺牲任何质量</u>** —— 所以它可以**无条件地**用在所有模型上，不需要重训、不需要评估掉点。**这就是它能成为标配的原因：零成本、零风险、纯收益。**

> **注意**：这条主线和第 13 章的"无损 vs 有损"是**同一个哲学**：
> - **无损优化**（FlashAttention、Chunked Prefill、投机解码、PagedAttention）→ **可以无脑上**
> - **有损优化**（量化、KV 驱逐、Linear Attention）→ **必须评估、必须权衡**
>
> **"先把所有无损的优化上满，再考虑有损的"** —— 这是推理优化的正确顺序，也是 Q8 系统设计题的答题框架。

**Q：既然 FlashAttention 这么好，为什么 vLLM 还要自己写 PagedAttention kernel？**

A：**因为内存布局不兼容——这是个很好的工程细节题。**

- **FlashAttention 假设 KV 是<u>连续</u>的**：`kv_cache[seq_id]` 直接就是一块连续的 $[N, d]$
- **PagedAttention 的 KV 是<u>分散</u>的**：要**先查 block table，再 gather**（第 12 章 Q4）

**所以 vLLM 必须写一个"**Paged 版的 FlashAttention**"** —— 它同时具备两个特性：
1. **分页的内存访问**（查 block table → gather block）
2. **Online softmax 的分块合并**（因为要分块处理，**必须**用 online softmax）

> **有意思的观察**：**PagedAttention 天然就要用 online softmax** —— 因为它本来就是分块的！所以说 "**PagedAttention = FlashAttention 的数学 + 分页的内存布局**"（第 12 章 Q4 提过），这句话在这里得到了印证。

**现在的生态**：
- **FlashInfer**（现在 vLLM/SGLang 的默认后端）—— 把 paged + flash + tree attention（第 14 章的投机解码！）**统一在一个库里**
- FlashAttention 官方也加入了 paged KV 的支持（`flash_attn_with_kvcache`）
- **两条线在合流**

**这题能答出"因为内存布局不兼容，所以要写 paged 版；现在 FlashInfer 把它们统一了"，说明你跟进了生态现状。**

### ⚠️ 常见陷阱

1. **说 FlashAttention 降低了计算复杂度** —— **没有！计算量还是 $O(N^2d)$，一点没少**。它降的是 **HBM 访问**。
2. **说它是近似算法** —— **完全精确**。这是它和 Linear Attention 的本质区别，也是它赢的原因。
3. **不知道 Decode 要用 FlashDecoding** —— **Prefill 和 Decode 的问题完全相反**（一个是 $[N,N]$ 太大，一个是并行度不够）。这是第 12 章 Q1 的延续。
4. **说不清 online softmax 的修正因子** —— 这是数学核心，要能推。
5. **不知道 non-matmul 的 16 倍惩罚** —— 这解释了 FA2 一大半的工作。

### 🏢 大厂偏好

- **字节 / 阿里（Infra、kernel）**：**必问，会让你推 online softmax**
- **Meta**：FlashAttention 生态深度使用者，会问版本差异
- **所有岗**：至少要能说清"**tiling + online softmax + 重算**"和"**它是精确的，优化的是访存**"

### 📚 延伸阅读
- [FlashAttention](https://arxiv.org/abs/2205.14135) - **原论文，本章必读**
- [FlashAttention-2](https://arxiv.org/abs/2307.08691)
- [FlashAttention-3](https://arxiv.org/abs/2407.08608) - Hopper 异步 + FP8
- [Flash-Decoding for long-context inference](https://crfm.stanford.edu/2023/10/12/flashdecoding.html) - 官方博客
- [Online normalizer calculation for softmax](https://arxiv.org/abs/1805.02867) - **online softmax 的原始出处（2018，比 FA 早 4 年）**

---

## Q3：vLLM / SGLang / TensorRT-LLM 选型 ⭐⭐⭐⭐⭐（必问·实战）

### 🎯 一句话标答

> **vLLM** = **生态之王**（PagedAttention 起家、模型支持最广、社区最大、上手最快）；**SGLang** = **Agent / 多轮 / 结构化输出之王**（**RadixAttention** 的树形前缀复用 + **jump-forward decoding** + 对 **DeepSeek MLA/大规模 EP 支持最好**）；**TensorRT-LLM** = **单卡极致性能之王**（编译式、kernel 融合最深、FP8 支持最好，但**编译慢、灵活性差**）；**三者在快速趋同**，选型的真实判据是"**你的场景 + 你的团队规模 + 你的模型更新频率**"，而不是 benchmark 数字。

### 🗣️ 30 秒口语版

"这题的正确姿态是"**分场景**"，**千万不要说'X 碾压 Y'**——三个引擎都在快速趋同，说碾压会显得没跟进。

**vLLM —— 默认选择**
- **起家**：PagedAttention（第 12 章 Q4）
- **V1 架构（2025 重写）**：多进程（EngineCore 独立进程，**把 Python 调度从关键路径上摘出去**）、**Prefix Caching 默认开且零开销**、**Chunked Prefill 默认开**、CPU-GPU overlap
- ✅ **模型支持最广**（新模型出来通常几天内支持）
- ✅ **社区最大**、文档最好、坑最少
- ✅ **OpenAI 兼容 API 开箱即用**
- ❌ 极致性能略逊 TRT-LLM
- **适合**：**90% 的团队。不知道选什么就选它。**

**SGLang —— Agent / 多轮的专家**
- **起家**：**RadixAttention**（第 12 章 Q7）——用**基数树**做前缀 KV 复用，比 vLLM 的 hash 方案**粒度更细**、且**天然支持树形结构**
- **杀手锏**：
  1. **树形前缀复用** → **Agent 的分支探索、多候选采样、多轮对话** 的命中率极高
  2. **Jump-forward decoding**（第 14 章 Q8）→ **结构化输出直接"填"，不用采样**
  3. **零开销 CPU 调度器**（调度和 GPU 计算完全 overlap）
  4. **一门 DSL**（SGLang 语言）—— 可以用代码表达复杂的 LLM 程序（分支、循环、并行），**引擎能看到你的程序结构，从而做全局优化**
- ✅ **DeepSeek 的首选引擎** —— 对 **MLA、大规模 EP、PD 分离** 支持最好（DeepSeek 官方推荐）
- **适合**：**Agent、多轮对话、结构化输出重、跑 DeepSeek**

**TensorRT-LLM —— 极致性能**
- **编译式**：要先 `build` 出一个 engine（**几十分钟**），然后加载运行
- ✅ **单卡性能最强**（NVIDIA 自家，kernel 融合到牙齿）
- ✅ **FP8 / INT4 支持最好**（自家硬件自家优化）
- ✅ 和 **Triton Inference Server** 深度集成（生产级的服务框架）
- ❌ **编译慢**——改个配置（batch、序列长度）就要**重新 build**
- ❌ **灵活性差**——engine 是"冻"死的
- ❌ **新模型支持慢**、部分闭源、**debug 困难**
- **适合**：**模型固定、追求极致成本、有 NVIDIA 支持的大团队**

**选型的真实判据（这才是重点）**：

| 你的情况 | 选 |
|---|---|
| 不知道选什么 / 团队小 / 模型经常换 | **vLLM** |
| Agent / 多轮 / 结构化输出重 / 跑 DeepSeek | **SGLang** |
| 模型固定几个月不换 / 极致压成本 / 有 NV 支持 | **TensorRT-LLM** |
| 端侧 / CPU / Mac | **llama.cpp** |
| 已经在用 Triton Server | **TRT-LLM** |

**最重要的一句话**：
> **"引擎选型的成本不在性能差异（可能就 10-20%），在<u>迁移和维护成本</u>。TRT-LLM 快 20% 但每次换模型要 build 半小时、调 kernel 要一周——**这笔账对小团队是亏的**。"**

**这个回答体现了"技术选型是工程决策不是性能竞赛"** —— 比背 benchmark 强得多。"

### 🔍 完整对比

| | **vLLM** | **SGLang** | **TensorRT-LLM** | llama.cpp |
|---|---|---|---|---|
| 起家技术 | **PagedAttention** | **RadixAttention** | kernel 融合 |量化 + CPU |
| 执行模式 | 解释式（即起即用） | 解释式 | **编译式**（要 build） | 解释式 |
| 启动时间 | 秒-分钟 | 秒-分钟 | **几十分钟** ❌ | 秒 |
| 模型支持 | **最广** ✅ | 广 | 慢 ❌ | GGUF 生态 |
| 社区 | **最大** ✅ | 增长快 | NVIDIA 官方 | 极大（端侧） |
| Prefix Caching | ✅ hash（默认开） | ✅ **Radix Tree**（更细粒度） | ✅ | ✅ |
| Chunked Prefill | ✅ 默认开 | ✅ | ✅ | — |
| 投机解码 | ✅ (EAGLE/ngram/Medusa) | ✅ (EAGLE) | ✅ | ✅ |
| **结构化输出** | ✅ XGrammar | ✅ **+ jump-forward** ✅ | ✅ | ✅ GBNF |
| **树形复用** | ⚠️ 一般 | ✅ **最强** | ⚠️ | ❌ |
| **MLA / 大规模 EP** | ✅ | ✅ **最好** | ⚠️ | ❌ |
| PD 分离 | ✅ (实验/生产) | ✅ | ✅ | ❌ |
| FP8 | ✅ | ✅ | ✅ **最好** | ⚠️ |
| **最佳场景** | **通用默认** | **Agent / 多轮 / DeepSeek** | **固定模型极致压榨** | **端侧 / CPU** |

> ⚠️ **这张表会过期。** 三个引擎迭代极快、互相抄、快速趋同（vLLM 也有 prefix caching，SGLang 也有 paged attention）。**面试时要说"我 2025 年底的认知是这样，具体要看当前版本和实测"** —— **这个态度本身就是加分项**，它说明你知道生态在动。

### 🔥 高频追问 Top 3

**Q：RadixAttention 和 vLLM 的 hash-based prefix caching 到底差在哪？**

A：**三个差异，第三个是本质的。**

**① 匹配粒度**
- **vLLM（hash）**：**block 对齐**（16 token 一块）。前缀有 100 个 token → 只能复用前 **96** 个（6 个完整 block），**最后 4 个不满一块，白算**
- **SGLang（radix tree）**：**任意 token 位置**都能匹配 → **100 个全复用**

**② 树形结构（这是关键）**
```
【vLLM 的 hash 表】—— 扁平的键值对
  hash("system+tools+历史A") → [块1,块2,...]
  hash("system+tools+历史B") → [块1,块2,...]
  ← 两条独立的记录，"它们共享前缀" 这个事实【表达不出来】

【SGLang 的 radix tree】—— 结构被显式表达
              [system + tools]        ← 公共前缀，一份
                    │
            ┌───────┴───────┐
        [历史A]          [历史B]      ← 分支
            │                │
        ┌───┴───┐        ┌───┴───┐
      [A1]    [A2]     [B1]    [B2]  ← 更深的分支
```

**③ 驱逐策略（本质差异）**
- **vLLM**：LRU over blocks —— **不知道哪些块是"公共前缀"**，可能**把 system prompt 淘汰掉**（如果它恰好一会儿没被访问）
- **SGLang**：**从<u>叶子</u>开始 LRU 驱逐** —— **公共前缀（靠近根）天然被保护**，因为它有很多后代依赖

$$\boxed{\text{Radix Tree 把"谁依赖谁"这个结构<u>显式地表达</u>出来，所以驱逐决策能更聪明}}$$

**为什么这对 Agent 特别重要**（这是答这题的落点）：

**Agent 的调用天然是树形的**：
- **ReAct 循环**：每轮 = 上一轮的 prompt + 新的 observation → **一条不断加长的链**
- **分支探索 / ToT**：同一个状态派生多个候选 → **真正的树**
- **多候选采样 / Best-of-N**：从同一个前缀分叉 → **树**
- **多轮对话**：整个会话是一条链，多个用户的 system prompt 共享 → **树的根**

**Agent 的访问模式就是一棵树，而 SGLang 用树来存它** —— **数据结构和访问模式同构，这是最优雅的匹配。**

**"Agent 的调用图是树形的，所以用树来管 KV Cache 是天然的"** —— 这句话能说出来，这题就满分了。

**Q：TensorRT-LLM 的"编译"到底在编译什么？为什么能更快？**

A：**它在做的事，本质是"用编译期的信息换运行时的性能"——和第 13 章 Outlines 的 FSM 预计算是同一个哲学。**

**编译期做什么**（`trtllm-build`）：
1. **Kernel 融合**：把 `LayerNorm + Linear + GELU + Linear` 融成**一个 kernel** → **省掉中间结果的 HBM 往返**（又是 memory-bound 的胜利！）
2. **Kernel 自动调优（auto-tuning）**：对**你的具体 shape**（batch、seq_len、hidden），**实测**几十个 kernel 配置（tile 大小、block 数、pipeline 深度），**挑最快的固化进 engine**
3. **精度策略固化**：哪层用 FP8、哪层用 INT4、scale 是多少 —— **全部编译进去**，运行时零决策
4. **内存布局优化**：预分配、对齐、消除动态分配

**为什么这些能快**：
- **vLLM 是"解释式"** —— 每次 forward 都要**动态决定**用哪个 kernel、算 shape、launch kernel（Python！）
- **TRT-LLM 是"编译式"** —— **全部固化**，运行时只是执行一张静态图

**代价（也是它输的原因）**：
$$\boxed{\text{编译期知道的信息越多 → 优化空间越大 → <u>但灵活性越差</u>}}$$
- **shape 变了要重 build**（所以要指定 `max_batch_size`、`max_input_len`）
- **模型变了要重 build**（几十分钟）
- **想改个采样参数？** 有些也要重 build
- **想 debug？** 图已经融死了，**看不见中间结果**

**这个 trade-off 在系统领域是<u>永恒的</u>**：
| 静态/编译 | 动态/解释 |
|---|---|
| TensorRT-LLM | vLLM |
| C++ | Python |
| Outlines 的 FSM 预编译 | 运行时跑解析器 |
| CUDA Graph | 逐个 kernel launch |
| **快，但僵** | **慢，但活** |

**"编译式引擎是在拿灵活性换性能。对模型频繁迭代的团队，这笔账是亏的。"** —— 这个判断比性能数字重要。

**Q：CUDA Graph 是什么？为什么 vLLM 要用它？**

A：**它是"解释式引擎向编译式借的一招"——很好的细节题。**

**问题：Decode 的 kernel launch 开销。**

Decode 一步只要 **7ms**（第 12 章），但这一步要 launch **几百个 kernel**（32 层 × 每层七八个算子）。每个 kernel launch 的 CPU 开销约 **5-10 微秒**：
$$300 \text{ kernels} \times 8\mu s = 2.4\text{ ms} \quad \Longrightarrow \quad \frac{2.4}{7} = \mathbf{34\%\ 的时间花在 CPU 上 launch kernel！}$$

**这就是所谓的 "CPU overhead" / "launch bound"** —— **GPU 在等 CPU 喂活**。

**CUDA Graph 的解法**：把这几百个 kernel 的调用序列**录制成一张图**，之后**一次 launch 整张图**：
$$300 \text{ 次 launch} \times 8\mu s \quad\longrightarrow\quad 1 \text{ 次 launch} \times 10\mu s$$

**开销从 2.4ms 降到 0.01ms。**

**为什么 Decode 特别需要它**：
- **Prefill**：一步要 90ms，2.4ms 的 launch 开销只占 **2.7%** → 无所谓
- **Decode**：一步只要 7ms，2.4ms 占 **34%** → **致命**

**$$\boxed{\text{每步越快，固定开销的占比越大}}$$**

（**这和第 14 章 Q5 追问 1 的"draft model 太小时固定开销主导"是<u>同一个道理</u>** —— 小模型/短步长会被固定开销吃掉。）

**代价**：
- **图是"录"死的** —— **shape 必须固定**！所以 vLLM 要**为每个常见的 batch size 各录一张图**（1, 2, 4, 8, 16, 32...），实际 batch 不匹配时 **padding 到最近的档位**
- 所以你会看到 vLLM 启动时那句 `Capturing CUDA graphs...` —— **它在录几十张图，占几百 MB 显存**
- **不支持动态控制流**（if/while）

**这又是"静态换性能"的同一个 trade-off** —— **CUDA Graph 是 vLLM 在局部"编译化"**。可以说：**vLLM 用 CUDA Graph 偷了 TRT-LLM 一半的功力，同时保留了整体的灵活性。**

**能把"CUDA Graph = 局部编译化 = 静态换性能"这条线讲出来，说明你理解了 Q3 的元结构。**

### ⚠️ 常见陷阱

1. **说"X 碾压 Y"** —— 三者快速趋同，性能差异可能只有 10-20%，而且每个版本都在变。
2. **只看 benchmark 不看场景** —— benchmark 通常是固定长度、固定 batch。**你的流量不长那样。**
3. **忽略迁移/维护成本** —— **这才是选型的主要成本**。
4. **不知道 SGLang 的树形优势对 Agent 的意义** —— 这是 Agent 岗的关键点。
5. **不知道 CUDA Graph** —— 被问"decode 为什么有 CPU 瓶颈"会卡住。

### 🏢 大厂偏好

- **字节 / 阿里**：**必问**，会问"为什么选 X 不选 Y"
- **应用岗 / Agent 岗**：**SGLang 的树形复用要能讲**
- **DeepSeek**：会问 SGLang 对 MLA/EP 的支持
- **有 NVIDIA 合作的团队**：会问 TRT-LLM

### 📚 延伸阅读
- [vLLM V1 架构博客](https://blog.vllm.ai/2025/01/27/v1-alpha-release.html)
- [SGLang: Efficient Execution of Structured Language Model Programs](https://arxiv.org/abs/2312.07104) - **RadixAttention 原论文**
- [TensorRT-LLM 文档](https://nvidia.github.io/TensorRT-LLM/)

---

## Q4：推理侧并行——TP / PP / EP / DP ⭐⭐⭐⭐⭐（必问）

### 🎯 一句话标答

> **推理侧的 TP 和训练侧机制相同，但<u>目的不同</u>——训练是为了"装得下"，推理<u>更是为了"把 HBM 带宽乘 N"</u>**（第 12 章：Decode 是 memory-bound，TP=8 → 每卡 1/8 权重 → 8 卡带宽并行 → TPOT 降 8 倍）；代价是**每层 2 次 AllReduce**，且**在 decode 的关键路径上**，所以 **TP 必须在 NVLink 域内、一般不超过 8**；**PP 通信量小可跨节点，但 decode 的 bubble 极大**；**核心反直觉点：TP 不是越大越好——TP=1 的多副本<u>吞吐</u>更高，TP=8 的<u>延迟</u>更低，又是三角权衡**。

### 🗣️ 30 秒口语版

"这题的关键是"**推理并行和训练并行，目的完全不同**"——很多人会把训练那套直接搬过来，那就错了。

**TP（Tensor Parallel）**

**机制和训练一样**（Megatron 的行列切分，见预训练章节）：
- FFN：第一个 Linear **按列切**，第二个 **按行切** → 中间不用通信，**最后一次 AllReduce**
- Attention：**按 head 切** → 每卡算自己的 head → **AllReduce**
- **每层 2 次 AllReduce**（attention 后 + FFN 后）

**但目的不同——这是题眼**：

| | 训练侧 TP | **推理侧 TP** |
|---|---|---|
| 主要目的 | **装得下**（显存不够） | **装得下 + ★把带宽乘 N★** |
| 为什么 | 优化器状态 + 梯度 + 激活太大 | **Decode 是 memory-bound**（第 12 章） |

**推理侧的关键账**：
```
70B fp16 = 140 GB
单卡 A100 (2000 GB/s)：装不下 ❌
TP=8：每卡 17.5 GB
  → 每卡只读 17.5 GB
  → ★8 张卡的带宽是【并行叠加】的★ = 16000 GB/s 等效
  → TPOT = 17.5 / 2000 = 8.75 ms  （不是 140/2000 = 70ms！）
  → ★TP 让 TPOT 降了 8 倍★
```

**所以推理侧的 TP，不只是"为了装下"，更是"为了快"。** 即使一个模型单卡装得下，**用 TP 也能降低延迟**。

**代价：AllReduce 在关键路径上。**
- **每层 2 次，32 层 = 64 次 AllReduce，<u>每个 decode step 都要</u>**
- **必须 NVLink**（A100 NVLink = 600 GB/s；PCIe 只有 64 GB/s，**差 10 倍**）
- **跨节点 TP（走 IB）会毁掉性能** → **所以 TP ≤ 8（一个 NVLink 域）**

**一个关键的架构约束（很多人不知道）**：
> **GQA 的 KV 头数限制了 TP 度！** GQA-8 只有 8 个 KV 头 → **TP > 8 就得复制 KV 头**（浪费显存）。
>
> **这正是"GQA-8 被广泛采用"的隐藏原因**（第 12 章 Q2 追问 3 提过）—— **8 正好配 TP=8，每卡一个 KV 头，不多不少。** 这个"凑巧"其实是设计出来的。

**PP（Pipeline Parallel）**

- ✅ **通信量极小**（只在 stage 边界传 activation，**不是 AllReduce**）→ **可以跨节点**
- ❌ **Decode 的 bubble 极大** —— decode 一次只有 1 个 token，**pipeline 里全是气泡**
- 缓解：**用不同请求填 pipeline**（stage 1 算请求 A，stage 2 同时算请求 B）
- **用途**：**TP=8 还装不下时，跨节点扩展**（TP 在节点内，PP 跨节点）

**DP（Data Parallel）= 多副本**
- 最简单：**N 张卡跑 N 个完整的模型副本**，前面挂个 load balancer
- ✅ **零通信** ✅ **吞吐线性扩展** ✅ **故障隔离**
- ❌ **每张卡都要装下整个模型**
- ❌ **延迟不改善**（单卡带宽）

**核心反直觉点（这是这题最重要的）**：

> **对于能单卡装下的模型，TP=1 的多副本【吞吐】更高，TP=8 的【延迟】更低。**

**为什么 TP 会降低吞吐？**
1. **通信是纯开销**：TP=8 时 AllReduce 可能占 **20-30%** 的时间 —— 这部分算力**纯浪费**
2. **每卡的 GEMM 变小 → 效率下降**：$[4096, 4096]$ 切成 8 份是 $[4096, 512]$ —— **矩阵太瘦，tensor core 利用率下降**
3. **固定开销被放大**：kernel launch、同步的开销，8 张卡都要付一遍

$$\boxed{\text{TP 是"用吞吐换延迟"—— 又一次三角权衡}}$$

**所以选择顺序是**：
1. **能单卡装下？** → **TP=1 + 多副本 DP**（吞吐最优）——除非你的 SLA 要求极低延迟
2. **单卡装不下？** → **TP，但不超过 NVLink 域（≤8）**
3. **TP=8 还不够？** → **加 PP 跨节点**
4. **MoE？** → **EP**（Q6）"

### 🔍 四种并行对比

| | **TP** | **PP** | **DP（多副本）** | **EP** |
|---|---|---|---|---|
| 切什么 | **单层的权重矩阵** | **按层切** | **不切**（复制） | **MoE 的专家** |
| 通信 | **每层 2 次 AllReduce** ❌ | **stage 边界传 activation** ✅ | **零** ✅ | **All-to-All** ❌ |
| 通信频率 | **每步、每层**（关键路径） | 每步、每 stage 边界 | 无 | **每步、每 MoE 层** |
| 跨节点 | ❌ **必须 NVLink** | ✅ 可以 | ✅ 可以 | ⚠️ 需要高速网 |
| 对**延迟** | ✅ **降低**（带宽 ×N） | ❌ **增加**（过所有 stage） | — | ⚠️ 增加（All-to-All） |
| 对**吞吐** | ⚠️ **通信是开销** | ⚠️ bubble | ✅ **线性扩展** | ✅ 必需 |
| 显存 | ✅ 权重 ÷N | ✅ 权重 ÷N | ❌ **每卡全量** | ✅ 专家 ÷N |
| 典型度 | **≤ 8** | 2-4 | 越多越好 | **8 - 320** |

### 📐 TP 的通信成本量化

每次 AllReduce 的通信量（Ring AllReduce）：
$$\text{Volume} = 2 \times \frac{N-1}{N} \times S \approx 2S \quad (N \text{ 大时})$$

Decode 时（batch=$B$，hidden=$d$=8192，fp16）：
$$S = B \times 1 \times d \times 2 = 2Bd \text{ 字节}$$

一层 2 次 AllReduce，$L$=80 层：
$$\text{总通信} \approx 80 \times 2 \times 2 \times 2Bd = 640 \, Bd \text{ 字节}$$

**代入 B=32、d=8192**：
$$640 \times 32 \times 8192 = 168 \text{ MB / step}$$

**在 NVLink（600 GB/s）上**：$168\text{MB} / 600\text{GB/s} = \mathbf{0.28\ ms}$
**在 PCIe（64 GB/s）上**：$168\text{MB} / 64\text{GB/s} = \mathbf{2.6\ ms}$

**对比 decode 一步的计算时间 ~8.75ms**：
| 互联 | 通信耗时 | **占比** | 结论 |
|---|---|---|---|
| **NVLink** | 0.28 ms | **3%** | ✅ 可接受 |
| **PCIe** | 2.6 ms | **30%** | ❌ 严重拖累 |
| **IB (跨节点, 50GB/s)** | 3.4 ms | **39%** | ❌❌ **不可接受** |

$$\boxed{\text{这就是"TP 不跨节点"的数字依据}}$$

> **注意通信量正比于 $B$，而计算时间在 memory-bound 区间<u>不随 $B$ 增长</u>**（读权重是固定的）！
>
> **推论：batch 越大，TP 的通信占比越高。** 这是个很少有人注意到的点——**大 batch 会让 TP 的性价比下降**。答出来很加分。

### 🔥 高频追问 Top 3

**Q：为什么推理不用 ZeRO / FSDP？**

A：**因为它们的核心机制在推理时是纯亏的。**

**ZeRO-3 / FSDP 干什么**：把权重**分片**存在各卡，**用到某层时才 AllGather 把它拼起来**，算完**立刻扔掉**。

**训练时这很赚**：
- 训练的**激活和优化器状态**是大头（Adam 的 $m$、$v$ 是权重的 2 倍，加上梯度和 fp32 master weight，总共是权重的 **~16 倍**！）
- 而且训练的 batch 大、**每层的计算时间长** → **AllGather 可以和计算 overlap，藏起来**

**推理时这很亏**：
- ❌ **推理没有优化器状态和梯度** —— ZeRO 省的那 16 倍，**推理时根本不存在**！只有权重要分片，那 **TP 也能做，还不用 AllGather**
- ❌ **Decode 每层只算 1 个 token（几微秒）**，而 AllGather 整层的权重要**几毫秒** —— **通信时间 >> 计算时间，根本藏不住**
- ❌ **等于每步都把整个模型在网络上搬一遍** —— 这是 memory-bound 之上再加一层"network-bound"，**灾难**

$$\boxed{\text{ZeRO 是"用通信换显存"，而推理时显存不是<u>那个</u>矛盾，通信才是}}$$

**正确的对比**：
| | ZeRO-3 | **TP** |
|---|---|---|
| 权重 | 分片，**用时 AllGather 拼起来** | **分片，永不拼**（各算各的） |
| 通信内容 | **整层的权重**（几百 MB） | **激活**（几 MB） |
| 推理时 | ❌ 灾难 | ✅ 正解 |

**"TP 传的是激活（小），ZeRO 传的是权重（大）——所以推理只能用 TP"** —— 这句话是这题的满分答案。

**Q：TP 和 SP（Sequence Parallel）什么关系？推理需要 SP 吗？**

A：**SP 是 TP 的补丁，推理侧价值不大——但 Prefill 阶段有用。**

**SP 补的是 TP 的什么洞**：TP 切了 Attention 和 FFN，但 **LayerNorm 和 Dropout 没切**（它们是 element-wise 的，没法按 head 或按行列切）。所以这些地方**每卡都持有完整的激活** $[B, S, d]$ → **冗余**。

**SP 的做法**：在 LayerNorm 处**按序列维度切**，用 **AllGather / ReduceScatter** 和 TP 的 AllReduce **拼接起来**（数学上 AllReduce = ReduceScatter + AllGather，**所以 SP 是"免费"的**——它把一次 AllReduce 拆成两半，中间插入切分的部分）。

**推理侧的价值**：
- **Decode**：$S=1$！**激活只有 $[B, 1, d]$，本来就极小** → **SP 完全没意义** ❌
- **Prefill**：$S$ 可以是 8K、32K → 激活 $[B, 32768, 8192]$ **很大** → **SP 有价值** ✅

$$\boxed{\text{SP 在推理侧只对 Prefill 有意义，对 Decode 无意义}}$$

**这正好又是一个"PD 分离"的论据**（Q5）：
> **Prefill 需要 SP，Decode 不需要 —— 混合部署下你只能开一套配置，PD 分离后各自最优。**

**DeepSeek-V3 的部署正是这样**：**Prefill 用 "TP4 + SP"，Decode 用 "TP4"（无 SP）** —— 这个细节能说出来，非常加分。

**Q：TP=8 vs 8 个 TP=1 的副本，到底怎么选？**

A：**这是这题的落点——用 SLA 来决定，用数字来算。**

假设 8×A100，模型 13B（fp16 = 26 GB，**单卡装得下**）：

| | **TP=8（1 个副本）** | **DP=8（8 个副本）** |
|---|---|---|
| **TPOT** | **~1.6 ms** (26/8/2000)✅ | ~13 ms (26/2000) |
| **通信开销** | **~25%** ❌ | **0** ✅ |
| 单副本可用 KV Cache | **8 × 54 GB = 432 GB** ✅ | 54 GB |
| **最大并发** | 高（KV 池大） | 中 × 8 副本 |
| **总吞吐** | ⚠️ 被通信吃掉 25% | ✅ **最高** |
| **故障影响** | ❌ **一卡挂全挂** | ✅ **挂 1/8** |
| 长上下文能力 | ✅ **KV 池大，能跑超长** | ❌ 单副本 KV 有限 |

**决策树（面试请这样答）**：
```
SLA 要求 TPOT < 5ms？
  ├─ 是 → TP=8   （没得选，DP 的 13ms 达不到）
  └─ 否 → 要跑超长上下文（>64K）吗？
           ├─ 是 → TP=8   （需要大 KV 池）
           └─ 否 → ★DP=8★（吞吐最高、故障隔离最好、最简单）
```

**结论**：
> **"能用 DP 就用 DP。TP 是<u>被迫</u>的选择——要么装不下，要么 SLA 逼你,要么要跑超长上下文。"**

**面试时的高阶答法**：
> **"我会先测：单卡的 TPOT 是多少？如果满足 SLA，就 DP=8 —— 吞吐最高、运维最简单、故障隔离最好。TP 是给'装不下'和'延迟不达标'准备的，不是默认选项。**
>
> **而且这个决策在 PD 分离后会更细：Prefill 集群可以用大 TP 降 TTFT，Decode 集群可以用小 TP 大 batch 提吞吐。"**

**最后那句把 Q4 和 Q5 连起来，是最好的收尾。**

### ⚠️ 常见陷阱

1. **把训练的并行策略直接搬到推理** —— **目的不同**（训练为装下，推理**更为带宽**），**约束不同**（推理没有优化器状态）。
2. **说 TP 越大越好** —— **通信是纯开销**，且 **GEMM 变瘦效率下降**。TP 是"用吞吐换延迟"。
3. **跨节点做 TP** —— **IB 的通信占比 39%**，灾难。
4. **不知道 GQA 头数限制 TP 度** —— **GQA-8 配 TP=8 是设计出来的**。
5. **推理用 ZeRO** —— **传权重 vs 传激活**，量级差几百倍。
6. **不知道 Decode 不需要 SP** —— $S=1$，激活本来就小。

### 🏢 大厂偏好

- **字节 / 阿里 Infra**：**必问**，会让你算通信量
- **Meta**：会问 TP vs DP 的选型
- **DeepSeek**：会引向 EP 和 V3 的部署方案（Q6）
- **应用岗**：知道"能 DP 就 DP，TP 是被迫的"就够了

### 📚 延伸阅读
- [Megatron-LM](https://arxiv.org/abs/1909.08053) - TP 的出处
- [Efficiently Scaling Transformer Inference](https://arxiv.org/abs/2211.05102) - **Google 的推理并行分析，本题最好的参考**
- [DeepSeek-V3 Technical Report](https://arxiv.org/abs/2412.19437) §5 - 真实的大规模部署配置

---

## Q5：PD 分离（Disaggregated Serving）⭐⭐⭐⭐⭐（必问·前沿）

### 🎯 一句话标答

> 混合部署下，**TTFT SLA 和 TPOT SLA 互相耦合**——为了满足前者要小 batch，为了满足后者也要小 batch，**两个约束叠加把 Goodput 死死压住**；PD 分离把 Prefill 和 Decode **放到物理隔离的集群**，让两者**独立优化并行策略、batch、甚至卡型**，DistServe 报告 **Goodput 提升 4.48×**（SLA 严格时 **10.2×**）；代价是 **KV Cache 要跨机传输**（一个 4K 的 7B 请求 = 2 GB！），所以**优化手段是 layer-wise 流水传输**，而 **MLA 这类小 KV Cache 的架构在 PD 分离下优势被极大放大**。

### 🗣️ 30 秒口语版

"这题是第 12 章 Q1 追问 2 的完整展开，也是当前最前沿的部署方案。

**动机：不是"prefill 卡 decode"这么简单，而是<u>四层</u>的不匹配。**

**① 计算特性不匹配**（第 12 章 Q1）
- Prefill = **compute-bound**，Decode = **memory-bound**
- 混在一起 → **两边都不是最优**

**② 干扰**（第 12 章 Q6）
- 长 prefill 插进来，**所有 decode 用户集体卡半秒**（generation stall）
- Chunked Prefill 是"融合"的解法，PD 分离是"**隔离**"的解法

**③ 最优并行策略不匹配**（Q4）
- **Prefill 想要大 TP**（降 TTFT，反正是 compute-bound，通信占比小）+ **SP**
- **Decode 想要小 TP + 大 batch**（提吞吐；TP 的通信在 decode 的关键路径上，很贵）
- **混合部署只能取一个折中，两边都不满意**

**④ SLA 耦合（这是 DistServe 的核心论证，也是这题的题眼）**
- 为了满足 **TTFT SLA** → batch 不能大（排队）
- 为了满足 **TPOT SLA** → batch 也不能大（每步算太多）
- **两个约束<u>互相叠加</u>** → **Goodput（Q1）被死死压住**

$$\boxed{\text{分离的本质：把两个耦合的 SLA <u>解耦</u>成两个独立的优化问题}}$$

这就是为什么 goodput 能提升 4.48× —— **不是因为哪个 kernel 变快了，而是因为约束解开了。**

**做法**：
- **P 集群**：只做 prefill。大 TP + SP、追求 TTFT。可以用**算力强的卡**
- **D 集群**：只做 decode。小 TP + 大 batch、追求吞吐。可以用**带宽大、显存大的卡**
- **中间传 KV Cache**

**代价（必须主动说，这是这题的深度所在）**：

**KV Cache 的跨机传输是致命的**：
```
7B 模型、4K prompt 的 KV Cache = 2 GB（第 12 章 Q2）
IB 400 Gbps = 50 GB/s
→ 传输耗时 = 2 GB / 50 GB/s = ★40 ms★

而这个 prefill 本身只要 90ms！
→ 传输开销 = 44%  ← ★可能把收益全吃掉★
```

**三个优化手段**：
1. **Layer-wise 流水传输**（关键）：**算完第 $i$ 层就开始传第 $i$ 层的 KV**，和第 $i+1$ 层的计算 **overlap** → **传输被完全藏在计算后面**
2. **MLA / GQA**：**直接减少要传的量**！MLA 是 70 KB/token vs MHA 的 500 KB/token → **传输量 ÷7**
3. **RDMA / NVLink 直连**：绕过 CPU 和 TCP 栈

> 🚨 **第 2 点值得强调**：**MLA 在 PD 分离下的价值被<u>放大</u>了**——它省的不只是显存（第 12 章），**更是网络带宽**。
>
> **这解释了一个"巧合"**：**DeepSeek 既发明了 MLA，又做了大规模 PD 分离部署。这不是巧合——是<u>架构设计和部署方案的协同</u>。** MLA 让 PD 分离在经济上可行，PD 分离让 MLA 的价值最大化。**能看出这层因果，是很高级的信号。**

**Mooncake（Kimi）的额外贡献 —— KVCache-centric**：
不只是分离，而是把 **KV Cache 当成一个<u>分布式缓存池</u>**：
- 用**所有节点的 CPU DRAM + SSD** 组成一个巨大的 KV 池（**比 GPU 显存大几十倍！**）
- 做**全局的 Prefix Caching**（第 12 章 Q7）—— 不只是单机命中，**跨机命中**
- **这是 Kimi 长文本能力的核心基础设施**

$$\text{Mooncake 的洞察：KV Cache 不是"计算的副产品"，而是"<u>系统的一等公民</u>"}$$

**什么时候用 PD 分离**：
- ✅ **大规模**（几十台以上）—— 小规模的话，分离的开销 > 收益
- ✅ **有明确的 SLA 分级**（付费用户 vs 免费用户）
- ✅ **长 prompt 场景**（RAG、Agent、长文档）—— prefill 重，干扰严重
- ❌ **小规模** → **老老实实用 Chunked Prefill 就够了**（第 12 章 Q6）"

### 📐 为什么分离能提升 Goodput——DistServe 的论证

**混合部署（Colocated）**：
```
一个 batch 里混着 prefill 和 decode
  ↓
调 batch size 时：
  batch ↑ → 吞吐 ↑，但 TTFT ↑（排队）且 TPOT ↑（每步算更多）
  batch ↓ → TTFT ✅ TPOT ✅，但吞吐 ↓
  ↓
★只有【一个】旋钮，要同时满足【两个】SLA★
  ↓
必须取最保守的那个 → Goodput 被压制
```

**分离部署**：
```
P 集群的旋钮：batch_p, TP_p        → 只优化 TTFT
D 集群的旋钮：batch_d, TP_d        → 只优化 TPOT
  ↓
★两个独立的旋钮，各管一个 SLA★
  ↓
各自推到自己的最优 → Goodput ↑↑
```

$$\boxed{\text{Goodput 提升的来源不是"更快"，而是"<u>约束解开了</u>"}}$$

**DistServe 的实测**：
| 场景 | Goodput 提升 |
|---|---|
| 一般 SLA | **4.48×** |
| **严格 SLA**（TTFT/TPOT 要求高） | **10.2×** |

> **注意"SLA 越严，收益越大"这个规律** —— 因为 SLA 越严，混合部署下的**耦合损失越大**（两个约束叠加得越狠）。**这是个很好的直觉**：**PD 分离是给"延迟敏感 + 大规模"场景准备的。**

### 🔍 PD 分离 vs Chunked Prefill

**这两个是同一个问题的两种哲学**（第 12 章 Q6 追问 2 说过，这里深化）：

| | **Chunked Prefill**（融合） | **PD 分离**（隔离） |
|---|---|---|
| 哲学 | **既然干扰，就混得更彻底**（让瓶颈互补，1+1>2） | **既然干扰，就物理分开**（各自最优） |
| 复杂度 | **一个开关** ✅ | **两个集群 + KV 传输 + 调度** ❌ |
| 适合规模 | **单机 - 小集群** | **大集群** |
| 硬件 | 同构 | **可异构** ✅ |
| 额外成本 | **无** | **KV 跨机传输** |
| 并行策略 | **只能一套** ❌ | **各自最优** ✅ |
| SLA 分级 | 难 | **容易** ✅ |
| Goodput | 中 | **高**（4.48-10.2×） |

**可以叠加吗？** ✅ **可以** —— PD 分离之后，**P 集群内部仍然可以用 chunked prefill** 来平滑长短 prompt 的混合。

**选择的判据**：
$$\boxed{\text{几台机器 → Chunked Prefill；几十台以上 + 严格 SLA → PD 分离}}$$

### 🔥 高频追问 Top 3

**Q：P 和 D 的比例怎么定？**

A：**按"总 prefill 计算量 : 总 decode 计算量"来定，且要动态调整。**

**静态估算**：
$$\frac{N_P}{N_D} \approx \frac{\text{prefill 的总耗时}}{\text{decode 的总耗时}} = \frac{\text{TTFT}}{\text{TPOT} \times L_{\text{out}}}$$

**举例**：
```
场景 A：RAG（长输入短输出）
  输入 4000 token → prefill ≈ 360 ms
  输出 100 token  → decode ≈ 100 × 8ms = 800 ms
  → P : D ≈ 360 : 800 ≈ 1 : 2.2

场景 B：创作（短输入长输出）
  输入 100 token  → prefill ≈ 10 ms
  输出 2000 token → decode ≈ 2000 × 8ms = 16000 ms
  → P : D ≈ 1 : 1600  ← ★几乎不需要 P 集群！★

场景 C：Agent（长输入中等输出，多轮）
  输入 8000 token → prefill ≈ 700 ms（但 ★Prefix Caching 命中后可能只有 50ms★）
  输出 300 token  → decode ≈ 2400 ms
  → P : D ≈ 1 : 3.4（无缓存）或 1 : 48（有缓存）
```

**三个关键洞察**：

1. **P:D 强烈依赖"输入/输出长度比"** —— 这是**业务决定的**，不是技术决定的
2. **Prefix Caching 会剧烈改变这个比例**！Agent 场景命中率 90% 时，**P 集群的需求直接降 10 倍** → **上 PD 分离前先上 Prefix Caching**
3. **真实流量的长度比是<u>波动的</u>** → 需要**动态调整** P/D 的节点数（弹性伸缩）。这是 PD 分离最大的运维复杂度来源 —— **你现在有两个池子要独立扩缩容，而且它们的负载不同步**

**"P:D 比例由业务的输入输出长度比决定，且 Prefix Caching 会剧烈改变它"** —— 这个回答体现了系统性思考。

**Q：KV Cache 传输真的能被完全隐藏吗？**

A：**Prefill 阶段能，但有两个前提；而且有一个"最后一层"的尾巴藏不住。**

**Layer-wise 流水的机制**：
```
时间 →
Layer 1 计算 ──┐
Layer 2 计算 ──┼─→ Layer 1 的 KV 传输 ┐
Layer 3 计算 ──┼─→ Layer 2 的 KV 传输 ┼─ 传输被藏在后续计算里 ✅
   ...        │         ...           │
Layer 80 计算 ─┘─→ Layer 79 的 KV 传输─┘
              └─→ ★Layer 80 的 KV 传输★ ← 后面没有计算了，★藏不住★
```

**能藏住的前提**：
$$\boxed{\text{每层的 KV 传输时间} \le \text{每层的计算时间}}$$

**算一下**（7B、4K prompt、32 层、IB 50 GB/s）：
- 每层 KV = 2 GB / 32 = **62.5 MB** → 传输 **1.25 ms**
- 每层 prefill 计算 = 90ms / 32 = **2.8 ms**
- **1.25 < 2.8 → 能藏住** ✅

**但换个场景就藏不住了**：
```
70B、32K prompt、MHA（假设）:
  每层 KV = 32000 × 320KB / 80 = 128 MB → 传输 2.6 ms
  每层计算 = ... 也许 8ms
  → 还能藏 ✅
  
但如果网络只有 25 GB/s（普通 IB）：
  传输 5.2 ms vs 计算 8 ms → 勉强
  
如果是 100 Gbps (12.5 GB/s)：
  传输 10.2 ms > 计算 8 ms → ★藏不住，成为瓶颈★ ❌
```

**结论**：
$$\boxed{\text{PD 分离对网络有<u>硬性</u>要求：至少 200-400 Gbps IB / RoCE}}$$

**"最后一层"的尾巴**：Layer 80 的 KV 传完之前，D 集群不能开始。这部分**必然暴露**——但只有 1/80 的量，可接受。

**这也再次说明为什么 MLA 重要**：MLA 的每层 KV 是 MHA 的 **1/7** → **传输时间 ÷7** → **网络要求大幅降低，藏得更轻松**。

**Q：PD 分离和投机解码/Prefix Caching 怎么配合？**

A：**三者协同得非常好——这是把第 12-15 章串起来的机会。**

**① PD 分离 + Prefix Caching**（第 12 章 Q7）
- **Prefix Caching 应该做在 P 集群**（它优化的是 prefill）
- **但有个新问题：跨 P 节点的缓存怎么共享？**
  - 请求 1 打到 P1，缓存了 system prompt
  - 请求 2 打到 P2 → **miss**！❌
- **解法一**：**亲和性路由**（affinity routing）—— 按 prompt 前缀的 hash 路由到固定的 P 节点，**让相同前缀总是打到同一台**
- **解法二（更优）**：**Mooncake 的全局 KV 池** —— 用所有节点的 CPU DRAM + SSD 做一个**共享的分布式缓存**，任何 P 节点都能命中
- **这个"跨节点 prefix caching"是 PD 分离的一个隐藏难点**，答出来很加分

**② PD 分离 + 投机解码**（第 14 章 Q7）
- **投机解码只需要部署在 D 集群**（prefill 本来就并行，不需要投机）
- → **draft model 的显存开销减半**（只有 D 节点要装）✅
- → 而且 **D 集群可以按 SLA 分级**：
  ```
  高优 D 集群：小 batch + EAGLE 投机解码  → 极低 TPOT，给付费用户
  低优 D 集群：大 batch + 不开投机        → 高吞吐，给免费用户
  ```
- **这是第 14 章 Q7 的完美解答**："投机解码在大 batch 下失效" → **PD 分离让你可以<u>刻意维持一个小 batch 的集群</u>**

$$\boxed{\text{PD 分离把"延迟 vs 吞吐"从<u>全局妥协</u>变成了<u>可分配的资源</u>}}$$

**③ PD 分离 + 量化**（第 13 章）
- **P 集群（compute-bound）** → **W8A8 / FP8**（省算力）
- **D 集群（memory-bound，小 batch）** → **W4A16**（省访存）
- **甚至可以用不同的量化方案！** ← **这在混合部署下是<u>不可能</u>的**（同一个模型只能有一份权重）

**这三条串起来，就是第三部分的完整收官**：
> **PD 分离不只是一个"部署技巧"，它是一个<u>解耦框架</u>——把混合部署下所有"只能取折中"的决策（并行策略、batch、量化方案、投机解码开关、卡型），全部变成"两边各自最优"。**

**能讲到这个高度，Q5 就满分了。**

### ⚠️ 常见陷阱

1. **只说"prefill 卡 decode"** —— 那只是**四个动机中的一个**。核心论证是 **SLA 解耦 + Goodput**。
2. **不提 KV 传输的代价** —— **一个 4K 请求要传 2 GB**！不提这个就是没想过工程实现。
3. **不知道 layer-wise 流水** —— 这是让传输可行的关键手段。
4. **不知道 MLA 在 PD 分离下的放大效应** —— **架构和部署的协同**，这是 DeepSeek 的设计精髓。
5. **小规模也上 PD 分离** —— **几台机器用 Chunked Prefill 就够了**，分离的运维成本远超收益。
6. **忽略跨 P 节点的 prefix cache 共享问题** —— 这是个真实的坑。

### 🏢 大厂偏好

- **DeepSeek**：**必问**（V3 的部署就是 PD 分离）
- **月之暗面 / Kimi**：**必问 Mooncake**
- **字节 / 阿里**：会问"你们什么规模，为什么/为什么不用 PD 分离"
- **应用岗**：知道"这是大规模才需要的，小规模用 chunked prefill"就够了

### 📚 延伸阅读
- [DistServe](https://arxiv.org/abs/2401.09670) - **OSDI'24，Goodput + PD 分离的奠基作，本章必读**
- [Splitwise](https://arxiv.org/abs/2311.18677) - Microsoft，异构卡型的 PD 分离
- [Mooncake](https://arxiv.org/abs/2407.00079) - **月之暗面，KVCache-centric 架构，FAST'25 最佳论文**
- [DeepSeek-V3 Technical Report](https://arxiv.org/abs/2412.19437) §5 - 真实的 PD 分离部署

---

## Q6：MoE 推理部署与超大 EP ⭐⭐⭐⭐（DeepSeek 必问）

### 🎯 一句话标答

> MoE 推理有三个独特挑战：**① 显存**（671B 总参数必须全装下，虽只激活 37B → **MoE 是量化的最大受益者**）；**② 算术强度塌陷**（batch 大时被激活的专家并集接近全部，**但每个专家只服务少数 token** → 强度上不去）；**③ 负载均衡**（热门专家成瓶颈，且**推理流量分布 ≠ 训练分布**）；解法是 **超大 EP**——DeepSeek-V3 的 Decode 用 **EP320（320 GPU，每卡一个专家）**，让**每个专家攒够 batch**，配 **冗余专家**（热门专家复制多份）和 **DeepEP 的 All-to-All 优化**。

### 🗣️ 30 秒口语版

"这题是 DeepSeek 面试的必考，也是"MoE 到底怎么部署"这个问题的完整答案。

**挑战一：显存的悖论。**
DeepSeek-V3：**671B 总参数，37B 激活**。
- **计算量**：只有 37B（便宜！）
- **显存**：**必须装下 671B**（fp8 也要 670 GB）
- **这是 MoE 的核心矛盾：算得少，但存得多**

$$\boxed{\text{MoE 是量化的最大受益者 —— 因为它的痛点恰好是"参数多"（第 13 章 Q6 追问 3）}}$$

**挑战二：算术强度塌陷（这是最深的一点，第 12 章 Q3 追问 2 埋的伏笔）。**

Dense 模型：算术强度 = batch（权重被全 batch 共享）。

**MoE 完全不同**：
- **batch=1**：只激活 top-8 个专家 → **只读这 8 个专家的权重** → **比同总参数的 Dense 快得多** ✅（这是 MoE 推理友好的核心）
- **batch=256**：256 个 token **路由到不同的专家** → **被激活的专家并集 ≈ 全部 256 个** → **要读全部专家的权重，但每个专家平均只服务 $\frac{256 \times 8}{256} = 8$ 个 token**！

$$\text{每个专家的等效 batch} = \frac{B \times k}{N_{\text{expert}}}$$

**代入 V3**：$B$=256、$k$=8、$N$=256 → **每个专家的等效 batch = 8** ❌

**算术强度只有 8，而不是 256！** —— **MoE 的算术强度被"稀释"了 $N/k = 32$ 倍。**

$$\boxed{\text{MoE 的算术强度} = \frac{B \times k}{N_{\text{expert}}} \ll B}$$

**这就是为什么 DeepSeek 的推理部署要用【超大 batch + 超大 EP】** —— **为了让每个专家攒够 batch**！

**挑战三：负载均衡。**
- 训练时有辅助 loss / bias 做均衡（DeepSeek 的 **auxiliary-loss-free** 方案）
- **但推理时的流量分布 ≠ 训练分布**！你的业务如果全是代码，那"代码专家"会被打爆，其他专家闲着
- 热门专家所在的 GPU 成为**木桶的短板** —— **All-to-All 是同步的，所有卡要等最慢的那个**

**解法：DeepSeek-V3 的部署方案（这是个绝佳的 case study，要能背）**

```
【Prefill 阶段】4 节点，32 GPU
  Attention:  TP4 + SP + DP8
  MoE:        EP32
  → 追求 TTFT，compute-bound，TP 的通信占比小

【Decode 阶段】★40 节点，320 GPU★
  Attention:  TP4 + DP80        ← ★注意 attention 用 DP 不用大 TP★
  MoE:        ★EP320★          ← 每个 GPU 只放【一个】专家！
  → 256 个路由专家 + 64 个 GPU 放【冗余专家 + 共享专家】
  → 追求吞吐，memory-bound，要攒超大 batch
```

**读懂这个配置的三个关键**：

**① 为什么 Decode 要 320 张卡？**
—— **不是因为装不下，是为了让每个专家<u>独占一张卡</u>，从而能攒到足够大的 batch**。320 卡 × 每卡 batch=128，那每个专家的等效 batch 就足够高了。

**② 为什么 Attention 用 DP 不用大 TP？**
—— **因为 MLA 的 KV Cache 已经极小了**（70 KB/token）！**不需要靠 TP 来分摊 KV 显存**。而 TP 会带来 AllReduce 通信（Q4）。**DP80 = 80 个独立的 attention 副本，零通信。**
—— **这是 MLA 带来的部署自由度** —— 又一次架构与部署的协同。

**③ 冗余专家（Redundant Experts）**
—— **热门专家复制多份**，分散到多张卡，**分摊负载**
—— **根据线上统计<u>定期调整</u>**（哪个专家热就多复制几份）
—— 64 张卡专门干这个

**All-to-All 是 EP 的命门**：
- 每个 MoE 层要 **2 次 All-to-All**（dispatch 到专家 + combine 回来）
- 通信量大，且是**同步的**——**一个慢，全部等**
- **DeepSeek 开源了 DeepEP** —— 专门的 All-to-All 通信库，用 **NVLink（节点内）+ RDMA（节点间）** 的分层策略，并做 **计算-通信 overlap**"

### 📐 MoE 算术强度的推导

**Dense 模型**（第 12 章 Q3）：
$$\text{Intensity} = \frac{\text{FLOPs}}{\text{Bytes}} = \frac{2PB}{2P} = B$$

**MoE 模型**（$N$ 个专家，每个 $P_e$ 参数，top-$k$ 路由，batch $B$）：

- **FLOPs** = $2 \cdot P_e \cdot k \cdot B$（每个 token 过 $k$ 个专家）
- **访存**：**被激活的专家的并集**。$B$ 大时，**并集 ≈ 全部 $N$ 个**：
$$\text{Bytes} \approx 2 \cdot P_e \cdot \min(N,\ Bk)$$

$$\text{Intensity} = \frac{2 P_e k B}{2 P_e \min(N, Bk)} = \frac{kB}{\min(N, Bk)}$$

**两个区间**：
| 条件 | 算术强度 | 状态 |
|---|---|---|
| $Bk < N$（batch 小） | $\dfrac{kB}{kB} = \mathbf{1}$ | **和 Dense 的 batch=1 一样差** |
| $Bk \ge N$（batch 大） | $\dfrac{kB}{N}$ | **只有 Dense 的 $k/N$ 倍！** |

**代入 DeepSeek-V3**（$N$=256、$k$=8）：
$$\text{Intensity}_{\text{MoE}} = \frac{8B}{256} = \frac{B}{32}$$

$$\boxed{\text{MoE 的算术强度只有同 batch 的 Dense 的 } \frac{k}{N} = \frac{1}{32}}$$

**推论（这是 EP320 的全部理由）**：
> **要达到 A100 的平衡点 150，Dense 需要 batch=150，MoE 需要 batch = $150 \times 32 = \mathbf{4800}$！**
>
> **一个 GPU 攒不到 4800 的 batch —— 但 320 个 GPU 一起可以。**
>
> **这就是"超大 EP"的数学依据：不是为了装下，是为了<u>攒 batch</u>。**

**能推出这个 $k/N$ 稀释因子，并用它解释 EP320，是这题的满分答案。**

### 🔍 EP 的通信

```
【每个 MoE 层的 All-to-All】

Step 1: Dispatch  —— 把每个 token 发到它的 top-k 个专家所在的卡
  GPU 0: [tok_a→E5, tok_b→E120, ...]  ──┐
  GPU 1: [tok_c→E5, tok_d→E33,  ...]  ──┼─→ ★All-to-All #1★
  ...                                    ─┘

Step 2: 各卡算自己的专家（本地 FFN）

Step 3: Combine —— 把结果发回原来的卡
  ──→ ★All-to-All #2★

★ 每层 2 次 All-to-All，V3 有 58 个 MoE 层 → 116 次 All-to-All / step ★
```

**为什么 All-to-All 比 AllReduce 更麻烦**：
| | AllReduce (TP) | **All-to-All (EP)** |
|---|---|---|
| 通信模式 | **规整**（Ring / Tree，可预测） | **不规整**（取决于路由！**每步都不一样**） |
| 通信量 | **固定** | **动态**（看多少 token 去哪个专家） |
| 负载 | **均匀** | ❌ **不均匀**（热门专家收到更多） |
| 优化 | 成熟（NCCL） | ❌ **难**（要动态调度） |
| **同步性** | 同步 | **同步，且木桶效应更严重** |

**DeepEP 的优化**：
1. **NVLink + RDMA 分层**：节点内走 NVLink（600 GB/s），节点间走 RDMA
2. **计算-通信 overlap**：用 **SM 专门跑通信**（把通信也变成一个 kernel），和 FFN 计算重叠
3. **FP8 通信**：**通信量直接减半** ✅

### 🔥 高频追问 Top 3

**Q：为什么不用 TP 切 MoE，非要用 EP？**

A：**可以，但很亏——这题考的是对两种切法的理解。**

**TP 切 MoE**：**每个专家的权重矩阵**被切到 8 张卡上
- ✅ 负载天然均衡（每卡都有每个专家的 1/8）
- ❌ **每卡都要存<u>所有</u> 256 个专家的 1/8** → **显存没省多少**（还是 $671B/8$）
- ❌ **每个专家的 GEMM 被切成 1/8** → **矩阵太瘦，效率暴跌**（Q4 说过）
- ❌ **每层还是 2 次 AllReduce**

**EP 切 MoE**：**整个专家**放一张卡
- ✅ **每卡只存 $\frac{256}{32}=8$ 个完整专家** → **显存真的省了**
- ✅ **每个专家的 GEMM 是<u>完整</u>的** → **效率高**
- ❌ **负载不均衡**（要靠冗余专家 + 均衡策略补）
- ❌ **All-to-All 不规整**

**核心对比**：
$$\boxed{\text{TP 切"矩阵"（越切越瘦，效率降）；EP 切"专家"（每个专家完整，效率保持）}}$$

**MoE 的结构天然适合 EP** —— **专家本来就是相互独立的模块，切开是"自然的边界"，而不是"人为的撕裂"**。

**实践**：**EP + TP 可以混用**。DeepSeek-V3 的 Prefill 就是 `TP4 + EP32` —— **EP 分专家，TP 再切每个专家内部**（当专家很大时）。

**Q：推理时的负载均衡和训练时有什么不同？**

A：**根本不同——训练时可以"改模型"，推理时只能"改部署"。**

| | **训练时** | **推理时** |
|---|---|---|
| 手段 | **改变模型**（aux loss / bias 引导路由） | ❌ **模型已冻结，动不了** |
| 目标 | 让**训练数据**上的路由均衡 | 让**线上流量**上的负载均衡 |
| 时间尺度 | 整个训练过程 | **实时** |
| 可用手段 | loss / bias | **冗余专家、专家放置、请求路由** |

**核心问题**：**训练时的均衡 ≠ 推理时的均衡！**

DeepSeek 用 auxiliary-loss-free 的方式，在**训练数据分布**上做到了均衡。但你的线上流量：
- 全是**中文客服** → 某些专家被打爆
- 全是**代码** → 另一批专家被打爆

**推理时的三种解法**：
1. **冗余专家（DeepSeek 的方案）**：**统计线上的专家激活频率，热门的复制多份**，分散到多卡。**定期（比如每 10 分钟）重新计算放置方案**
2. **动态专家放置**：更激进——**在线迁移专家**（把热门专家从拥挤的卡迁到空闲的卡）。**运维复杂度极高**
3. **请求级路由**：**把相似的请求聚到一起**（比如所有代码请求打到一组卡）—— 反直觉地**加剧**了不均衡，但让**每组卡内部**的专家激活更集中，**反而提高了每个专家的等效 batch**！

> **第 3 点很有意思**：它和 Prefix Caching 的"亲和性路由"（Q5 追问 3）是**同一个思想** —— **让相似的请求聚在一起，从而提高局部性**。**在 MoE 里，这个局部性体现在"专家的等效 batch"上。**

**"训练时改模型，推理时改部署"** —— 这个辨析是这题的题眼。

**Q：共享专家（Shared Expert）在部署时要特别注意什么？**

A：**它是<u>所有 token 都过</u>的，所以有两个特殊性。**

DeepSeek 的架构里有 **shared expert** —— **不参与路由，每个 token 都过**。

**特殊性 1：它不需要 All-to-All！**
- 路由专家：token 要**发到**专家所在的卡 → All-to-All
- **共享专家**：每个 token 都要过 → **可以在<u>本地</u>复制一份，直接算** → **零通信** ✅

**所以共享专家应该在<u>每张卡上都复制一份</u>**（而不是 EP 切开）—— 这是个重要的部署细节。

**特殊性 2：它是量化的高风险区**（第 13 章 Q6 追问 3 提过）
- **它的误差会影响<u>所有</u> token**（路由专家只影响被路由到的那些）
- → **必须保精度**，量化要更保守

**特殊性 3：它天然负载均衡**
- 因为**所有 token 都过** → **完美均衡**，没有热点问题

> **共享专家的设计动机（回顾 Frontier Topics）**：**让路由专家专注于"专业化"，而把"通用知识"交给共享专家**。**部署上它反而是最省心的那部分** —— 零通信、零热点、只是要小心量化。
>
> **"共享专家在架构上是为了专业化，在部署上是零通信零热点的那部分"** —— 这个双重视角很好。

### ⚠️ 常见陷阱

1. **说 MoE 推理"只算 37B 所以很快"** —— **只在小 batch 下成立**！大 batch 下算术强度被稀释 $N/k$ 倍。
2. **不知道超大 EP 是为了"攒 batch"** —— 很多人以为是"为了装下"。**装下只要 EP32 就够了，EP320 是为了算术强度。**
3. **不知道 Attention 用 DP 不用 TP** —— **这是 MLA 带来的自由度**。
4. **把 EP 说成 TP 的变体** —— **EP 切专家（完整），TP 切矩阵（变瘦）**。
5. **忘了共享专家要本地复制** —— 它不参与 All-to-All。

### 🏢 大厂偏好

- **DeepSeek**：**必问，且会深挖 V3 的部署配置**
- **字节 / 阿里（有 MoE 模型的团队）**：会问 EP 和负载均衡
- **应用岗**：知道"MoE 显存大、要超大 EP、量化收益最大"就够了

### 📚 延伸阅读
- [DeepSeek-V3 Technical Report](https://arxiv.org/abs/2412.19437) - **§5 部署方案，必读**
- [DeepEP](https://github.com/deepseek-ai/DeepEP) - All-to-All 通信库
- [MegaScale-Infer](https://arxiv.org/abs/2504.02263) - 字节的 MoE 推理系统（把 attention 和 FFN 也分离！）

---

## Q7：容量规划——需要几张卡 ⭐⭐⭐⭐（实战·要算数）

### 🎯 一句话标答

> 五步法：**① 算显存**（权重 + 激活 + KV）→ **② 算最大并发**（剩余显存 ÷ 单请求 KV）→ **③ 算吞吐**（Roofline 上界 × MFU，或直接压测）→ **④ 算 QPS**（吞吐 ÷ 平均输出长度）→ **⑤ 反推卡数**（目标 QPS ÷ 单卡 QPS × 峰值系数 × 冗余）；**三个最常见的翻车点是：用平均长度做规划（分布是长尾的）、忘记峰均比（通常 3-5×）、忘记 KV Cache 的预留和碎片**。

### 🗣️ 30 秒口语版

"这题会给你一个具体场景让你现场算。**流程要背下来，数字要能现推。**

**场景**：LLaMA-3-70B（GQA-8）、A100-80G、fp16、目标 100 QPS、平均输入 1000 token / 输出 300 token、TTFT SLA < 1s、TPOT SLA < 50ms。

**Step 1：显存**
```
权重：70B × 2 (fp16) = 140 GB   → ★单卡装不下，TP=2 最少★
     TP=2 时每卡 70 GB → 加上激活和 KV 就爆了
     → ★TP=4：每卡 35 GB★
     
每卡预算：
  权重       35 GB
  激活/临时   5 GB
  ─────────────
  剩给 KV    40 GB   ← 每卡
  TP=4 总 KV 池 = 160 GB
```

**Step 2：单请求的 KV**
```
LLaMA-3-70B: 80 层, GQA-8, head_dim=128
每 token = 2 × 80 × 8 × 128 × 2 = 327,680 B = ★320 KB★  (第 12 章的表)

平均序列长度 = 1000(输入) + 300(输出) = 1300
单请求 KV = 1300 × 320 KB = ★416 MB★
```

**Step 3：最大并发**
```
160 GB / 416 MB = ★384 并发★（理论）
实际打 7 折（碎片、预留、长尾请求）→ ★~270 并发★
```

**Step 4：吞吐**
```
Roofline 上界（第 12 章）：
  TP=4，每卡读 35 GB，A100 带宽 2000 GB/s
  → TPOT_min = 35/2000 = 17.5 ms
  → 但要加 TP 的 AllReduce（~10%）和大 batch 的开销
  → 实际 TPOT ≈ 25-30 ms  ← ★满足 SLA < 50ms ✅★

吞吐 = batch / TPOT = 270 / 0.028 ≈ ★9600 token/s★（4 卡）
     = 2400 token/s/GPU
```

**Step 5：QPS 和卡数**
```
每个请求要 300 个输出 token
→ 单机（4卡）QPS = 9600 / 300 = ★32 QPS★

目标 100 QPS
→ 需要 100/32 = 3.1 台 → ★4 台（16 卡）★

★但还没完★：
  峰均比 3×      → 12 台
  冗余/滚动更新 20% → ★14-15 台（56-60 卡）★
```

**三个必须主动说的翻车点**：

**① 用平均长度做规划 —— 分布是长尾的！**
```
输入长度：中位数 200，均值 1000，★P99 = 8000★
→ P99 的请求，KV 是均值的 8 倍！
→ 几个长请求就能把 KV 池吃掉，把并发从 270 打到 50
```
**正确做法**：用**真实的长度分布**做仿真，不是用均值。

**② 忘记峰均比。**
- C 端产品的日峰/日均 ≈ **3-5×**
- 有热点事件时 **10×+**
- **只按均值配容量 = 高峰必挂**

**③ 忘记 KV Cache 的预留。**
- 抢占（第 12 章 Q4）需要缓冲
- 显存碎片
- CUDA Graph 要占几百 MB
- → **打 7 折是经验值**

**最后一句（这才是面试要的）**：
> **"这套算法给的是<u>数量级</u>，最终必须<u>压测</u>。因为真实流量的长度分布、prefix 命中率、并发模式，都不是公式能算出来的。我会用真实流量回放做压测，找到饱和点（Q1），再按峰值配容量。"**"

### 📐 完整的容量规划公式

$$\boxed{
\begin{aligned}
\text{可用 KV 池} &= (\text{单卡显存} - \text{权重}/\text{TP} - \text{激活}) \times \text{TP} \times 0.7 \\[4pt]
\text{最大并发} &= \frac{\text{可用 KV 池}}{\overline{L} \times \text{KV per token}} \\[4pt]
\text{吞吐 (tok/s)} &= \frac{\text{最大并发}}{\text{TPOT}} \\[4pt]
\text{单机 QPS} &= \frac{\text{吞吐}}{L_{\text{out}}} \\[4pt]
\text{机器数} &= \frac{\text{目标 QPS}}{\text{单机 QPS}} \times \underbrace{\text{峰均比}}_{3\text{-}5\times} \times \underbrace{(1+\text{冗余})}_{1.2\times}
\end{aligned}
}$$

### 🔍 压测的正确姿势

| ❌ 错误做法 | ✅ 正确做法 |
|---|---|
| 固定长度（如全部 1024→128） | **真实流量回放**，或用**实测的长度分布**采样 |
| 只测一个并发 | **扫并发**，画出 **吞吐-延迟曲线**，找**饱和点**（Q1） |
| 只看均值 | **看 P50 / P95 / P99**，尤其 **ITL 的 P99** |
| 只测吞吐 | **测 Goodput**（Q1）——满足 SLA 的吞吐 |
| 不开 prefix caching 测 | **按真实的前缀重复率测** —— 命中率会剧烈改变结论 |
| 冷启动就测 | **预热**（CUDA Graph 捕获、cache 填充） |

**推荐工具**：
- `vllm bench serve`（原 `benchmark_serving.py`）—— 支持真实数据集（ShareGPT）和泊松到达
- 自己写：**按真实分布采样长度 + 泊松过程模拟到达**

### 🔥 高频追问 Top 3

**Q：怎么估 Prefix Caching 的命中率？它对容量规划影响多大？**

A：**影响极大——它可能把你的 P 侧需求砍掉 90%。**

**估算方法**：
```
命中率 ≈ 共享前缀长度 / 平均输入长度
```

| 场景 | 共享前缀 | 平均输入 | **命中率** | 对 prefill 的影响 |
|---|---|---|---|---|
| 无状态单轮 QA | system (500) | 800 | **62%** | prefill 量 ÷2.6 |
| **多轮对话（第 5 轮）** | 前 4 轮 (4000) | 4500 | **89%** | prefill 量 **÷9** |
| **Agent（第 8 轮 ReAct）** | system+tools+7轮 (7500) | 8000 | **94%** | prefill 量 **÷16** ✅ |
| RAG（文档不重复） | system (500) | 4500 | **11%** | 几乎没用 |
| RAG（**热门文档**） | system + 文档 (4000) | 4500 | **89%** | ÷9 ✅ |

**对容量规划的影响**：
```
Agent 场景，94% 命中：
  无 cache：每轮 prefill 8000 token → TTFT ≈ 700ms
  有 cache：每轮 prefill  500 token → TTFT ≈ ★45ms★  （↓15×）
  
→ ★prefill 的算力需求降 16 倍★
→ 如果你在做 PD 分离（Q5），★P 集群的规模直接砍掉 90%★
```

$$\boxed{\text{结论：<u>先上 Prefix Caching，再做容量规划</u>。顺序反了会严重高估需求。}}$$

**这个顺序性的洞察，比任何公式都值钱。**

**Q：输入输出长度分布怎么影响卡数？给个直觉。**

A：**同样的 QPS，长度分布不同，卡数能差 10 倍。**

```
【场景 A】RAG：输入 4000，输出 100
  KV 占用：4100 × 320KB = 1.3 GB/请求  ← ★大★
  prefill：重（4000 token）
  decode： 轻（100 步）
  → ★KV 池是瓶颈，并发上不去★
  → 160 GB / 1.3 GB = 123 并发
  → QPS = 123 / 0.028 / 100 = ★44 QPS/机★

【场景 B】创作：输入 100，输出 2000
  KV 占用：2100 × 320KB = 0.67 GB/请求  ← 中
  prefill：轻
  decode： ★重（2000 步）★
  → 239 并发
  → QPS = 239 / 0.028 / 2000 = ★4.3 QPS/机★  ← ★只有 A 的 1/10！★

【场景 C】客服：输入 500，输出 200
  KV：0.22 GB/请求
  → 727 并发（但会先被 max_num_seqs 限制）
  → QPS = 727 / 0.028 / 200 = ★130 QPS/机★
```

**读出三个规律**：
1. **输出长度是 QPS 的<u>除数</u>** —— **输出越长，QPS 越低**（因为每个请求占用 GPU 的时间越久）
2. **输入长度决定 KV 占用** —— **输入越长，并发越低**
3. **场景 B 的 QPS 只有 C 的 1/30** —— **同样的硬件，业务不同差 30 倍！**

$$\boxed{\text{"我们需要几张卡" 这个问题，<u>不给长度分布就无法回答</u>}}$$

**面试时被问"70B 需要几张卡"，正确的第一反应是<u>反问</u>："输入输出长度分布是什么？QPS 目标？SLA？"** —— **这个反问本身就是最好的答案**，它说明你知道这题没有标准答案。

**Q：怎么做弹性伸缩？LLM 服务的扩容有什么特殊性？**

A：**LLM 服务的扩容<u>极慢</u>，这是它和传统微服务最大的不同。**

**传统微服务**：容器几秒起来，秒级扩容 ✅

**LLM 服务**：
```
拉镜像（几十 GB）        : 1-5 min
加载模型权重（140 GB）    : 2-5 min   ← ★从对象存储读！★
CUDA Graph 捕获          : 30-60 s
Prefix Cache 预热        : 几分钟（冷缓存的命中率是 0！）
────────────────────────────────
总计：★5-15 分钟★
```

**这意味着**：
1. ❌ **不能靠"扩容"应对突发流量** —— **等你起来了，高峰过去了**
2. ✅ **必须<u>预留</u>容量**（按峰值配，而不是按均值 + 弹性）—— **这就是为什么 LLM 服务这么贵**
3. ✅ **弹性只能做"<u>慢变化</u>"**（日间/夜间的潮汐，提前 30 分钟预热）

**四个缓解手段**：
| 手段 | 效果 |
|---|---|
| **模型权重放本地 NVMe / 用 P2P 分发** | 加载 5min → **1min** |
| **预热池**（keep-warm，跑着但不接流量） | **秒级**接入 ✅ |
| **分级降级**：高峰时**自动关掉投机解码、加大 batch、降 SLA** | **不用扩容也能扛** ✅ |
| **排队 + 限流**（网关层） | 保护 GPU 不被打爆（超过饱和点的请求排队） |

> **第 3 点是最实用的** —— **LLM 服务的"弹性"不在机器数量上，在<u>质量档位</u>上**：
> ```
> 平时：投机解码开 + batch 32 + TPOT 20ms   （体验好）
> 高峰：投机解码关 + batch 128 + TPOT 60ms  （体验降，但★吞吐涨 3 倍★，不用加机器）
> ```
> **"用体验换容量"** —— 这比加机器快得多，也便宜得多。**这个思路能说出来，是很强的工程经验信号。**

### ⚠️ 常见陷阱

1. **用平均长度算** —— 分布是**长尾**的，P99 可能是均值的 8 倍。
2. **忘记峰均比** —— 3-5×，热点时 10×。
3. **忘记 KV 预留** —— 打 7 折是经验。
4. **不问长度分布就报数字** —— **这题不给分布就无法回答，反问才是正确答案**。
5. **以为能靠弹性扩容** —— **LLM 服务扩容要 5-15 分钟**，高峰早过了。
6. **先规划再上 Prefix Caching** —— **顺序反了，会高估 10 倍**。

### 🏢 大厂偏好

- **字节 / 阿里**：**必问，且会给具体数字让你现场算**
- **应用岗**：**这是最实战的一题**，答得好说明真的部署过
- **所有岗**：**"先反问长度分布"是最好的开场**

### 📚 延伸阅读
- [vLLM benchmark 工具](https://github.com/vllm-project/vllm/tree/main/benchmarks)
- [LLM Inference Performance Engineering](https://www.databricks.com/blog/llm-inference-performance-engineering-best-practices)

---

## Q8：系统设计实战 ⭐⭐⭐⭐⭐（压轴）

### 🎯 一句话标答

> 答题框架四步：**① 反问澄清**（长度分布、QPS、SLA、预算、模型是否固定）→ **② 先上无损优化**（PagedAttention、Continuous Batching、Chunked Prefill、Prefix Caching、FlashAttention——**零风险纯收益，无脑上**）→ **③ 再按瓶颈上有损/权衡优化**（量化、投机解码——**必须评估**）→ **④ 最后考虑架构级**（PD 分离、多集群 SLA 分级——**大规模才值**）；核心判据永远是 **Roofline（你是 memory-bound 还是 compute-bound）** 和 **Goodput（而不是吞吐）**。

### 🗣️ 30 秒口语版

"系统设计题**没有标准答案，考的是<u>框架</u>**。我给你一套可以直接套的。

**第一步：反问澄清（★不要直接开始设计★）**

这一步**本身就是得分点**——它说明你知道"没有需求就没有设计"：

| 必问 | 为什么 |
|---|---|
| **输入/输出长度分布**（中位数、P99） | **决定卡数，能差 30 倍**（Q7） |
| **QPS 目标 + 峰均比** | 决定容量 |
| **SLA**：TTFT?  TPOT? 还是只关心 E2E? | **决定所有技术选型**（Q1） |
| **是 C 端聊天还是 Agent？** | **C 端要 TPOT，Agent 要 E2E**（Q1） |
| **prompt 有多少是重复的？** | **决定 Prefix Caching 的收益**（可能是最大的一刀） |
| **模型固定吗？** | 决定引擎选型（Q3） |
| **预算 / 卡型？** | A100 还是 H100（决定能不能用 FP8） |

**第二步：无损优化——无脑上**

这些**零风险、纯收益**，没有任何理由不开：

| 技术 | 收益 | 章节 |
|---|---|---|
| **PagedAttention** | 显存浪费 60-80% → 4% | 12-Q4 |
| **Continuous Batching** | 吞吐 2-20× | 12-Q5 |
| **FlashAttention / FlashDecoding** | prefill 2-4×，长上下文 decode 8× | 15-Q2 |
| **Chunked Prefill** | **ITL P99 从 500ms → 35ms** | 12-Q6 |
| **Prefix Caching** | **Agent 场景 TTFT ↓15×** | 12-Q7 |
| **CUDA Graph** | decode 的 CPU 开销 34% → ~0 | 15-Q3 |

**用 vLLM/SGLang 的话，这些基本都是默认开的** —— **所以"选对引擎"本身就完成了 80% 的优化**。

**第三步：按瓶颈上有损/权衡优化 —— ★必须先测瓶颈★**

$$\text{先测：你是 memory-bound 还是 compute-bound？（第 12 章 Roofline）}$$

| 瓶颈 | 上什么 | 代价 |
|---|---|---|
| **显存不够**（装不下 / 并发低） | **量化 W4A16 (AWQ)** + **KV fp8** | ⚠️ **有损，必测 GSM8K** |
| **TPOT 不达标 + batch 小** | **投机解码（EAGLE / n-gram）** | ⚠️ **大 batch 会倒挂，要设开关** |
| **TTFT 不达标** | **Prefix Caching** + 加 prefill 算力 | ✅ 无损 |
| **吞吐不够 + 有 H100** | **FP8 (W8A8)** | ⚠️ 轻微有损 |
| **吞吐不够 + 只有 A100** | **SmoothQuant W8A8** 或加机器 | ⚠️ 有损 |

**第四步：架构级 —— 大规模才值**

- **几台机器** → 到第三步就够了
- **几十台 + 严格 SLA** → **PD 分离**（Q5）
- **有 SLA 分级** → **多集群**（高优小 batch + 投机；低优大 batch）
- **MoE** → **超大 EP + 冗余专家**（Q6）

**贯穿全程的两个判据（这是压轴题的灵魂）**：
1. **Roofline**：**memory-bound 还是 compute-bound？** —— **这决定了每个优化是赚是亏**
2. **Goodput 而不是吞吐** —— **满足 SLA 的吞吐才是真吞吐**"

### 🔍 三个典型场景的完整方案

#### 场景 A：IDE 代码补全（第 14 章自测题 11）

**需求**：7B、A100 单卡、**batch=1-2**、**TPOT < 15ms**、输入 2000（大量重复的文件上下文）、输出 50

**分析**：
- batch=1-2 → **极度 memory-bound**（算术强度 ≈ 1）
- 7B fp16 = 14 GB → TPOT 下界 = **7ms**（第 12 章）——**够，但没余量**
- **输入 2000 且高度重复**（同一个文件反复补全）→ **Prefix Caching 命中率极高**
- **代码场景 → 投机解码的 $\alpha$ 极高（0.95）**（第 14 章 Q5）

**方案**：
```
① Prefix Caching   → TTFT: 180ms → ★~20ms★（命中 90%+）
② AWQ int4         → 权重 14GB → 3.5GB → TPOT 下界 7ms → ★1.75ms★
③ n-gram lookup 投机 → 代码补全大量复制上下文，★零成本★，先试这个
④ 不行再上 EAGLE   → α≈0.95, γ=7 → ★额外 3-4×★
⑤ CUDA Graph       → 干掉 launch 开销（batch 小，占比大！）
⑥ 关闭所有惩罚     → ★代码必须重复变量名★（第 14 章 Q2）
⑦ temperature=0    → 代码要确定性

预期 TPOT: 7ms → ★2-4ms★  ✅ 远超 SLA
```

**这个场景是投机解码的<u>完美舞台</u>**：batch=1（算力全闲）+ $\alpha$=0.95（代码好猜）。

#### 场景 B：C 端聊天（高并发）

**需求**：70B、8×A100、**1000 QPS**、TTFT<1s、TPOT<50ms、输入 500 / 输出 300

**分析**：
- 高 QPS → **必然大 batch** → **compute-bound 区**
- → **投机解码会倒挂**（第 14 章 Q7）❌
- → **W4A16 优势衰减**（第 13 章 Q6）❌

**方案**：
```
① TP=4（70B 装不下单卡；TP=4 而不是 8，留通信余量）× 2 副本 DP
② ★FP8 (W8A8)★ 如果是 H100 / SmoothQuant W8A8 如果是 A100
   → ★算力翻倍，大 batch 下这才是对的★（不是 W4A16！）
③ KV Cache fp8   → 并发翻倍
④ Chunked Prefill → ITL P99 从 500ms 压到 35ms
⑤ Prefix Caching  → system prompt 共享
⑥ ★不开投机解码★  → batch 太大会倒挂
⑦ max_num_seqs 设在★饱和点★（压测得出）
⑧ 网关限流：超过饱和点的请求排队，★不要塞给 GPU★
⑨ 高峰降级：batch↑ + SLA 放宽（"用体验换容量"）
```

**关键判断**：**高并发场景，W8A8 > W4A16，且不开投机解码** —— 这两个"反直觉"的选择，正是第 13、14 章的核心结论。

#### 场景 C：Agent 平台（长 prompt、多轮）

**需求**：70B、输入 8000（system 500 + tools 3000 + 历史 4500）、输出 300、**10 轮/任务**、关心 **E2E**

**分析**：
- **输入极长 + ReAct 每轮只追加** → **Prefix Caching 命中率 94%**（Q7 追问 1）
- **输出不给人看** → **不关心 TPOT，关心 E2E**（Q1）
- **10 轮串行** → **E2E 被放大 10 倍**，P99 被放大到 18%（Q1 追问 2）
- **工具调用要合法 JSON** → **约束解码**（第 14 章 Q8）

**方案**：
```
① ★SGLang★（不是 vLLM！）
   → RadixAttention 的★树形前缀复用★，Agent 的调用图就是树（Q3）
   → jump-forward decoding，工具调用的 JSON 直接"填"
② ★Prefix Caching 是最大的一刀★
   → 8000 → 500 token 的 prefill → ★TTFT ↓15×★
   → ★这一刀的收益超过后面所有优化的总和★
③ prompt 结构：★不变的放前面★（第 12 章 Q7）
   system → tools(固定顺序!) → few-shot → 历史(只追加) → 新 observation
   ⚠️ JSON 序列化 sort_keys=True；★不要在开头放时间戳★
   ⚠️ ★历史的"滑动窗口修剪"会毁掉全部缓存★ → 只在检查点压缩
④ XGrammar 约束解码 → 工具调用 100% 合法
   → 但★先自由 CoT，最后才约束格式★（第 14 章 Q8）
⑤ AWQ int4 + KV fp8 → 长 prompt，KV 是大头
⑥ 不追求 TPOT → ★可以用大 batch 提吞吐降成本★
⑦ ★P99 是重点★ → 10 轮任务有 18% 概率踩到 P99（Q1）
   → Chunked Prefill 必开（防 stall）
```

**这个场景的核心洞察（Agent 岗必答）**：
> **"Agent 场景最大的优化是 Prefix Caching，不是量化也不是投机解码。**
> **因为 ReAct 每轮只在末尾追加，前缀命中率 94%——**
> **没开 Prefix Caching 的 Agent，等于把整个历史重复 prefill 了 10 遍。"**

### 📐 优化的优先级（可以直接背）

$$
\boxed{
\begin{array}{ll}
\textbf{第 0 优先} & \text{选对引擎（vLLM/SGLang）—— 80\% 的优化是默认开的} \\
\textbf{第 1 优先} & \textbf{Prefix Caching} \text{ —— 免费，Agent/多轮场景收益最大} \\
\textbf{第 2 优先} & \text{Chunked Prefill —— 免费，救 ITL P99} \\
\textbf{第 3 优先} & \textbf{测 Roofline} \text{ —— 你是 memory-bound 还是 compute-bound？} \\
\textbf{第 4 优先} & \text{按瓶颈量化：小 batch → W4A16；大 batch → W8A8/FP8} \\
\textbf{第 5 优先} & \text{投机解码 —— 仅当 batch 小；必设 disable\_by\_batch\_size} \\
\textbf{第 6 优先} & \text{PD 分离 —— 仅当几十台以上 + 严格 SLA} \\
\end{array}
}
$$

**"先把无损的上满，再考虑有损的；先测瓶颈，再选方案。"** —— 这是第三部分的最终结论。

### 🔥 高频追问 Top 3

**Q：老板说"成本太高，降一半"。你怎么办？**

A：**按"收益/风险"排序，逐级往下走。这题考的是优先级判断。**

```
【第 1 级：免费的，先榨干】风险 0
  □ Prefix Caching 开了吗？命中率多少？
    → Agent/多轮场景，这一刀可能就是 ★30-50%★
  □ prompt 结构对吗？（变化的放最后了吗？tools 顺序固定吗？）
  □ max_num_seqs 设在饱和点了吗？（低了 = 浪费；高了 = 抢占）
  □ Chunked Prefill 开了吗？
  → ★通常这一级就能降 20-40%，零风险★

【第 2 级：轻度有损】风险低
  □ KV Cache → fp8    → ★并发翻倍 = 成本减半★ ← ★性价比最高的一刀★
  □ 权重 → AWQ int4 / FP8
  → ★必测：GSM8K、你的业务评测集、长上下文★（第 13 章 Q8）
  → 通常再降 30-40%

【第 3 级：换模型】风险中
  □ 70B-int4 vs 13B-fp16？
  → ★第 13 章的结论：70B-int4 通常显著更好，且显存相近★
  □ 能不能用 MoE？（DeepSeek-V3 的激活参数只有 37B）
  □ 蒸馏一个小模型？（有成本，但一劳永逸）

【第 4 级：降体验】风险高，但立竿见影
  □ 加大 batch，放宽 TPOT SLA  → ★吞吐涨 2-3×★
  □ 关掉投机解码（如果开了）
  □ 限制 max_tokens（输出长度是 QPS 的除数！Q7）
  → ★"用体验换成本"，要和产品谈★

【第 5 级：架构】风险高，周期长
  □ PD 分离 + 异构卡型（P 用算力卡，D 用便宜的大显存卡）
  □ 换引擎（TRT-LLM 可能再榨 10-20%，但迁移成本高）
```

**关键的答题姿态**：
> **"我会从<u>零风险</u>的开始，而不是一上来就换模型。因为很多时候，光是 Prefix Caching 没开对、prompt 结构不对、max_num_seqs 设错，就能白白浪费 50% 的成本。"**

**这个"先查免费的，再考虑有损的"的顺序，本身就是最好的答案。**

**Q：怎么设计一个多租户 / 多 SLA 的服务？**

A：**核心是"隔离 + 分级"，四个层次。**

```
【第 1 层：网关】
  □ 按租户/优先级★限流★（超过配额直接拒，保护 GPU）
  □ ★亲和性路由★：相同前缀 → 同一节点（提高 prefix cache 命中，Q5）
  □ 超过饱和点 → 排队，★不要塞给 GPU★（Q1）

【第 2 层：集群分级】★最关键★
  高优集群：小 batch + EAGLE 投机 + TP=8  → TPOT 15ms  （付费用户）
  低优集群：大 batch + 无投机 + TP=4      → TPOT 60ms  （免费用户）
  批处理集群：超大 batch + 不管延迟        → 成本最低   （离线任务）
  → ★这正是 PD 分离的价值：让"延迟 vs 吞吐"变成可分配的资源★（Q5）

【第 3 层：调度】
  □ 优先级队列（高优先调度）
  □ ★抢占时优先踢低优先级★（第 12 章 Q4 追问 1）
  □ ⚠️ ★防饿死★：低优先级要有最低保障，否则永远排不上

【第 4 层：★安全★】
  □ ⚠️ ★Prefix Cache 必须按租户隔离！★
    → 跨租户共享 KV 有★时序侧信道★（第 12 章 Q7 追问 1）
    → 攻击者构造 prompt，看 TTFT 是否异常快 → ★探测其他租户的输入★
  □ 按租户的显存配额（防止一个租户的长请求吃光 KV 池）
```

> 🚨 **第 4 层是最容易被漏掉、但在公有云场景是<u>硬性合规要求</u>的**。答出来会显著加分——**它说明你不只想着性能，还想着安全。**

**Q：怎么建立监控体系？**

A：**分三层：业务、系统、GPU。要能说出每层看什么。**

```
【业务层】—— 用户感知
  ★TTFT P50/P95/P99★     ← 包含排队时间！
  ★ITL P99★              ← ★不是 TPOT 均值！★（Q1）
  E2E P99
  ★Goodput★              ← ★满足 SLA 的 QPS，这才是北极星指标★
  错误率 / 超时率

【系统层】—— 引擎内部
  ★KV Cache 使用率★      ← >90% = 要抢占了，是并发不足的先行指标
  ★抢占次数 / swap 量★    ← >0 = 显存不够，P99 会爆
  ★等待队列长度★         ← 持续增长 = 已经过饱和点了
  ★Prefix Cache 命中率★  ← 突然下降 = ★prompt 结构被改坏了★（很常见的事故！）
  running batch size 分布 ← 用来决定投机解码的开关阈值
  ★接受率 α★（如果开了投机）← <0.5 就该关了

【GPU 层】—— 硬件
  ★显存使用★（分：权重 / KV / 激活）
  ★SM 利用率 + HBM 带宽利用率★  ← ★判断 memory-bound 还是 compute-bound★
  NVLink / IB 带宽              ← TP/EP 的通信是否成为瓶颈
  温度 / 功耗                    ← 降频会让 P99 莫名其妙变差
```

**三个"报警组合"（这才是有经验的答法）**：

| 症状 | 诊断 | 动作 |
|---|---|---|
| **KV 使用率 >90% + 抢占 >0 + P99 飙升** | **显存不够** | 上 KV fp8 / 量化 / 降 max_num_seqs |
| **等待队列增长 + 吞吐不涨** | **过饱和了** | 网关限流 / 加机器 |
| **Prefix 命中率突降** | ⚠️ **有人改了 prompt 模板！** | **查最近的发布** ← 这是最隐蔽的事故 |
| **SM 利用率低 + HBM 打满** | memory-bound | 加 batch / 量化 |
| **SM 打满 + HBM 不满** | compute-bound | **别再上投机解码了**，考虑 W8A8 |

**最后一句**：
> **"最重要的指标是 <u>Goodput</u>，不是吞吐。吞吐可以靠堆 batch 作弊，Goodput 不能。"**（Q1）

### ⚠️ 常见陷阱

1. **不问需求直接设计** —— **反问本身就是得分点**。
2. **一上来就上最花哨的**（PD 分离、投机解码）—— **先把免费的无损优化上满**。
3. **不测瓶颈就选方案** —— Roofline 是一切的前提。**小 batch 上 W8A8、大 batch 上 W4A16，都是错的。**
4. **只优化吞吐** —— **要优化 Goodput**。
5. **忘记 Agent 场景 Prefix Caching 是最大的一刀** —— 比量化和投机解码加起来还大。
6. **忘记多租户的 cache 隔离** —— 公有云的合规红线。

### 🏢 大厂偏好

- **所有大厂的高级岗**：**必考**，且会不断加约束（"如果 QPS 翻 10 倍呢"、"如果预算砍半呢"）
- **字节 / 阿里**：会问得非常具体（给真实数字）
- **Agent 岗**：**场景 C 是标准考题**

### 📚 延伸阅读
- 把第 12-15 章串起来读一遍就是最好的准备
- [DeepSeek-V3 §5](https://arxiv.org/abs/2412.19437) - 一个真实的、极致的系统设计案例

---

## 📝 本章小结

| 关键点 | 你必须能脱口而出 |
|---|---|
| **四个指标** | TTFT（**含排队！**）/ TPOT（均值）/ **ITL P99（抓 stall，才是真相）** / 吞吐 |
| **★Goodput★** | **满足 SLA 的吞吐**。**吞吐可以靠堆 batch 作弊，Goodput 不能** |
| **三角权衡** | batch↑ → 吞吐↑ 但 TTFT↑ TPOT↑；**PD 分离是唯一跳出三角的** |
| **Agent 的特殊性** | 输出不给人看 → **优化 E2E 和 TTFT，不是 TPOT**；但轮次放大延迟和 P99（10 轮 → 18% 踩 P99） |
| **FlashAttention** | **Tiling + Online Softmax + 重算**；**精确不是近似**；**优化访存不是计算** |
| **FA 的本质** | 算术强度 $d/2 = 64 < 150$ → **attention 本是 memory-bound**；FA 后强度 $\propto N$ → **变成 compute-bound** |
| **Online Softmax** | 修正因子 $e^{m^{(i-1)}-m^{(i)}}$ 把旧结果换算到新 max 基准；**分块算 attention 就必须用它**（FA / PagedAttention / FlashDecoding 全都是） |
| **FlashDecoding** | Decode 的问题**不是 $[N,N]$ 太大，是并行度不够**（seq_Q=1）→ **切 KV 维度** |
| **non-matmul 惩罚** | tensor core 的 matmul 吞吐是 non-matmul 的 **16 倍** → 1% 的 FLOPs 可能占 16% 的时间 |
| **引擎选型** | vLLM=生态 / **SGLang=Agent+多轮+DeepSeek** / TRT-LLM=极致但僵；**成本在迁移不在性能** |
| **RadixAttention** | **树形结构被显式表达** → 更细粒度匹配 + **从叶子驱逐（保护公共前缀）**；**Agent 的调用图就是树** |
| **编译 vs 解释** | TRT-LLM/CUDA Graph/Outlines 的 FSM = **静态换性能**；永恒的 trade-off |
| **推理 TP 的目的** | 训练为**装下**，推理**更是为了把带宽 ×N**；**GQA-8 配 TP=8 是设计出来的** |
| **TP 的反直觉** | **TP 是"用吞吐换延迟"**；**能 DP 就 DP，TP 是被迫的选择** |
| **推理不用 ZeRO** | **TP 传激活（小），ZeRO 传权重（大）**；且推理没有优化器状态那 16 倍要省 |
| **SP** | 只对 **Prefill** 有意义（$S$ 大），Decode 的 $S=1$ → 无意义 → **又一个 PD 分离的论据** |
| **★PD 分离的本质★** | **不是"防干扰"，是<u>把两个耦合的 SLA 解耦</u>** → Goodput **4.48×**（严格 SLA **10.2×**） |
| **PD 的代价** | **KV 跨机传 2GB！** → **layer-wise 流水** + **MLA（传输量 ÷7）**；**架构与部署的协同** |
| **MoE 算术强度** | $\dfrac{kB}{N}$ —— **被稀释 $N/k$ = 32 倍** → 要 batch=4800 才饱和 → **这就是 EP320 的理由** |
| **V3 部署** | Prefill: TP4+SP+DP8, EP32 / **Decode: TP4+DP80, EP320（每卡一专家）+ 冗余专家** |
| **为什么 Attention 用 DP** | **MLA 的 KV 已经极小，不需要 TP 分摊** → 这是 MLA 带来的部署自由度 |
| **容量规划** | 显存→并发→吞吐→QPS→卡数 × **峰均比(3-5×)** × 冗余；**先上 Prefix Caching 再规划** |
| **规划的铁律** | **不给长度分布就无法回答"要几张卡"**——反问才是正确答案 |
| **LLM 扩容** | **5-15 分钟** → **弹性不在机器数量上，在"质量档位"上**（用体验换容量） |
| **★优化优先级★** | 选引擎 → **Prefix Caching** → Chunked Prefill → **测 Roofline** → 量化 → 投机(小 batch) → PD 分离(大规模) |
| **★最终原则★** | **先把无损的上满，再考虑有损的；先测瓶颈，再选方案** |

## ✅ 自测题

1. 什么是 Goodput？为什么"优化吞吐"这个目标本身是错的？举一个"吞吐涨 2.5× 但 Goodput 掉 4×"的例子。
2. 算出标准 attention 的算术强度，说明**为什么它和 $N$ 无关**。FlashAttention 之后呢？用这个解释"为什么 Linear Attention 输了"。
3. **推导 online softmax 的修正因子**，说明为什么它是精确的。列出你在本教材里见过它的**三个**地方。
4. 为什么 Prefill 用 FlashAttention、Decode 用 FlashDecoding？两者解决的是**同一个问题吗**？
5. 为什么推理不能用 ZeRO-3？用"传激活 vs 传权重"一句话回答。
6. 8×A100 跑 13B（单卡装得下），TP=8 还是 DP=8？给出**决策树**和判据。
7. 算 TP=8 在 NVLink / PCIe / IB 上的通信占比，解释"**TP 不跨节点**"。为什么 **batch 越大 TP 的性价比越低**？
8. **PD 分离的本质是什么？**（提示：不是"防干扰"）为什么 SLA 越严格，收益越大？
9. 一个 4K prompt 的 70B 请求，PD 分离要传多少 KV？在 400 Gbps IB 上要多久？**layer-wise 流水能藏住吗**（给出判据）？MLA 帮了多大忙？
10. **推导 MoE 的算术强度 $\frac{kB}{N}$**，并用它解释 **DeepSeek-V3 为什么用 EP320**（提示：不是为了装下）。
11. **容量规划**：LLaMA-3-70B、H100-80G、目标 200 QPS、输入中位数 500/P99 6000、输出中位数 200/P99 1500、TTFT<800ms、TPOT<40ms。**算出需要几张卡**，并说出你的三个假设。
12. **压轴**：设计一个 Agent 平台的推理服务（70B、输入 8000、输出 300、10 轮/任务、500 并发任务）。**按优先级列出你的优化，并说明每一步的收益量级和风险。**

---

> ## 🎓 第三部分完结
>
> **第 12-15 章走完了一条完整的因果链**：
>
> $$\underbrace{\text{Decode 是 memory-bound}}_{\text{第 12 章 Roofline}} \Rightarrow \begin{cases} \text{减小模型} &\to \textbf{第 13 章 量化} \\ \text{提高 batch} &\to \textbf{第 12 章 调度（Paged + Continuous）} \\ \text{一次多出几个 token} &\to \textbf{第 14 章 投机解码} \end{cases} \Rightarrow \underbrace{\text{装进一个系统}}_{\textbf{第 15 章}}$$
>
> **一句话贯穿全部**：**推理优化的一切，都是在回答"你是 memory-bound 还是 compute-bound"这一个问题。**
>
> ---
>
> **下一章预告**：**第四部分开篇 —— 第 16 章｜RAG 系统**。完整链路、Chunking 策略、Embedding 选型、向量库与 HNSW、**混合检索 + RRF**、Rerank、**Query 改写与路由**、HyDE、GraphRAG、Self-RAG / CRAG、**幻觉归因**，以及 **RAGAS 评估体系**。
>
> 第三部分回答的是"**怎么把模型跑起来**"，第四部分回答的是"**怎么把模型用起来**"——**这正是你目标岗位（Agent 工程）的主场**。

